[
  {
    "q": "What is the primary purpose of Django ORM?",
    "c": null,
    "o": [
      "To map database tables to Python classes",
      "To create HTML templates",
      "To handle URL routing",
      "To manage static files"
    ]
  },
  {
    "q": "Which class should you inherit from when defining a Django model?",
    "c": null,
    "o": [
      "models.Model",
      "models.Base",
      "models.Field",
      "models.Form"
    ]
  },
  {
    "q": "What command is used to create database migrations after defining models?",
    "c": null,
    "o": [
      "python manage.py makemigrations",
      "python manage.py migrate",
      "python manage.py runserver",
      "python manage.py startapp"
    ]
  },
  {
    "q": "What does the following Django model define?",
    "c": "class Book(models.Model):\n    title = models.CharField(max_length=100)\n    author = models.CharField(max_length=50)\n    published_date = models.DateField()",
    "o": [
      "A model representing a book with title, author, and published date fields",
      "A form for entering book details",
      "A database migration file",
      "A template for displaying books"
    ]
  },
  {
    "q": "Which field type would you use to create a relationship where one author can have many books?",
    "c": null,
    "o": [
      "models.ForeignKey",
      "models.OneToOneField",
      "models.ManyToManyField",
      "models.CharField"
    ]
  },
  {
    "q": "What is the purpose of running `python manage.py migrate`?",
    "c": null,
    "o": [
      "To apply changes defined in migration files to the database",
      "To generate new Django apps",
      "To create superusers",
      "To start the development server"
    ]
  },
  {
    "q": "Consider the following code snippet. What type of relationship does it define?",
    "c": "class Author(models.Model):\n    name = models.CharField(max_length=50)\n\nclass Book(models.Model):\n    title = models.CharField(max_length=100)\n    author = models.ForeignKey(Author, on_delete=models.CASCADE)",
    "o": [
      "One-to-many relationship (one author can have many books)",
      "Many-to-many relationship",
      "One-to-one relationship",
      "No relationship"
    ]
  },
  {
    "q": "Which Django field is suitable for storing long text content?",
    "c": null,
    "o": [
      "models.TextField",
      "models.CharField",
      "models.IntegerField",
      "models.DateTimeField"
    ]
  },
  {
    "q": "Which option is used to automatically add the current timestamp when a model instance is created?",
    "c": null,
    "o": [
      "models.DateTimeField(auto_now_add=True)",
      "models.DateTimeField(auto_now=True)",
      "models.DateField(default=datetime.now)",
      "models.TimeField(auto_create=True)"
    ]
  },
  {
    "q": "What will happen if you run `makemigrations` but forget to run `migrate`?",
    "c": null,
    "o": [
      "The migration files are created but the database schema is not updated",
      "The database schema is updated immediately",
      "The server will not start",
      "All models will be deleted"
    ]
  },
  {
    "q": "What is the effect of setting `on_delete=models.CASCADE` in a ForeignKey?",
    "c": null,
    "o": [
      "Deleting the referenced object will also delete related objects",
      "It prevents deletion of the referenced object",
      "It sets the related field to null when deleted",
      "It creates a Many-to-Many relationship automatically"
    ]
  },
  {
    "q": "Examine the model code. What will `author.books_set.all()` return?",
    "c": "class Author(models.Model):\n    name = models.CharField(max_length=50)\n\nclass Book(models.Model):\n    title = models.CharField(max_length=100)\n    author = models.ForeignKey(Author, on_delete=models.CASCADE)",
    "o": [
      "All books related to that author",
      "All authors related to that book",
      "An empty queryset",
      "The first book only"
    ]
  },
  {
    "q": "Which option correctly creates a many-to-many relationship between Book and Category models?",
    "c": "class Category(models.Model):\n    name = models.CharField(max_length=50)\n\nclass Book(models.Model):\n    title = models.CharField(max_length=100)",
    "o": [
      "categories = models.ManyToManyField(Category)",
      "category = models.ForeignKey(Category, on_delete=models.CASCADE)",
      "category = models.OneToOneField(Category, on_delete=models.CASCADE)",
      "category = models.TextField()"
    ]
  },
  {
    "q": "What is the difference between `auto_now` and `auto_now_add` in DateTimeField?",
    "c": null,
    "o": [
      "`auto_now_add` sets timestamp when creating, `auto_now` updates on every save",
      "`auto_now` sets timestamp when creating, `auto_now_add` updates on every save",
      "Both are identical",
      "Both are deprecated"
    ]
  },
  {
    "q": "What does the following model Meta option do?",
    "c": "class Book(models.Model):\n    title = models.CharField(max_length=100)\n\n    class Meta:\n        ordering = ['title']",
    "o": [
      "Automatically orders querysets by the title field",
      "Creates an index on the title field",
      "Prevents duplicate titles",
      "Sets the default database table name"
    ]
  },
  {
    "q": "Which method is used to manually create a migration file for a new model?",
    "c": null,
    "o": [
      "python manage.py makemigrations",
      "python manage.py migrate",
      "python manage.py createsuperuser",
      "python manage.py runserver"
    ]
  },
  {
    "q": "Which of the following will result in a database error if the related object is deleted?",
    "c": null,
    "o": [
      "models.ForeignKey(Author, on_delete=models.PROTECT)",
      "models.ForeignKey(Author, on_delete=models.CASCADE)",
      "models.ForeignKey(Author, on_delete=models.SET_NULL, null=True)",
      "models.ManyToManyField(Author)"
    ]
  },
  {
    "q": "What does the following ORM query do?",
    "c": "Book.objects.filter(author__name__icontains='John')",
    "o": [
      "Fetches all books whose author's name contains 'John', case-insensitive",
      "Fetches authors whose book title contains 'John'",
      "Updates books with author name containing 'John'",
      "Deletes books with author name 'John'"
    ]
  },
  {
    "q": "Which option will create a custom table name for a Django model?",
    "c": "class Book(models.Model):\n    title = models.CharField(max_length=100)\n\n    class Meta:\n        db_table = 'library_books'",
    "o": [
      "db_table in Meta class",
      "verbose_name in Meta class",
      "ordering in Meta class",
      "unique_together in Meta class"
    ]
  },
  {
    "q": "What happens if you rename a model field and run `makemigrations`?",
    "c": null,
    "o": [
      "Django creates a migration to rename the column in the database",
      "Django automatically deletes the old column without migration",
      "The server crashes on start",
      "Nothing happens until you delete the old migration files"
    ]
  },
  {
    "q": "Examine the code. What type of relationship does this define?",
    "c": "class Teacher(models.Model):\n    name = models.CharField(max_length=50)\n\nclass Subject(models.Model):\n    name = models.CharField(max_length=50)\n    teachers = models.ManyToManyField(Teacher, related_name='subjects')",
    "o": [
      "Many-to-Many relationship between Teacher and Subject",
      "One-to-One relationship",
      "One-to-Many relationship",
      "No relationship"
    ]
  },
  {
    "q": "Which option prevents duplicate combinations of fields in a Django model?",
    "c": "class Enrollment(models.Model):\n    student = models.ForeignKey(Student, on_delete=models.CASCADE)\n    course = models.ForeignKey(Course, on_delete=models.CASCADE)",
    "o": [
      "unique_together in Meta class",
      "ordering in Meta class",
      "db_table in Meta class",
      "verbose_name in Meta class"
    ]
  },
  {
    "q": "What is the effect of `select_related` in Django ORM queries?",
    "c": null,
    "o": [
      "Performs a SQL join and retrieves related ForeignKey objects in a single query",
      "Creates a new related object automatically",
      "Filters objects without hitting the database",
      "Deletes related objects automatically"
    ]
  },
  {
    "q": "Which of the following statements about Django migrations is TRUE?",
    "c": null,
    "o": [
      "Migrations track model changes and update the database schema",
      "Migrations automatically update Python code",
      "Migrations prevent server restarts",
      "Migrations are only used for Many-to-Many fields"
    ]
  },
  {
    "q": "What is the effect of setting `null=True` on a model field?",
    "c": null,
    "o": [
      "Allows the database column to store NULL values",
      "Prevents the field from being displayed in forms",
      "Automatically sets default values",
      "Enforces uniqueness for the field"
    ]
  },
  {
    "q": "Consider this model. What does `related_name='books_written'` allow?",
    "c": "class Author(models.Model):\n    name = models.CharField(max_length=50)\n\nclass Book(models.Model):\n    title = models.CharField(max_length=100)\n    author = models.ForeignKey(Author, on_delete=models.CASCADE, related_name='books_written')",
    "o": [
      "Access all books of an author via `author.books_written`",
      "Change the database table name",
      "Automatically orders books by title",
      "Prevents deleting an author"
    ]
  },
  {
    "q": "What does the `blank=True` option do in a Django model field?",
    "c": null,
    "o": [
      "Allows the field to be empty in forms and admin",
      "Makes the field read-only in the database",
      "Automatically populates the field with a default value",
      "Deletes the field if empty"
    ]
  },
  {
    "q": "Which command will show the SQL statements Django will execute for a migration?",
    "c": null,
    "o": [
      "python manage.py sqlmigrate <app_name> <migration_name>",
      "python manage.py showmigrations",
      "python manage.py makemigrations",
      "python manage.py dbshell"
    ]
  },
  {
    "q": "What happens if you set `ManyToManyField` with `through` parameter?",
    "c": null,
    "o": [
      "Django uses an explicit intermediate model for the relationship",
      "It creates a ForeignKey instead",
      "Prevents migrations from being created",
      "Automatically deletes all related objects on save"
    ]
  },
  {
    "q": "Examine the query. What does it do?",
    "c": "Book.objects.prefetch_related('categories').all()",
    "o": [
      "Fetches all books and preloads related categories in a separate query for efficiency",
      "Performs a SQL join to fetch categories in a single query",
      "Deletes all categories related to books",
      "Updates book titles based on categories"
    ]
  },
  {
    "q": "Which Meta option ensures the combination of two fields is unique in the database?",
    "c": "class Enrollment(models.Model):\n    student = models.ForeignKey(Student, on_delete=models.CASCADE)\n    course = models.ForeignKey(Course, on_delete=models.CASCADE)",
    "o": [
      "unique_together = (('student', 'course'),)",
      "ordering = ['student', 'course']",
      "db_table = 'enrollments'",
      "verbose_name = 'Enrollment'"
    ]
  },
  {
    "q": "Which Django ORM method will update multiple records at once without loading them into Python?",
    "c": null,
    "o": [
      "QuerySet.update()",
      "save() on each instance",
      "QuerySet.create()",
      "QuerySet.get()"
    ]
  },
  {
    "q": "What happens if you try to access a ForeignKey field that is NULL in the database?",
    "c": null,
    "o": [
      "It returns None",
      "It raises a ValueError",
      "It raises a DoesNotExist exception",
      "It automatically creates a related object"
    ]
  },
  {
    "q": "Which Django model field option enforces uniqueness at the database level?",
    "c": null,
    "o": [
      "unique=True",
      "primary_key=True",
      "db_index=True",
      "editable=False"
    ]
  },
  {
    "q": "What is the purpose of `get_absolute_url` in a Django model?",
    "c": null,
    "o": [
      "Returns the canonical URL for the model instance",
      "Creates a database index for the model",
      "Defines a primary key automatically",
      "Specifies related fields for ForeignKey"
    ]
  },
  {
    "q": "Which of the following statements about `OneToOneField` is TRUE?",
    "c": null,
    "o": [
      "Each instance of the model can only be linked to one instance of the related model",
      "It behaves identically to ManyToManyField",
      "It allows multiple instances to link to the same related object",
      "It automatically creates a new table for each instance"
    ]
  },
  {
    "q": "Consider the following model. What does `ordering = ['-published_date']` do?",
    "c": "class Article(models.Model):\n    title = models.CharField(max_length=100)\n    published_date = models.DateTimeField()\n\n    class Meta:\n        ordering = ['-published_date']",
    "o": [
      "Orders querysets in descending order of published_date by default",
      "Creates a database index on published_date",
      "Prevents duplicate published_date values",
      "Deletes older articles automatically"
    ]
  },
  {
    "q": "What does `select_for_update()` do in a Django ORM query?",
    "c": null,
    "o": [
      "Locks rows until the end of the transaction to prevent race conditions",
      "Deletes selected rows immediately",
      "Prefetches related objects for efficiency",
      "Orders the queryset for display"
    ]
  },
  {
    "q": "Which of the following commands will show all applied migrations for your project?",
    "c": null,
    "o": [
      "python manage.py showmigrations",
      "python manage.py sqlmigrate",
      "python manage.py makemigrations",
      "python manage.py migrate --plan"
    ]
  },
  {
    "q": "What happens if a model field has `default=datetime.now` without parentheses?",
    "c": null,
    "o": [
      "Each new instance uses the time when the model class was first loaded",
      "Each new instance gets the current time at creation",
      "The server raises a syntax error",
      "It automatically updates on save"
    ]
  },
  {
    "q": "What is the effect of setting `editable=False` on a model field?",
    "c": null,
    "o": [
      "Field will not appear in admin or forms but still exists in the database",
      "Field is completely removed from the database",
      "Field automatically generates default values",
      "Field is locked for updates only in the database"
    ]
  },
  {
    "q": "Which method should you override to customize how a model instance is displayed in the Django admin?",
    "c": null,
    "o": [
      "__str__",
      "__repr__",
      "save",
      "get_absolute_url"
    ]
  },
  {
    "q": "Which option in a Django model field allows null values in the database but requires input in forms?",
    "c": null,
    "o": [
      "null=True and blank=False",
      "null=False and blank=True",
      "null=True and blank=True",
      "null=False and blank=False"
    ]
  },
  {
    "q": "What does `db_index=True` do when added to a model field?",
    "c": null,
    "o": [
      "Creates a database index for faster lookups",
      "Prevents NULL values",
      "Automatically makes the field unique",
      "Deletes old data in that column"
    ]
  },
  {
    "q": "What does the following query do?",
    "c": "Book.objects.filter(author__isnull=True)",
    "o": [
      "Fetches all books that do not have an author assigned",
      "Fetches all books that have an author assigned",
      "Deletes books with no author",
      "Raises a syntax error"
    ]
  },
  {
    "q": "Which Meta option can be used to set a human-readable plural name for a model?",
    "c": null,
    "o": [
      "verbose_name_plural",
      "db_table",
      "ordering",
      "unique_together"
    ]
  },
  {
    "q": "What happens if you use `ManyToManyField` without migrations after creating the model?",
    "c": null,
    "o": [
      "The relationship table is not created in the database",
      "It works automatically without any issue",
      "All related objects are deleted",
      "Django raises a runtime exception immediately"
    ]
  },
  {
    "q": "Which of the following correctly limits the choices for a CharField in a Django model?",
    "c": null,
    "o": [
      "choices=[('A', 'Option A'), ('B', 'Option B')]",
      "limit_choices=[('A', 'Option A')]",
      "validators=[MaxLengthValidator(1)]",
      "default='A'"
    ]
  },
  {
    "q": "What is the effect of setting `primary_key=True` on a model field?",
    "c": null,
    "o": [
      "It designates the field as the table’s primary key and implies `unique=True` and `null=False`",
      "It allows duplicate values in that field",
      "It automatically creates an index but does not enforce uniqueness",
      "It removes the field from migrations"
    ]
  },
  {
    "q": "Which of the following statements about `OneToOneField` is TRUE?",
    "c": null,
    "o": [
      "Each instance of the model can be linked to exactly one instance of the related model",
      "It behaves identically to a ForeignKey with `unique=False`",
      "It allows multiple instances to reference the same related object",
      "It automatically creates a Many-to-Many table"
    ]
  },
  {
    "q": "What does the `through` parameter in ManyToManyField enable?",
    "c": null,
    "o": [
      "Specifies an explicit intermediate model to store relationship data",
      "Automatically populates the field with default related objects",
      "Prevents migrations from being created",
      "Automatically deletes related objects on save"
    ]
  },
  {
    "q": "Which of the following statements about `unique_together` is correct?",
    "c": null,
    "o": [
      "It enforces uniqueness for the combination of specified fields at the database level",
      "It prevents saving any object with a duplicate primary key",
      "It enforces a global uniqueness constraint across all models",
      "It automatically creates a ManyToMany relationship"
    ]
  },
  {
    "q": "What does the following query do?",
    "c": "Book.objects.exclude(author__name='John')",
    "o": [
      "Returns all books whose author is not named John",
      "Returns all books whose title does not contain 'John'",
      "Deletes books whose author is John",
      "Raises a syntax error"
    ]
  },
  {
    "q": "What is the purpose of `Meta.managed = False` in a Django model?",
    "c": null,
    "o": [
      "Django will not manage or create the database table for this model",
      "It prevents the model from being displayed in admin",
      "It automatically sets all fields as read-only",
      "It creates a temporary table for testing purposes"
    ]
  },
  {
    "q": "Which method allows filtering across reverse relationships in Django ORM?",
    "c": null,
    "o": [
      "Using double underscores (`__`) in filter queries",
      "Using `select_for_update()`",
      "Using `values_list()` only",
      "Using `annotate()` without `filter()`"
    ]
  },
  {
    "q": "What happens if you delete an object that has a OneToOneField pointing to it without `on_delete` specified?",
    "c": null,
    "o": [
      "Django raises an error because `on_delete` is required for OneToOneField",
      "It automatically deletes the related object",
      "It sets the related field to NULL",
      "It silently ignores the deletion"
    ]
  },
  {
    "q": "Which field option should you use to store a decimal value with fixed precision in Django models?",
    "c": null,
    "o": [
      "models.DecimalField(max_digits=10, decimal_places=2)",
      "models.FloatField()",
      "models.IntegerField()",
      "models.CharField(max_length=10)"
    ]
  },
  {
    "q": "What is the purpose of `validators` in a Django model field?",
    "c": null,
    "o": [
      "To enforce custom validation logic for field values",
      "To automatically generate form widgets",
      "To make a field unique",
      "To index a field in the database"
    ]
  },
  {
    "q": "Which of the following statements about `prefetch_related` is correct?",
    "c": null,
    "o": [
      "It fetches related objects in a separate query and caches them for efficiency",
      "It performs a SQL join like `select_related`",
      "It locks related rows for update",
      "It automatically deletes unused related objects"
    ]
  },
  {
    "q": "What does `db_column` in a model field do?",
    "c": null,
    "o": [
      "Specifies the actual column name to use in the database",
      "Creates a new table for the field",
      "Automatically indexes the field",
      "Renames the table for the model"
    ]
  },
  {
    "q": "Consider this model. What is the effect of `unique=True` on the `email` field?",
    "c": "class User(models.Model):\n    username = models.CharField(max_length=50)\n    email = models.EmailField(unique=True)",
    "o": [
      "Ensures no two users can have the same email address",
      "Prevents email from being null",
      "Automatically sets a default email",
      "Creates a ManyToMany relationship with username"
    ]
  },
  {
    "q": "Which option correctly describes `DateTimeField(auto_now=True)`?",
    "c": null,
    "o": [
      "Updates the field to the current timestamp every time the object is saved",
      "Sets the timestamp only when the object is created",
      "Prevents updates to the field",
      "Automatically deletes older records"
    ]
  },
  {
    "q": "What is the purpose of `related_name` in a Django model field?",
    "c": null,
    "o": [
      "Defines the reverse relationship name to access related objects",
      "Sets the database column name",
      "Specifies a default value",
      "Prevents null entries"
    ]
  },
  {
    "q": "Which of the following statements about `signals` and models is TRUE?",
    "c": null,
    "o": [
      "Signals can execute custom logic before or after saving or deleting model instances",
      "Signals automatically update foreign keys",
      "Signals replace the need for migrations",
      "Signals prevent database constraints from being enforced"
    ]
  },
  {
    "q": "Which of the following correctly enforces a default value for a Django model field?",
    "c": null,
    "o": [
      "default='Unknown'",
      "null=True",
      "blank=True",
      "editable=False"
    ]
  },
  {
    "q": "What is the purpose of the `unique_together` option in a model's Meta class?",
    "c": null,
    "o": [
      "To ensure that the combination of specified fields is unique across all records",
      "To create a ManyToMany relationship automatically",
      "To enforce uniqueness on a single field",
      "To prevent the model from being managed by Django"
    ]
  },
  {
    "q": "Which Django ORM method can be used to annotate additional calculated fields to a queryset?",
    "c": null,
    "o": [
      "annotate()",
      "aggregate()",
      "filter()",
      "exclude()"
    ]
  },
  {
    "q": "What does the `on_delete=models.SET_DEFAULT` option do for a ForeignKey?",
    "c": null,
    "o": [
      "Sets the field to its default value when the referenced object is deleted",
      "Deletes all related objects",
      "Raises an error on deletion",
      "Sets the field to NULL"
    ]
  },
  {
    "q": "What happens if you forget to add `null=True` for a ForeignKey that can be optional?",
    "c": null,
    "o": [
      "The database will require a value and throw an error if not provided",
      "Django will automatically set NULL",
      "The field will be ignored",
      "It automatically creates a default object"
    ]
  },
  {
    "q": "Which field type is most suitable for storing large binary files in a Django model?",
    "c": null,
    "o": [
      "models.BinaryField",
      "models.FileField",
      "models.TextField",
      "models.ImageField"
    ]
  },
  {
    "q": "What does `ordering = ['-created_at']` in a Meta class do?",
    "c": null,
    "o": [
      "Orders querysets by `created_at` in descending order by default",
      "Automatically sets `created_at` values",
      "Creates a database index on `created_at`",
      "Prevents updates to the `created_at` field"
    ]
  },
  {
    "q": "Which Django model method can you override to customize save behavior?",
    "c": null,
    "o": [
      "save()",
      "create()",
      "update()",
      "delete()"
    ]
  },
  {
    "q": "Which of the following is true about `ManyToManyField` with a `through` model?",
    "c": null,
    "o": [
      "It allows you to define additional fields on the relationship",
      "It automatically creates a ForeignKey instead",
      "It prevents the creation of migrations",
      "It deletes related objects automatically on save"
    ]
  },
  {
    "q": "What is the effect of `Meta.abstract = True` in a Django model?",
    "c": null,
    "o": [
      "The model will not create a database table and can be used as a base class",
      "It creates a temporary table for testing",
      "It automatically indexes all fields",
      "It prevents inheritance"
    ]
  },
  {
    "q": "Which of the following is true about `db_index=True`?",
    "c": null,
    "o": [
      "It creates a database index to improve query performance",
      "It prevents NULL values in the field",
      "It enforces uniqueness",
      "It automatically creates a ForeignKey relationship"
    ]
  },
  {
    "q": "What is the purpose of `unique_for_date`, `unique_for_month`, and `unique_for_year` in a DateField?",
    "c": null,
    "o": [
      "Enforces uniqueness of a field for a particular date, month, or year",
      "Automatically updates the date field on save",
      "Prevents NULL values in the DateField",
      "Creates a database index for the field"
    ]
  },
  {
    "q": "Which Django model field can automatically handle file uploads?",
    "c": null,
    "o": [
      "FileField",
      "TextField",
      "CharField",
      "BinaryField"
    ]
  },
  {
    "q": "Which Meta option is used to specify the human-readable singular name of a model?",
    "c": null,
    "o": [
      "verbose_name",
      "verbose_name_plural",
      "db_table",
      "ordering"
    ]
  },
  {
    "q": "Which of the following queries will efficiently fetch related ForeignKey objects in a single database hit?",
    "c": null,
    "o": [
      "select_related()",
      "prefetch_related()",
      "annotate()",
      "aggregate()"
    ]
  },
  {
    "q": "What does `Model.objects.get_or_create()` do?",
    "c": null,
    "o": [
      "Fetches an object if it exists, otherwise creates a new one",
      "Always creates a new object",
      "Deletes the object if it exists",
      "Fetches all objects matching the filter"
    ]
  },
  {
    "q": "Which Django model field type would you use to store email addresses?",
    "c": null,
    "o": [
      "EmailField",
      "CharField",
      "TextField",
      "URLField"
    ]
  },
  {
    "q": "What is the purpose of `auto_now_add=True` in a DateTimeField?",
    "c": null,
    "o": [
      "Sets the field to the current timestamp only when the object is first created",
      "Updates the timestamp every time the object is saved",
      "Prevents the field from being null",
      "Automatically deletes old records"
    ]
  },
  {
    "q": "What does `Model.objects.bulk_create()` do?",
    "c": null,
    "o": [
      "Inserts multiple objects into the database efficiently in a single query",
      "Updates multiple existing objects at once",
      "Deletes multiple objects at once",
      "Fetches multiple objects at once"
    ]
  },
  {
    "q": "Which of the following statements about `ForeignKey` is TRUE?",
    "c": null,
    "o": [
      "It creates a one-to-many relationship between models",
      "It creates a many-to-many relationship automatically",
      "It creates a one-to-one relationship automatically",
      "It cannot be used with `on_delete`"
    ]
  },
  {
    "q": "Which option is used to enforce a unique constraint on a single field?",
    "c": null,
    "o": [
      "unique=True",
      "db_index=True",
      "editable=False",
      "null=True"
    ]
  },
  {
    "q": "Which method should you override to customize deletion behavior of a model instance?",
    "c": null,
    "o": [
      "delete()",
      "save()",
      "create()",
      "update()"
    ]
  },
  {
    "q": "What does `Model.objects.aggregate(Sum('field'))` do?",
    "c": null,
    "o": [
      "Returns the sum of all values in the specified field",
      "Updates all field values to their sum",
      "Deletes all objects if the sum exceeds a limit",
      "Creates a new field with the sum value"
    ]
  },
  {
    "q": "Which field type is suitable for storing URLs in Django models?",
    "c": null,
    "o": [
      "URLField",
      "CharField",
      "TextField",
      "EmailField"
    ]
  },
  {
    "q": "What is a proxy model in Django?",
    "c": null,
    "o": [
      "A model that uses an existing database table but allows different Python behavior",
      "A model that creates a separate database table",
      "A model that automatically inherits all ForeignKey relationships",
      "A model that cannot be queried using ORM"
    ]
  },
  {
    "q": "Which option is required to make a Django model a proxy model?",
    "c": null,
    "o": [
      "proxy = True in the Meta class",
      "abstract = True in the Meta class",
      "managed = False in the Meta class",
      "db_table specified in the Meta class"
    ]
  },
  {
    "q": "Which feature allows you to add custom query methods to a model?",
    "c": null,
    "o": [
      "Custom model managers",
      "Meta ordering",
      "Model inheritance",
      "Field choices"
    ]
  },
  {
    "q": "Which of the following is TRUE about model inheritance in Django?",
    "c": null,
    "o": [
      "Child models can inherit fields and methods from parent models",
      "Child models always use the same database table as the parent",
      "Parent models cannot have ForeignKey fields",
      "Inheritance automatically prevents migrations"
    ]
  },
  {
    "q": "How do you limit a CharField to specific allowed values?",
    "c": null,
    "o": [
      "Use the choices parameter, e.g., choices=[('A', 'Option A'), ('B', 'Option B')]",
      "Use max_length",
      "Use unique=True",
      "Use null=True"
    ]
  },
  {
    "q": "What is the purpose of Django signals like `pre_save` and `post_save`?",
    "c": null,
    "o": [
      "To execute custom logic before or after a model instance is saved",
      "To automatically update ForeignKey fields",
      "To replace the need for migrations",
      "To prevent database constraints from being enforced"
    ]
  },
  {
    "q": "Which of the following is TRUE about `managed = False` in a model's Meta class?",
    "c": null,
    "o": [
      "Django does not manage the database table for this model, useful for legacy tables",
      "Django automatically creates and deletes the table",
      "It prevents any queries from being executed",
      "It makes all fields read-only in the admin"
    ]
  },
  {
    "q": "How can you add a custom method that is accessible via a model’s queryset?",
    "c": null,
    "o": [
      "Define a custom manager and attach it to the model",
      "Override the save() method",
      "Use Meta ordering",
      "Use choices in the field"
    ]
  },
  {
    "q": "What is multi-table inheritance in Django?",
    "c": null,
    "o": [
      "Each model in the inheritance chain gets its own database table",
      "All fields from parent and child are stored in a single table",
      "It only works for abstract models",
      "It automatically creates a proxy model"
    ]
  },
  {
    "q": "Which Django feature allows a model to relate to multiple other models without specifying them directly?",
    "c": null,
    "o": [
      "GenericForeignKey from ContentTypes framework",
      "ForeignKey with null=True",
      "ManyToManyField with through model",
      "OneToOneField with related_name"
    ]
  },
  {
    "q": "How can you create a custom field type in Django?",
    "c": null,
    "o": [
      "By subclassing models.Field and implementing get_prep_value and from_db_value",
      "By adding Meta options",
      "By setting unique=True",
      "By using a proxy model"
    ]
  },
  {
    "q": "Which method allows you to perform aggregation such as Sum, Avg, Max, Min in Django ORM?",
    "c": null,
    "o": [
      "aggregate()",
      "annotate()",
      "select_related()",
      "prefetch_related()"
    ]
  },
  {
    "q": "Which of the following is TRUE about `GenericRelation` in Django?",
    "c": null,
    "o": [
      "It allows reverse access from a model to objects of another model using a GenericForeignKey",
      "It creates a ForeignKey to multiple tables automatically",
      "It replaces ManyToManyField",
      "It only works with abstract models"
    ]
  },
  {
    "q": "What is the effect of overriding `get_queryset()` in a custom model manager?",
    "c": null,
    "o": [
      "You can define default filters or behavior for all queries using that manager",
      "It prevents migrations from being applied",
      "It automatically deletes old records",
      "It disables ForeignKey relationships"
    ]
  },
  {
    "q": "Which field type is best suited for storing JSON data in Django?",
    "c": null,
    "o": [
      "JSONField",
      "TextField",
      "CharField",
      "BinaryField"
    ]
  },
  {
    "q": "What is the primary purpose of using `annotate()` in a queryset?",
    "c": null,
    "o": [
      "To calculate additional fields for each object in the queryset",
      "To fetch all related ForeignKey objects in a single query",
      "To create new database tables dynamically",
      "To delete aggregated rows automatically"
    ]
  },
  {
    "q": "Which Meta option allows you to create a database-level unique constraint on multiple fields in Django 2.2+?",
    "c": null,
    "o": [
      "constraints = [models.UniqueConstraint(fields=['field1','field2'], name='unique_combination')]",
      "unique_together = ('field1','field2')",
      "db_index = True",
      "ordering = ['field1']"
    ]
  },
  {
    "q": "What is the purpose of a CheckConstraint in Django models?",
    "c": null,
    "o": [
      "To enforce a condition on field values at the database level",
      "To create a ForeignKey relationship",
      "To enforce ordering of querysets",
      "To automatically populate default values"
    ]
  },
  {
    "q": "Which of the following can be used to perform complex queries using conditional expressions?",
    "c": null,
    "o": [
      "F and Q objects",
      "annotate() only",
      "aggregate() only",
      "select_related()"
    ]
  },
  {
    "q": "Which Django function allows you to perform database-level arithmetic operations in queries?",
    "c": null,
    "o": [
      "F expressions",
      "Q expressions",
      "annotate()",
      "prefetch_related()"
    ]
  },
  {
    "q": "What is the effect of `db_index=True` on a model field?",
    "c": null,
    "o": [
      "Creates a database index to improve query performance",
      "Prevents NULL values",
      "Makes the field unique",
      "Automatically creates a ForeignKey relationship"
    ]
  },
  {
    "q": "Which database function can be used in Django ORM to perform case-insensitive string comparisons?",
    "c": null,
    "o": [
      "Lower() and Upper()",
      "Concat()",
      "Length()",
      "Coalesce()"
    ]
  },
  {
    "q": "What is the purpose of `UniqueConstraint` with `conditions` in Django 3.0+?",
    "c": null,
    "o": [
      "To enforce uniqueness only when certain conditions are met",
      "To always enforce uniqueness globally",
      "To enforce uniqueness at the Python level only",
      "To create a ManyToMany relationship automatically"
    ]
  },
  {
    "q": "Which ORM method allows combining multiple Q objects for complex filters?",
    "c": null,
    "o": [
      "filter(Q(...) & Q(...)) or filter(Q(...) | Q(...))",
      "annotate(Q(...))",
      "aggregate(Q(...))",
      "select_related(Q(...))"
    ]
  },
  {
    "q": "What does `select_for_update()` do in Django ORM?",
    "c": null,
    "o": [
      "Locks the selected rows until the end of the transaction to prevent race conditions",
      "Deletes the selected rows immediately",
      "Prefetches related objects for efficiency",
      "Automatically orders the queryset"
    ]
  },
  {
    "q": "Which `on_delete` option prevents deletion of a referenced object if related objects exist?",
    "c": null,
    "o": [
      "models.PROTECT",
      "models.CASCADE",
      "models.SET_NULL",
      "models.DO_NOTHING"
    ]
  },
  {
    "q": "What happens if you use `defer('field_name')` in a Django queryset?",
    "c": null,
    "o": [
      "The specified field is not loaded from the database until accessed",
      "The field is automatically deleted",
      "The field is set to NULL for all objects",
      "The field is included in all query results as usual"
    ]
  },
  {
    "q": "Which method is used to query JSONField in Django?",
    "c": null,
    "o": [
      "Using key lookups, e.g., Model.objects.filter(json_field__key='value')",
      "Using ForeignKey lookups",
      "Using defer()",
      "Using select_related()"
    ]
  },
  {
    "q": "What is the effect of `models.SET_DEFAULT` for a ForeignKey when the related object is deleted?",
    "c": null,
    "o": [
      "Sets the field to its default value",
      "Deletes the related object automatically",
      "Raises a database error",
      "Sets the field to NULL"
    ]
  },
  {
    "q": "Which Django ORM method can be used to efficiently bulk update multiple rows without fetching them into Python?",
    "c": null,
    "o": [
      "QuerySet.update()",
      "save() on each instance",
      "bulk_create()",
      "annotate()"
    ]
  },
  {
    "q": "Which option allows deferred fields to be loaded later individually?",
    "c": null,
    "o": [
      "only('field_name')",
      "select_related()",
      "prefetch_related()",
      "aggregate()"
    ]
  },
  {
    "q": "Which cascading option sets a ForeignKey to NULL when the referenced object is deleted?",
    "c": null,
    "o": [
      "models.SET_NULL",
      "models.CASCADE",
      "models.PROTECT",
      "models.DO_NOTHING"
    ]
  },
  {
    "q": "Which Django feature allows creating indexes on expressions or computed fields?",
    "c": null,
    "o": [
      "Index(fields=['field1'], name='idx_name', opclasses=['text_pattern_ops'])",
      "db_index=True",
      "unique=True",
      "ordering in Meta"
    ]
  },
  {
    "q": "What does `UniqueConstraint` with `condition` allow in Django models?",
    "c": null,
    "o": [
      "Enforces uniqueness only when the condition is met",
      "Enforces uniqueness for all rows regardless of condition",
      "Automatically deletes rows violating uniqueness",
      "Prevents creation of migrations"
    ]
  },
  {
    "q": "Which database function can you use to calculate the length of a string field in Django ORM?",
    "c": null,
    "o": [
      "Length()",
      "Concat()",
      "Lower()",
      "Coalesce()"
    ]
  },
  {
    "q": "Which database function allows concatenating two or more fields in Django ORM?",
    "c": null,
    "o": [
      "Concat()",
      "Length()",
      "Upper()",
      "Lower()"
    ]
  },
  {
    "q": "How can you filter ManyToManyField relationships based on a condition in Django ORM?",
    "c": null,
    "o": [
      "Use the `through` model and filter() on the related model",
      "Use defer() on the ManyToManyField",
      "Use aggregate()",
      "Use select_for_update()"
    ]
  },
  {
    "q": "Which database function can be used to coalesce NULL values to a default in Django ORM?",
    "c": null,
    "o": [
      "Coalesce()",
      "Concat()",
      "Length()",
      "Lower()"
    ]
  },
  {
    "q": "Which Django Meta option allows specifying multiple indexes on a model?",
    "c": null,
    "o": [
      "indexes = [models.Index(fields=['field1']), models.Index(fields=['field2'])]",
      "db_index=True",
      "unique_together",
      "ordering"
    ]
  },
  {
    "q": "Which ORM function allows conditional aggregation based on a filter in Django?",
    "c": null,
    "o": [
      "Filter argument inside aggregate(), e.g., Sum('field', filter=Q(...))",
      "annotate() without Q",
      "select_related()",
      "prefetch_related()"
    ]
  },
  {
    "q": "Which of the following correctly defines a self-referential ForeignKey in Django?",
    "c": null,
    "o": [
      "parent = models.ForeignKey('self', on_delete=models.CASCADE, null=True, blank=True)",
      "parent = models.OneToOneField('Parent', on_delete=models.CASCADE)",
      "parent = models.ManyToManyField('self')",
      "parent = models.ForeignKey('Parent', on_delete=models.CASCADE)"
    ]
  },
  {
    "q": "How can you perform a recursive relationship query in Django ORM?",
    "c": null,
    "o": [
      "Use a self-referential ForeignKey and follow relationships with double underscores in filter()",
      "Use select_related() without filter",
      "Use annotate() only",
      "Use prefetch_related() with aggregate()"
    ]
  },
  {
    "q": "Which Django feature allows directing database operations to different databases?",
    "c": null,
    "o": [
      "Custom database routers",
      "Meta.db_table",
      "Meta.indexes",
      "select_for_update()"
    ]
  },
  {
    "q": "Which transform allows case-insensitive string containment queries in Django ORM?",
    "c": null,
    "o": [
      "__icontains",
      "__contains",
      "__iexact",
      "__exact"
    ]
  },
  {
    "q": "Which of the following is TRUE about self-referential ManyToManyField?",
    "c": null,
    "o": [
      "It allows a model to relate to other instances of the same model",
      "It automatically prevents duplicate entries",
      "It can only be used with abstract models",
      "It does not require migrations"
    ]
  },
  {
    "q": "Which of the following is a valid way to perform an aggregate over a related model in Django ORM?",
    "c": null,
    "o": [
      "Model.objects.annotate(total=Sum('related_model__field'))",
      "Model.objects.filter(Sum('related_model__field'))",
      "Model.objects.prefetch_related(Sum('related_model__field'))",
      "Model.objects.defer(Sum('related_model__field'))"
    ]
  },
  {
    "q": "Which field lookup allows querying for null values in Django ORM?",
    "c": null,
    "o": [
      "__isnull",
      "__exact",
      "__iexact",
      "__icontains"
    ]
  },
  {
    "q": "Which of the following statements about custom database routers is TRUE?",
    "c": null,
    "o": [
      "They allow controlling which database handles read and write operations for each model",
      "They automatically create indexes for all models",
      "They enforce uniqueness across databases",
      "They replace the need for migrations"
    ]
  },
  {
    "q": "Which signal is sent just before a model’s `save()` method is called?",
    "c": null,
    "o": [
      "pre_save",
      "post_save",
      "pre_delete",
      "post_delete"
    ]
  },
  {
    "q": "Which signal is triggered after a model instance is deleted?",
    "c": null,
    "o": [
      "post_delete",
      "pre_delete",
      "pre_save",
      "post_save"
    ]
  },
  {
    "q": "Which of the following is true about multi-table inheritance in Django?",
    "c": null,
    "o": [
      "Each model in the inheritance hierarchy gets its own database table",
      "All fields are combined into a single table automatically",
      "It only works with abstract models",
      "It cannot have ForeignKey fields"
    ]
  },
  {
    "q": "What happens when you combine `defer()` and `only()` in a queryset?",
    "c": null,
    "o": [
      "You can load only specific fields immediately and defer others until accessed",
      "It deletes the deferred fields automatically",
      "It forces all fields to be loaded",
      "It raises a runtime error"
    ]
  },
  {
    "q": "Which method allows querying nested keys inside a JSONField?",
    "c": null,
    "o": [
      "Use double underscores for key lookups, e.g., json_field__key__subkey='value'",
      "Use select_related()",
      "Use defer()",
      "Use aggregate()"
    ]
  },
  {
    "q": "Which signal would you use to perform an action immediately after a model instance is saved?",
    "c": null,
    "o": [
      "post_save",
      "pre_save",
      "post_delete",
      "pre_delete"
    ]
  },
  {
    "q": "What happens if two child models inherit from the same parent model using multi-table inheritance?",
    "c": null,
    "o": [
      "Each child gets its own table and a OneToOneField linking to the parent table",
      "All children share a single table",
      "It raises an error during migration",
      "The parent table is ignored"
    ]
  },
  {
    "q": "Which of the following is TRUE about JSONField lookups?",
    "c": null,
    "o": [
      "You can filter nested keys, check for existence, and perform path lookups",
      "You cannot filter JSONField at all",
      "You can only check top-level keys with contains",
      "It automatically converts JSON to Python dict for queries"
    ]
  },
  {
    "q": "Which of the following allows performing arithmetic operations on model fields at the database level?",
    "c": null,
    "o": [
      "F expressions",
      "Q expressions",
      "annotate() only",
      "aggregate() only"
    ]
  },
  {
    "q": "How can you chain multiple database functions in Django ORM?",
    "c": null,
    "o": [
      "By nesting functions inside annotate() or annotate() with expressions, e.g., Upper(Lower('field'))",
      "By using select_related() only",
      "By using prefetch_related() only",
      "By using defer()"
    ]
  },
  {
    "q": "Which of the following allows conditional aggregation in Django ORM?",
    "c": null,
    "o": [
      "Using aggregate() or annotate() with filter=Q(...) inside the aggregation function",
      "Using select_related() only",
      "Using defer() only",
      "Using prefetch_related() only"
    ]
  },
  {
    "q": "How can you query a ManyToMany relationship with a `through` model conditionally?",
    "c": null,
    "o": [
      "Filter on the `through` model and access related objects via the related manager",
      "Use select_related() on the ManyToMany field",
      "Use defer() on the ManyToMany field",
      "Aggregate() directly on the ManyToMany field"
    ]
  },
  {
    "q": "Which of the following is TRUE about using F expressions with conditional logic?",
    "c": null,
    "o": [
      "You can use F expressions inside Case and When expressions for database-side conditional updates",
      "F expressions can only be used for reading values",
      "F expressions cannot be combined with aggregate()",
      "F expressions automatically lock rows for update"
    ]
  },
  {
    "q": "Which Django function allows you to perform a SQL COALESCE operation in ORM?",
    "c": null,
    "o": [
      "Coalesce()",
      "Concat()",
      "Length()",
      "Lower()"
    ]
  },
  {
    "q": "Which of the following statements is TRUE about ManyToMany `through` models?",
    "c": null,
    "o": [
      "They allow storing additional fields on the relationship and filtering based on them",
      "They automatically delete all related objects on save",
      "They replace ForeignKey fields",
      "They do not require migrations"
    ]
  },
  {
    "q": "Which expression allows updating a model field conditionally based on other fields in the same table?",
    "c": null,
    "o": [
      "Case and When expressions combined with F expressions",
      "Q expressions only",
      "aggregate() only",
      "prefetch_related() only"
    ]
  },
  {
    "q": "Which of the following allows creating a conditional unique constraint in Django?",
    "c": null,
    "o": [
      "UniqueConstraint with a condition parameter in the Meta class",
      "unique_together only",
      "db_index=True",
      "ordering in Meta"
    ]
  },
  {
    "q": "Which feature allows creating a database index on a computed expression instead of a field?",
    "c": null,
    "o": [
      "Index with expressions in the Meta class",
      "db_index=True",
      "unique=True",
      "select_for_update()"
    ]
  },
  {
    "q": "How can you defer a JSONField and load its nested keys only when needed?",
    "c": null,
    "o": [
      "Use defer() on the JSONField and access nested keys later via key lookups",
      "Use select_related()",
      "Use aggregate()",
      "Use annotate()"
    ]
  },
  {
    "q": "Which of the following allows a model to reference itself recursively in a ManyToMany relationship?",
    "c": null,
    "o": [
      "ManyToManyField('self', symmetrical=False, through='ThroughModel')",
      "ForeignKey('self')",
      "OneToOneField('self')",
      "GenericForeignKey('self')"
    ]
  },
  {
    "q": "Which of the following expressions allows conditional updates of a field based on the value of another field?",
    "c": null,
    "o": [
      "Case and When combined with F expressions",
      "Q expressions only",
      "aggregate() only",
      "select_related() only"
    ]
  },
  {
    "q": "Which Meta option allows specifying multiple complex constraints, including CheckConstraint and UniqueConstraint?",
    "c": null,
    "o": [
      "constraints = [CheckConstraint(...), UniqueConstraint(...)]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "How can you filter objects in a recursive self-referential ForeignKey relationship?",
    "c": null,
    "o": [
      "Use double underscores to traverse the relationship, e.g., parent__parent__field=value",
      "Use defer() on the field",
      "Use aggregate() only",
      "Use select_related() only"
    ]
  },
  {
    "q": "Which of the following statements about deferred fields is TRUE?",
    "c": null,
    "o": [
      "Deferred fields are not loaded from the database until accessed, improving query performance",
      "Deferred fields are automatically deleted",
      "Deferred fields cannot be JSONField",
      "Deferred fields are always loaded immediately"
    ]
  },
  {
    "q": "Which of the following is true about `through_defaults` in ManyToManyField's add() method?",
    "c": null,
    "o": [
      "It allows specifying default values for fields in the `through` model when adding relationships",
      "It automatically creates a new ManyToMany table",
      "It prevents adding duplicates in all cases",
      "It is used to delete existing relations"
    ]
  },
  {
    "q": "What happens if a `ForeignKey` has `on_delete=models.DO_NOTHING` and the referenced object is deleted?",
    "c": null,
    "o": [
      "No automatic action is taken; database integrity may be violated",
      "The field is set to NULL",
      "All related objects are deleted",
      "Django raises a runtime error immediately"
    ]
  },
  {
    "q": "Which of the following is TRUE about using `db_tablespace` in a Django model field?",
    "c": null,
    "o": [
      "It allows specifying the database tablespace for storing the field",
      "It automatically creates indexes for the field",
      "It enforces uniqueness at the database level",
      "It prevents migrations for the field"
    ]
  },
  {
    "q": "Which of the following correctly defines a self-referential ManyToMany relationship with a `through` model?",
    "c": null,
    "o": [
      "friends = models.ManyToManyField('self', through='Friendship', symmetrical=False)",
      "friends = models.ForeignKey('self', through='Friendship')",
      "friends = models.ManyToManyField('self') without through",
      "friends = models.OneToOneField('self', through='Friendship')"
    ]
  },
  {
    "q": "Which ORM method can be used to update fields conditionally for multiple rows in a single query?",
    "c": null,
    "o": [
      "update() with Case and When expressions",
      "save() on each instance",
      "bulk_create()",
      "annotate() only"
    ]
  },
  {
    "q": "Which Meta option allows specifying a custom database table name for a Django model?",
    "c": null,
    "o": [
      "db_table",
      "ordering",
      "unique_together",
      "verbose_name"
    ]
  },
  {
    "q": "Which of the following allows creating a filtered index in Django 3.2+?",
    "c": null,
    "o": [
      "models.Index(fields=['field'], name='idx_name', condition=Q(active=True))",
      "db_index=True",
      "unique=True",
      "ordering in Meta"
    ]
  },
  {
    "q": "Which of the following is TRUE about `select_for_update(of=...)`?",
    "c": null,
    "o": [
      "It allows locking specific tables or rows in a multi-table query for transactional safety",
      "It deletes selected rows immediately",
      "It prefetches related objects automatically",
      "It automatically updates related ForeignKey fields"
    ]
  },
  {
    "q": "Which transform allows performing case-insensitive startswith queries in Django ORM?",
    "c": null,
    "o": [
      "__istartswith",
      "__startswith",
      "__icontains",
      "__iexact"
    ]
  },
  {
    "q": "Which database function can be used to return the first non-null value among multiple fields in Django ORM?",
    "c": null,
    "o": [
      "Coalesce()",
      "Concat()",
      "Length()",
      "Lower()"
    ]
  },
  {
    "q": "Which ORM feature allows filtering JSONField for a nested key without loading the entire field?",
    "c": null,
    "o": [
      "Key lookups using double underscores, e.g., json_field__key__subkey='value'",
      "aggregate()",
      "annotate()",
      "defer() without key lookups"
    ]
  },
  {
    "q": "What happens when you combine `only()` with `select_related()` in a queryset?",
    "c": null,
    "o": [
      "Only specified fields of the main model are loaded, and related objects are fetched via join",
      "All fields are loaded normally",
      "select_related() is ignored",
      "It raises a runtime error"
    ]
  },
  {
    "q": "Which database function allows performing string concatenation of multiple fields in Django ORM?",
    "c": null,
    "o": [
      "Concat()",
      "Length()",
      "Coalesce()",
      "Lower()"
    ]
  },
  {
    "q": "Which of the following allows performing arithmetic updates conditionally on a queryset?",
    "c": null,
    "o": [
      "update() with Case and When expressions using F() objects",
      "annotate() only",
      "aggregate() only",
      "select_for_update() only"
    ]
  },
  {
    "q": "Which field transform can be used to perform exact matches ignoring case?",
    "c": null,
    "o": [
      "__iexact",
      "__exact",
      "__icontains",
      "__contains"
    ]
  },
  {
    "q": "Which feature allows deferring certain fields while eagerly loading related objects?",
    "c": null,
    "o": [
      "Combination of only() and select_related()",
      "defer() alone without select_related()",
      "aggregate()",
      "annotate()"
    ]
  },
  {
    "q": "Which of the following allows filtering across multiple tables efficiently in Django ORM?",
    "c": null,
    "o": [
      "select_related() for ForeignKey joins and prefetch_related() for ManyToMany relationships",
      "defer() only",
      "aggregate() only",
      "annotate() only"
    ]
  },
  {
    "q": "Which Meta option allows defining a partial index with a condition in Django 3.2+?",
    "c": null,
    "o": [
      "indexes = [models.Index(fields=['field'], condition=Q(active=True))]",
      "unique_together",
      "ordering",
      "db_table"
    ]
  },
  {
    "q": "Which feature allows adding custom methods to a queryset for a model?",
    "c": null,
    "o": [
      "Custom model managers with get_queryset() overridden",
      "Meta ordering",
      "Field choices",
      "Proxy models only"
    ]
  },
  {
    "q": "In multi-table inheritance, how is a child model linked to its parent?",
    "c": null,
    "o": [
      "Via an implicit OneToOneField automatically created linking child to parent table",
      "They share the same table automatically",
      "No database link is created",
      "Via a ForeignKey that must be manually defined"
    ]
  },
  {
    "q": "Which of the following allows performing conditional updates on multiple rows using ORM?",
    "c": null,
    "o": [
      "update() with Case and When expressions",
      "save() on each instance individually",
      "bulk_create()",
      "aggregate() only"
    ]
  },
  {
    "q": "Which of the following is TRUE about filtered ManyToMany relationships using a `through` model?",
    "c": null,
    "o": [
      "You can filter relationships by fields in the `through` model",
      "You cannot filter through models",
      "Filtering is only allowed on the related model",
      "It automatically deletes related objects when filtering"
    ]
  },
  {
    "q": "Which database function allows converting a string field to uppercase in a queryset annotation?",
    "c": null,
    "o": [
      "Upper()",
      "Lower()",
      "Concat()",
      "Coalesce()"
    ]
  },
  {
    "q": "Which of the following is TRUE about custom managers in Django?",
    "c": null,
    "o": [
      "They can override get_queryset() to define default query behavior",
      "They automatically create indexes",
      "They replace the need for migrations",
      "They can only be used with proxy models"
    ]
  },
  {
    "q": "Which of the following is TRUE about `unique_for_date`, `unique_for_month`, and `unique_for_year`?",
    "c": null,
    "o": [
      "They enforce uniqueness of a field relative to a DateField or DateTimeField",
      "They enforce global uniqueness across all records",
      "They automatically create indexes on the fields",
      "They only work with ForeignKey fields"
    ]
  },
  {
    "q": "Which of the following allows using a database function inside an annotation for a queryset?",
    "c": null,
    "o": [
      "annotate(field_upper=Upper('field'))",
      "aggregate(field_upper=Upper('field')) only",
      "select_related()",
      "prefetch_related()"
    ]
  },
  {
    "q": "What is the purpose of `related_name` in a ForeignKey or ManyToManyField?",
    "c": null,
    "o": [
      "Defines the reverse relationship name to access related objects",
      "Specifies the database table name",
      "Creates a unique constraint automatically",
      "Prevents migrations from being created"
    ]
  },
  {
    "q": "Which of the following statements about `editable=False` is TRUE?",
    "c": null,
    "o": [
      "The field will not appear in ModelForms or admin forms, but is still stored in the database",
      "The field cannot be saved to the database",
      "The field is automatically unique",
      "The field will be excluded from migrations"
    ]
  },
  {
    "q": "Which Meta option prevents Django from managing a database table?",
    "c": null,
    "o": [
      "managed = False",
      "abstract = True",
      "proxy = True",
      "db_table = 'custom_name'"
    ]
  },
  {
    "q": "Which of the following allows performing an EXISTS subquery in Django ORM?",
    "c": null,
    "o": [
      "Subquery with Exists expression",
      "annotate() only",
      "aggregate() only",
      "select_related() only"
    ]
  },
  {
    "q": "Which option in a ManyToManyField allows specifying a custom intermediate table with additional fields?",
    "c": null,
    "o": [
      "through",
      "related_name",
      "db_index",
      "editable=False"
    ]
  },
  {
    "q": "Which of the following statements about `db_constraint=False` is TRUE?",
    "c": null,
    "o": [
      "It disables database-level foreign key constraints while keeping ORM-level validation",
      "It prevents the field from being saved to the database",
      "It makes the field unique automatically",
      "It automatically creates an index"
    ]
  },
  {
    "q": "Which of the following allows deferring multiple fields while fetching related objects?",
    "c": null,
    "o": [
      "Use defer('field1', 'field2') together with select_related() for related objects",
      "Use only('field1', 'field2') alone",
      "Use annotate() with the fields",
      "Use aggregate() with the fields"
    ]
  },
  {
    "q": "Which Meta option allows enforcing uniqueness only under certain conditions?",
    "c": null,
    "o": [
      "UniqueConstraint with condition=Q(...) in the Meta class",
      "unique=True on the field",
      "unique_together only",
      "db_index=True"
    ]
  },
  {
    "q": "How can you query self-referential ForeignKey relationships recursively?",
    "c": null,
    "o": [
      "Use double underscores to traverse multiple levels, e.g., parent__parent__field=value",
      "Use defer() on the field",
      "Use aggregate() only",
      "Use select_related() only"
    ]
  },
  {
    "q": "Which of the following allows querying nested keys in a JSONField efficiently?",
    "c": null,
    "o": [
      "Use double underscore key lookups, e.g., json_field__key__subkey='value'",
      "Use defer() only",
      "Use select_related() only",
      "Use aggregate() only"
    ]
  },
  {
    "q": "Which combination allows loading only specific fields of the main model and related models efficiently?",
    "c": null,
    "o": [
      "only('field1').select_related('related_model')",
      "defer('field1').aggregate('related_model')",
      "annotate('field1').select_related('related_model')",
      "prefetch_related('related_model').aggregate('field1')"
    ]
  },
  {
    "q": "Which of the following statements about conditional updates in Django ORM is TRUE?",
    "c": null,
    "o": [
      "You can use update() with Case and When expressions combined with F expressions for database-side conditional updates",
      "update() always updates all rows unconditionally",
      "save() automatically performs conditional updates",
      "aggregate() can be used to update rows conditionally"
    ]
  },
  {
    "q": "Which feature allows creating indexes specifically for JSONField keys?",
    "c": null,
    "o": [
      "Use GinIndex or BTreeIndex with expressions targeting JSONField keys",
      "db_index=True automatically creates JSON indexes",
      "unique=True creates JSONField indexes",
      "defer() creates an index dynamically"
    ]
  },
  {
    "q": "Which of the following is TRUE about self-referential ManyToMany relationships?",
    "c": null,
    "o": [
      "They allow a model to relate to other instances of the same model and can use a through model for extra fields",
      "They only work with abstract models",
      "They automatically prevent duplicates without a through model",
      "They cannot be queried recursively"
    ]
  },
  {
    "q": "Which of the following allows performing arithmetic calculations on fields during a queryset update?",
    "c": null,
    "o": [
      "F expressions combined with update()",
      "annotate() only",
      "aggregate() only",
      "select_related() only"
    ]
  },
  {
    "q": "How can you chain multiple database functions in a Django queryset annotation?",
    "c": null,
    "o": [
      "By nesting them inside each other, e.g., Upper(Lower('field')) in annotate()",
      "Using defer() only",
      "Using select_related() only",
      "Using aggregate() only"
    ]
  },
  {
    "q": "Which of the following is TRUE about using Case and When expressions with F expressions?",
    "c": null,
    "o": [
      "They allow conditional updates at the database level without fetching objects into Python",
      "They require fetching objects into Python first",
      "They cannot be combined with annotate()",
      "They only work on JSONField"
    ]
  },
  {
    "q": "Which Meta option in multi-table inheritance prevents Django from automatically creating a parent table?",
    "c": null,
    "o": [
      "abstract = True",
      "proxy = True",
      "managed = False",
      "db_table = 'custom_name'"
    ]
  },
  {
    "q": "Which of the following is TRUE about querying related models in multi-table inheritance?",
    "c": null,
    "o": [
      "You can access parent fields via the automatically created OneToOneField linking child to parent",
      "Parent fields are not accessible from child models",
      "All child models share the same table automatically",
      "ForeignKey fields in the parent are ignored in children"
    ]
  },
  {
    "q": "Which database function allows returning the greatest of multiple values in Django ORM?",
    "c": null,
    "o": [
      "Greatest()",
      "Least()",
      "Coalesce()",
      "Concat()"
    ]
  },
  {
    "q": "Which ORM method allows annotating each object in a queryset with a computed value based on its fields?",
    "c": null,
    "o": [
      "annotate()",
      "aggregate()",
      "select_related()",
      "defer()"
    ]
  },
  {
    "q": "Which of the following is TRUE about using aggregate() with conditional filters?",
    "c": null,
    "o": [
      "You can pass filter=Q(...) inside aggregation functions to apply conditions",
      "aggregate() always applies to all rows unconditionally",
      "aggregate() can only be used on single fields without conditions",
      "aggregate() automatically locks rows for update"
    ]
  },
  {
    "q": "Which of the following allows creating an index on a JSONField key in Django?",
    "c": null,
    "o": [
      "GinIndex(fields=['json_field'], name='json_idx', opclasses=['jsonb_path_ops'])",
      "db_index=True automatically",
      "unique=True",
      "defer()"
    ]
  },
  {
    "q": "How can you query nested ManyToMany relationships recursively?",
    "c": null,
    "o": [
      "Use double underscores to traverse relationships, e.g., friends__friends__field=value",
      "Use defer()",
      "Use aggregate() only",
      "Use select_related() only"
    ]
  },
  {
    "q": "Which database function allows returning the smallest value among multiple fields in Django ORM?",
    "c": null,
    "o": [
      "Least()",
      "Greatest()",
      "Coalesce()",
      "Concat()"
    ]
  },
  {
    "q": "Which ORM method allows computing a value based on other fields of the same object for each row?",
    "c": null,
    "o": [
      "annotate()",
      "aggregate()",
      "select_related()",
      "defer()"
    ]
  },
  {
    "q": "Which of the following allows applying conditional filters inside an aggregate function?",
    "c": null,
    "o": [
      "Use filter=Q(...) parameter inside aggregation functions",
      "aggregate() always applies to all rows without conditions",
      "aggregate() can only be applied to single fields unconditionally",
      "select_related() applies conditional aggregation automatically"
    ]
  },
  {
    "q": "Which of the following is TRUE about self-referential ManyToMany relationships?",
    "c": null,
    "o": [
      "They can relate a model to itself and support a through model for extra fields",
      "They only work with abstract models",
      "They automatically enforce symmetry without specifying symmetrical=False",
      "They cannot be queried recursively"
    ]
  },
  {
    "q": "Which of the following allows chaining multiple database functions in a single annotation?",
    "c": null,
    "o": [
      "Nest functions inside each other, e.g., Upper(Concat('field1', 'field2'))",
      "Use only() with the functions",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "Which Meta option allows creating multiple indexes with expressions and conditions?",
    "c": null,
    "o": [
      "indexes = [models.Index(fields=['field'], condition=Q(active=True)), models.Index(fields=['other_field'])]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "Which of the following allows enforcing a unique constraint only for rows matching a condition?",
    "c": null,
    "o": [
      "UniqueConstraint(fields=['field'], condition=Q(active=True))",
      "unique=True on the field",
      "unique_together",
      "db_index=True"
    ]
  },
  {
    "q": "Which statement about multi-table inheritance is TRUE?",
    "c": null,
    "o": [
      "Each child model gets its own table with an implicit OneToOneField linking to the parent",
      "All fields are merged into the parent table automatically",
      "ForeignKey fields are ignored in children",
      "Abstract base classes create tables automatically"
    ]
  },
  {
    "q": "Which combination allows fetching only specific fields from a model while also loading related objects efficiently?",
    "c": null,
    "o": [
      "only('field1', 'field2').select_related('related_model')",
      "defer('field1', 'field2').aggregate('related_model')",
      "annotate('field1').select_related('related_model')",
      "prefetch_related('related_model').aggregate('field1')"
    ]
  },
  {
    "q": "Which signal is triggered immediately after a model instance is created or updated?",
    "c": null,
    "o": [
      "post_save",
      "pre_save",
      "post_delete",
      "pre_delete"
    ]
  },
  {
    "q": "Which of the following allows performing conditional updates on multiple rows in a single query?",
    "c": null,
    "o": [
      "update() with Case and When expressions combined with F expressions",
      "save() on each instance",
      "bulk_create()",
      "aggregate() only"
    ]
  },
  {
    "q": "Which Meta option allows specifying multiple complex constraints including both CheckConstraint and UniqueConstraint?",
    "c": null,
    "o": [
      "constraints = [CheckConstraint(...), UniqueConstraint(...)]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "Which of the following is TRUE about deferred fields in Django?",
    "c": null,
    "o": [
      "Deferred fields are not loaded from the database until accessed, reducing query load",
      "Deferred fields are automatically deleted",
      "Deferred fields cannot be JSONField",
      "Deferred fields are always loaded immediately"
    ]
  },
  {
    "q": "Which signal is triggered before a model instance is deleted?",
    "c": null,
    "o": [
      "pre_delete",
      "post_delete",
      "pre_save",
      "post_save"
    ]
  },
  {
    "q": "Which of the following allows creating a filtered index on a JSONField key for faster queries?",
    "c": null,
    "o": [
      "GinIndex(fields=['json_field'], name='json_idx', opclasses=['jsonb_path_ops'], condition=Q(active=True))",
      "db_index=True automatically",
      "unique=True",
      "defer() only"
    ]
  },
  {
    "q": "How can you traverse recursive self-referential ManyToMany relationships in Django ORM?",
    "c": null,
    "o": [
      "Use double underscore lookups, e.g., friends__friends__field=value",
      "Use select_related() only",
      "Use defer() only",
      "Use aggregate() only"
    ]
  },
  {
    "q": "Which combination allows computing a nested annotation based on multiple fields?",
    "c": null,
    "o": [
      "annotate(new_field=Upper(Concat('field1', 'field2')))",
      "aggregate(new_field=Upper('field1')) only",
      "select_related('field1')",
      "defer('field1')"
    ]
  },
  {
    "q": "Which of the following allows performing conditional updates on a ManyToMany through model?",
    "c": null,
    "o": [
      "Use update() on the through model with Case and When expressions",
      "Use update() on the main model only",
      "Use annotate() only",
      "Use select_related() only"
    ]
  },
  {
    "q": "Which database function allows returning the greatest value among multiple fields in an annotation?",
    "c": null,
    "o": [
      "Greatest()",
      "Least()",
      "Coalesce()",
      "Concat()"
    ]
  },
  {
    "q": "Which of the following allows querying nested keys inside a JSONField efficiently without loading the whole field?",
    "c": null,
    "o": [
      "Use double underscore lookups, e.g., json_field__key__subkey='value'",
      "Use defer() only",
      "Use select_related() only",
      "Use aggregate() only"
    ]
  },
  {
    "q": "Which Meta option allows defining multiple indexes with expressions, including conditional indexes?",
    "c": null,
    "o": [
      "indexes = [models.Index(fields=['field'], condition=Q(active=True)), models.Index(fields=['other_field'])]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "Which of the following is TRUE about self-referential ManyToMany relationships with a through model?",
    "c": null,
    "o": [
      "They allow storing extra fields on the relationship and filtering based on them",
      "They automatically prevent duplicate relationships without a through model",
      "They only work with abstract models",
      "They cannot be queried recursively"
    ]
  },
  {
    "q": "Which of the following allows updating a field conditionally across multiple rows using ORM?",
    "c": null,
    "o": [
      "update() with Case and When expressions combined with F expressions",
      "save() on each instance individually",
      "bulk_create()",
      "annotate() only"
    ]
  },
  {
    "q": "Which Meta option in multi-table inheritance prevents Django from creating a database table for the base class?",
    "c": null,
    "o": [
      "abstract = True",
      "proxy = True",
      "managed = False",
      "db_table = 'custom_name'"
    ]
  },
  {
    "q": "Which combination allows fetching only certain fields of a model while prefetching related objects efficiently?",
    "c": null,
    "o": [
      "only('field1', 'field2').prefetch_related('related_model')",
      "defer('field1').aggregate('related_model')",
      "annotate('field1').select_related('related_model')",
      "aggregate('field1').select_related('related_model')"
    ]
  },
  {
    "q": "Which ORM method allows locking specific rows in a transaction to prevent concurrent updates?",
    "c": null,
    "o": [
      "select_for_update()",
      "defer()",
      "annotate()",
      "aggregate()"
    ]
  },
  {
    "q": "Which of the following allows using F expressions inside a Case/When annotation?",
    "c": null,
    "o": [
      "F expressions can be used in Then or Else clauses for database-side conditional updates",
      "F expressions can only be used in aggregate()",
      "F expressions require loading objects into Python first",
      "F expressions cannot be used with annotations"
    ]
  },
  {
    "q": "Which signal is triggered immediately before a model instance is saved?",
    "c": null,
    "o": [
      "pre_save",
      "post_save",
      "pre_delete",
      "post_delete"
    ]
  },
  {
    "q": "Which Meta option allows creating multiple complex constraints including CheckConstraint and UniqueConstraint?",
    "c": null,
    "o": [
      "constraints = [CheckConstraint(...), UniqueConstraint(...)]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "Which of the following is TRUE about deferred fields in Django ORM?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, which can improve query performance",
      "Deferred fields are automatically deleted",
      "Deferred fields cannot be JSONField",
      "Deferred fields are always loaded immediately"
    ]
  },
  {
    "q": "Which of the following allows performing calculations on JSONField values using F expressions?",
    "c": null,
    "o": [
      "Use KeyTransform with F expressions, e.g., F('json_field__key') + 1",
      "Use annotate() only",
      "Use select_related() only",
      "Use defer() only"
    ]
  },
  {
    "q": "How can you traverse recursive self-referential ForeignKey relationships and annotate a computed value?",
    "c": null,
    "o": [
      "Use double underscores to traverse, e.g., parent__parent__field, combined with annotate()",
      "Use defer() only",
      "Use aggregate() only",
      "Use select_related() only"
    ]
  },
  {
    "q": "Which Meta option allows creating conditional indexes on computed expressions in Django 3.2+?",
    "c": null,
    "o": [
      "indexes = [models.Index(fields=[], name='idx_expr', expressions=[Upper('field')], condition=Q(active=True))]",
      "db_index=True",
      "unique=True",
      "ordering only"
    ]
  },
  {
    "q": "Which of the following is TRUE about multi-table inheritance with custom managers?",
    "c": null,
    "o": [
      "Child models can define custom managers without affecting parent table queries",
      "Custom managers on children automatically override parent managers globally",
      "Parent managers cannot be used if children define managers",
      "Custom managers are ignored in multi-table inheritance"
    ]
  },
  {
    "q": "Which ORM method allows conditional aggregation based on related model fields?",
    "c": null,
    "o": [
      "aggregate() or annotate() with filter=Q(...) inside aggregation functions",
      "select_related() only",
      "prefetch_related() only",
      "defer() only"
    ]
  },
  {
    "q": "Which of the following is TRUE about deferred fields combined with annotations?",
    "c": null,
    "o": [
      "Deferred fields are loaded lazily, but annotations can still compute values at the database level",
      "Deferred fields cannot be used with annotate()",
      "Deferred fields are ignored if annotations are present",
      "Deferred fields are loaded immediately when any annotation exists"
    ]
  },
  {
    "q": "Which of the following allows traversing a ManyToMany relationship with a through model and filtering by extra fields?",
    "c": null,
    "o": [
      "Query the through model directly and filter on its fields, then access related objects via the manager",
      "Use select_related() only",
      "Use aggregate() only",
      "Use defer() only"
    ]
  },
  {
    "q": "Which database function allows returning the first non-null value among multiple fields in an annotation?",
    "c": null,
    "o": [
      "Coalesce()",
      "Greatest()",
      "Least()",
      "Concat()"
    ]
  },
  {
    "q": "What does the following code do?",
    "c": "from django.db.models import F\nMyModel.objects.update(counter=F('counter') + 1)",
    "o": [
      "Increments the counter field by 1 for all rows at the database level",
      "Fetches all objects and increments the counter in Python",
      "Creates a new row with counter=1",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following is TRUE about the code snippet?",
    "c": "class ChildModel(ParentModel):\n    extra_field = models.IntegerField()",
    "o": [
      "ChildModel will have its own table and a OneToOneField linking to ParentModel",
      "ChildModel shares the same table as ParentModel",
      "ParentModel fields are ignored in ChildModel",
      "No database table is created for ChildModel"
    ]
  },
  {
    "q": "What does the following query do?",
    "c": "MyModel.objects.filter(json_field__settings__theme='dark')",
    "o": [
      "Filters rows where the nested key 'settings' in JSONField has a 'theme' value equal to 'dark'",
      "Fetches all rows and checks theme in Python",
      "Filters only top-level JSON keys",
      "Raises an error because nested keys cannot be queried"
    ]
  },
  {
    "q": "Which signal is triggered immediately before a model instance is deleted?",
    "c": null,
    "o": [
      "pre_delete",
      "post_delete",
      "pre_save",
      "post_save"
    ]
  },
  {
    "q": "What is the effect of this annotation?",
    "c": "from django.db.models import Case, When, F\nMyModel.objects.annotate(status_label=Case(\n    When(status='active', then=F('status')),\n    default='inactive'\n))",
    "o": [
      "Adds a new field 'status_label' with the value of 'status' if it is 'active', else 'inactive'",
      "Updates the status field in the database",
      "Filters rows with status='active'",
      "Raises a syntax error"
    ]
  },
  {
    "q": "Which of the following allows creating a conditional unique constraint?",
    "c": null,
    "o": [
      "UniqueConstraint(fields=['email'], condition=Q(is_active=True))",
      "unique=True on the field",
      "unique_together only",
      "db_index=True"
    ]
  },
  {
    "q": "Which query efficiently prefetches related objects while only loading specific fields?",
    "c": null,
    "o": [
      "MyModel.objects.only('name', 'age').prefetch_related('related_model')",
      "MyModel.objects.defer('name', 'age').select_related('related_model')",
      "MyModel.objects.annotate('name').select_related('related_model')",
      "MyModel.objects.aggregate('name').select_related('related_model')"
    ]
  },
  {
    "q": "Which of the following is TRUE about the code snippet?",
    "c": "from django.db.models import F, Case, When\nMyModel.objects.update(counter=Case(\n    When(active=True, then=F('counter') + 1),\n    default=F('counter')\n))",
    "o": [
      "Conditionally increments 'counter' by 1 for rows where active=True at the database level",
      "Increments all counters by 1 unconditionally",
      "Fetches objects and increments in Python",
      "Raises a runtime error"
    ]
  },
  {
    "q": "What does the following query do?",
    "c": "MyModel.objects.filter(parent__parent__name='John')",
    "o": [
      "Filters objects whose grandparent's name is 'John'",
      "Filters only the parent objects with name 'John'",
      "Raises an error because parent__parent is invalid",
      "Fetches all objects and filters in Python"
    ]
  },
  {
    "q": "What is the effect of this code?",
    "c": "MyModel.objects.select_for_update().filter(active=True)",
    "o": [
      "Locks the rows matching active=True until the transaction ends to prevent concurrent updates",
      "Fetches the rows without locking",
      "Deletes the rows immediately",
      "Raises a runtime error because select_for_update cannot be combined with filter"
    ]
  },
  {
    "q": "Which of the following is TRUE about ManyToMany through models?",
    "c": null,
    "o": [
      "You can store extra fields on the relationship and query/filter based on them",
      "Through models automatically enforce uniqueness without configuration",
      "You cannot filter through models directly",
      "They only work with abstract models"
    ]
  },
  {
    "q": "What does this code snippet accomplish?",
    "c": "from django.db.models import F, JSONField\nMyModel.objects.update(data=F('data').jsonb_set(['counter'], F('data__counter') + 1))",
    "o": [
      "Increments the 'counter' key inside the JSONField 'data' at the database level",
      "Fetches all rows and increments the counter in Python",
      "Replaces the entire JSONField with a new dict",
      "Raises a runtime error because jsonb_set is not available"
    ]
  },
  {
    "q": "What is the effect of this annotation?",
    "c": "from django.db.models import Case, When, F\nMyModel.objects.annotate(priority_label=Case(\n    When(priority__gte=10, then='High'),\n    default='Low'\n))",
    "o": [
      "Adds a new field 'priority_label' with value 'High' if priority>=10, else 'Low'",
      "Updates the priority field in the database",
      "Filters objects with priority>=10",
      "Raises a syntax error"
    ]
  },
  {
    "q": "Which of the following statements about deferred fields is TRUE?",
    "c": null,
    "o": [
      "Deferred fields are not loaded from the database until accessed, improving query efficiency",
      "Deferred fields are automatically deleted",
      "Deferred fields cannot be used with JSONField",
      "Deferred fields are always loaded immediately"
    ]
  },
  {
    "q": "What does the following query do?",
    "c": "MyModel.objects.prefetch_related('friends').only('name', 'age')",
    "o": [
      "Fetches only the 'name' and 'age' fields from MyModel while prefetching related friends",
      "Fetches all fields from MyModel and friends",
      "Fetches only 'name' and 'age' without loading friends",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which signal is triggered after a model instance is saved?",
    "c": null,
    "o": [
      "post_save",
      "pre_save",
      "pre_delete",
      "post_delete"
    ]
  },
  {
    "q": "What does the following query accomplish?",
    "c": "MyModel.objects.filter(friends__friends__age__gte=18)",
    "o": [
      "Filters objects whose friends of friends are at least 18 years old",
      "Filters objects whose direct friends are at least 18 years old",
      "Raises an error because double underscore traversal is invalid",
      "Fetches all objects and filters in Python"
    ]
  },
  {
    "q": "What is the effect of this code?",
    "c": "MyModel.objects.select_for_update(of=('self', 'related_model')).filter(active=True)",
    "o": [
      "Locks both the main model rows and the related_model rows matching the filter until the transaction ends",
      "Locks only the main model rows",
      "Locks all rows in the database",
      "Raises a runtime error because of=() is invalid"
    ]
  },
  {
    "q": "Which of the following is TRUE about conditional ManyToMany annotations?",
    "c": null,
    "o": [
      "You can annotate related counts or flags conditionally using Case and When with filtered ManyToMany relationships",
      "You cannot filter ManyToMany relationships in annotations",
      "Annotations automatically update the database",
      "Only direct ForeignKey relationships can be annotated conditionally"
    ]
  },
  {
    "q": "What does the following JSONField update do?",
    "c": "from django.db.models import F\nMyModel.objects.update(data=F('data').jsonb_set(['settings','volume'], 75))",
    "o": [
      "Updates the nested key ['settings']['volume'] to 75 in all rows at the database level",
      "Replaces the entire JSONField with {'settings': {'volume': 75}}",
      "Fetches rows and updates JSON in Python",
      "Raises a runtime error because jsonb_set cannot be used this way"
    ]
  },
  {
    "q": "Which of the following is TRUE about multi-level deferred fields?",
    "c": null,
    "o": [
      "You can defer specific fields on the main model while still loading related objects via select_related or prefetch_related",
      "Deferred fields cannot be used with related objects",
      "All deferred fields are automatically loaded immediately when any related object is accessed",
      "Deferred fields cannot be combined with annotations"
    ]
  },
  {
    "q": "What does the following annotation do?",
    "c": "from django.db.models import Count, Q\nMyModel.objects.annotate(active_friends=Count('friends', filter=Q(friends__active=True)))",
    "o": [
      "Adds a field 'active_friends' counting only the friends where active=True",
      "Counts all friends regardless of active status",
      "Updates the friends table setting active=True",
      "Raises an error because filter cannot be used in Count"
    ]
  },
  {
    "q": "Which signal is triggered after a model instance is deleted?",
    "c": null,
    "o": [
      "post_delete",
      "pre_delete",
      "pre_save",
      "post_save"
    ]
  },
  {
    "q": "Which of the following allows updating multiple JSONField keys at once using database-level expressions?",
    "c": null,
    "o": [
      "Use nested jsonb_set calls with F expressions for each key",
      "Use aggregate() only",
      "Use select_related() only",
      "Fetch objects in Python and update manually"
    ]
  },
  {
    "q": "What does the following query do?",
    "c": "from django.db.models import F\nMyModel.objects.update(score=F('score') + F('bonus'))",
    "o": [
      "Adds the value of 'bonus' to 'score' for all rows at the database level",
      "Fetches objects into Python and updates score manually",
      "Creates a new field combining score and bonus",
      "Raises a runtime error"
    ]
  },
  {
    "q": "What is the effect of this annotation?",
    "c": "from django.db.models import Case, When, F\nMyModel.objects.annotate(rating_label=Case(\n    When(rating__gte=8, then='Excellent'),\n    When(rating__gte=5, then='Good'),\n    default='Poor'\n))",
    "o": [
      "Adds a new field 'rating_label' categorizing ratings into 'Excellent', 'Good', or 'Poor'",
      "Updates the rating field in the database",
      "Filters objects based on rating",
      "Raises a syntax error"
    ]
  },
  {
    "q": "Which of the following is TRUE about multi-table inheritance with overridden managers?",
    "c": null,
    "o": [
      "Child models can define custom managers without affecting parent table queries",
      "Child managers automatically override parent managers globally",
      "Parent managers cannot be used if children define managers",
      "Custom managers are ignored in multi-table inheritance"
    ]
  },
  {
    "q": "What does the following JSONField update accomplish?",
    "c": "from django.db.models import F\nMyModel.objects.update(settings=F('settings').jsonb_set(['volume'], F('settings__volume') + 10))",
    "o": [
      "Increments the nested 'volume' key in 'settings' JSONField by 10 for all rows at the database level",
      "Replaces the entire JSONField with {'volume': 10}",
      "Fetches rows and updates in Python",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which query counts only the active friends of each object?",
    "c": "from django.db.models import Count, Q\nMyModel.objects.annotate(active_friends=Count('friends', filter=Q(friends__active=True)))",
    "o": [
      "Adds 'active_friends' field counting only friends with active=True",
      "Counts all friends regardless of active status",
      "Updates friends to active=True",
      "Raises an error"
    ]
  },
  {
    "q": "Which signal is triggered before saving a model instance?",
    "c": null,
    "o": [
      "pre_save",
      "post_save",
      "pre_delete",
      "post_delete"
    ]
  },
  {
    "q": "Which of the following allows performing complex arithmetic updates conditionally on multiple fields?",
    "c": null,
    "o": [
      "Use update() with Case, When, and F expressions",
      "Use aggregate() only",
      "Use annotate() only",
      "Use select_for_update() only"
    ]
  },
  {
    "q": "What is the effect of using only() with prefetch_related()?",
    "c": null,
    "o": [
      "Loads only the specified fields from the main model while prefetching all fields of related models",
      "Loads all fields from both main and related models",
      "Loads only specified fields from related models",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following Meta options prevents Django from managing the database table?",
    "c": null,
    "o": [
      "managed = False",
      "abstract = True",
      "proxy = True",
      "db_table = 'custom_name'"
    ]
  },
  {
    "q": "What is the effect of this model definition?",
    "c": "class ProxyModel(MyModel):\n    class Meta:\n        proxy = True",
    "o": [
      "Creates a proxy class with the same database table but different Python behavior",
      "Creates a new database table",
      "Merges fields into the parent table automatically",
      "Prevents the model from being used in queries"
    ]
  },
  {
    "q": "Which field option ensures a field is not editable in Django admin or forms but still stored in the database?",
    "c": null,
    "o": [
      "editable=False",
      "blank=True",
      "null=True",
      "db_index=True"
    ]
  },
  {
    "q": "Which Meta option allows specifying multiple database-level constraints including CheckConstraint and UniqueConstraint?",
    "c": null,
    "o": [
      "constraints = [CheckConstraint(...), UniqueConstraint(...)]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "Which of the following allows defining a partial index in Django 3.2+?",
    "c": null,
    "o": [
      "models.Index(fields=['field'], condition=Q(active=True))",
      "db_index=True",
      "unique=True",
      "unique_together only"
    ]
  },
  {
    "q": "What does the following model option do?",
    "c": "class MyModel(models.Model):\n    class Meta:\n        ordering = ['-created_at']",
    "o": [
      "Sets the default ordering of querysets by 'created_at' descending",
      "Automatically indexes 'created_at'",
      "Makes 'created_at' unique",
      "Prevents the model from being queried"
    ]
  },
  {
    "q": "Which of the following is TRUE about abstract base classes?",
    "c": null,
    "o": [
      "They do not create a database table and are intended to share fields and behavior",
      "They automatically create a table merged with child classes",
      "They cannot define methods",
      "They must be registered in admin"
    ]
  },
  {
    "q": "Which of the following field options automatically creates a database index?",
    "c": null,
    "o": [
      "db_index=True",
      "editable=False",
      "blank=True",
      "unique_for_date"
    ]
  },
  {
    "q": "What does the following Meta option do?",
    "c": "class MyModel(models.Model):\n    class Meta:\n        db_table = 'custom_table_name'",
    "o": [
      "Specifies a custom database table name instead of the default app_model format",
      "Prevents Django from creating the table",
      "Automatically adds a primary key field",
      "Merges this model into another table"
    ]
  },
  {
    "q": "Which field option enforces uniqueness of a field relative to a DateField?",
    "c": null,
    "o": [
      "unique_for_date",
      "unique=True",
      "db_index=True",
      "blank=True"
    ]
  },
  {
    "q": "Which of the following statements about `unique_for_month` is TRUE?",
    "c": null,
    "o": [
      "It ensures the field value is unique for the given month of a DateField or DateTimeField",
      "It creates a global uniqueness across all records",
      "It automatically creates a database index",
      "It only works with ForeignKey fields"
    ]
  },
  {
    "q": "Which of the following statements about deferred fields with indexes is TRUE?",
    "c": null,
    "o": [
      "Deferred fields are loaded lazily but the database index can still be used in queries",
      "Deferred fields cannot have indexes",
      "All deferred fields are loaded immediately if indexed",
      "Deferred fields are ignored by queries if indexed"
    ]
  },
  {
    "q": "Which Meta option in multi-table inheritance automatically creates a OneToOneField linking child to parent?",
    "c": null,
    "o": [
      "default behavior in standard multi-table inheritance",
      "abstract = True",
      "proxy = True",
      "managed = False"
    ]
  },
  {
    "q": "What is the effect of this proxy model?",
    "c": "class AdminUser(User):\n    class Meta:\n        proxy = True",
    "o": [
      "Creates a different Python-level class with the same database table and allows custom behavior or managers",
      "Creates a new database table",
      "Merges fields into the parent table automatically",
      "Prevents the model from being queried"
    ]
  },
  {
    "q": "Which of the following is TRUE about `db_tablespaces` in Meta options?",
    "c": null,
    "o": [
      "Allows specifying which database tablespaces to use for the model and its indexes",
      "Automatically partitions the table across multiple databases",
      "Overrides primary key fields automatically",
      "Is only used with abstract models"
    ]
  },
  {
    "q": "Which of the following field options is used to make a field optional in forms but still required in the database?",
    "c": null,
    "o": [
      "blank=True",
      "null=True",
      "editable=False",
      "unique=True"
    ]
  },
  {
    "q": "What does this Meta option do?",
    "c": "class MyModel(models.Model):\n    class Meta:\n        get_latest_by = 'created_at'",
    "o": [
      "Specifies the default field to use with latest() and earliest() queryset methods",
      "Automatically indexes 'created_at'",
      "Creates a unique constraint on 'created_at'",
      "Prevents ordering by 'created_at'"
    ]
  },
  {
    "q": "Which of the following Meta options allows customizing default permissions for a model?",
    "c": null,
    "o": [
      "default_permissions = ('add', 'change', 'delete', 'view')",
      "permissions only",
      "managed = False",
      "ordering only"
    ]
  },
  {
    "q": "Which of the following is TRUE about field validators?",
    "c": null,
    "o": [
      "Validators are functions or callables that are executed before saving a model instance to enforce custom constraints",
      "Validators are automatically applied at the database level",
      "Validators cannot be used on TextField",
      "Validators automatically make the field unique"
    ]
  },
  {
    "q": "What is the effect of this field definition?",
    "c": "email = models.EmailField(error_messages={'invalid': 'Enter a valid email address!'})",
    "o": [
      "Overrides the default validation error message for invalid email addresses",
      "Prevents saving duplicate emails",
      "Automatically indexes the email field",
      "Disables form validation for this field"
    ]
  },
  {
    "q": "Which of the following is TRUE about combining conditional constraints with model inheritance?",
    "c": null,
    "o": [
      "Child models inherit constraints from the parent, and you can also define additional constraints specific to the child",
      "Constraints from parent are ignored in children",
      "Child models cannot have any constraints if the parent has any",
      "Constraints are applied only in Python, not at the database level"
    ]
  },
  {
    "q": "Which field option ensures that a DateTimeField value must be after a given date using a validator?",
    "c": "from django.core.validators import MinValueValidator\nfrom django.utils import timezone\nstart_date = models.DateTimeField(validators=[MinValueValidator(limit_value=timezone.now)])",
    "o": [
      "MinValueValidator ensures the field value is not earlier than the specified datetime",
      "MinValueValidator automatically sets the default value",
      "MinValueValidator creates a database-level constraint",
      "Raises an error if the field is blank"
    ]
  },
  {
    "q": "What does this Meta option do?",
    "c": "class MyModel(models.Model):\n    class Meta:\n        constraints = [models.CheckConstraint(check=models.Q(score__gte=0), name='score_non_negative')]",
    "o": [
      "Enforces a database-level constraint ensuring 'score' is greater than or equal to 0",
      "Automatically sets score to 0 if negative",
      "Only validates in Python at save() time",
      "Prevents saving any rows with score field"
    ]
  },
  {
    "q": "Which of the following is TRUE about custom error messages on model fields?",
    "c": null,
    "o": [
      "You can override default error messages using the error_messages dictionary for fields",
      "Custom error messages are ignored in forms",
      "Custom error messages automatically enforce uniqueness",
      "You cannot override messages for built-in validators"
    ]
  },
  {
    "q": "Which field option ensures that a field value is unique for a given year of a DateField?",
    "c": null,
    "o": [
      "unique_for_year",
      "unique=True",
      "db_index=True",
      "blank=True"
    ]
  },
  {
    "q": "Which field option ensures that a field value is unique for a given month of a DateField?",
    "c": null,
    "o": [
      "unique_for_month",
      "unique=True",
      "db_index=True",
      "editable=False"
    ]
  },
  {
    "q": "What does this annotation accomplish?",
    "c": "from django.db.models import F, Count\nMyModel.objects.annotate(total_friends=Count('friends')).defer('bio')",
    "o": [
      "Adds 'total_friends' count while deferring loading of the 'bio' field to improve query efficiency",
      "Counts friends and immediately loads the 'bio' field",
      "Deletes the 'bio' field from all rows",
      "Raises an error because defer() cannot be combined with annotate()"
    ]
  },
  {
    "q": "Which of the following is TRUE about `permissions` and `default_permissions` in Meta options?",
    "c": null,
    "o": [
      "default_permissions defines the standard set of permissions ('add', 'change', 'delete', 'view') and `permissions` allows adding custom permissions",
      "`permissions` overrides default_permissions completely",
      "`default_permissions` disables admin access",
      "Custom permissions cannot be used with proxy models"
    ]
  },
  {
    "q": "Which Meta option allows specifying indexes using database expressions?",
    "c": null,
    "o": [
      "indexes = [models.Index(fields=[], name='upper_name_idx', expressions=[Upper('name')])]",
      "db_index=True",
      "unique=True",
      "ordering only"
    ]
  },
  {
    "q": "What does this field definition do?",
    "c": "created = models.DateTimeField(auto_now_add=True, unique_for_year='year')",
    "o": [
      "Automatically sets the timestamp on creation and ensures the value is unique within the same year",
      "Updates the timestamp every time the object is saved",
      "Creates a global unique constraint ignoring the year",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following is TRUE about deferred fields combined with annotations?",
    "c": null,
    "o": [
      "Deferred fields are loaded lazily, but annotations are still computed at the database level",
      "Deferred fields cannot be used with annotate()",
      "All deferred fields are loaded immediately if any annotation exists",
      "Deferred fields are ignored in queries when annotations are present"
    ]
  },
  {
    "q": "What is the effect of this Meta option?",
    "c": "class MyModel(models.Model):\n    class Meta:\n        constraints = [models.UniqueConstraint(fields=['email'], condition=Q(is_active=True))]",
    "o": [
      "Enforces uniqueness of 'email' only for rows where 'is_active' is True",
      "Enforces global uniqueness for all email values",
      "Filters rows where is_active=True automatically",
      "Raises an error because conditional unique constraints are invalid"
    ]
  },
  {
    "q": "What does this annotation do?",
    "c": "from django.db.models import Coalesce, F\nMyModel.objects.annotate(score_total=Coalesce(F('score1'), 0) + Coalesce(F('score2'), 0))",
    "o": [
      "Computes 'score_total' as the sum of 'score1' and 'score2', treating nulls as 0",
      "Updates 'score1' and 'score2' in the database",
      "Filters rows where score1 and score2 are not null",
      "Raises an error"
    ]
  },
  {
    "q": "Which of the following allows performing transactional updates on a ManyToMany through model?",
    "c": "from django.db import transaction\nwith transaction.atomic():\n    ThroughModel.objects.filter(active=True).update(flag=True)",
    "o": [
      "Ensures the updates are atomic, preventing partial updates in case of errors",
      "Automatically commits after each row",
      "Ignores database constraints",
      "Raises an error because ManyToMany cannot be updated in transactions"
    ]
  },
  {
    "q": "Which of the following statements about multi-table inheritance is TRUE?",
    "c": null,
    "o": [
      "Each child model has its own table with an implicit OneToOneField to the parent",
      "Child models share the same table as the parent",
      "Parent fields are ignored in children",
      "Multi-table inheritance cannot have custom managers"
    ]
  },
  {
    "q": "What does this JSONField update accomplish?",
    "c": "from django.db.models import F\nMyModel.objects.update(config=F('config').jsonb_set(['settings','theme'], 'dark'))",
    "o": [
      "Updates the nested 'settings.theme' key in 'config' JSONField to 'dark' for all rows at the database level",
      "Replaces the entire JSONField with {'settings': {'theme': 'dark'}}",
      "Fetches objects and updates JSON in Python",
      "Raises an error"
    ]
  },
  {
    "q": "Which database function returns the greatest value among multiple fields?",
    "c": null,
    "o": [
      "Greatest()",
      "Least()",
      "Coalesce()",
      "Concat()"
    ]
  },
  {
    "q": "Which Meta option allows defining multiple indexes with expressions and conditional constraints?",
    "c": null,
    "o": [
      "indexes = [models.Index(fields=[], expressions=[Upper('name')], condition=Q(active=True))]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "Which of the following is TRUE about using F expressions inside Case/When?",
    "c": null,
    "o": [
      "F expressions can be used in Then or Else clauses for database-side conditional updates",
      "F expressions can only be used in aggregate()",
      "F expressions require loading objects into Python first",
      "F expressions cannot be used with annotations"
    ]
  },
  {
    "q": "What is the effect of this deferred field combined with select_related?",
    "c": "MyModel.objects.only('name').select_related('profile')",
    "o": [
      "Loads only the 'name' field from MyModel while fetching the full 'profile' related object efficiently",
      "Loads all fields from MyModel and profile",
      "Defers loading profile fields only",
      "Raises a runtime error"
    ]
  },
  {
    "q": "What does the following annotation do?",
    "c": "from django.db.models import Count, Q\nMyModel.objects.annotate(active_friends=Count('friends', filter=Q(friends__active=True)))",
    "o": [
      "Adds a field 'active_friends' counting only related friends where active=True",
      "Counts all friends regardless of active status",
      "Updates friends to active=True",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following statements about recursive self-referential ManyToMany relationships is TRUE?",
    "c": null,
    "o": [
      "You can traverse multiple levels using double underscores, e.g., friends__friends__field",
      "Self-referential ManyToMany cannot be queried recursively",
      "They automatically prevent duplicate relationships",
      "They only work with abstract models"
    ]
  },
  {
    "q": "Which of the following is TRUE about unique constraints with inheritance?",
    "c": null,
    "o": [
      "Child models inherit constraints from parent models, and can define additional constraints specific to the child",
      "Constraints from parent are ignored in children",
      "Child models cannot have constraints if the parent has any",
      "Constraints are applied only in Python, not at the database level"
    ]
  },
  {
    "q": "Which Meta option allows specifying tablespaces for the model and its indexes?",
    "c": null,
    "o": [
      "db_tablespaces",
      "db_table",
      "managed",
      "ordering"
    ]
  },
  {
    "q": "What does this query accomplish?",
    "c": "MyModel.objects.filter(parent__parent__name='Alice')",
    "o": [
      "Filters objects whose grandparent's name is 'Alice'",
      "Filters objects whose parent name is 'Alice'",
      "Raises an error because parent__parent is invalid",
      "Fetches all objects and filters in Python"
    ]
  },
  {
    "q": "Which of the following allows conditional aggregation on a related ManyToMany field?",
    "c": null,
    "o": [
      "Use Count or Sum with filter=Q(...) inside annotate() or aggregate()",
      "Use select_related() only",
      "Use defer() only",
      "Use update() only"
    ]
  },
  {
    "q": "What does this deferred field combined with annotate accomplish?",
    "c": "MyModel.objects.annotate(total_score=F('score1') + F('score2')).only('name')",
    "o": [
      "Computes 'total_score' at the database level while deferring loading of all other fields except 'name'",
      "Loads all fields immediately",
      "Updates 'score1' and 'score2' in the database",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following is TRUE about advanced expression-based indexes?",
    "c": null,
    "o": [
      "You can define indexes on computed expressions, e.g., Upper('name'), optionally with a condition",
      "Expression-based indexes cannot be conditional",
      "All fields must be included in the index",
      "They automatically enforce uniqueness"
    ]
  },
  {
    "q": "What does this annotation do?",
    "c": "from django.db.models import Coalesce, F\nMyModel.objects.annotate(max_score=Coalesce(F('score1'), 0) + Coalesce(F('score2'), 0))",
    "o": [
      "Computes 'max_score' by summing 'score1' and 'score2', treating nulls as 0",
      "Updates the score fields in the database",
      "Filters rows where score1 or score2 is not null",
      "Raises an error"
    ]
  },
  {
    "q": "Which of the following allows transactional updates on a ManyToMany through model combined with JSONField updates?",
    "c": "from django.db import transaction\nwith transaction.atomic():\n    ThroughModel.objects.filter(active=True).update(flag=True)\n    MyModel.objects.update(config=F('config').jsonb_set(['theme'], 'dark'))",
    "o": [
      "Ensures both the ManyToMany updates and JSONField updates happen atomically",
      "Updates JSONField immediately but ignores ManyToMany updates",
      "Updates ManyToMany but JSONField updates occur outside the transaction",
      "Raises a runtime error"
    ]
  },
  {
    "q": "What is the effect of this query?",
    "c": "MyModel.objects.prefetch_related('friends').only('name', 'age')",
    "o": [
      "Loads only 'name' and 'age' fields from MyModel while prefetching all fields of related 'friends'",
      "Loads all fields from MyModel and friends",
      "Loads only 'name' and 'age' for both MyModel and friends",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which database function returns the smallest value among multiple fields?",
    "c": null,
    "o": [
      "Least()",
      "Greatest()",
      "Coalesce()",
      "Concat()"
    ]
  },
  {
    "q": "Which of the following statements about combining deferred fields and annotations is TRUE?",
    "c": null,
    "o": [
      "Deferred fields are loaded lazily while annotations are still computed at the database level",
      "Deferred fields cannot be used with annotate()",
      "Annotations force deferred fields to be loaded immediately",
      "Deferred fields are ignored if any annotation exists"
    ]
  },
  {
    "q": "Which of the following allows defining multiple conditional expression-based indexes in Django 3.2+?",
    "c": null,
    "o": [
      "indexes = [models.Index(fields=[], expressions=[Upper('name')], condition=Q(active=True))]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "What does this nested annotation accomplish?",
    "c": "from django.db.models import F, Case, When\nMyModel.objects.annotate(final_score=Case(\n    When(score1__gt=10, then=F('score1') + F('bonus')), default=F('score2')))",
    "o": [
      "Computes 'final_score' conditionally: if score1>10, adds score1 and bonus; else uses score2",
      "Updates score1 and score2 in the database",
      "Filters rows where score1>10",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following statements about transactional updates is TRUE?",
    "c": null,
    "o": [
      "Using transaction.atomic() ensures all operations within the block succeed or fail together",
      "Each update is committed immediately, ignoring the transaction block",
      "Transactional updates only work with single tables",
      "Transactions automatically rollback only JSONField updates"
    ]
  },
  {
    "q": "What does this multi-table inheritance query accomplish?",
    "c": "ChildModel.objects.filter(parentmodel__status='active')",
    "o": [
      "Filters ChildModel objects where the parent model's status is 'active'",
      "Filters only the parent models",
      "Raises an error because parentmodel__status is invalid",
      "Fetches all child objects without filtering"
    ]
  },
  {
    "q": "Which of the following is TRUE about conditional CheckConstraints?",
    "c": null,
    "o": [
      "They allow enforcing constraints only for rows that satisfy a given condition using Q objects",
      "Constraints are always applied to all rows regardless of conditions",
      "Conditional constraints only work in Python, not at the database level",
      "They cannot be used with JSONField"
    ]
  },
  {
    "q": "What does this JSONField annotation accomplish?",
    "c": "from django.db.models import F, Value\nMyModel.objects.annotate(theme_value=F('config__settings__theme'))",
    "o": [
      "Extracts the nested key 'settings.theme' from the JSONField 'config' as 'theme_value' at the database level",
      "Updates the JSONField with 'theme_value'",
      "Filters rows where 'theme' exists",
      "Raises an error because nested JSONField lookups cannot be annotated"
    ]
  },
  {
    "q": "Which of the following is TRUE about F expressions inside conditional updates?",
    "c": null,
    "o": [
      "F expressions can be used in Then or Else clauses to perform database-side arithmetic without fetching objects",
      "F expressions can only be used with aggregate()",
      "F expressions require loading objects into Python first",
      "F expressions cannot be used with Case/When"
    ]
  },
  {
    "q": "Which Meta option allows combining multiple constraints including UniqueConstraint, CheckConstraint, and conditional constraints?",
    "c": null,
    "o": [
      "constraints",
      "unique_together",
      "ordering",
      "db_table"
    ]
  },
  {
    "q": "Which of the following is TRUE about deferred fields combined with select_related and prefetch_related?",
    "c": null,
    "o": [
      "You can defer specific fields while still efficiently fetching related objects using select_related or prefetch_related",
      "Deferred fields force related objects to be loaded immediately",
      "select_related cannot be used with deferred fields",
      "prefetch_related ignores deferred fields entirely"
    ]
  },
  {
    "q": "What is the effect of this expression-based index?",
    "c": "from django.db.models import Index, Upper\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n    class Meta:\n        indexes = [Index(fields=[], name='upper_name_idx', expressions=[Upper('name')])]",
    "o": [
      "Creates a database index on the uppercase value of 'name', improving query performance for case-insensitive searches",
      "Indexes the raw 'name' field only",
      "Automatically enforces uniqueness on 'name'",
      "Raises a syntax error"
    ]
  },
  {
    "q": "Which of the following statements about transactional updates is TRUE?",
    "c": null,
    "o": [
      "All operations within transaction.atomic() succeed or fail together, ensuring database consistency",
      "Each operation commits immediately, ignoring the transaction block",
      "Transactions only work with single-table updates",
      "transaction.atomic() is ignored when updating JSONFields"
    ]
  },
  {
    "q": "What does this nested JSONField update do?",
    "c": "from django.db.models import F\nMyModel.objects.update(settings=F('settings').jsonb_set(['preferences','notifications'], True))",
    "o": [
      "Sets the nested key 'preferences.notifications' to True in all rows at the database level",
      "Replaces the entire JSONField with {'preferences': {'notifications': True}}",
      "Fetches objects into Python and updates the field manually",
      "Raises an error because nested JSON updates are not allowed"
    ]
  },
  {
    "q": "Which of the following is TRUE about multi-level recursive self-referential ManyToMany relationships?",
    "c": null,
    "o": [
      "You can filter objects by related objects of related objects using double underscores, e.g., friends__friends__active=True",
      "Recursive ManyToMany relationships cannot be queried beyond one level",
      "They automatically prevent circular relationships",
      "Self-referential ManyToMany only works in abstract models"
    ]
  },
  {
    "q": "What does this aggregate annotation accomplish?",
    "c": "from django.db.models import Sum, Coalesce, F\nMyModel.objects.aggregate(total_points=Coalesce(Sum(F('score') + F('bonus')), 0))",
    "o": [
      "Computes the sum of score plus bonus across all rows, treating null as 0",
      "Updates the score and bonus fields in the database",
      "Counts all rows with non-null score or bonus",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following statements about database-level computed fields using F expressions is TRUE?",
    "c": null,
    "o": [
      "F expressions allow performing arithmetic or referencing other fields directly in the database without loading objects into Python",
      "F expressions are only evaluated in Python after fetching",
      "F expressions require raw SQL for arithmetic operations",
      "F expressions cannot be combined with annotations"
    ]
  },
  {
    "q": "Which Meta option allows defining conditional unique constraints using Q objects?",
    "c": null,
    "o": [
      "constraints = [models.UniqueConstraint(fields=['email'], condition=Q(active=True))]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "What is the effect of this deferred field combined with prefetch_related?",
    "c": "MyModel.objects.only('name').prefetch_related('friends')",
    "o": [
      "Loads only the 'name' field from MyModel while fetching all fields of related 'friends' efficiently",
      "Loads all fields from MyModel and friends",
      "Loads only 'name' and 'friends.name' fields",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following statements about Coalesce, Greatest, and Least expressions is TRUE?",
    "c": null,
    "o": [
      "They allow computing values at the database level, handling nulls and comparing multiple fields efficiently",
      "They only work in Python after fetching data",
      "They cannot be used inside annotate()",
      "They automatically update fields in the database"
    ]
  },
  {
    "q": "What does this transaction block ensure?",
    "c": "from django.db import transaction\nwith transaction.atomic():\n    MyModel.objects.filter(active=True).update(flag=True)\n    MyModel.objects.update(score=F('score')+10)",
    "o": [
      "Both updates happen atomically: either all succeed or all fail",
      "Only the first update is transactional",
      "Each update commits immediately, ignoring the transaction",
      "Raises an error because multiple updates cannot be in the same transaction"
    ]
  },
  {
    "q": "What does this conditional JSONField update accomplish?",
    "c": "from django.db.models import F, Q\nMyModel.objects.filter(active=True).update(settings=F('settings').jsonb_set(['theme'], 'dark'))",
    "o": [
      "Updates the 'theme' key in the JSONField only for rows where active=True, at the database level",
      "Updates all rows regardless of active status",
      "Fetches objects into Python and updates JSON manually",
      "Raises an error because JSONField updates cannot be conditional"
    ]
  },
  {
    "q": "Which of the following is TRUE about multi-table inheritance combined with proxy models?",
    "c": null,
    "o": [
      "A proxy child model can customize Python behavior without creating a new table, while multi-table children create new tables with OneToOneField links",
      "Proxy models always create new database tables",
      "Multi-table inheritance ignores proxy models entirely",
      "Proxy models cannot have custom managers"
    ]
  },
  {
    "q": "What does this deferred and related annotation accomplish?",
    "c": "MyModel.objects.only('name').annotate(friend_count=Count('friends')).prefetch_related('friends')",
    "o": [
      "Loads only 'name' from MyModel while prefetching all friends and computing friend_count at the database level",
      "Loads all fields from MyModel and friends",
      "Only counts friends in Python",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which Meta option allows defining multiple conditional unique constraints across inherited models?",
    "c": null,
    "o": [
      "constraints = [models.UniqueConstraint(fields=['email'], condition=Q(active=True))]",
      "unique_together only",
      "ordering only",
      "db_table only"
    ]
  },
  {
    "q": "What does this nested annotation do?",
    "c": "from django.db.models import Case, When, F\nMyModel.objects.annotate(final_score=Case(\n    When(score1__gt=50, then=F('score1')+F('bonus')), default=F('score2')))",
    "o": [
      "Computes 'final_score' conditionally: if score1>50, adds score1 and bonus; else uses score2",
      "Updates score1 and score2 in the database",
      "Filters rows where score1>50",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following is TRUE about combining JSONField, annotations, and F expressions?",
    "c": null,
    "o": [
      "You can compute derived values or update nested keys at the database level using F expressions combined with annotations",
      "Annotations cannot reference JSONField keys",
      "F expressions require fetching JSONField data into Python first",
      "JSONField updates cannot be combined with annotations"
    ]
  },
  {
    "q": "What is the effect of this expression-based index?",
    "c": "from django.db.models import Index, Upper\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n    class Meta:\n        indexes = [Index(fields=[], name='upper_name_idx', expressions=[Upper('name')], condition=Q(active=True))]",
    "o": [
      "Creates a database index on the uppercase value of 'name' only for active rows",
      "Indexes the raw 'name' field for all rows",
      "Automatically enforces uniqueness for 'name'",
      "Raises a syntax error"
    ]
  },
  {
    "q": "Which of the following statements about transactional updates with JSONField and ManyToMany is TRUE?",
    "c": null,
    "o": [
      "Using transaction.atomic() ensures all JSONField and ManyToMany updates succeed or fail together",
      "Each update commits immediately regardless of the transaction",
      "Transactions only work with single-table updates",
      "transaction.atomic() cannot be used with JSONField updates"
    ]
  },
  {
    "q": "Which of the following is TRUE about recursive ManyToMany updates?",
    "c": "from django.db import transaction\nwith transaction.atomic():\n    user.friends.update(active=True)",
    "o": [
      "Updates all related friends atomically, and can cascade to related objects recursively if implemented in code",
      "Only updates the first-level related objects",
      "Automatically updates all nested related friends without additional code",
      "Raises an error because ManyToMany updates cannot be recursive"
    ]
  },
  {
    "q": "What does this nested aggregate accomplish?",
    "c": "from django.db.models import Sum, F\nMyModel.objects.aggregate(total_points=Sum(F('score') + F('bonus')))",
    "o": [
      "Computes the sum of score plus bonus across all rows at the database level",
      "Updates the database fields score and bonus",
      "Counts rows where score plus bonus is not null",
      "Raises an error"
    ]
  },
  {
    "q": "Which Meta option allows specifying default permissions for a model?",
    "c": null,
    "o": [
      "default_permissions = ('add', 'change', 'delete', 'view')",
      "permissions only",
      "managed = False",
      "ordering only"
    ]
  },
  {
    "q": "Which Meta option allows specifying tablespaces for the model and its indexes?",
    "c": null,
    "o": [
      "db_tablespaces",
      "db_table",
      "managed",
      "ordering"
    ]
  },
  {
    "q": "Which of the following is TRUE about combining annotations across related fields?",
    "c": null,
    "o": [
      "You can reference related fields using double underscores, e.g., annotate(total=F('related__score')+F('related__bonus'))",
      "Annotations cannot reference related fields",
      "Related fields must be fetched into Python first",
      "F expressions cannot be used across relationships"
    ]
  },
  {
    "q": "What is the effect of this expression-based conditional index?",
    "c": "from django.db.models import Index, Upper\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n    class Meta:\n        indexes = [Index(fields=[], name='upper_name_active_idx', expressions=[Upper('name')], condition=Q(active=True))]",
    "o": [
      "Indexes the uppercase value of 'name' only for rows where active=True, improving query performance for case-insensitive searches on active rows",
      "Indexes all 'name' values regardless of active status",
      "Enforces uniqueness of 'name' automatically",
      "Raises a syntax error"
    ]
  },
  {
    "q": "Which of the following statements about transactional updates with nested annotations is TRUE?",
    "c": null,
    "o": [
      "Using transaction.atomic() ensures all updates using annotations, F expressions, or JSONField modifications succeed or fail together",
      "Each annotated update commits immediately regardless of transaction",
      "Transactional updates cannot include JSONField modifications",
      "Annotations inside transactions are ignored"
    ]
  },
  {
    "q": "What does this deferred field combined with select_related accomplish?",
    "c": "MyModel.objects.only('title').select_related('author')",
    "o": [
      "Loads only the 'title' field from MyModel while fetching all fields of related 'author' efficiently",
      "Loads all fields from MyModel and author",
      "Loads only title and author.name fields",
      "Raises a runtime error"
    ]
  },
  {
    "q": "What does this multi-level JSONField update accomplish?",
    "c": "from django.db.models import F, Q\nMyModel.objects.filter(active=True).update(settings=F('settings').jsonb_set(['preferences','notifications','email'], False))",
    "o": [
      "Sets the nested key 'preferences.notifications.email' to False only for active rows, at the database level",
      "Replaces the entire JSONField with {'preferences': {'notifications': {'email': False}}} for all rows",
      "Fetches objects into Python and updates manually",
      "Raises an error because JSONField updates cannot be nested"
    ]
  },
  {
    "q": "Which of the following statements about conditional JSONField constraints is TRUE?",
    "c": null,
    "o": [
      "You can enforce constraints only for rows matching a Q object condition when combined with CheckConstraint",
      "JSONField constraints are always applied to all rows",
      "Conditional constraints cannot reference JSONField keys",
      "They only validate in Python, not at the database level"
    ]
  },
  {
    "q": "What does this complex aggregate across related fields do?",
    "c": "from django.db.models import Sum, F\nMyModel.objects.aggregate(total_score=Sum(F('related__score') + F('related__bonus')))",
    "o": [
      "Computes the sum of score plus bonus across all related objects at the database level",
      "Updates score and bonus in the related table",
      "Counts rows where score or bonus is not null",
      "Raises an error"
    ]
  },
  {
    "q": "Which of the following is TRUE about combining deferred fields with annotations across related models?",
    "c": null,
    "o": [
      "You can defer loading specific fields while still computing annotations at the database level using related fields",
      "Deferred fields must always load all related fields when annotating",
      "Annotations cannot reference deferred fields",
      "F expressions cannot be used on deferred fields"
    ]
  },
  {
    "q": "Which Meta option allows combining expression-based indexes with conditional constraints?",
    "c": null,
    "o": [
      "indexes",
      "unique_together",
      "ordering",
      "db_table"
    ]
  },
  {
    "q": "What does this transaction block ensure?",
    "c": "from django.db import transaction\nwith transaction.atomic():\n    MyModel.objects.filter(active=True).update(flag=True)\n    MyModel.objects.update(score=F('score')+F('bonus'))",
    "o": [
      "Both updates occur atomically: either all succeed or all fail",
      "Only the first update is transactional",
      "Each update commits immediately, ignoring the transaction",
      "Raises an error because multiple updates cannot be in the same transaction"
    ]
  },
  {
    "q": "Which of the following is TRUE about nested Coalesce, Greatest, and Least expressions?",
    "c": null,
    "o": [
      "They allow computing values at the database level, handling nulls and comparing multiple fields efficiently",
      "They are only evaluated in Python after fetching",
      "They cannot be used inside annotate()",
      "They automatically update fields in the database"
    ]
  },
  {
    "q": "Which of the following statements about transactional JSONField updates combined with ManyToMany updates is TRUE?",
    "c": null,
    "o": [
      "Using transaction.atomic() ensures both JSONField updates and ManyToMany updates succeed or fail together",
      "JSONField updates are committed immediately, ignoring the transaction",
      "ManyToMany updates are always independent of transactions",
      "transaction.atomic() cannot include JSONField updates"
    ]
  },
  {
    "q": "Which of the following is TRUE about recursive self-referential ManyToMany with annotations?",
    "c": "from django.db.models import Count\nUser.objects.annotate(active_friends=Count('friends', filter=Q(friends__active=True)))",
    "o": [
      "Counts only related friends where active=True, and can be extended recursively across multiple levels using double underscores",
      "Counts all friends regardless of active status",
      "Automatically updates friend objects to active=True",
      "Raises an error because recursion is not allowed in ManyToMany annotations"
    ]
  },
  {
    "q": "Which of the following statements about proxy models combined with multi-table inheritance is TRUE?",
    "c": null,
    "o": [
      "A proxy model can customize Python behavior without creating a new table, while multi-table children create new tables with OneToOneField links",
      "Proxy models always create new database tables",
      "Multi-table inheritance ignores proxy models",
      "Proxy models cannot have custom managers"
    ]
  },
  {
    "q": "What does this deferred field combined with expression-based annotation accomplish?",
    "c": "MyModel.objects.only('title').annotate(max_score=Greatest(F('score1'), F('score2')))",
    "o": [
      "Loads only 'title' while computing the maximum of score1 and score2 at the database level",
      "Loads all fields immediately",
      "Updates score1 and score2 in the database",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which Meta option allows combining db_tablespaces with expression-based indexes?",
    "c": null,
    "o": [
      "db_tablespaces",
      "ordering",
      "unique_together",
      "managed"
    ]
  },
  {
    "q": "Which of the following is TRUE about combining transactional updates with annotations and F expressions?",
    "c": null,
    "o": [
      "Using transaction.atomic() ensures all updates using F expressions or annotations succeed or fail together",
      "Each update commits immediately, ignoring the transaction",
      "Transactional updates cannot include annotations",
      "F expressions are ignored in transactions"
    ]
  },
  {
    "q": "What does this nested Case/When annotation accomplish?",
    "c": "from django.db.models import Case, When, F\nMyModel.objects.annotate(final_score=Case(\n    When(score1__gt=50, then=F('score1')+F('bonus')), default=F('score2')))",
    "o": [
      "Computes 'final_score' conditionally: if score1>50, adds score1 and bonus; else uses score2",
      "Updates score1 and score2 in the database",
      "Filters rows where score1>50",
      "Raises a runtime error"
    ]
  },
  {
    "q": "Which of the following statements about advanced default_permissions and db_tablespaces is TRUE?",
    "c": null,
    "o": [
      "default_permissions sets the standard permissions and db_tablespaces allows specifying storage locations for tables and indexes",
      "default_permissions overrides db_tablespaces",
      "db_tablespaces automatically enforces unique constraints",
      "default_permissions disables admin access"
    ]
  },
  {
    "q": "What is the effect of this expression-based conditional index combined with a deferred field?",
    "c": "from django.db.models import Index, Upper\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n    class Meta:\n        indexes = [Index(fields=[], name='upper_name_active_idx', expressions=[Upper('name')], condition=Q(active=True))]",
    "o": [
      "Creates an index on uppercase 'name' for active rows while deferred fields are still loaded lazily",
      "Indexes all 'name' values regardless of active status",
      "Automatically enforces uniqueness for 'name'",
      "Raises a syntax error"
    ]
  },
  {
    "q": "Which of the following is TRUE about combining recursive ManyToMany, annotations, and transaction.atomic()?",
    "c": null,
    "o": [
      "All updates and annotations can be executed atomically, ensuring consistency across related objects",
      "Recursive ManyToMany updates cannot be transactional",
      "Annotations are ignored in transactions",
      "Transactions only affect single-table updates"
    ]
  },
  {
    "q": "Which of the following is TRUE about multi-level ManyToMany annotations?",
    "c": "from django.db.models import Count\nUser.objects.annotate(active_friends_of_friends=Count('friends__friends', filter=Q(friends__friends__active=True)))",
    "o": [
      "Counts friends of friends where active=True at the database level",
      "Counts all friends of friends regardless of active status",
      "Automatically updates nested friends to active=True",
      "Raises an error because multi-level ManyToMany annotations are not allowed"
    ]
  },
  {
    "q": "What does this conditional JSONField constraint accomplish?",
    "c": "from django.db.models import Q, CheckConstraint\nclass MyModel(models.Model):\n    config = models.JSONField()\n    class Meta:\n        constraints = [CheckConstraint(check=Q(config__settings__enabled=True), name='enabled_config')]",
    "o": [
      "Enforces that config.settings.enabled must be True for rows that exist, at the database level",
      "Applies to all rows regardless of condition",
      "Validates the constraint only in Python",
      "Automatically sets enabled=True if missing"
    ]
  },
  {
    "q": "Which of the following statements about nested aggregate annotations across related models is TRUE?",
    "c": null,
    "o": [
      "You can compute sums, counts, or other aggregates across related models using F expressions and double underscores",
      "Aggregates cannot reference related models",
      "You must fetch all related objects into Python first",
      "F expressions cannot be used in aggregate()"
    ]
  },
  {
    "q": "What is the effect of combining deferred fields with prefetch_related and annotations?",
    "c": null,
    "o": [
      "Deferred fields are loaded lazily while annotations and related object prefetches are executed efficiently at the database level",
      "Deferred fields force all related objects to be loaded immediately",
      "Annotations cannot be applied if fields are deferred",
      "Prefetch_related ignores deferred fields entirely"
    ]
  },
  {
    "q": "Which of the following Meta options allows creating multiple expression-based indexes with conditional filters?",
    "c": null,
    "o": [
      "indexes",
      "unique_together",
      "ordering",
      "db_table"
    ]
  },
  {
    "q": "What does this nested Coalesce and Greatest expression do?",
    "c": "from django.db.models import Coalesce, Greatest, F\nMyModel.objects.annotate(max_score=Greatest(Coalesce(F('score1'), 0), Coalesce(F('score2'), 0)))",
    "o": [
      "Computes the maximum value between score1 and score2, treating nulls as 0, at the database level",
      "Updates score1 and score2 in the database",
      "Filters rows where score1 or score2 is not null",
      "Raises an error"
    ]
  },
  {
    "q": "Which of the following is TRUE about transactional updates with nested JSONField and ManyToMany operations?",
    "c": null,
    "o": [
      "transaction.atomic() ensures all updates succeed or fail together, preserving database consistency",
      "Each update commits immediately, ignoring the transaction",
      "Transactions only work with single-table updates",
      "JSONField updates cannot be included in transactions"
    ]
  },
  {
    "q": "Which of the following statements about advanced deferred + select_related + prefetch_related combinations is TRUE?",
    "c": null,
    "o": [
      "You can defer fields while still efficiently loading related objects using select_related and prefetch_related",
      "Deferred fields cannot be combined with select_related",
      "Prefetch_related forces all deferred fields to be loaded",
      "Annotations cannot be applied if fields are deferred"
    ]
  },
  {
    "q": "Which of the following is TRUE about using `UniqueConstraint` with conditions?",
    "c": "from django.db.models import UniqueConstraint, Q\nclass Booking(models.Model):\n    room = models.IntegerField()\n    date = models.DateField()\n    class Meta:\n        constraints = [UniqueConstraint(fields=['room', 'date'], condition=Q(cancelled=False), name='unique_active_booking')]",
    "o": [
      "It enforces uniqueness of room and date only for rows where cancelled=False",
      "It enforces uniqueness of room and date for all rows",
      "It enforces uniqueness in Python only, not in the database",
      "It prevents rows with cancelled=True from being saved"
    ]
  },
  {
    "q": "Which is TRUE about `Meta.indexes` vs `Meta.constraints`?",
    "c": null,
    "o": [
      "`indexes` improves query performance, while `constraints` enforce data integrity",
      "`constraints` improve query performance, while `indexes` enforce data integrity",
      "Both serve the same purpose and are interchangeable",
      "Constraints can only be defined at the database level, not in Django"
    ]
  },
  {
    "q": "Which of the following is TRUE about custom model managers?",
    "c": "class ActiveManager(models.Manager):\n    def get_queryset(self):\n        return super().get_queryset().filter(active=True)",
    "o": [
      "They allow customizing the default queryset returned by model queries",
      "They disable filtering capabilities on the model",
      "They can only be used with proxy models",
      "They must be defined inside Meta class"
    ]
  },
  {
    "q": "What happens when you define `ordering = ['-created']` in Meta?",
    "c": null,
    "o": [
      "All queries will return results ordered by created in descending order unless overridden",
      "The database enforces descending order on inserted rows",
      "Only the admin uses the ordering",
      "It prevents custom order_by() from being used"
    ]
  },
  {
    "q": "Which of the following is TRUE about the `select_for_update()` method?",
    "c": null,
    "o": [
      "It locks the selected rows for the duration of the transaction to prevent concurrent updates",
      "It deletes rows after they are selected",
      "It forces eager loading of related objects",
      "It skips transaction handling and commits immediately"
    ]
  },
  {
    "q": "Which is the effect of using `unique_together = (('field1', 'field2'),)` in Meta?",
    "c": null,
    "o": [
      "It enforces uniqueness across the combination of field1 and field2",
      "It enforces uniqueness individually on both field1 and field2",
      "It prevents both fields from being null",
      "It creates an index automatically but does not enforce uniqueness"
    ]
  },
  {
    "q": "What is the difference between `Meta.db_table` and `Meta.db_tablespace`?",
    "c": null,
    "o": [
      "`db_table` changes the table name, while `db_tablespace` specifies the storage location of indexes and tables",
      "`db_tablespace` changes the table name, while `db_table` specifies index storage",
      "Both specify the same thing",
      "db_tablespace is ignored in modern Django versions"
    ]
  },
  {
    "q": "What does the following query do?",
    "c": "Book.objects.defer('summary').only('title')",
    "o": [
      "Loads only the title field immediately, deferring summary and other fields until accessed",
      "Loads all fields including summary immediately",
      "Loads summary but not title",
      "Raises a runtime error due to conflict"
    ]
  },
  {
    "q": "What happens when multiple filters are chained in Django ORM?",
    "c": "qs = Book.objects.filter(published=True).filter(rating__gte=4)",
    "o": [
      "They are combined with SQL AND logic",
      "They are combined with SQL OR logic",
      "Only the last filter is applied",
      "It raises an error if both filters are applied"
    ]
  },
  {
    "q": "Which of the following is TRUE about using raw() queries in Django?",
    "c": null,
    "o": [
      "raw() allows execution of custom SQL queries and maps the results to model instances",
      "raw() can only be used for SELECT queries without joins",
      "raw() ignores model fields and returns dictionaries",
      "raw() automatically adds migrations for the query"
    ]
  },
  {
    "q": "Which of the following is TRUE about database routers?",
    "c": null,
    "o": [
      "They control which database a model's read and write operations go to",
      "They are only used for caching queries",
      "They prevent using migrations across multiple databases",
      "They are required for every Django project"
    ]
  },
  {
    "q": "What does using `Meta.unique_together` vs `UniqueConstraint` signify in modern Django?",
    "c": null,
    "o": [
      "unique_together is deprecated in favor of UniqueConstraint",
      "UniqueConstraint is deprecated in favor of unique_together",
      "They are both identical and equally recommended",
      "unique_together is required when multiple databases are used"
    ]
  },
  {
    "q": "Which Meta option specifies the default database tablespace used for indexes?",
    "c": null,
    "o": [
      "index_tablespace",
      "db_table",
      "db_constraints",
      "db_index"
    ]
  },
  {
    "q": "What does this queryset return?",
    "c": "Author.objects.none()",
    "o": [
      "An empty queryset that can still be combined with others",
      "All authors in the database",
      "Raises an error",
      "None object instead of queryset"
    ]
  },
  {
    "q": "Which of the following is TRUE about multi-database setups?",
    "c": null,
    "o": [
      "Queries can be directed to specific databases using `using('db_alias')`",
      "Django ORM automatically balances queries across databases",
      "Models can only belong to the default database",
      "Transactions cannot span multiple databases"
    ]
  },
  {
    "q": "Which of the following statements about `QuerySet.exists()` is TRUE?",
    "c": null,
    "o": [
      "It performs a highly optimized query that stops at the first matching row",
      "It loads all rows into memory before returning True or False",
      "It is identical to checking `len(QuerySet)`",
      "It always raises an error if the queryset is empty"
    ]
  },
  {
    "q": "Which of the following is TRUE about abstract base classes in Django?",
    "c": "class Base(models.Model):\n    created = models.DateTimeField(auto_now_add=True)\n    class Meta:\n        abstract = True",
    "o": [
      "The Base model is not created in the database, but its fields are inherited by subclasses",
      "The Base model creates its own database table and subclasses link to it with a foreign key",
      "The Base model can only be used with proxy inheritance",
      "The Base model cannot define fields"
    ]
  },
  {
    "q": "Which of the following is TRUE about proxy models?",
    "c": "class ProxyBook(Book):\n    class Meta:\n        proxy = True",
    "o": [
      "They do not create a new table but allow changing behavior (methods, managers, ordering) of the base model",
      "They create a new table linked to the original one",
      "They are required for model inheritance to work",
      "They cannot override managers"
    ]
  },
  {
    "q": "Which of the following is TRUE about multi-table inheritance?",
    "c": "class Place(models.Model):\n    name = models.CharField(max_length=50)\nclass Restaurant(Place):\n    serves_pizza = models.BooleanField(default=False)",
    "o": [
      "Each subclass gets its own table linked to the parent with a OneToOneField",
      "Only one table is created for both Place and Restaurant",
      "Subclasses cannot define new fields",
      "Multi-table inheritance is identical to abstract inheritance"
    ]
  },
  {
    "q": "Which of the following is TRUE about Meta `order_with_respect_to`?",
    "c": null,
    "o": [
      "It allows ordering related objects with respect to a ForeignKey, often used for ordered relationships",
      "It forces global ordering across all models",
      "It disables ordering completely",
      "It only works with ManyToMany fields"
    ]
  },
  {
    "q": "What happens when `Meta.app_label` is specified in a model?",
    "c": null,
    "o": [
      "It forces the model to belong to a different app than the one it is defined in",
      "It removes the model from migrations",
      "It sets the verbose name of the app",
      "It enforces database schema changes automatically"
    ]
  },
  {
    "q": "Which of the following is TRUE about model signals?",
    "c": "from django.db.models.signals import post_save\n@receiver(post_save, sender=Book)\ndef book_created(sender, instance, created, **kwargs):\n    if created:\n        print('Book created:', instance.title)",
    "o": [
      "Signals like post_save can execute custom logic automatically when model instances are saved",
      "Signals must always return a value to save the model",
      "Signals replace model methods completely",
      "Signals cannot access the model instance"
    ]
  },
  {
    "q": "Which of the following statements is TRUE about indexes defined in Meta?",
    "c": "class Meta:\n    indexes = [models.Index(fields=['title', 'author'])]",
    "o": [
      "They improve lookup performance on the specified fields",
      "They enforce uniqueness on the specified fields",
      "They replace constraints completely",
      "They prevent duplicate rows automatically"
    ]
  },
  {
    "q": "Which of the following is TRUE about deferred fields?",
    "c": "book = Book.objects.only('title').get(id=1)",
    "o": [
      "Only the title field is loaded immediately, other fields are deferred until accessed",
      "All fields are loaded immediately",
      "Only the primary key is loaded",
      "Deferred fields are never loaded even if accessed"
    ]
  },
  {
    "q": "Which of the following is TRUE about UniqueConstraint in Django?",
    "c": "class Meta:\n    constraints = [models.UniqueConstraint(fields=['title', 'author'], name='unique_book_author')]",
    "o": [
      "It enforces uniqueness across the specified fields at the database level",
      "It only enforces uniqueness at the Python layer",
      "It is identical to unique_together and does not require a name",
      "It enforces uniqueness only on primary keys"
    ]
  },
  {
    "q": "Which of the following is TRUE about CheckConstraint?",
    "c": "class Meta:\n    constraints = [models.CheckConstraint(check=models.Q(price__gte=0), name='price_non_negative')]",
    "o": [
      "It enforces database-level validation for conditions such as non-negative fields",
      "It only applies validation in Django forms",
      "It automatically converts to a Python if-statement",
      "It only works with BooleanFields"
    ]
  },
  {
    "q": "What happens when a constraint name is not unique across models?",
    "c": null,
    "o": [
      "It causes a migration error since constraint names must be unique per database",
      "Django auto-generates a unique name silently",
      "It ignores the duplicate and continues",
      "It overwrites the existing constraint automatically"
    ]
  },
  {
    "q": "Which of the following is TRUE about `Meta.indexes`?",
    "c": null,
    "o": [
      "They can define single-field, multi-field, or functional indexes to improve query performance",
      "They enforce uniqueness on specified fields",
      "They are required for ForeignKey relationships",
      "They automatically apply to all CharFields"
    ]
  },
  {
    "q": "Which of the following is TRUE about conditional indexes?",
    "c": "from django.db.models import Q, Index\nclass Meta:\n    indexes = [Index(fields=['status'], name='status_idx', condition=Q(active=True))]",
    "o": [
      "They create an index only for rows matching the given condition",
      "They create a global index regardless of condition",
      "They enforce uniqueness conditionally",
      "They are ignored during migrations"
    ]
  },
  {
    "q": "Which Meta option can be used to set the default order of querysets?",
    "c": null,
    "o": [
      "ordering",
      "indexes",
      "constraints",
      "app_label"
    ]
  },
  {
    "q": "Which of the following is TRUE about `db_index=True` on a model field?",
    "c": "class Product(models.Model):\n    sku = models.CharField(max_length=50, db_index=True)",
    "o": [
      "It creates a database index on the field to speed up lookups",
      "It enforces uniqueness on the field",
      "It prevents null values in the field",
      "It makes the field read-only"
    ]
  },
  {
    "q": "What is the effect of setting `unique=True` on a field?",
    "c": "class User(models.Model):\n    email = models.EmailField(unique=True)",
    "o": [
      "It enforces uniqueness at the database level on that column",
      "It only enforces uniqueness in Django admin forms",
      "It prevents null values automatically",
      "It creates a ForeignKey implicitly"
    ]
  },
  {
    "q": "Which command is used to generate new migrations after changing models?",
    "c": null,
    "o": [
      "python manage.py makemigrations",
      "python manage.py migrate",
      "python manage.py sqlmigrate",
      "python manage.py collectstatic"
    ]
  },
  {
    "q": "Which command actually applies migrations to the database?",
    "c": null,
    "o": [
      "python manage.py migrate",
      "python manage.py makemigrations",
      "python manage.py sqlflush",
      "python manage.py inspectdb"
    ]
  },
  {
    "q": "Which of the following is TRUE about `RunPython` migration operation?",
    "c": "from django.db import migrations\n\ndef forwards(apps, schema_editor):\n    Book = apps.get_model('library', 'Book')\n    Book.objects.create(title='Default Book')\n\nclass Migration(migrations.Migration):\n    operations = [migrations.RunPython(forwards)]",
    "o": [
      "It allows executing custom Python code during a migration",
      "It only works for creating database indexes",
      "It is used only to rename database tables",
      "It replaces `migrate` command"
    ]
  },
  {
    "q": "Which of the following is TRUE about `RunSQL` in migrations?",
    "c": null,
    "o": [
      "It allows executing raw SQL statements within a migration",
      "It is used only for PostgreSQL databases",
      "It automatically converts raw SQL into ORM queries",
      "It replaces `RunPython` in all cases"
    ]
  },
  {
    "q": "What is the purpose of squashing migrations?",
    "c": null,
    "o": [
      "To combine multiple migrations into a single file for optimization",
      "To delete unused migrations automatically",
      "To apply migrations without downtime",
      "To rollback only the latest migration"
    ]
  },
  {
    "q": "Which of the following best describes a migration conflict?",
    "c": null,
    "o": [
      "When two different branches create migrations that depend on the same previous migration",
      "When a migration file is missing",
      "When the database is offline during migration",
      "When migrations are squashed incorrectly"
    ]
  },
  {
    "q": "Which command shows the SQL statements generated by a migration without applying it?",
    "c": null,
    "o": [
      "python manage.py sqlmigrate",
      "python manage.py showmigrations",
      "python manage.py migrate --dry-run",
      "python manage.py inspectdb"
    ]
  },
  {
    "q": "What does `Migration.dependencies` define in a migration file?",
    "c": "class Migration(migrations.Migration):\n    dependencies = [('library', '0002_auto')]\n    operations = []",
    "o": [
      "Other migrations that must be applied before this one",
      "The database engine used for migration",
      "The apps excluded from migration",
      "The initial models to inspect"
    ]
  },
  {
    "q": "Which of the following is TRUE about reversible migrations in Django?",
    "c": "class Migration(migrations.Migration):\n    operations = [migrations.RunPython(forwards, backwards)]",
    "o": [
      "They provide both a forwards and backwards function so the migration can be undone",
      "They can only move the database schema forward",
      "They never require a backwards function",
      "They automatically undo themselves"
    ]
  },
  {
    "q": "Which of the following describes an irreversible migration?",
    "c": null,
    "o": [
      "A migration that cannot be undone because no reverse logic was provided",
      "A migration that only works with PostgreSQL",
      "A migration that deletes an entire database",
      "A migration that renames an app"
    ]
  },
  {
    "q": "What does the `--fake` option do when running `migrate`?",
    "c": null,
    "o": [
      "Marks a migration as applied in the database without running its SQL or Python operations",
      "Skips migration dependencies entirely",
      "Forces a rollback of the latest migration",
      "Runs migrations in test mode only"
    ]
  },
  {
    "q": "Which of the following is TRUE about `separate_database_and_state`?",
    "c": "class Migration(migrations.Migration):\n    operations = [\n        migrations.SeparateDatabaseAndState(\n            database_operations=[migrations.RunSQL(\"CREATE INDEX idx_name ON my_table(name)\")],\n            state_operations=[migrations.AddField('Book', 'name', models.CharField(max_length=100))]\n        )\n    ]",
    "o": [
      "It allows database schema and Django model state to be changed independently",
      "It forces database and state changes to always match",
      "It prevents schema changes from being applied",
      "It removes model fields automatically"
    ]
  },
  {
    "q": "Which command lists all migrations and whether they have been applied?",
    "c": null,
    "o": [
      "python manage.py showmigrations",
      "python manage.py sqlmigrate",
      "python manage.py inspectdb",
      "python manage.py check"
    ]
  },
  {
    "q": "Which of the following best describes a data migration?",
    "c": null,
    "o": [
      "A migration that manipulates or initializes data, often with RunPython",
      "A migration that only creates database tables",
      "A migration used only for fixtures",
      "A migration that deletes old migration files"
    ]
  },
  {
    "q": "Which of the following is TRUE about squashed migrations?",
    "c": null,
    "o": [
      "They include a `replaces` attribute listing the original migrations that were combined",
      "They automatically delete the original migration files",
      "They cannot be edited manually",
      "They cannot contain data migrations"
    ]
  },
  {
    "q": "Which of the following is TRUE about fake-initial migrations?",
    "c": "python manage.py migrate --fake-initial",
    "o": [
      "They mark initial migrations as applied if the database schema already matches the models",
      "They skip all future migrations automatically",
      "They force creation of empty migration files",
      "They rollback all migrations to the first"
    ]
  },
  {
    "q": "What is the recommended way to rename a field in Django to avoid data loss?",
    "c": null,
    "o": [
      "Use the RenameField migration operation",
      "Delete the old field and add a new one with the new name",
      "Manually edit the database table column",
      "Change the field name in models.py and rerun makemigrations"
    ]
  },
  {
    "q": "What happens if you add a new non-nullable field without a default value?",
    "c": "class Book(models.Model):\n    title = models.CharField(max_length=100)\n    pages = models.IntegerField()  # newly added non-nullable field",
    "o": [
      "Django will ask for a one-off default during makemigrations",
      "The migration will silently assign NULL values",
      "The migration will fail until you set unique=True",
      "Django automatically fills with zeros"
    ]
  },
  {
    "q": "Which of the following is TRUE about circular migration dependencies?",
    "c": null,
    "o": [
      "They occur when two migration files depend on each other, creating a loop",
      "They occur only when using third-party apps",
      "They are automatically resolved by Django",
      "They only happen with data migrations"
    ]
  },
  {
    "q": "What is a common best practice for handling large data migrations?",
    "c": null,
    "o": [
      "Break the migration into batches to avoid locking or timeouts",
      "Always run them inside RunSQL instead of RunPython",
      "Disable transactions for better consistency",
      "Run them only on SQLite"
    ]
  },
  {
    "q": "Which of the following is TRUE about the `preserve_default` option in AddField migrations?",
    "c": "migrations.AddField(\n    model_name='book',\n    name='published',\n    field=models.BooleanField(default=True),\n    preserve_default=False\n)",
    "o": [
      "It applies the default value to existing rows but does not keep it for future model instances",
      "It permanently removes the default value from the database",
      "It preserves unique constraints only",
      "It prevents migrations from running on large tables"
    ]
  },
  {
    "q": "Which command can be used to detect unapplied or conflicting migrations?",
    "c": null,
    "o": [
      "python manage.py makemigrations --check",
      "python manage.py check",
      "python manage.py sqlflush",
      "python manage.py validate"
    ]
  },
  {
    "q": "Which of the following is TRUE about altering a field’s type?",
    "c": "class Book(models.Model):\n    title = models.CharField(max_length=100)\n    pages = models.CharField(max_length=10)  # changed from IntegerField",
    "o": [
      "It generates an AlterField migration, which may involve data conversion at the database level",
      "It always deletes and recreates the column, losing data",
      "It silently ignores the type change",
      "It only works for CharFields"
    ]
  },
  {
    "q": "Which of the following is TRUE about `RunPython.noop`?",
    "c": "operations = [migrations.RunPython(migrations.RunPython.noop)]",
    "o": [
      "It acts as a placeholder operation that does nothing",
      "It resets all data in the model",
      "It reverses the migration automatically",
      "It generates raw SQL instead of Python"
    ]
  },
  {
    "q": "Which of the following is TRUE about a ForeignKey in Django?",
    "c": "class Book(models.Model):\n    author = models.ForeignKey('Author', on_delete=models.CASCADE)",
    "o": [
      "It creates a many-to-one relationship between Book and Author",
      "It creates a one-to-one relationship between Book and Author",
      "It creates a many-to-many relationship automatically",
      "It does not enforce referential integrity"
    ]
  },
  {
    "q": "Which of the following is TRUE about OneToOneField?",
    "c": "class Profile(models.Model):\n    user = models.OneToOneField(User, on_delete=models.CASCADE)",
    "o": [
      "It ensures a unique, one-to-one mapping between two models",
      "It behaves the same as ForeignKey with null=False",
      "It is only used for admin models",
      "It cannot be used with the built-in User model"
    ]
  },
  {
    "q": "What is the effect of `related_name` in a relationship field?",
    "c": "class Book(models.Model):\n    author = models.ForeignKey(Author, related_name='books', on_delete=models.CASCADE)",
    "o": [
      "It changes the reverse lookup name from Author to access related Book objects",
      "It enforces uniqueness on the relationship",
      "It prevents the creation of database indexes",
      "It disables cascading deletes"
    ]
  },
  {
    "q": "Which of the following is TRUE about ManyToManyField?",
    "c": "class Student(models.Model):\n    courses = models.ManyToManyField('Course')",
    "o": [
      "It creates an intermediate join table to manage the relationship",
      "It duplicates all related records into both tables",
      "It requires defining a custom through table manually",
      "It can only be used between models in the same app"
    ]
  },
  {
    "q": "Which of the following is TRUE about symmetrical ManyToManyField?",
    "c": "class Person(models.Model):\n    friends = models.ManyToManyField('self')",
    "o": [
      "Adding one relation automatically creates the reverse relation",
      "It disables reverse relationships",
      "It is only valid with OneToOneField",
      "It requires through models always"
    ]
  },
  {
    "q": "Which argument is required for ForeignKey to define how deletions are handled?",
    "c": null,
    "o": [
      "on_delete",
      "cascade",
      "related_name",
      "blank"
    ]
  },
  {
    "q": "Which of the following is TRUE about using `through` in ManyToManyField?",
    "c": "class Membership(models.Model):\n    person = models.ForeignKey(Person, on_delete=models.CASCADE)\n    group = models.ForeignKey(Group, on_delete=models.CASCADE)\n    date_joined = models.DateField()\n\nclass Group(models.Model):\n    members = models.ManyToManyField(Person, through='Membership')",
    "o": [
      "It allows defining a custom intermediate model with extra fields",
      "It disables reverse relationships entirely",
      "It removes the need for a join table",
      "It only works with symmetrical relationships"
    ]
  },
  {
    "q": "Which of the following is TRUE about `related_query_name`?",
    "c": "class Book(models.Model):\n    author = models.ForeignKey(Author, related_query_name='literature', on_delete=models.CASCADE)",
    "o": [
      "It defines the name used for reverse filtering in queries",
      "It disables reverse lookups",
      "It is identical to related_name",
      "It forces the use of select_related automatically"
    ]
  },
  {
    "q": "What happens if two ForeignKeys in the same model point to the same related model without specifying `related_name`?",
    "c": "class Friendship(models.Model):\n    from_user = models.ForeignKey(User, on_delete=models.CASCADE)\n    to_user = models.ForeignKey(User, on_delete=models.CASCADE)",
    "o": [
      "Django will raise an error asking for distinct related_names",
      "Django automatically appends '_set' for one of them",
      "It works fine with no conflicts",
      "It disables reverse relationships for both"
    ]
  },
  {
    "q": "What does `related_name='+'` do in a relationship field?",
    "c": "class Employee(models.Model):\n    manager = models.ForeignKey('self', on_delete=models.CASCADE, related_name='+')",
    "o": [
      "It disables the reverse relation from the target model",
      "It creates a duplicate reverse accessor",
      "It enforces uniqueness on the relation",
      "It creates a through table automatically"
    ]
  },
  {
    "q": "Which of the following is TRUE about self-referential ForeignKey?",
    "c": "class Category(models.Model):\n    parent = models.ForeignKey('self', null=True, blank=True, on_delete=models.SET_NULL)",
    "o": [
      "It allows hierarchical relationships within the same model",
      "It always requires symmetrical=True",
      "It cannot be used with null=True",
      "It automatically deletes all children when parent is deleted, regardless of on_delete"
    ]
  },
  {
    "q": "Which is the default reverse accessor for a ForeignKey if no related_name is set?",
    "c": "class Book(models.Model):\n    author = models.ForeignKey(Author, on_delete=models.CASCADE)",
    "o": [
      "author.book_set",
      "author.books",
      "author.related",
      "author.reverse"
    ]
  },
  {
    "q": "Which of the following is TRUE about `unique_together` in relationships?",
    "c": "class Enrollment(models.Model):\n    student = models.ForeignKey(Student, on_delete=models.CASCADE)\n    course = models.ForeignKey(Course, on_delete=models.CASCADE)\n\n    class Meta:\n        unique_together = ('student', 'course')",
    "o": [
      "It prevents duplicate pairs of (student, course) in the database",
      "It prevents duplicate students globally",
      "It enforces uniqueness only on the student field",
      "It enforces uniqueness only on the course field"
    ]
  },
  {
    "q": "Which of the following is TRUE about reverse relationship managers?",
    "c": "author = Author.objects.get(id=1)\nbooks = author.book_set.all()",
    "o": [
      "They return QuerySets of related objects and support filtering",
      "They always return lists, not QuerySets",
      "They can only be accessed in admin, not in code",
      "They cannot be chained with filter()"
    ]
  },
  {
    "q": "What does `related_query_name` affect in relationships?",
    "c": "class Book(models.Model):\n    author = models.ForeignKey(Author, related_query_name='literature', on_delete=models.CASCADE)\n\nAuthor.objects.filter(literature__title__icontains='Python')",
    "o": [
      "It changes the keyword used in reverse lookups for filtering queries",
      "It changes the manager name from book_set to literature",
      "It disables forward lookups",
      "It enforces uniqueness in reverse queries"
    ]
  },
  {
    "q": "Which of the following is TRUE about ManyToManyField with `symmetrical=False`?",
    "c": "class Person(models.Model):\n    followers = models.ManyToManyField('self', symmetrical=False, related_name='following')",
    "o": [
      "It makes the relationship one-directional instead of mutual",
      "It forces Django to create two separate join tables",
      "It prevents the use of related_name",
      "It disables filtering on the reverse relation"
    ]
  },
  {
    "q": "What is the difference between `unique_together` and `UniqueConstraint` in Django models?",
    "c": "class Enrollment(models.Model):\n    student = models.ForeignKey(Student, on_delete=models.CASCADE)\n    course = models.ForeignKey(Course, on_delete=models.CASCADE)\n\n    class Meta:\n        constraints = [\n            models.UniqueConstraint(fields=['student', 'course'], name='unique_enrollment')\n        ]",
    "o": [
      "UniqueConstraint is more flexible and can be conditionally applied, while unique_together is older and less flexible",
      "They are identical and interchangeable in all cases",
      "unique_together enforces uniqueness only at the Python level, not in the database",
      "UniqueConstraint can only be used with ManyToManyField"
    ]
  },
  {
    "q": "What does setting `db_constraint=False` on a ForeignKey do?",
    "c": "class Order(models.Model):\n    customer = models.ForeignKey(Customer, on_delete=models.CASCADE, db_constraint=False)",
    "o": [
      "It prevents Django from creating a database-level foreign key constraint",
      "It disables the ability to query the related object",
      "It automatically makes the field nullable",
      "It forces cascading deletes even if on_delete is set to PROTECT"
    ]
  },
  {
    "q": "Which of the following is TRUE about CheckConstraint across relationships?",
    "c": "class Order(models.Model):\n    product = models.ForeignKey(Product, on_delete=models.CASCADE)\n    quantity = models.PositiveIntegerField()\n\n    class Meta:\n        constraints = [\n            models.CheckConstraint(check=models.Q(quantity__gte=1), name='min_quantity')\n        ]",
    "o": [
      "It enforces the condition at the database level for all records",
      "It only validates at the Django form level",
      "It can only be applied to CharField values",
      "It requires raw SQL, not ORM expressions"
    ]
  },
  {
    "q": "What does `select_related()` do with ForeignKey relationships?",
    "c": "books = Book.objects.select_related('author').all()",
    "o": [
      "It performs a SQL join to fetch related objects in a single query",
      "It fetches related objects lazily, one per query",
      "It creates a prefetch cache that must be manually refreshed",
      "It disables reverse relationship lookups"
    ]
  },
  {
    "q": "What is the main difference between `select_related()` and `prefetch_related()`?",
    "c": null,
    "o": [
      "select_related() uses SQL joins for single-valued relations, while prefetch_related() issues separate queries for multi-valued relations",
      "prefetch_related() is faster in all cases than select_related()",
      "select_related() only works in the Django admin",
      "prefetch_related() cannot be used with ManyToManyField"
    ]
  },
  {
    "q": "Which of the following is TRUE about using `limit_choices_to` in a relationship field?",
    "c": "class Book(models.Model):\n    author = models.ForeignKey(Author, limit_choices_to={'active': True}, on_delete=models.CASCADE)",
    "o": [
      "It restricts the choices available in forms and admin for that relationship",
      "It enforces filtering at the database level on all queries",
      "It disables the relationship unless the condition is met",
      "It prevents migrations from being applied"
    ]
  },
  {
    "q": "What does `to_field` do in a ForeignKey definition?",
    "c": "class Order(models.Model):\n    customer = models.ForeignKey(Customer, to_field='email', on_delete=models.CASCADE)",
    "o": [
      "It makes the relationship reference a non-primary key field",
      "It creates a secondary unique constraint automatically",
      "It disables reverse relationships",
      "It forces the use of AutoField only"
    ]
  },
  {
    "q": "Which of the following is TRUE about `PROTECT` in on_delete?",
    "c": null,
    "o": [
      "It raises a ProtectedError and prevents deletion if related objects exist",
      "It silently ignores the deletion request",
      "It cascades the delete operation to related objects",
      "It sets the related field to NULL automatically"
    ]
  },
  {
    "q": "What is the main difference between an abstract base class and multi-table inheritance in Django?",
    "c": null,
    "o": [
      "Abstract base classes do not create a table, while multi-table inheritance creates a separate table for each subclass",
      "Abstract base classes enforce single-table usage, while multi-table inheritance enforces proxy usage",
      "Multi-table inheritance removes the need for ForeignKey, while abstract bases require one",
      "They behave the same; both always create separate tables"
    ]
  },
  {
    "q": "What is the purpose of `abstract = True` in Django model Meta options?",
    "c": "class TimestampedModel(models.Model):\n    created_at = models.DateTimeField(auto_now_add=True)\n    updated_at = models.DateTimeField(auto_now=True)\n\n    class Meta:\n        abstract = True",
    "o": [
      "It marks the model as abstract so no table is created for it",
      "It makes the model immutable",
      "It enforces all subclasses to use ForeignKey relationships",
      "It requires proxy models to subclass it"
    ]
  },
  {
    "q": "Which of the following is TRUE about proxy models in Django?",
    "c": "class CustomUser(User):\n    class Meta:\n        proxy = True",
    "o": [
      "They do not create a new table but allow modifying behavior like managers or ordering",
      "They duplicate the database schema of the parent model",
      "They require multi-table inheritance",
      "They cannot override model managers"
    ]
  },
  {
    "q": "In multi-table inheritance, how does Django link child models to the parent?",
    "c": "class Place(models.Model):\n    name = models.CharField(max_length=50)\n\nclass Restaurant(Place):\n    serves_pizza = models.BooleanField(default=False)",
    "o": [
      "By creating an implicit OneToOneField from child to parent",
      "By using a ForeignKey from parent to child",
      "By duplicating parent fields in the child table",
      "By enforcing abstract=True implicitly"
    ]
  },
  {
    "q": "Which of the following is TRUE about using `Meta: ordering` in models?",
    "c": "class Book(models.Model):\n    title = models.CharField(max_length=100)\n    published = models.DateField()\n\n    class Meta:\n        ordering = ['-published']",
    "o": [
      "It applies default ordering to QuerySets of the model",
      "It enforces sorting at the database schema level",
      "It prevents custom ordering using order_by()",
      "It only affects the Django admin, not ORM queries"
    ]
  },
  {
    "q": "What happens if a proxy model and its base model are both registered in the Django admin?",
    "c": null,
    "o": [
      "They appear as separate entries in the admin",
      "The proxy model replaces the base model automatically",
      "The admin refuses to register the proxy model",
      "The base model inherits the proxy’s Meta options"
    ]
  },
  {
    "q": "Which of the following is TRUE about `Meta: db_table`?",
    "c": "class Customer(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        db_table = 'my_custom_table'",
    "o": [
      "It allows overriding the default table name Django generates",
      "It enforces database schema migrations to always reset",
      "It prevents the model from being synced to the database",
      "It creates a separate index automatically"
    ]
  },
  {
    "q": "What is the purpose of `Meta: unique_together` in an abstract base class?",
    "c": "class BaseEnrollment(models.Model):\n    student = models.ForeignKey(Student, on_delete=models.CASCADE)\n    course = models.ForeignKey(Course, on_delete=models.CASCADE)\n\n    class Meta:\n        abstract = True\n        unique_together = ('student', 'course')",
    "o": [
      "It is inherited by subclasses and applied at their table level",
      "It applies globally across all subclasses",
      "It raises an error because unique_together cannot be used in abstract models",
      "It automatically adds indexes to all related models"
    ]
  },
  {
    "q": "What does `Meta: indexes` allow you to do in a Django model?",
    "c": "class Product(models.Model):\n    name = models.CharField(max_length=100)\n    price = models.DecimalField(max_digits=10, decimal_places=2)\n\n    class Meta:\n        indexes = [models.Index(fields=['name', 'price'])]",
    "o": [
      "It defines custom database indexes to optimize queries",
      "It enforces uniqueness across the fields",
      "It prevents filtering by multiple fields",
      "It is only applicable in abstract base models"
    ]
  },
  {
    "q": "What is the difference between `unique_together` and `indexes`?",
    "c": null,
    "o": [
      "unique_together enforces uniqueness, while indexes only improve query performance",
      "They both enforce uniqueness but in different ways",
      "indexes enforce uniqueness, while unique_together improves query performance",
      "They are synonyms for the same functionality"
    ]
  },
  {
    "q": "What does `Meta: constraints` allow that `unique_together` does not?",
    "c": "class Employee(models.Model):\n    age = models.IntegerField()\n\n    class Meta:\n        constraints = [\n            models.CheckConstraint(check=models.Q(age__gte=18), name='adult_only')\n        ]",
    "o": [
      "It supports conditional and check constraints beyond uniqueness",
      "It makes migrations optional",
      "It only applies constraints in Python, not the database",
      "It automatically generates proxy models"
    ]
  },
  {
    "q": "How do you enforce that a ForeignKey must point only to active users?",
    "c": "class Profile(models.Model):\n    user = models.ForeignKey(User, limit_choices_to={'is_active': True}, on_delete=models.CASCADE)",
    "o": [
      "By using limit_choices_to in the ForeignKey definition",
      "By overriding the model’s save() method",
      "By setting db_constraint=False",
      "By using abstract base models"
    ]
  },
  {
    "q": "Which of the following is TRUE about `Meta: ordering` when combined with related fields?",
    "c": "class Book(models.Model):\n    author = models.ForeignKey(Author, on_delete=models.CASCADE)\n\n    class Meta:\n        ordering = ['author__name']",
    "o": [
      "You can order by related fields using double underscores",
      "It raises an error since ordering cannot use relationships",
      "It only works inside the Django admin, not in ORM queries",
      "It disables filtering by related fields"
    ]
  },
  {
    "q": "What is the effect of setting `db_table = '\"CustomName\"'` in Meta options?",
    "c": null,
    "o": [
      "It forces Django to use a quoted custom table name",
      "It disables migrations for the model",
      "It creates a temporary table instead of a permanent one",
      "It makes the model abstract implicitly"
    ]
  },
  {
    "q": "What does `unique_for_date` do in a model field?",
    "c": "class Article(models.Model):\n    title = models.CharField(max_length=200, unique_for_date='published')\n    published = models.DateField()",
    "o": [
      "It prevents duplicate values of the field for the same date",
      "It prevents saving any duplicate titles at all",
      "It enforces uniqueness across all models in the app",
      "It only applies in the admin and not in ORM queries"
    ]
  },
  {
    "q": "What happens when you use `Index(fields=['-field_name'])` in Meta?",
    "c": "class Event(models.Model):\n    start_time = models.DateTimeField()\n\n    class Meta:\n        indexes = [models.Index(fields=['-start_time'])]",
    "o": [
      "It creates a descending index on the field",
      "It disables filtering by that field",
      "It enforces uniqueness on that field",
      "It only works with CharField types"
    ]
  },
  {
    "q": "In Django multi-table inheritance, how are queries on the parent model affected?",
    "c": "class Vehicle(models.Model):\n    name = models.CharField(max_length=50)\n\nclass Car(Vehicle):\n    seats = models.IntegerField()",
    "o": [
      "A query on Vehicle will not automatically include fields from Car unless explicitly joined",
      "A query on Vehicle will always include Car fields",
      "Vehicle queries automatically filter out child models",
      "Django prevents querying the parent model directly"
    ]
  },
  {
    "q": "What happens if you define `proxy = True` and `abstract = True` in the same model’s Meta?",
    "c": null,
    "o": [
      "Django raises an error because a model cannot be both abstract and proxy",
      "The model becomes both abstract and proxy simultaneously",
      "Django silently ignores `abstract = True`",
      "Django treats the model as a regular multi-table inheritance class"
    ]
  },
  {
    "q": "Which statement is TRUE about `CheckConstraint` with related fields?",
    "c": "class Order(models.Model):\n    quantity = models.IntegerField()\n    product = models.ForeignKey(Product, on_delete=models.CASCADE)\n\n    class Meta:\n        constraints = [\n            models.CheckConstraint(check=models.Q(quantity__gte=1), name='positive_quantity')\n        ]",
    "o": [
      "CheckConstraint can only be applied on the current model’s fields, not across relationships",
      "CheckConstraint automatically validates across relationships",
      "CheckConstraint is ignored in migrations for related fields",
      "CheckConstraint enforces uniqueness implicitly"
    ]
  },
  {
    "q": "How does Django enforce uniqueness across multiple tables in multi-table inheritance?",
    "c": null,
    "o": [
      "It does not; uniqueness constraints apply only at the table level",
      "It enforces uniqueness globally across the inheritance tree",
      "It creates database triggers automatically",
      "It merges child and parent fields into a single table"
    ]
  },
  {
    "q": "Which of the following is TRUE about `Meta: default_related_name`?",
    "c": "class Author(models.Model):\n    name = models.CharField(max_length=100)\n\nclass Book(models.Model):\n    author = models.ForeignKey(Author, on_delete=models.CASCADE)\n\n    class Meta:\n        default_related_name = 'books'",
    "o": [
      "It defines the default reverse relation name used in ORM queries",
      "It enforces a unique constraint on the related field",
      "It overrides ForeignKey’s `related_name` for all apps",
      "It is required in proxy models"
    ]
  },
  {
    "q": "What is the effect of `db_constraint=False` in a ForeignKey field?",
    "c": "class Profile(models.Model):\n    user = models.ForeignKey(User, on_delete=models.CASCADE, db_constraint=False)",
    "o": [
      "The database does not enforce the foreign key constraint, but Django still treats it as a relationship",
      "It disables ORM-level relationship handling",
      "It makes the model abstract",
      "It creates a proxy relationship only in admin"
    ]
  },
  {
    "q": "If two proxy models inherit from the same base model, how do they behave in the database?",
    "c": null,
    "o": [
      "They share the same database table as the base model",
      "Each proxy model creates its own separate table",
      "The base model cannot be used with multiple proxy models",
      "Django raises an error on migration"
    ]
  },
  {
    "q": "What happens if you set `unique_together` on a proxy model?",
    "c": "class CustomUser(User):\n    class Meta:\n        proxy = True\n        unique_together = ('username', 'email')",
    "o": [
      "It raises an error because proxy models cannot define unique_together",
      "It applies a uniqueness constraint at the proxy level only",
      "It silently applies constraints to the base model’s table",
      "It creates a new table to enforce uniqueness"
    ]
  },
  {
    "q": "What happens if two Django apps define models with the same database table name via `db_table`?",
    "c": null,
    "o": [
      "Django raises a migration error due to conflicting table names",
      "Django automatically merges both models into one table",
      "Django creates both tables but with random suffixes",
      "The second model silently overrides the first"
    ]
  },
  {
    "q": "When does a migration conflict typically occur in Django?",
    "c": null,
    "o": [
      "When two branches of development generate migrations affecting the same app",
      "When models use the same verbose_name",
      "When a proxy model is created",
      "When default values are added to fields"
    ]
  },
  {
    "q": "Which constraint would you use to ensure that `end_date` in a model is always greater than `start_date`?",
    "c": "class Event(models.Model):\n    start_date = models.DateField()\n    end_date = models.DateField()\n\n    class Meta:\n        constraints = [\n            models.CheckConstraint(check=models.Q(end_date__gt=models.F('start_date')), name='end_after_start')\n        ]",
    "o": [
      "CheckConstraint",
      "UniqueConstraint",
      "Index",
      "unique_together"
    ]
  },
  {
    "q": "In Django, what is TRUE about unmanaged models (`Meta: managed = False`)?",
    "c": null,
    "o": [
      "Django does not create or modify their database tables",
      "Django ignores them completely in queries",
      "They cannot have ForeignKey fields",
      "They must always be abstract"
    ]
  },
  {
    "q": "What is the purpose of `Meta: app_label` in a Django model?",
    "c": null,
    "o": [
      "It allows placing the model in a different app for database purposes",
      "It enforces uniqueness of model names",
      "It is required for all proxy models",
      "It controls which migrations folder is used globally"
    ]
  },
  {
    "q": "If you remove a field from a model but keep using it in queries, what will happen?",
    "c": null,
    "o": [
      "The ORM will raise a FieldError at runtime",
      "The ORM will ignore the field silently",
      "The database will create a placeholder column",
      "The query will succeed but with NULL values"
    ]
  },
  {
    "q": "How does `OneToOneField` behave compared to `ForeignKey(unique=True)`?",
    "c": null,
    "o": [
      "They are functionally the same, but OneToOneField provides a clearer semantic meaning",
      "OneToOneField allows multiple related objects, unlike ForeignKey",
      "ForeignKey(unique=True) cannot be used across apps",
      "OneToOneField does not create database constraints"
    ]
  },
  {
    "q": "What is the effect of defining multiple `Meta: ordering` options across an inheritance chain?",
    "c": null,
    "o": [
      "Only the child model’s ordering is respected",
      "Both parent and child orderings are merged",
      "The ORM randomly picks one ordering",
      "Ordering must always be set in the parent"
    ]
  },
  {
    "q": "What is the effect of using `select_related()` on a ForeignKey field?",
    "c": "Book.objects.select_related('author').all()",
    "o": [
      "It performs a SQL JOIN to fetch related objects in a single query",
      "It always creates a separate query for related objects",
      "It caches results but does not affect query count",
      "It only works for ManyToMany fields"
    ]
  },
  {
    "q": "Which method is preferred for optimizing queries on ManyToMany relationships?",
    "c": "Author.objects.prefetch_related('books').all()",
    "o": [
      "prefetch_related()",
      "select_related()",
      "only()",
      "defer()"
    ]
  },
  {
    "q": "What happens if two migrations create different indexes with the same name?",
    "c": null,
    "o": [
      "Django raises a migration error due to duplicate index names",
      "Django merges both indexes automatically",
      "Django ignores one of the indexes silently",
      "The database renames the second index automatically"
    ]
  },
  {
    "q": "Which statement about `QuerySet.defer()` is TRUE?",
    "c": "qs = User.objects.defer('email')",
    "o": [
      "The `email` field will not be loaded until it is accessed",
      "The query will raise an error if `email` is accessed",
      "It removes the field permanently from ORM queries",
      "It works only with ManyToMany relationships"
    ]
  },
  {
    "q": "What is the difference between `objects.create()` and `objects.get_or_create()`?",
    "c": null,
    "o": [
      "get_or_create() checks for existing objects before creating, while create() always inserts",
      "create() checks for existing records first",
      "get_or_create() cannot accept default values",
      "They are functionally identical"
    ]
  },
  {
    "q": "If you try to delete a model instance that is referenced by a ForeignKey with `on_delete=PROTECT`, what happens?",
    "c": null,
    "o": [
      "A ProtectedError is raised",
      "The related object is also deleted",
      "The ForeignKey is set to NULL",
      "The operation is silently ignored"
    ]
  },
  {
    "q": "Which migration operation is used to rename a model field?",
    "c": null,
    "o": [
      "RenameField",
      "AlterField",
      "RenameModel",
      "AlterUniqueTogether"
    ]
  },
  {
    "q": "What happens if you call `.annotate()` after `.values()` in a queryset?",
    "c": "Book.objects.values('author').annotate(total=Count('id'))",
    "o": [
      "The aggregation is applied per dictionary of values",
      "The annotate step is ignored",
      "It raises a QuerySet error",
      "It always returns only model instances"
    ]
  },
  {
    "q": "Which Django feature allows you to interact with the database using Python classes instead of SQL queries?",
    "c": null,
    "o": [
      "ORM (Object-Relational Mapping)",
      "Forms",
      "Middleware",
      "Signals"
    ]
  },
  {
    "q": "What will the following model definition create in the database?",
    "c": "class Book(models.Model):\n    title = models.CharField(max_length=100)\n    published_date = models.DateField()",
    "o": [
      "A table named 'appname_book' with columns 'id', 'title', and 'published_date'",
      "A Python dictionary representing the book data",
      "A SQL view instead of a table",
      "Nothing until you call a form"
    ]
  },
  {
    "q": "Which command is used to generate a new migration file after modifying a Django model?",
    "c": null,
    "o": [
      "python manage.py makemigrations",
      "python manage.py migrate",
      "python manage.py runserver",
      "python manage.py sqlmigrate"
    ]
  },
  {
    "q": "In a Django model, which field type is most appropriate for storing multiple choice options as integers?",
    "c": null,
    "o": [
      "IntegerField with choices",
      "CharField with choices",
      "TextField with multiple=True",
      "BooleanField"
    ]
  },
  {
    "q": "What type of relationship does the following code create?",
    "c": "class Author(models.Model):\n    name = models.CharField(max_length=50)\n\nclass Book(models.Model):\n    author = models.ForeignKey(Author, on_delete=models.CASCADE)",
    "o": [
      "Many-to-One relationship (multiple books can have the same author)",
      "One-to-One relationship",
      "Many-to-Many relationship",
      "One-to-Many relationship (one book has multiple authors)"
    ]
  },
  {
    "q": "When you delete a record referenced by a ForeignKey with `on_delete=models.CASCADE`, what happens?",
    "c": null,
    "o": [
      "All related records referencing it will also be deleted",
      "Nothing happens, the record stays as orphan",
      "It raises a database integrity error",
      "The reference is set to null automatically"
    ]
  },
  {
    "q": "Which method in a Django model can be overridden to define the string representation of a model instance?",
    "c": null,
    "o": [
      "__str__()",
      "__repr__()",
      "save()",
      "get_absolute_url()"
    ]
  },
  {
    "q": "After defining a new ManyToManyField in a model, what must you do to apply the change to the database?",
    "c": null,
    "o": [
      "Run `makemigrations` and then `migrate`",
      "Just restart the server",
      "Delete the database and recreate it",
      "Call `save()` on all existing instances"
    ]
  },
  {
    "q": "What will the following code achieve?",
    "c": "class Student(models.Model):\n    name = models.CharField(max_length=50)\n    courses = models.ManyToManyField('Course')\n\nclass Course(models.Model):\n    title = models.CharField(max_length=100)",
    "o": [
      "Allows a student to be enrolled in multiple courses and a course to have multiple students",
      "Restricts each student to only one course",
      "Automatically deletes all courses when a student is deleted",
      "Creates a One-to-One relationship between Student and Course"
    ]
  },
  {
    "q": "Which Django feature allows you to see SQL statements generated by the ORM without executing them?",
    "c": null,
    "o": [
      "QuerySet's `query` attribute",
      "Model's `save()` method",
      "Admin interface",
      "Middleware"
    ]
  },
  {
    "q": "What happens if you forget to run `makemigrations` after changing a model field type?",
    "c": null,
    "o": [
      "The database schema will not reflect the change, possibly causing runtime errors",
      "Django automatically updates the database when the server restarts",
      "The old data is automatically converted to the new type",
      "The model change is ignored silently"
    ]
  },
  {
    "q": "Which option ensures that a ForeignKey allows null values in the database?",
    "c": null,
    "o": [
      "null=True",
      "blank=True",
      "default=None",
      "on_delete=models.SET_NULL"
    ]
  },
  {
    "q": "What will the following code produce in Django ORM?",
    "c": "books = Book.objects.filter(author__name__icontains='doe')",
    "o": [
      "All Book objects whose author's name contains 'doe', case-insensitive",
      "All Book objects with name containing 'doe'",
      "All Author objects whose name contains 'doe'",
      "A syntax error due to double underscore"
    ]
  },
  {
    "q": "In a Django ManyToManyField, what is the default name of the automatically created intermediate table?",
    "c": null,
    "o": [
      "appname_modelname1_modelname2",
      "modelname1_modelname2_table",
      "m2m_modelname1_modelname2",
      "No table is created by default"
    ]
  },
  {
    "q": "What is the difference between `blank=True` and `null=True` in Django models?",
    "c": null,
    "o": [
      "`blank=True` affects form validation, `null=True` affects database storage",
      "Both do the same thing",
      "`blank=True` affects database, `null=True` affects templates",
      "`blank=True` allows NULL in DB, `null=True` allows empty string in forms"
    ]
  },
  {
    "q": "What will happen if you try to delete an Author referenced by multiple Book records with `on_delete=models.PROTECT`?",
    "c": null,
    "o": [
      "The deletion will be blocked and raise a ProtectedError",
      "All related Book records will be deleted",
      "The ForeignKey will be set to null",
      "Nothing, the deletion proceeds normally"
    ]
  },
  {
    "q": "How can you retrieve all books for a given author instance in Django ORM?",
    "c": null,
    "o": [
      "author.book_set.all()",
      "Book.objects.all(author=author)",
      "author.books.all()",
      "Book.objects.filter(author_id=author.id)"
    ]
  },
  {
    "q": "What is the purpose of `related_name` in Django model relationships?",
    "c": null,
    "o": [
      "Defines the attribute name for reverse relation from the related model",
      "Specifies the database table name",
      "Sets the default ordering for querysets",
      "Automatically creates an admin interface"
    ]
  },
  {
    "q": "What will the following migration command do?",
    "c": "python manage.py sqlmigrate appname 0003",
    "o": [
      "Display the SQL statements that would be executed for migration 0003",
      "Execute the migration 0003 directly on the database",
      "Undo migration 0003",
      "Create a new migration file named 0003"
    ]
  },
  {
    "q": "Which is the correct way to enforce a unique constraint across multiple fields in a Django model?",
    "c": "class Enrollment(models.Model):\n    student = models.ForeignKey(Student, on_delete=models.CASCADE)\n    course = models.ForeignKey(Course, on_delete=models.CASCADE)\n\n    class Meta:\n        constraints = [\n            models.UniqueConstraint(fields=['student', 'course'], name='unique_enrollment')\n        ]",
    "o": [
      "Use `UniqueConstraint` in the model’s Meta class",
      "Add `unique=True` on both fields",
      "Add `unique=True` on only one field",
      "Use `blank=False` on both fields"
    ]
  },
  {
    "q": "Which Django ORM method can be used to efficiently update multiple records without fetching them into memory?",
    "c": null,
    "o": [
      "QuerySet.update()",
      "Model.save()",
      "Model.objects.create()",
      "QuerySet.save()"
    ]
  },
  {
    "q": "What is the effect of setting `db_index=True` on a Django model field?",
    "c": null,
    "o": [
      "Creates a database index to speed up lookups on that field",
      "Automatically enforces uniqueness",
      "Creates a foreign key relationship",
      "Makes the field required in forms"
    ]
  },
  {
    "q": "What will the following code accomplish?",
    "c": "class Profile(models.Model):\n    user = models.OneToOneField(User, on_delete=models.CASCADE)\n    bio = models.TextField(blank=True)",
    "o": [
      "Each User will have exactly one Profile, and deleting the User deletes the Profile",
      "Multiple Profiles can be linked to one User",
      "Profiles exist independently of Users",
      "Prevents User deletion entirely"
    ]
  },
  {
    "q": "Which Django feature allows you to specify a custom database table name for a model?",
    "c": null,
    "o": [
      "Meta.db_table",
      "Meta.table_name",
      "Meta.database_name",
      "Meta.model_name"
    ]
  },
  {
    "q": "What is the behavior of `select_related()` in Django ORM?",
    "c": null,
    "o": [
      "Performs a SQL JOIN to fetch related ForeignKey objects in the same query",
      "Fetches all related ManyToMany objects eagerly",
      "Executes raw SQL queries instead of ORM queries",
      "Deletes related objects automatically"
    ]
  },
  {
    "q": "What is the difference between `prefetch_related()` and `select_related()`?",
    "c": null,
    "o": [
      "`select_related` is for ForeignKey/OneToOne, `prefetch_related` is for ManyToMany/Reverse FK",
      "They are exactly the same",
      "`select_related` is slower than `prefetch_related`",
      "Both only work for OneToOne fields"
    ]
  },
  {
    "q": "How can you create a self-referential ManyToMany relationship in Django?",
    "c": "class Employee(models.Model):\n    name = models.CharField(max_length=50)\n    mentors = models.ManyToManyField('self', symmetrical=False, related_name='mentees')",
    "o": [
      "By using `ManyToManyField('self', symmetrical=False)`",
      "By using ForeignKey to self",
      "By creating a separate intermediate model manually",
      "It is not possible in Django"
    ]
  },
  {
    "q": "Which migration operation should be used to rename a model field without losing existing data?",
    "c": null,
    "o": [
      "RenameField",
      "AlterField",
      "AddField",
      "RemoveField"
    ]
  },
  {
    "q": "What will happen if you call `Book.objects.all()` after adding a new field to the Book model but before running migrations?",
    "c": null,
    "o": [
      "The query works with the old schema; the new field does not exist in the database yet",
      "The ORM automatically adds the new field to the database",
      "An exception is raised immediately",
      "The server refuses to start"
    ]
  },
  {
    "q": "Which option is correct to enforce cascading delete for ManyToMany relationships?",
    "c": null,
    "o": [
      "Use `on_delete=models.CASCADE` in the intermediate model",
      "Add `cascade=True` to the ManyToManyField",
      "Set `null=True` on both fields",
      "ManyToManyField automatically cascades deletes by default"
    ]
  },
  {
    "q": "What is the effect of using `Meta.ordering` in a Django model?",
    "c": null,
    "o": [
      "Specifies the default order of QuerySet results for the model",
      "Forces the database to index the model fields",
      "Automatically sorts related models in the admin",
      "Overrides primary key behavior"
    ]
  },
  {
    "q": "Which Django field type should be used for storing JSON data natively in supported databases?",
    "c": null,
    "o": [
      "JSONField",
      "TextField",
      "CharField",
      "BinaryField"
    ]
  },
  {
    "q": "How can you prevent a Django model instance from being deleted through the ORM?",
    "c": null,
    "o": [
      "Override the `delete()` method and raise an exception",
      "Set `blank=False` on all fields",
      "Add `null=False` to all ForeignKeys",
      "Set `unique=True` on the primary key"
    ]
  },
  {
    "q": "What is the difference between `on_delete=models.SET_NULL` and `on_delete=models.SET_DEFAULT`?",
    "c": null,
    "o": [
      "`SET_NULL` sets the FK to NULL, `SET_DEFAULT` sets it to the field's default value",
      "They behave identically",
      "`SET_NULL` deletes the related object, `SET_DEFAULT` leaves it intact",
      "`SET_NULL` raises an error if null is not allowed, `SET_DEFAULT` raises an IntegrityError"
    ]
  },
  {
    "q": "What does the `through` parameter in a ManyToManyField do?",
    "c": null,
    "o": [
      "Specifies a custom intermediate model to manage the relationship",
      "Forces a cascading delete on all related objects",
      "Automatically indexes the relation in the database",
      "Creates a reverse ForeignKey for the related model"
    ]
  },
  {
    "q": "What is the behavior of `unique_together` in Django models?",
    "c": null,
    "o": [
      "Enforces that a combination of fields must be unique across the table",
      "Makes each field unique individually",
      "Prevents null values in the fields",
      "Automatically indexes the fields for faster queries"
    ]
  },
  {
    "q": "What will happen if you try to run `migrate` after deleting a model from `models.py` but without creating a migration?",
    "c": null,
    "o": [
      "The database table remains; Django does not know it should be removed",
      "The table is deleted automatically",
      "An error is raised during `migrate`",
      "Django ignores the deletion and recreates the model"
    ]
  },
  {
    "q": "Which method is used to fetch only certain fields from a QuerySet for performance optimization?",
    "c": null,
    "o": [
      "values() or values_list()",
      "only_fields()",
      "select_related()",
      "filter_fields()"
    ]
  },
  {
    "q": "How can you create a reverse relationship name for a ForeignKey without using the default `<model>_set`?",
    "c": null,
    "o": [
      "By setting `related_name` in the ForeignKey",
      "By renaming the model class",
      "By changing the primary key field",
      "By using `Meta.db_table`"
    ]
  },
  {
    "q": "What is the most efficient way to prefetch related ManyToMany objects for a QuerySet?",
    "c": null,
    "o": [
      "Use `prefetch_related()` on the QuerySet",
      "Use `select_related()` on the QuerySet",
      "Access them in a loop one by one",
      "Use raw SQL queries"
    ]
  },
  {
    "q": "Which method would you override to customize model instance save behavior without affecting other instances?",
    "c": null,
    "o": [
      "save()",
      "clean()",
      "__str__()",
      "get_absolute_url()"
    ]
  },
  {
    "q": "What is the purpose of the `Meta.managed = False` option in a Django model?",
    "c": null,
    "o": [
      "Django will not manage database table creation or migrations for this model",
      "Prevents the model from being used in QuerySets",
      "Disables admin interface for this model",
      "Marks the model as abstract"
    ]
  },
  {
    "q": "How can you create a polymorphic relationship in Django models where multiple types inherit from a base model?",
    "c": null,
    "o": [
      "Use model inheritance with an abstract base model or multi-table inheritance",
      "Use only ForeignKeys pointing to the base model",
      "Use ManyToManyFields for all child models",
      "Use a single table with JSONField to store type-specific data"
    ]
  },
  {
    "q": "What is the effect of adding `db_constraint=False` on a ForeignKey?",
    "c": null,
    "o": [
      "Disables database-level integrity constraint while keeping ORM-level validation",
      "Disables the ORM from checking the relationship",
      "Automatically deletes related objects",
      "Prevents migrations from being created"
    ]
  },
  {
    "q": "Which Django field option enforces that a field value is unique per date?",
    "c": null,
    "o": [
      "unique_for_date",
      "unique=True",
      "unique_for_field",
      "unique_per_day"
    ]
  },
  {
    "q": "How can you create an index on multiple fields in a Django model?",
    "c": "class Order(models.Model):\n    customer = models.ForeignKey(Customer, on_delete=models.CASCADE)\n    order_date = models.DateField()\n\n    class Meta:\n        indexes = [\n            models.Index(fields=['customer', 'order_date'])\n        ]",
    "o": [
      "Use `models.Index` in the model’s Meta class",
      "Use `unique_together` instead",
      "Add `db_index=True` to both fields individually",
      "Indexes cannot be created on multiple fields"
    ]
  },
  {
    "q": "Which of the following best describes a OneToOneField in Django?",
    "c": null,
    "o": [
      "Each instance of a model is related to exactly one instance of another model",
      "Each instance can relate to multiple instances of another model",
      "It creates a Many-to-Many relationship",
      "It is used only for primary key fields"
    ]
  },
  {
    "q": "What is the difference between `auto_now` and `auto_now_add` in DateTimeField?",
    "c": null,
    "o": [
      "`auto_now_add` sets the field once on creation, `auto_now` updates it on every save",
      "They are identical in behavior",
      "`auto_now_add` updates on every save, `auto_now` sets only on creation",
      "Both require manual assignment of date values"
    ]
  },
  {
    "q": "Which option correctly allows a reverse relationship name on a ManyToManyField?",
    "c": null,
    "o": [
      "related_name='related_objects'",
      "reverse_name='related_objects'",
      "backref='related_objects'",
      "relation_name='related_objects'"
    ]
  },
  {
    "q": "What happens when you define a model with `abstract = True` in its Meta class?",
    "c": null,
    "o": [
      "No database table is created, but it can be inherited by other models",
      "A table is created but cannot be queried",
      "The model cannot have fields",
      "It behaves like a normal model but cannot be migrated"
    ]
  },
  {
    "q": "What is the purpose of the `defer()` method in Django QuerySets?",
    "c": null,
    "o": [
      "Defers loading of specified fields until they are accessed, improving query performance",
      "Deletes specified fields from the database",
      "Fetches only the deferred fields immediately",
      "Prevents the model from being saved"
    ]
  },
  {
    "q": "Which option is used to define a database-level check constraint in a Django model?",
    "c": "class Product(models.Model):\n    name = models.CharField(max_length=100)\n    price = models.DecimalField(max_digits=10, decimal_places=2)\n\n    class Meta:\n        constraints = [\n            models.CheckConstraint(check=models.Q(price__gte=0), name='price_positive')\n        ]",
    "o": [
      "CheckConstraint",
      "UniqueConstraint",
      "db_constraint=True",
      "ForeignKey with on_delete"
    ]
  },
  {
    "q": "What is the effect of using `select_for_update()` in a QuerySet?",
    "c": null,
    "o": [
      "Locks the selected rows until the end of the transaction to prevent race conditions",
      "Fetches all related objects eagerly",
      "Updates all selected rows immediately",
      "Marks rows as read-only in the ORM"
    ]
  },
  {
    "q": "Which option is correct to define a composite unique constraint across three fields in Django?",
    "c": null,
    "o": [
      "Use `UniqueConstraint(fields=['field1','field2','field3'], name='unique_combo')` in Meta",
      "Set `unique=True` on each field individually",
      "Use `unique_together=('field1','field2','field3')` in Meta (deprecated but works)",
      "Use `db_index=True` on all three fields"
    ]
  },
  {
    "q": "How can you efficiently count the number of related objects in a reverse ForeignKey relationship?",
    "c": null,
    "o": [
      "Use `annotate()` with `Count('related_model')`",
      "Iterate through all related objects and count in Python",
      "Use `len(related_set)`",
      "Django does not support counting related objects"
    ]
  },
  {
    "q": "What happens if you set `primary_key=True` on a non-integer field in Django?",
    "c": null,
    "o": [
      "The field becomes the model's primary key instead of the default 'id'",
      "Django ignores it and uses the default id field",
      "It raises an error during migrations",
      "It automatically converts the field to an integer"
    ]
  },
  {
    "q": "Which Django feature allows you to define a model field that only exists for queries but not in the database table?",
    "c": null,
    "o": [
      "annotate() or `@property` methods on the model",
      "VirtualField",
      "db_column=None",
      "deferred=True"
    ]
  },
  {
    "q": "What is the impact of `managed = False` in combination with database views?",
    "c": null,
    "o": [
      "Django treats the model as unmanaged, so migrations won't attempt to create or modify the view",
      "The view will be automatically updated when the model changes",
      "The view cannot be queried through the ORM",
      "Django raises an error if the view exists"
    ]
  },
  {
    "q": "Which Django model option allows you to define default ordering for QuerySets?",
    "c": null,
    "o": [
      "Meta.ordering",
      "Meta.default_order",
      "Meta.sort_by",
      "ordering=True"
    ]
  },
  {
    "q": "How can you enforce a nullable ForeignKey to always have a default value instead of NULL?",
    "c": null,
    "o": [
      "Use `default=<object>` when defining the ForeignKey",
      "Use `blank=False`",
      "Use `null=False`",
      "Override the save() method without a default"
    ]
  },
  {
    "q": "What is the effect of using `prefetch_related()` with a ManyToMany relationship in Django?",
    "c": null,
    "o": [
      "Performs a separate query to fetch related objects and caches them, reducing N+1 query problems",
      "Joins related tables using SQL JOINs like `select_related()`",
      "Deletes all related objects when the parent is deleted",
      "Automatically adds indexes to the related table"
    ]
  },
  {
    "q": "Which Django signal is sent before a model instance is saved?",
    "c": null,
    "o": [
      "pre_save",
      "post_save",
      "pre_delete",
      "post_delete"
    ]
  },
  {
    "q": "Which Django signal is triggered after a model instance is deleted?",
    "c": null,
    "o": [
      "post_delete",
      "pre_delete",
      "pre_save",
      "post_save"
    ]
  },
  {
    "q": "How can you access the reverse ForeignKey relationship for a model instance?",
    "c": null,
    "o": [
      "Using the default `<model>_set` manager or the `related_name` if defined",
      "By querying the database manually",
      "By using the ForeignKey directly on the child model",
      "Django does not allow reverse access"
    ]
  },
  {
    "q": "What happens if you define a `unique=True` field on a model and try to insert duplicate values?",
    "c": null,
    "o": [
      "A `IntegrityError` is raised at the database level",
      "Django automatically changes the value to be unique",
      "The duplicate row is silently ignored",
      "The save() method returns False instead of saving"
    ]
  },
  {
    "q": "Which Django QuerySet method allows you to lock selected rows for update within a transaction?",
    "c": null,
    "o": [
      "select_for_update()",
      "prefetch_related()",
      "select_related()",
      "annotate()"
    ]
  },
  {
    "q": "How can you define a default value for a ForeignKey field?",
    "c": null,
    "o": [
      "Use the `default=<object>` parameter when defining the ForeignKey",
      "Use `null=True`",
      "Use `blank=True`",
      "Override the save() method without specifying a default"
    ]
  },
  {
    "q": "What is the correct way to create a nullable OneToOneField in Django?",
    "c": null,
    "o": [
      "Set `null=True` on the OneToOneField",
      "Use `blank=True` only",
      "Use `unique=False`",
      "Django does not support nullable OneToOneField"
    ]
  },
  {
    "q": "Which option allows you to define an explicit database column name for a Django model field?",
    "c": null,
    "o": [
      "db_column='column_name'",
      "column_name='column_name'",
      "db_field='column_name'",
      "field_name='column_name'"
    ]
  },
  {
    "q": "How can you create a custom manager that filters only active objects in a model?",
    "c": null,
    "o": [
      "Override `get_queryset()` in a custom manager class",
      "Use `filter()` in the model Meta class",
      "Override `save()` in the model",
      "Set `active=True` in the field definition"
    ]
  },
  {
    "q": "What happens when you define `abstract = True` in a Django model Meta class?",
    "c": null,
    "o": [
      "No database table is created; the model is used only for inheritance",
      "A table is created but cannot be queried",
      "The model cannot have fields",
      "It behaves like a normal model but cannot be migrated"
    ]
  },
  {
    "q": "How does multi-table inheritance work in Django?",
    "c": null,
    "o": [
      "Each model in the inheritance chain gets its own database table linked via a OneToOneField",
      "All fields are stored in a single table for the base model",
      "Child models share the same table as the parent",
      "It creates a ManyToMany relationship between base and child tables"
    ]
  },
  {
    "q": "What is a proxy model in Django?",
    "c": null,
    "o": [
      "A model that uses the same database table as its parent but can have a different Python behavior",
      "A model that automatically creates an intermediate table for ManyToMany fields",
      "A model that cannot be queried via the ORM",
      "A model that exists only for migrations"
    ]
  },
  {
    "q": "Which Django QuerySet method allows you to load a field later to save initial query performance?",
    "c": null,
    "o": [
      "defer()",
      "only()",
      "select_related()",
      "prefetch_related()"
    ]
  },
  {
    "q": "Which QuerySet method restricts the fields loaded to only the ones specified?",
    "c": null,
    "o": [
      "only()",
      "defer()",
      "select_related()",
      "values()"
    ]
  },
  {
    "q": "What is the behavior of `Meta.default_permissions` in a Django model?",
    "c": null,
    "o": [
      "Specifies which default permissions (`add`, `change`, `delete`, `view`) are created for the model",
      "Automatically sets permissions on the database",
      "Restricts admin users from accessing the model",
      "Overrides `permissions` defined on fields"
    ]
  },
  {
    "q": "What happens if you define `managed = False` in a Django model using multi-table inheritance?",
    "c": null,
    "o": [
      "Django does not create or modify the database table for that model, even though it inherits from a managed parent",
      "The parent table is also ignored",
      "The model cannot be queried via ORM",
      "It automatically becomes abstract"
    ]
  },
  {
    "q": "Which of the following allows creating a custom table name for a proxy model?",
    "c": null,
    "o": [
      "You cannot change the database table of a proxy model; it uses the parent’s table",
      "Use `Meta.db_table`",
      "Use `table_name` parameter in the model",
      "Override `save()` to change table"
    ]
  },
  {
    "q": "How can you override the default manager of a Django model while still keeping the default manager accessible?",
    "c": null,
    "o": [
      "Define a custom manager and assign it to a different attribute, keeping the original `objects` manager",
      "Replace `objects` with the custom manager only",
      "Delete the default manager",
      "Override `get_queryset()` without defining a manager"
    ]
  },
  {
    "q": "Which field option allows a ForeignKey or OneToOneField to be optional and accept NULL values?",
    "c": null,
    "o": [
      "null=True",
      "blank=True",
      "default=None",
      "unique=False"
    ]
  },
  {
    "q": "Which Django ORM feature allows you to perform calculations on fields in a query without fetching objects into Python?",
    "c": null,
    "o": [
      "F expressions",
      "Q objects",
      "annotate()",
      "aggregate()"
    ]
  },
  {
    "q": "What is the purpose of the `annotate()` method in a QuerySet?",
    "c": null,
    "o": [
      "Adds aggregate values (like Count, Sum) for each object in the QuerySet",
      "Filters the QuerySet based on conditions",
      "Updates fields in the database",
      "Performs raw SQL queries"
    ]
  },
  {
    "q": "How can you perform a conditional update on a QuerySet based on field values?",
    "c": null,
    "o": [
      "Use F expressions and the `update()` method",
      "Iterate through objects and call save() manually",
      "Use annotate()",
      "Use pre_save signal only"
    ]
  },
  {
    "q": "What is the effect of using `Subquery` in Django ORM?",
    "c": null,
    "o": [
      "Allows embedding a QuerySet as a subquery in another QuerySet",
      "Deletes related rows automatically",
      "Prefetches related objects",
      "Performs an immediate SQL JOIN"
    ]
  },
  {
    "q": "Which expression allows conditional logic (like SQL CASE) in Django ORM queries?",
    "c": null,
    "o": [
      "Case and When expressions",
      "F expressions",
      "Q objects",
      "Value expressions"
    ]
  },
  {
    "q": "How can you perform a query to filter objects where a field is greater than another field in the same row?",
    "c": null,
    "o": [
      "Use F expressions, e.g., `filter(field1__gt=F('field2'))`",
      "Use annotate()",
      "Use Q objects with constants",
      "Use raw SQL only"
    ]
  },
  {
    "q": "Which method allows combining multiple QuerySets with a union operation in Django?",
    "c": null,
    "o": [
      "union()",
      "chain()",
      "combine()",
      "merge()"
    ]
  },
  {
    "q": "How can you efficiently count related objects in a reverse relationship without loading them into memory?",
    "c": null,
    "o": [
      "Use annotate() with Count('related_field')",
      "Use len(instance.related_set.all())",
      "Iterate and count manually",
      "Use select_related()"
    ]
  },
  {
    "q": "What is the effect of using `Exists` expression in Django ORM?",
    "c": null,
    "o": [
      "Checks if a subquery returns any rows, returning a boolean value per row",
      "Deletes rows in a subquery",
      "Joins two tables together",
      "Performs a raw SQL query"
    ]
  },
  {
    "q": "Which method allows adding an aggregate value for the entire QuerySet rather than per object?",
    "c": null,
    "o": [
      "aggregate()",
      "annotate()",
      "values()",
      "filter()"
    ]
  },
  {
    "q": "Which method is most efficient for fetching ManyToMany related objects without causing N+1 queries?",
    "c": null,
    "o": [
      "prefetch_related()",
      "select_related()",
      "annotate()",
      "values()"
    ]
  },
  {
    "q": "How can you filter a model based on a field in a related ManyToMany model?",
    "c": null,
    "o": [
      "Use double underscores to traverse relationships, e.g., `filter(related_model__field=value)`",
      "Use raw SQL only",
      "Iterate over objects in Python",
      "Use annotate() without a filter"
    ]
  },
  {
    "q": "What is the effect of setting `through` in a ManyToManyField?",
    "c": null,
    "o": [
      "Specifies a custom intermediate model for the relationship",
      "Automatically creates a reverse ForeignKey",
      "Deletes related objects on parent deletion",
      "Indexes all related fields automatically"
    ]
  },
  {
    "q": "How can you optimize queries when you only need a subset of fields from a ManyToMany relationship?",
    "c": null,
    "o": [
      "Use `prefetch_related(Prefetch('relation', queryset=Model.objects.only('field1','field2')))`",
      "Use select_related()",
      "Use values() on the main model only",
      "Load all fields and discard unnecessary ones in Python"
    ]
  },
  {
    "q": "Which ORM expression allows filtering ManyToMany objects that meet a condition on the intermediate table?",
    "c": null,
    "o": [
      "Through model filtering using `filter(intermediate_field__condition=value)`",
      "annotate() without filter",
      "select_related()",
      "prefetch_related() without Prefetch object"
    ]
  },
  {
    "q": "How can you limit the number of related objects returned in a prefetch_related query?",
    "c": null,
    "o": [
      "Use Prefetch object with a custom QuerySet that includes slicing",
      "Use select_related() with limit",
      "Use only() on the main model",
      "Set max_related attribute on the field"
    ]
  },
  {
    "q": "What happens if you access a ManyToManyField without using prefetch_related or select_related?",
    "c": null,
    "o": [
      "A separate query is executed for each object, potentially causing N+1 queries",
      "All objects are loaded in memory automatically",
      "Django raises an error",
      "The ORM joins all tables automatically"
    ]
  },
  {
    "q": "Which technique allows filtering related ManyToMany objects based on a condition and prefetching them?",
    "c": null,
    "o": [
      "Use `prefetch_related(Prefetch('relation', queryset=RelatedModel.objects.filter(...)))`",
      "Use select_related() only",
      "Use annotate() without prefetch_related",
      "Use values() on the parent model"
    ]
  },
  {
    "q": "How can you access the intermediate model of a ManyToManyField?",
    "c": null,
    "o": [
      "Use the `through` attribute of the ManyToManyField",
      "Use the `_set` reverse relation only",
      "Access via select_related()",
      "Intermediate models cannot be accessed directly"
    ]
  },
  {
    "q": "Which ORM method allows filtering objects based on the existence of related ManyToMany objects?",
    "c": null,
    "o": [
      "filter(related_model__isnull=False)",
      "annotate() without a filter",
      "select_related()",
      "prefetch_related() alone"
    ]
  },
  {
    "q": "What happens if you change the type of a model field and run `makemigrations`?",
    "c": null,
    "o": [
      "Django generates a migration to alter the field type in the database",
      "The change is ignored until the server restarts",
      "Django automatically converts all existing data to the new type without migration",
      "It raises a syntax error"
    ]
  },
  {
    "q": "Which migration operation allows you to rename a model field without losing data?",
    "c": null,
    "o": [
      "RenameField",
      "AlterField",
      "RemoveField",
      "AddField"
    ]
  },
  {
    "q": "What is the effect of setting `unique=True` on a model field?",
    "c": null,
    "o": [
      "Enforces uniqueness at the database level for that field",
      "Prevents null values in the field",
      "Automatically creates a primary key",
      "Makes the field optional"
    ]
  },
  {
    "q": "Which migration operation allows creating a composite unique constraint across multiple fields?",
    "c": null,
    "o": [
      "UniqueConstraint in Meta.constraints",
      "unique=True on each field individually",
      "db_index=True on all fields",
      "AddField with unique=True"
    ]
  },
  {
    "q": "What happens if you try to delete an object referenced by a ForeignKey with `on_delete=models.PROTECT`?",
    "c": null,
    "o": [
      "The deletion is blocked and a ProtectedError is raised",
      "All related objects are deleted automatically",
      "The ForeignKey is set to null",
      "The deletion proceeds normally"
    ]
  },
  {
    "q": "Which field option allows a ForeignKey to accept NULL values?",
    "c": null,
    "o": [
      "null=True",
      "blank=True",
      "default=None",
      "unique=False"
    ]
  },
  {
    "q": "What is the behavior of `CheckConstraint` in Django models?",
    "c": null,
    "o": [
      "Enforces a condition at the database level for field values",
      "Prevents null values in the field",
      "Automatically sets default values",
      "Applies only to the ORM validation, not database"
    ]
  },
  {
    "q": "Which migration command shows the SQL statements for a specific migration without applying it?",
    "c": null,
    "o": [
      "sqlmigrate",
      "makemigrations",
      "migrate",
      "showmigrations"
    ]
  },
  {
    "q": "What happens if you remove a model from `models.py` but do not create a migration?",
    "c": null,
    "o": [
      "The database table remains unchanged until a migration is created",
      "The table is deleted automatically",
      "Django raises an error during migrate",
      "The server refuses to start"
    ]
  },
  {
    "q": "Which option allows defining a default ordering for a model in all QuerySets?",
    "c": null,
    "o": [
      "Meta.ordering",
      "Meta.default_order",
      "Meta.sort_by",
      "ordering=True"
    ]
  },
  {
    "q": "Which Django model field allows storing JSON-encoded data natively in supported databases?",
    "c": null,
    "o": [
      "JSONField",
      "TextField",
      "CharField",
      "BinaryField"
    ]
  },
  {
    "q": "Which field type allows storing key-value pairs as a dictionary in PostgreSQL?",
    "c": null,
    "o": [
      "HStoreField",
      "JSONField",
      "TextField",
      "ArrayField"
    ]
  },
  {
    "q": "Which Django field allows storing an array of values (like integers or strings) in PostgreSQL?",
    "c": null,
    "o": [
      "ArrayField",
      "ManyToManyField",
      "TextField",
      "JSONField"
    ]
  },
  {
    "q": "Which model option enforces database-level constraints conditionally based on field values?",
    "c": null,
    "o": [
      "CheckConstraint with Q objects",
      "UniqueConstraint",
      "Meta.ordering",
      "db_index=True"
    ]
  },
  {
    "q": "Which Django ORM expression allows you to compare a field to another field in the same model row?",
    "c": null,
    "o": [
      "F expression",
      "Q object",
      "Case/When",
      "Value expression"
    ]
  },
  {
    "q": "Which expression allows performing conditional updates based on field values?",
    "c": null,
    "o": [
      "Case and When expressions inside update()",
      "F expressions alone",
      "annotate() without update",
      "Q objects alone"
    ]
  },
  {
    "q": "Which Meta option allows defining database indexes on multiple fields?",
    "c": "class Order(models.Model):\n    customer = models.ForeignKey(Customer, on_delete=models.CASCADE)\n    date = models.DateField()\n\n    class Meta:\n        indexes = [\n            models.Index(fields=['customer', 'date'])\n        ]",
    "o": [
      "Meta.indexes",
      "Meta.db_index",
      "Meta.unique_together",
      "Meta.constraints"
    ]
  },
  {
    "q": "Which Django field option allows storing a default dictionary value for JSONField?",
    "c": null,
    "o": [
      "default=dict",
      "default={}",
      "default=list",
      "blank=True"
    ]
  },
  {
    "q": "What is the behavior of HStoreField when storing non-string values?",
    "c": null,
    "o": [
      "Values are automatically converted to strings",
      "Raises a TypeError",
      "Automatically stores as JSON",
      "Ignores non-string keys only"
    ]
  },
  {
    "q": "Which feature allows you to define database-level conditional uniqueness, e.g., unique only if a field is not null?",
    "c": null,
    "o": [
      "UniqueConstraint with condition",
      "unique=True",
      "unique_together",
      "db_index=True"
    ]
  },
  {
    "q": "What is a proxy model in Django?",
    "c": null,
    "o": [
      "A model that uses the same database table as its parent but can have a different Python behavior",
      "A model that automatically creates an intermediate table for ManyToMany fields",
      "A model that cannot be queried via the ORM",
      "A model that exists only for migrations"
    ]
  },
  {
    "q": "Which option in a Django model Meta class allows defining indexes using expressions?",
    "c": null,
    "o": [
      "Meta.indexes with expressions",
      "Meta.db_index",
      "Meta.unique_together",
      "Meta.constraints"
    ]
  },
  {
    "q": "What is the purpose of `defer()` in a Django QuerySet?",
    "c": null,
    "o": [
      "Delays loading of specified fields until they are accessed, improving performance",
      "Excludes objects from the QuerySet",
      "Deletes deferred fields from the database",
      "Locks selected rows for update"
    ]
  },
  {
    "q": "How does `only()` differ from `defer()` in Django QuerySets?",
    "c": null,
    "o": [
      "`only()` loads only the specified fields immediately; all other fields are deferred",
      "`only()` excludes objects from the QuerySet",
      "`only()` locks rows in the database",
      "`only()` works only for related fields"
    ]
  },
  {
    "q": "Which Meta option allows defining custom database table options, such as tablespace or unique constraints?",
    "c": null,
    "o": [
      "Meta.constraints and Meta.db_table",
      "Meta.ordering",
      "Meta.indexes only",
      "Meta.default_permissions"
    ]
  },
  {
    "q": "How can you prevent Django from managing a model’s database table entirely?",
    "c": null,
    "o": [
      "Set `managed = False` in the model’s Meta class",
      "Set `abstract = True`",
      "Use a proxy model",
      "Set `db_table = None`"
    ]
  },
  {
    "q": "Which ORM feature allows creating conditional indexes on a subset of rows?",
    "c": null,
    "o": [
      "Index with condition",
      "db_index=True",
      "Meta.unique_together",
      "CheckConstraint"
    ]
  },
  {
    "q": "What happens if you access a deferred field in a model instance?",
    "c": null,
    "o": [
      "Django performs a separate query to fetch the field’s value",
      "An error is raised",
      "The field remains NULL",
      "All fields in the model are loaded automatically"
    ]
  },
  {
    "q": "Which feature allows a model to change Python-level behavior without creating a new database table?",
    "c": null,
    "o": [
      "Proxy model",
      "Abstract model",
      "Multi-table inheritance",
      "Managed = False"
    ]
  },
  {
    "q": "Which Meta option allows setting custom database constraints that use expressions on fields?",
    "c": null,
    "o": [
      "Meta.constraints with CheckConstraint and Q objects",
      "Meta.indexes",
      "Meta.unique_together",
      "Meta.ordering"
    ]
  },
  {
    "q": "Which Django signal is sent before a model instance is saved?",
    "c": null,
    "o": [
      "pre_save",
      "post_save",
      "pre_delete",
      "post_delete"
    ]
  },
  {
    "q": "Which Django signal is sent after a model instance is deleted?",
    "c": null,
    "o": [
      "post_delete",
      "pre_delete",
      "pre_save",
      "post_save"
    ]
  },
  {
    "q": "How can you prevent recursive save signals when overriding save() in Django?",
    "c": null,
    "o": [
      "Use update_fields or disconnect signals temporarily",
      "Raise an exception in the save() method",
      "Call save() twice",
      "Use pre_save signal only"
    ]
  },
  {
    "q": "Which ORM method locks selected rows for update within a transaction?",
    "c": null,
    "o": [
      "select_for_update()",
      "prefetch_related()",
      "select_related()",
      "annotate()"
    ]
  },
  {
    "q": "Which method prefetches related ManyToMany objects efficiently and allows customizing the query?",
    "c": null,
    "o": [
      "prefetch_related() with Prefetch object",
      "select_related()",
      "annotate()",
      "values()"
    ]
  },
  {
    "q": "What is the difference between `select_related()` and `prefetch_related()`?",
    "c": null,
    "o": [
      "`select_related()` performs a SQL JOIN for ForeignKey/OneToOne, `prefetch_related()` performs a separate query for ManyToMany or reverse relationships",
      "They are identical",
      "`select_related()` is used only for ManyToMany, `prefetch_related()` only for ForeignKey",
      "Both fetch related objects lazily without extra queries"
    ]
  },
  {
    "q": "How can you annotate a QuerySet with the count of related objects?",
    "c": null,
    "o": [
      "Use annotate() with Count('related_field')",
      "Use len(related_set.all())",
      "Iterate through objects in Python",
      "Use select_related()"
    ]
  },
  {
    "q": "Which Django signal is useful for performing actions immediately after a model instance is saved, such as logging or caching?",
    "c": null,
    "o": [
      "post_save",
      "pre_save",
      "post_delete",
      "pre_delete"
    ]
  },
  {
    "q": "Which technique prevents N+1 query problems when accessing reverse relationships in loops?",
    "c": null,
    "o": [
      "Use prefetch_related() or select_related() appropriately",
      "Iterate and query manually in Python",
      "Use raw SQL only",
      "Disable ORM lazy loading"
    ]
  },
  {
    "q": "How can you perform a conditional update on multiple rows without loading them into Python memory?",
    "c": null,
    "o": [
      "Use update() with F expressions or Case/When expressions",
      "Iterate and call save() on each instance",
      "Use annotate() only",
      "Use pre_save signal"
    ]
  },
  {
    "q": "Which PostgreSQL-specific Django model field allows storing arrays of values?",
    "c": null,
    "o": [
      "ArrayField",
      "JSONField",
      "HStoreField",
      "ManyToManyField"
    ]
  },
  {
    "q": "Which PostgreSQL-specific Django model field allows storing key-value pairs as strings?",
    "c": null,
    "o": [
      "HStoreField",
      "JSONField",
      "ArrayField",
      "TextField"
    ]
  },
  {
    "q": "Which database function allows converting text to lowercase in a Django QuerySet annotation?",
    "c": null,
    "o": [
      "Lower",
      "Upper",
      "Length",
      "Coalesce"
    ]
  },
  {
    "q": "Which database function allows concatenating two fields in a Django QuerySet?",
    "c": null,
    "o": [
      "Concat",
      "ConcatWS",
      "Combine",
      "F expression"
    ]
  },
  {
    "q": "Which expression allows conditional logic similar to SQL CASE in Django ORM?",
    "c": null,
    "o": [
      "Case and When",
      "F expression",
      "Q object",
      "Value expression"
    ]
  },
  {
    "q": "Which Django model Meta option allows defining database-level partial unique constraints?",
    "c": null,
    "o": [
      "UniqueConstraint with condition",
      "unique=True",
      "unique_together",
      "db_index=True"
    ]
  },
  {
    "q": "How can you filter rows in a QuerySet where a JSONField contains a specific key-value pair?",
    "c": null,
    "o": [
      "Use field__key=value syntax, e.g., `filter(data__key='value')`",
      "Use raw SQL only",
      "Iterate objects in Python",
      "Use select_related()"
    ]
  },
  {
    "q": "Which Django ORM function allows performing mathematical operations on fields at the database level?",
    "c": null,
    "o": [
      "F expressions",
      "Case/When",
      "annotate() without expressions",
      "Q objects"
    ]
  },
  {
    "q": "How can you create an index on a JSONField key in PostgreSQL using Django?",
    "c": null,
    "o": [
      "Use GinIndex with expressions in Meta.indexes",
      "Set db_index=True on the JSONField",
      "Use UniqueConstraint",
      "Use HStoreField instead"
    ]
  },
  {
    "q": "Which PostgreSQL function allows replacing NULL values with a default in Django QuerySets?",
    "c": null,
    "o": [
      "Coalesce",
      "Concat",
      "Length",
      "Lower"
    ]
  },
  {
    "q": "Which Django Meta option allows creating a functional index on a model field?",
    "c": null,
    "o": [
      "Meta.indexes with expressions",
      "Meta.db_index",
      "Meta.unique_together",
      "Meta.constraints"
    ]
  },
  {
    "q": "How can you enforce a conditional unique constraint in Django, e.g., unique only if a field is not null?",
    "c": null,
    "o": [
      "Use UniqueConstraint with condition in Meta.constraints",
      "Use unique=True on the field",
      "Use db_index=True",
      "Use CheckConstraint instead"
    ]
  },
  {
    "q": "Which type of index is most efficient for JSONField key lookups in PostgreSQL?",
    "c": null,
    "o": [
      "GinIndex",
      "BTreeIndex",
      "HashIndex",
      "UniqueIndex"
    ]
  },
  {
    "q": "Which Django expression allows performing arithmetic operations between two fields in a model?",
    "c": null,
    "o": [
      "F expression",
      "Case/When",
      "Q object",
      "Value expression"
    ]
  },
  {
    "q": "How can you filter objects based on a conditional relationship, e.g., only related objects with a status of 'active'?",
    "c": null,
    "o": [
      "Use Prefetch with a filtered QuerySet inside prefetch_related()",
      "Use select_related()",
      "Use values()",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which index type should you use to speed up text search on a JSONField or HStoreField in PostgreSQL?",
    "c": null,
    "o": [
      "GinIndex",
      "BTreeIndex",
      "HashIndex",
      "PartialIndex"
    ]
  },
  {
    "q": "How can you enforce that a ForeignKey is unique only when another boolean field is True?",
    "c": null,
    "o": [
      "Use UniqueConstraint with a condition in Meta.constraints",
      "Set unique=True on the ForeignKey",
      "Use CheckConstraint",
      "Override save() method to enforce uniqueness"
    ]
  },
  {
    "q": "Which Django ORM feature allows updating a field based on another field in the same row without fetching objects?",
    "c": null,
    "o": [
      "update() with F expressions",
      "annotate()",
      "select_for_update()",
      "prefetch_related()"
    ]
  },
  {
    "q": "Which Django feature allows creating a partial index that only applies to rows matching a condition?",
    "c": null,
    "o": [
      "Index with condition in Meta.indexes",
      "db_index=True",
      "unique=True",
      "CheckConstraint"
    ]
  },
  {
    "q": "Which ORM expression allows using SQL-like conditional logic (CASE WHEN) in annotations or updates?",
    "c": null,
    "o": [
      "Case and When expressions",
      "F expressions",
      "Q objects",
      "Value expressions"
    ]
  },
  {
    "q": "How does multi-table inheritance work in Django?",
    "c": null,
    "o": [
      "Each model in the inheritance chain gets its own database table linked via a OneToOneField",
      "All fields are stored in a single table for the base model",
      "Child models share the same table as the parent",
      "It creates a ManyToMany relationship between base and child tables"
    ]
  },
  {
    "q": "What happens when you define `abstract = True` in a model Meta class?",
    "c": null,
    "o": [
      "No database table is created; the model is used only for inheritance",
      "A table is created but cannot be queried",
      "The model cannot have fields",
      "It behaves like a normal model but cannot be migrated"
    ]
  },
  {
    "q": "What is a proxy model in Django?",
    "c": null,
    "o": [
      "A model that uses the same database table as its parent but can have a different Python behavior",
      "A model that automatically creates an intermediate table for ManyToMany fields",
      "A model that cannot be queried via the ORM",
      "A model that exists only for migrations"
    ]
  },
  {
    "q": "What happens if you access a deferred field in a model instance?",
    "c": null,
    "o": [
      "Django performs a separate query to fetch the field’s value",
      "An error is raised",
      "The field remains NULL",
      "All fields in the model are loaded automatically"
    ]
  },
  {
    "q": "How can you limit the fields loaded from the database when querying a model?",
    "c": null,
    "o": [
      "Use `only()` or `defer()` in the QuerySet",
      "Use select_related()",
      "Use annotate()",
      "Use raw SQL only"
    ]
  },
  {
    "q": "Which Meta option allows a model to inherit fields and methods without creating a database table?",
    "c": null,
    "o": [
      "abstract = True",
      "managed = False",
      "proxy = True",
      "db_table = None"
    ]
  },
  {
    "q": "How can a proxy model change Python-level behavior without creating a new table?",
    "c": null,
    "o": [
      "By inheriting from a parent model and setting `proxy = True` in Meta",
      "By using abstract models",
      "By setting `managed = False`",
      "By using multi-table inheritance"
    ]
  },
  {
    "q": "What is the effect of `managed = False` in a model using multi-table inheritance?",
    "c": null,
    "o": [
      "Django does not create or modify the database table for that model, even though it inherits from a managed parent",
      "The parent table is also ignored",
      "The model cannot be queried via ORM",
      "It automatically becomes abstract"
    ]
  },
  {
    "q": "Which method allows loading only specific fields immediately while deferring others?",
    "c": null,
    "o": [
      "only()",
      "defer()",
      "select_related()",
      "prefetch_related()"
    ]
  },
  {
    "q": "How can you create a model that behaves like another model but stores data in the same table?",
    "c": null,
    "o": [
      "Use a proxy model",
      "Use multi-table inheritance",
      "Use an abstract model",
      "Use a deferred field"
    ]
  },
  {
    "q": "Which option allows customizing the intermediate table for a ManyToManyField?",
    "c": null,
    "o": [
      "through",
      "related_name",
      "db_table",
      "unique_together"
    ]
  },
  {
    "q": "How can you prefetch only a subset of related objects in a ManyToMany relationship?",
    "c": null,
    "o": [
      "Use prefetch_related() with a Prefetch object containing a filtered QuerySet",
      "Use select_related()",
      "Use annotate()",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How do you access the intermediate model of a ManyToManyField?",
    "c": null,
    "o": [
      "Through the `through` attribute of the ManyToManyField",
      "Using `_set` reverse relationship only",
      "Via select_related()",
      "Intermediate models cannot be accessed directly"
    ]
  },
  {
    "q": "Which method helps avoid N+1 query problems when accessing reverse ManyToMany relationships?",
    "c": null,
    "o": [
      "prefetch_related()",
      "select_related()",
      "annotate()",
      "values()"
    ]
  },
  {
    "q": "How can you filter a ManyToMany relationship based on a field in the intermediate table?",
    "c": null,
    "o": [
      "Use a custom QuerySet on the `through` model and Prefetch it",
      "Use select_related() only",
      "Use annotate() without filter",
      "Iterate and filter manually in Python"
    ]
  },
  {
    "q": "Which option allows defining a reverse name for a ManyToManyField relationship?",
    "c": null,
    "o": [
      "related_name",
      "related_query_name",
      "reverse_name",
      "db_column"
    ]
  },
  {
    "q": "How can you limit the number of related objects returned in a prefetch_related query?",
    "c": null,
    "o": [
      "Use Prefetch object with a sliced QuerySet",
      "Use select_related() with limit",
      "Use only() on the main model",
      "Set max_related attribute on the field"
    ]
  },
  {
    "q": "What happens if you access a ManyToManyField without prefetch_related in a loop?",
    "c": null,
    "o": [
      "A separate query is executed for each object, causing N+1 queries",
      "All related objects are loaded automatically in one query",
      "Django raises an error",
      "The ORM caches the results automatically"
    ]
  },
  {
    "q": "Which technique allows filtering related ManyToMany objects and prefetching them efficiently?",
    "c": null,
    "o": [
      "prefetch_related(Prefetch('relation', queryset=RelatedModel.objects.filter(...)))",
      "select_related() only",
      "annotate() without prefetch_related",
      "values() on the parent model only"
    ]
  },
  {
    "q": "How can you enforce a unique constraint on the combination of fields in an intermediate ManyToMany table?",
    "c": null,
    "o": [
      "Use Meta.unique_together in the intermediate model",
      "Use unique=True on the ManyToManyField",
      "Use CheckConstraint in the parent model",
      "Use db_index=True on all fields"
    ]
  },
  {
    "q": "Which Django Meta option allows defining database-level constraints using expressions?",
    "c": null,
    "o": [
      "Meta.constraints with CheckConstraint or UniqueConstraint",
      "Meta.indexes",
      "Meta.unique_together",
      "Meta.ordering"
    ]
  },
  {
    "q": "How can you enforce a field to be unique only if another boolean field is True?",
    "c": null,
    "o": [
      "Use UniqueConstraint with a condition in Meta.constraints",
      "Set unique=True on the field",
      "Use CheckConstraint only",
      "Override save() to enforce uniqueness"
    ]
  },
  {
    "q": "Which type of index should you use to speed up lookups on JSONField or HStoreField in PostgreSQL?",
    "c": null,
    "o": [
      "GinIndex",
      "BTreeIndex",
      "HashIndex",
      "UniqueIndex"
    ]
  },
  {
    "q": "Which Django ORM expression allows performing arithmetic or comparison operations between two fields in a model?",
    "c": null,
    "o": [
      "F expressions",
      "Case/When",
      "Q objects",
      "Value expressions"
    ]
  },
  {
    "q": "How can you create a partial index that only applies to rows matching a condition?",
    "c": null,
    "o": [
      "Use Index with condition in Meta.indexes",
      "Set db_index=True on the field",
      "Use unique=True",
      "Use CheckConstraint"
    ]
  },
  {
    "q": "Which ORM expression allows using SQL-like conditional logic (CASE WHEN) in annotations or updates?",
    "c": null,
    "o": [
      "Case and When expressions",
      "F expressions",
      "Q objects",
      "Value expressions"
    ]
  },
  {
    "q": "How can you enforce a ForeignKey to be unique only when another field is not null?",
    "c": null,
    "o": [
      "Use UniqueConstraint with a condition in Meta.constraints",
      "Set unique=True on the ForeignKey",
      "Use CheckConstraint only",
      "Override save() to enforce uniqueness"
    ]
  },
  {
    "q": "Which Django feature allows creating a database-level index based on a function of a field?",
    "c": null,
    "o": [
      "Functional Index using expressions in Meta.indexes",
      "db_index=True",
      "Meta.unique_together",
      "CheckConstraint"
    ]
  },
  {
    "q": "Which Django model Meta option allows defining a database index on multiple fields together?",
    "c": null,
    "o": [
      "Meta.indexes with fields list",
      "Meta.unique_together",
      "db_index=True on each field",
      "Meta.constraints"
    ]
  },
  {
    "q": "Which expression allows updating a field based on the value of another field without fetching objects into Python memory?",
    "c": null,
    "o": [
      "update() with F expressions",
      "annotate()",
      "select_for_update()",
      "prefetch_related()"
    ]
  },
  {
    "q": "How can you perform a bulk update on a ManyToMany relationship efficiently?",
    "c": null,
    "o": [
      "Update the intermediate `through` model directly using QuerySet operations",
      "Iterate through related objects and call save() individually",
      "Use select_related() on the parent model",
      "Django does not allow bulk updates on ManyToMany"
    ]
  },
  {
    "q": "Which technique allows filtering ManyToMany related objects based on a condition in the intermediate table?",
    "c": null,
    "o": [
      "Use a Prefetch object with a filtered QuerySet on the `through` model",
      "Use select_related() only",
      "Use values() on the main model",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can a custom manager be used to return only active objects in a model?",
    "c": null,
    "o": [
      "Override `get_queryset()` in a custom manager class to filter active=True",
      "Use a pre_save signal",
      "Set default ordering in Meta",
      "Use unique=True on a field"
    ]
  },
  {
    "q": "What is the purpose of `super().get_queryset()` in a custom manager?",
    "c": null,
    "o": [
      "To get the base QuerySet before applying custom filters or annotations",
      "To bypass database queries",
      "To call a model method automatically",
      "To delete all objects in the table"
    ]
  },
  {
    "q": "Which feature allows an abstract model to define fields and methods that are inherited by concrete models?",
    "c": null,
    "o": [
      "abstract = True in Meta",
      "managed = False",
      "proxy = True",
      "db_table = None"
    ]
  },
  {
    "q": "How can a concrete model override a method defined in an abstract base model?",
    "c": null,
    "o": [
      "Define the method with the same name in the concrete model",
      "Use super() in the abstract model",
      "Set proxy = True",
      "Change Meta.managed to False"
    ]
  },
  {
    "q": "Which technique allows limiting which fields are loaded in a QuerySet for performance?",
    "c": null,
    "o": [
      "Use only() or defer() in the QuerySet",
      "Use select_related() only",
      "Use annotate()",
      "Use raw SQL exclusively"
    ]
  },
  {
    "q": "How can you enforce that a field in a concrete model remains unique only among non-deleted rows when inheriting from an abstract model?",
    "c": null,
    "o": [
      "Use UniqueConstraint with a condition in Meta.constraints",
      "Set unique=True on the field",
      "Use CheckConstraint only",
      "Override save() to enforce uniqueness manually"
    ]
  },
  {
    "q": "Which Django feature allows customizing the QuerySet returned by a related manager for reverse relations?",
    "c": null,
    "o": [
      "Use a custom manager with `base_manager_name` or `use_for_related_fields`",
      "Use select_related()",
      "Use values()",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "How can you ensure that a custom manager is available for all QuerySets including related models?",
    "c": null,
    "o": [
      "Set `use_for_related_fields = True` in the manager class",
      "Override save()",
      "Use a proxy model",
      "Use select_for_update()"
    ]
  },
  {
    "q": "Which Django feature allows filtering reverse relationships efficiently?",
    "c": null,
    "o": [
      "Prefetch object with a filtered QuerySet inside prefetch_related()",
      "select_related()",
      "annotate()",
      "values()"
    ]
  },
  {
    "q": "How can you annotate each object with the count of related objects meeting a condition?",
    "c": null,
    "o": [
      "Use annotate() with Count('related_field', filter=Q(related_field__condition=value))",
      "Iterate through objects and count manually in Python",
      "Use select_related() only",
      "Use prefetch_related() without Prefetch"
    ]
  },
  {
    "q": "Which ORM method allows chaining multiple conditions on reverse relationships?",
    "c": null,
    "o": [
      "filter() with Q objects",
      "annotate() without conditions",
      "select_related()",
      "values() only"
    ]
  },
  {
    "q": "How can you perform an aggregation only on related objects that satisfy a certain condition?",
    "c": null,
    "o": [
      "Use annotate() with Count or Sum and the `filter` parameter",
      "Use values() on the parent model only",
      "Use select_related()",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which technique avoids N+1 queries when accessing reverse ManyToMany relationships with conditions?",
    "c": null,
    "o": [
      "Use prefetch_related() with Prefetch object containing a filtered QuerySet",
      "Use select_related()",
      "Use annotate() only",
      "Use raw SQL exclusively"
    ]
  },
  {
    "q": "Which expression allows performing conditional calculations in annotations or updates?",
    "c": null,
    "o": [
      "Case and When expressions",
      "F expressions only",
      "Q objects only",
      "Value expressions only"
    ]
  },
  {
    "q": "How can you combine multiple filtered related QuerySets into one annotation?",
    "c": null,
    "o": [
      "Use multiple annotations with conditional expressions like Case/When",
      "Use select_related() for each related model",
      "Iterate manually in Python",
      "Use values() on the parent model only"
    ]
  },
  {
    "q": "How can you limit the prefetch of related objects to the top N results ordered by a field?",
    "c": null,
    "o": [
      "Use Prefetch with a sliced QuerySet in prefetch_related()",
      "Use select_related() with limit",
      "Use annotate() with limit",
      "Use only() on the parent model"
    ]
  },
  {
    "q": "Which technique allows filtering reverse ForeignKey relationships in annotations without loading all objects?",
    "c": null,
    "o": [
      "Use conditional aggregation with Count or Sum and the `filter` parameter",
      "Use prefetch_related() only",
      "Use select_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you annotate a model with a value based on related objects’ field using an expression?",
    "c": null,
    "o": [
      "Use annotate() with F expressions, Case/When, or Subquery expressions",
      "Use values() only",
      "Use prefetch_related() without Prefetch",
      "Use select_related() only"
    ]
  },
  {
    "q": "Which database function allows computing the length of a text field in a Django QuerySet?",
    "c": null,
    "o": [
      "Length",
      "Lower",
      "Coalesce",
      "Concat"
    ]
  },
  {
    "q": "Which Django ORM function allows replacing NULL values with a default in annotations or updates?",
    "c": null,
    "o": [
      "Coalesce",
      "Concat",
      "Length",
      "Lower"
    ]
  },
  {
    "q": "Which index type is most efficient for JSONField key/value lookups in PostgreSQL?",
    "c": null,
    "o": [
      "GinIndex",
      "BTreeIndex",
      "HashIndex",
      "UniqueIndex"
    ]
  },
  {
    "q": "How can you create an index based on a function of a field, like lowercased text, in Django?",
    "c": null,
    "o": [
      "Use Meta.indexes with an ExpressionIndex",
      "Use db_index=True on the field",
      "Use unique=True",
      "Use CheckConstraint"
    ]
  },
  {
    "q": "Which expression allows performing arithmetic operations between two model fields at the database level?",
    "c": null,
    "o": [
      "F expressions",
      "Q objects",
      "Case/When",
      "Value expressions"
    ]
  },
  {
    "q": "How can you enforce a database-level constraint only for rows satisfying a condition?",
    "c": null,
    "o": [
      "Use UniqueConstraint or CheckConstraint with a condition in Meta.constraints",
      "Set unique=True on the field",
      "Use db_index=True",
      "Use annotate() in QuerySet"
    ]
  },
  {
    "q": "Which PostgreSQL-specific field allows storing key-value pairs as text strings in Django?",
    "c": null,
    "o": [
      "HStoreField",
      "JSONField",
      "ArrayField",
      "TextField"
    ]
  },
  {
    "q": "Which PostgreSQL-specific field allows storing arrays of values natively in Django?",
    "c": null,
    "o": [
      "ArrayField",
      "JSONField",
      "HStoreField",
      "ManyToManyField"
    ]
  },
  {
    "q": "How can you filter a JSONField to find objects containing a specific key-value pair?",
    "c": null,
    "o": [
      "Use field__key=value syntax in filter(), e.g., filter(data__key='value')",
      "Use select_related() only",
      "Iterate manually in Python",
      "Use annotate() without filter"
    ]
  },
  {
    "q": "Which database function allows converting text to lowercase for comparisons or ordering in Django QuerySets?",
    "c": null,
    "o": [
      "Lower",
      "Upper",
      "Length",
      "Coalesce"
    ]
  },
  {
    "q": "In multi-table inheritance, how does Django enforce the relationship between parent and child tables?",
    "c": null,
    "o": [
      "Using an implicit OneToOneField linking the child to the parent",
      "Using a ForeignKey from parent to child",
      "Using a ManyToManyField automatically",
      "No relationship is enforced"
    ]
  },
  {
    "q": "Which signal can be used to perform actions immediately after a model instance is saved within a transaction?",
    "c": null,
    "o": [
      "post_save",
      "pre_save",
      "post_delete",
      "pre_delete"
    ]
  },
  {
    "q": "Which Django feature ensures that multiple related database updates happen atomically?",
    "c": null,
    "o": [
      "Transaction.atomic() context manager",
      "select_for_update()",
      "prefetch_related()",
      "F expressions"
    ]
  },
  {
    "q": "How can you prevent a save signal from triggering recursively when modifying a model inside a signal handler?",
    "c": null,
    "o": [
      "Disconnect the signal temporarily or use update_fields in save()",
      "Raise an exception",
      "Use only pre_save",
      "Use select_for_update()"
    ]
  },
  {
    "q": "Which ORM method locks rows to prevent concurrent modifications within a transaction?",
    "c": null,
    "o": [
      "select_for_update()",
      "prefetch_related()",
      "select_related()",
      "annotate()"
    ]
  },
  {
    "q": "How can you ensure that cascading deletions occur only after certain conditions are met?",
    "c": null,
    "o": [
      "Use on_delete=models.PROTECT or a custom signal handler",
      "Use unique=True",
      "Use prefetch_related()",
      "Use select_related()"
    ]
  },
  {
    "q": "Which signal is useful for performing cleanup or logging after a model instance is deleted?",
    "c": null,
    "o": [
      "post_delete",
      "pre_delete",
      "pre_save",
      "post_save"
    ]
  },
  {
    "q": "How can you ensure database integrity when performing complex updates across multiple related models?",
    "c": null,
    "o": [
      "Wrap updates inside transaction.atomic() to ensure all-or-nothing execution",
      "Use only save() on individual models",
      "Use select_related() without transactions",
      "Use annotate()"
    ]
  },
  {
    "q": "Which technique allows safely updating related objects in bulk without violating constraints?",
    "c": null,
    "o": [
      "Use QuerySet.update() with F expressions inside a transaction",
      "Iterate and call save() on each instance individually",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "How can you combine signals and transactions to perform complex post-save operations safely?",
    "c": null,
    "o": [
      "Use post_save signals inside a transaction.atomic() block",
      "Use pre_save only",
      "Use only annotate()",
      "Perform operations outside the transaction"
    ]
  },
  {
    "q": "Which Django feature allows annotating a QuerySet with a value based on a condition on related fields?",
    "c": null,
    "o": [
      "Case and When expressions inside annotate()",
      "F expressions alone",
      "Q objects alone",
      "Value() expression only"
    ]
  },
  {
    "q": "How can you combine multiple expressions to compute a derived value in a QuerySet?",
    "c": null,
    "o": [
      "Use F expressions, combined with arithmetic operators and Case/When inside annotate()",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which index type allows storing a functional transformation of a field in PostgreSQL for efficient lookups?",
    "c": null,
    "o": [
      "ExpressionIndex",
      "BTreeIndex",
      "HashIndex",
      "GinIndex"
    ]
  },
  {
    "q": "How can you enforce a unique constraint based on a calculated expression rather than a raw field value?",
    "c": null,
    "o": [
      "Use UniqueConstraint with an expression in Meta.constraints",
      "Use unique=True on the field",
      "Use CheckConstraint only",
      "Use db_index=True"
    ]
  },
  {
    "q": "In multi-table inheritance, how can you annotate a child model with a value derived from the parent model?",
    "c": null,
    "o": [
      "Use annotate() with F expressions referencing the parent’s fields",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you create a functional index on a lowercased text field for ordering and filtering?",
    "c": null,
    "o": [
      "Use ExpressionIndex with Lower(F('field_name')) in Meta.indexes",
      "Set db_index=True on the field",
      "Use BTreeIndex without expressions",
      "Use unique=True"
    ]
  },
  {
    "q": "Which technique allows computing a conditional sum on related objects using the ORM without raw SQL?",
    "c": null,
    "o": [
      "Use annotate() with Sum('related_field', filter=Q(related_field__condition=value))",
      "Use values() on the parent model only",
      "Use select_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you define an index that applies only to rows satisfying a certain condition in Django?",
    "c": null,
    "o": [
      "Use Index with condition in Meta.indexes",
      "Use db_index=True on the field",
      "Use unique=True",
      "Use CheckConstraint"
    ]
  },
  {
    "q": "Which ORM feature allows performing calculations on related objects and storing the result as an annotation in the parent QuerySet?",
    "c": null,
    "o": [
      "annotate() with F, Case/When, or Subquery expressions",
      "select_related() only",
      "prefetch_related() only",
      "values() only"
    ]
  },
  {
    "q": "How can you ensure that an annotation uses values from multiple tables in a multi-table inheritance hierarchy?",
    "c": null,
    "o": [
      "Use annotate() with F expressions that reference parent fields through multi-table joins",
      "Use select_related() alone",
      "Use prefetch_related() alone",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which index type is most efficient for querying JSONField key/value pairs in PostgreSQL?",
    "c": null,
    "o": [
      "GinIndex",
      "BTreeIndex",
      "HashIndex",
      "UniqueIndex"
    ]
  },
  {
    "q": "How can you create a functional index on a JSONField key in Django?",
    "c": null,
    "o": [
      "Use ExpressionIndex with KeyTextTransform('key', F('json_field')) in Meta.indexes",
      "Set db_index=True on the JSONField",
      "Use unique=True",
      "Use CheckConstraint"
    ]
  },
  {
    "q": "Which database function allows extracting a text value from a JSONField for indexing or filtering?",
    "c": null,
    "o": [
      "KeyTextTransform",
      "Lower",
      "Coalesce",
      "Length"
    ]
  },
  {
    "q": "How can you enforce a unique constraint on a specific key within a JSONField in PostgreSQL?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform expression in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True",
      "Use CheckConstraint"
    ]
  },
  {
    "q": "Which PostgreSQL-specific field allows storing a dictionary of string key-value pairs?",
    "c": null,
    "o": [
      "HStoreField",
      "JSONField",
      "ArrayField",
      "TextField"
    ]
  },
  {
    "q": "How can you create a partial index on an HStoreField key for rows meeting a condition?",
    "c": null,
    "o": [
      "Use GinIndex with condition in Meta.indexes and KeyTransform expression",
      "Set db_index=True on the HStoreField",
      "Use unique=True",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "Which expression allows performing conditional updates on JSONField or HStoreField without loading objects into Python?",
    "c": null,
    "o": [
      "F expressions combined with KeyTransform or KeyTextTransform in update()",
      "Case/When only",
      "Q objects only",
      "annotate() only"
    ]
  },
  {
    "q": "How can you filter objects based on a JSONField key containing a specific value efficiently?",
    "c": null,
    "o": [
      "Use field__key=value syntax with a GinIndex on that key",
      "Iterate manually in Python",
      "Use select_related() only",
      "Use values() without indexing"
    ]
  },
  {
    "q": "Which ORM expression allows performing arithmetic or string operations directly on JSONField keys in PostgreSQL?",
    "c": null,
    "o": [
      "F expressions with KeyTransform or KeyTextTransform",
      "Case/When only",
      "Q objects only",
      "Value expressions only"
    ]
  },
  {
    "q": "How can you combine a functional index with a conditional constraint for JSONField in Django?",
    "c": null,
    "o": [
      "Use ExpressionIndex and UniqueConstraint or CheckConstraint with condition in Meta",
      "Use db_index=True only",
      "Use unique=True only",
      "Use annotate() without Meta.indexes"
    ]
  },
  {
    "q": "In multi-table inheritance, how can you annotate a child model with a value derived from the parent model efficiently?",
    "c": null,
    "o": [
      "Use annotate() with F expressions referencing the parent’s fields",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which Django feature allows performing post-save operations safely inside a transaction?",
    "c": null,
    "o": [
      "Use post_save signals inside a transaction.atomic() block",
      "Use pre_save signals only",
      "Use annotate() only",
      "Use select_related() only"
    ]
  },
  {
    "q": "How can you prevent recursive signal execution when updating a model inside a signal handler?",
    "c": null,
    "o": [
      "Temporarily disconnect the signal or use update_fields in save()",
      "Raise an exception in the handler",
      "Use pre_save only",
      "Use select_for_update() only"
    ]
  },
  {
    "q": "Which ORM method locks rows to prevent concurrent modifications during complex updates?",
    "c": null,
    "o": [
      "select_for_update()",
      "prefetch_related()",
      "select_related()",
      "annotate()"
    ]
  },
  {
    "q": "How can you create a functional index on a lowercased field in a child model of a multi-table inheritance hierarchy?",
    "c": null,
    "o": [
      "Use ExpressionIndex with Lower(F('field_name')) in Meta.indexes",
      "Set db_index=True on the field",
      "Use BTreeIndex without expressions",
      "Use unique=True"
    ]
  },
  {
    "q": "Which technique ensures multiple related model updates occur atomically?",
    "c": null,
    "o": [
      "Wrap updates inside transaction.atomic()",
      "Iterate and save each object individually",
      "Use select_related() only",
      "Use annotate() only"
    ]
  },
  {
    "q": "How can you enforce a unique constraint across child and parent tables conditionally?",
    "c": null,
    "o": [
      "Use UniqueConstraint with an expression or condition in Meta.constraints",
      "Set unique=True on the field only",
      "Use CheckConstraint only",
      "Use db_index=True only"
    ]
  },
  {
    "q": "Which expression allows performing conditional calculations on fields from both parent and child models?",
    "c": null,
    "o": [
      "Case and When combined with F expressions in annotate()",
      "F expressions alone",
      "Q objects alone",
      "Value expressions alone"
    ]
  },
  {
    "q": "How can you combine a functional index with a conditional constraint in a multi-table inheritance setup?",
    "c": null,
    "o": [
      "Use ExpressionIndex with a UniqueConstraint or CheckConstraint and a condition in Meta",
      "Set db_index=True only",
      "Use unique=True only",
      "Use annotate() without Meta.indexes"
    ]
  },
  {
    "q": "Which technique allows safely performing bulk updates on related child objects without violating constraints?",
    "c": null,
    "o": [
      "Use QuerySet.update() with F expressions inside transaction.atomic()",
      "Iterate and call save() on each child object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "How can you annotate a model with the sum of related objects that meet a specific condition?",
    "c": null,
    "o": [
      "Use annotate() with Sum('related_field', filter=Q(related_field__condition=value))",
      "Use values() only",
      "Use select_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which technique avoids N+1 queries when accessing filtered reverse relationships?",
    "c": null,
    "o": [
      "Use prefetch_related() with Prefetch containing a filtered QuerySet",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you annotate a child model in a multi-table inheritance hierarchy using a parent field with a calculated value?",
    "c": null,
    "o": [
      "Use annotate() with F expressions referencing parent fields",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which PostgreSQL-specific feature allows creating an index on a transformed field in an inherited model?",
    "c": null,
    "o": [
      "ExpressionIndex with a function like Lower(F('field_name')) in Meta.indexes",
      "db_index=True on the field",
      "BTreeIndex without expressions",
      "unique=True"
    ]
  },
  {
    "q": "How can you annotate a QuerySet with conditional counts of related objects without fetching all related rows?",
    "c": null,
    "o": [
      "Use annotate() with Count('related_field', filter=Q(related_field__condition=value))",
      "Use values() on the parent model",
      "Use select_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you combine multiple filtered annotations on related models efficiently?",
    "c": null,
    "o": [
      "Use multiple annotate() calls with conditional expressions like Case/When",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which expression allows performing arithmetic calculations across related fields in a QuerySet?",
    "c": null,
    "o": [
      "F expressions combined with Case/When inside annotate()",
      "Q objects only",
      "Value expressions only",
      "select_related() only"
    ]
  },
  {
    "q": "How can you ensure an annotation respects database-level constraints when combining parent and child models?",
    "c": null,
    "o": [
      "Use expressions compatible with UniqueConstraint or CheckConstraint in Meta",
      "Use annotate() alone",
      "Use select_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you create a conditional functional index on a field in a multi-table inherited model?",
    "c": null,
    "o": [
      "Use ExpressionIndex with a condition and function in Meta.indexes",
      "Set db_index=True only",
      "Use unique=True only",
      "Use annotate() without Meta.indexes"
    ]
  },
  {
    "q": "Which technique allows updating related objects in bulk with conditional logic safely?",
    "c": null,
    "o": [
      "Use update() with F expressions inside transaction.atomic()",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What happens when you access a deferred field in a model instance?",
    "c": null,
    "o": [
      "Django performs a separate query to fetch the field’s value",
      "An error is raised",
      "The field remains NULL",
      "All fields in the model are loaded automatically"
    ]
  },
  {
    "q": "How can you limit which fields are loaded immediately from the database for performance optimization?",
    "c": null,
    "o": [
      "Use only() or defer() in the QuerySet",
      "Use select_related() only",
      "Use annotate()",
      "Use raw() exclusively"
    ]
  },
  {
    "q": "Which technique allows prefetching only a subset of related objects based on a condition?",
    "c": null,
    "o": [
      "Use prefetch_related() with a Prefetch object containing a filtered QuerySet",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you safely perform post-save operations that depend on deferred or filtered related fields?",
    "c": null,
    "o": [
      "Use post_save signals inside a transaction.atomic() block",
      "Use pre_save signals only",
      "Use annotate() only",
      "Use select_related() only"
    ]
  },
  {
    "q": "Which PostgreSQL-specific feature allows creating a functional index on a JSONField key for efficient lookups?",
    "c": null,
    "o": [
      "ExpressionIndex with KeyTextTransform in Meta.indexes",
      "db_index=True on the JSONField",
      "unique=True",
      "CheckConstraint only"
    ]
  },
  {
    "q": "How can you combine a conditional unique constraint with a functional index on a JSONField?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and condition in Meta.constraints",
      "Set unique=True on the field",
      "Use db_index=True only",
      "Use annotate() without Meta.indexes"
    ]
  },
  {
    "q": "Which technique allows updating JSONField or HStoreField keys in bulk without loading objects into Python?",
    "c": null,
    "o": [
      "Use update() with F expressions combined with KeyTransform/KeyTextTransform inside transaction.atomic()",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "How can you annotate a QuerySet with a value computed from deferred fields or filtered related objects?",
    "c": null,
    "o": [
      "Use annotate() with F expressions, Case/When, or Subquery expressions referencing the deferred or filtered fields",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you ensure signals respect transactional integrity when modifying related objects in bulk?",
    "c": null,
    "o": [
      "Wrap the signal logic inside transaction.atomic() and use update() with F expressions",
      "Use pre_save only",
      "Use annotate() only",
      "Use select_related() only"
    ]
  },
  {
    "q": "Which ORM feature allows performing conditional calculations on JSONField or HStoreField keys during annotation?",
    "c": null,
    "o": [
      "Case/When expressions combined with KeyTransform or KeyTextTransform",
      "F expressions only",
      "Q objects only",
      "Value expressions only"
    ]
  },
  {
    "q": "How can you annotate a child model with a value computed from a parent model’s JSONField?",
    "c": null,
    "o": [
      "Use annotate() with KeyTextTransform and F expressions referencing the parent field",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which technique allows creating a functional index on a JSONField key in a child model of a multi-table inheritance hierarchy?",
    "c": null,
    "o": [
      "Use ExpressionIndex with KeyTextTransform in Meta.indexes of the child model",
      "Set db_index=True on the JSONField",
      "Use unique=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "How can you filter related JSONField keys in a multi-table inheritance setup efficiently?",
    "c": null,
    "o": [
      "Use Prefetch with a filtered QuerySet on the related child model",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you perform conditional aggregation on a JSONField key in an inherited child model?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and the filter parameter using Q expressions",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which Django feature allows combining functional indexes with conditional constraints in inherited models?",
    "c": null,
    "o": [
      "Use ExpressionIndex with UniqueConstraint or CheckConstraint and a condition in Meta",
      "Use db_index=True only",
      "Use unique=True only",
      "Use annotate() only"
    ]
  },
  {
    "q": "How can you safely update JSONField keys across parent and child models in bulk?",
    "c": null,
    "o": [
      "Use update() with F expressions and KeyTextTransform inside transaction.atomic()",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "How can you annotate a QuerySet with a value derived from multiple JSONField keys in related child models?",
    "c": null,
    "o": [
      "Use annotate() with Subquery or ExpressionWrapper combining KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which technique allows combining deferred fields and JSONField annotations in a single QuerySet efficiently?",
    "c": null,
    "o": [
      "Use only() or defer() to load necessary fields and annotate() with KeyTextTransform/F expressions",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you enforce a conditional unique constraint on a JSONField key across parent and child models?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform expression and condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "Which ORM feature allows performing arithmetic operations across JSONField keys in parent and child models?",
    "c": null,
    "o": [
      "F expressions combined with KeyTextTransform inside annotate() or update()",
      "Case/When only",
      "Q objects only",
      "Value expressions only"
    ]
  },
  {
    "q": "Which PostgreSQL-specific field allows storing arrays of values natively in Django?",
    "c": null,
    "o": [
      "ArrayField",
      "JSONField",
      "HStoreField",
      "TextField"
    ]
  },
  {
    "q": "How can you create a functional index on a specific element of an ArrayField in Django?",
    "c": null,
    "o": [
      "Use ExpressionIndex with a Subscript expression in Meta.indexes",
      "Set db_index=True on the ArrayField",
      "Use unique=True",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "Which expression allows filtering objects based on a specific key-value pair in an HStoreField efficiently?",
    "c": null,
    "o": [
      "KeyTextTransform combined with filter() or Q expressions",
      "F expressions only",
      "Case/When only",
      "Value expressions only"
    ]
  },
  {
    "q": "How can you enforce a conditional unique constraint on an ArrayField element in a child model?",
    "c": null,
    "o": [
      "Use UniqueConstraint with a Subscript expression and condition in Meta.constraints",
      "Set unique=True on the ArrayField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "How can you annotate a QuerySet with the length of an ArrayField or number of elements meeting a condition?",
    "c": null,
    "o": [
      "Use annotate() with Length('array_field') or Count('array_field', filter=Q(...))",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which technique allows combining functional indexes on HStore keys with conditional constraints in inherited models?",
    "c": null,
    "o": [
      "Use ExpressionIndex and UniqueConstraint or CheckConstraint with condition in Meta",
      "Use db_index=True only",
      "Use unique=True only",
      "Use annotate() without Meta.indexes"
    ]
  },
  {
    "q": "How can you safely update specific elements of an ArrayField across parent and child models in bulk?",
    "c": null,
    "o": [
      "Use update() with F expressions and Subscript expressions inside transaction.atomic()",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "How can you annotate a QuerySet with values computed from multiple HStore keys in inherited models?",
    "c": null,
    "o": [
      "Use annotate() with KeyTextTransform expressions combined with F or Subquery",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you enforce database-level constraints on HStore or ArrayField elements in child models?",
    "c": null,
    "o": [
      "Use CheckConstraint or UniqueConstraint with expressions in Meta.constraints",
      "Set unique=True only",
      "Use db_index=True only",
      "Use annotate() only"
    ]
  },
  {
    "q": "Which ORM feature allows performing arithmetic or string operations on ArrayField or HStore keys in parent and child models?",
    "c": null,
    "o": [
      "F expressions combined with Subscript or KeyTextTransform expressions inside annotate() or update()",
      "Case/When only",
      "Q objects only",
      "Value expressions only"
    ]
  },
  {
    "q": "How can you annotate a model with the sum of values from multiple keys in an HStoreField?",
    "c": null,
    "o": [
      "Use annotate() with Sum combined with KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which technique allows combining multiple conditional functional indexes on a JSONField?",
    "c": null,
    "o": [
      "Use multiple ExpressionIndex entries in Meta.indexes with conditions",
      "Use db_index=True only",
      "Use unique=True only",
      "Use annotate() without Meta.indexes"
    ]
  },
  {
    "q": "How can you safely perform bulk updates on JSONField or HStoreField keys while ensuring signal integrity?",
    "c": null,
    "o": [
      "Use update() with F expressions inside transaction.atomic() and handle signals carefully",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "Which Django ORM expression allows computing a conditional sum on HStore or JSONField keys?",
    "c": null,
    "o": [
      "Case/When combined with Sum inside annotate()",
      "F expressions only",
      "Q objects only",
      "Value expressions only"
    ]
  },
  {
    "q": "How can you filter objects based on multiple JSONField key values efficiently?",
    "c": null,
    "o": [
      "Use filter() with multiple KeyTextTransform expressions and Q objects",
      "Iterate manually in Python",
      "Use select_related() only",
      "Use annotate() only"
    ]
  },
  {
    "q": "How can you enforce a multi-key unique constraint on a JSONField across inherited models?",
    "c": null,
    "o": [
      "Use UniqueConstraint with expressions referencing multiple keys in Meta.constraints",
      "Set unique=True only",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "Which technique allows combining deferred fields, conditional annotations, and JSON/HStore functional indexes in a single QuerySet?",
    "c": null,
    "o": [
      "Use only()/defer() with annotate() and ExpressionIndex-compatible expressions",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you annotate a child model in multi-table inheritance with aggregated values from JSON/HStore keys in the parent?",
    "c": null,
    "o": [
      "Use annotate() with Subquery, KeyTextTransform, and F expressions referencing parent fields",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you combine functional indexes with conditional constraints and JSONField/HStore aggregation for optimized queries?",
    "c": null,
    "o": [
      "Use ExpressionIndex and UniqueConstraint/CheckConstraint with conditions, and annotate() for aggregation",
      "Use db_index=True only",
      "Use unique=True only",
      "Use annotate() only without indexes"
    ]
  },
  {
    "q": "How can transactional signals be combined with bulk updates and JSON/HStore field calculations safely?",
    "c": null,
    "o": [
      "Use post_save or pre_save signals inside transaction.atomic() and F/KeyTextTransform expressions in update()",
      "Use signals outside transactions",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What will the following code do when executed?",
    "c": "from django.db.models import F, Case, When\nMyModel.objects.update(score=Case(\n    When(score__lt=50, then=F('score') + 10),\n    default=F('score')\n))",
    "o": [
      "It conditionally increments the 'score' field by 10 only for rows with score < 50",
      "It increments all 'score' fields by 10",
      "It sets all 'score' fields to 10",
      "It raises an error due to invalid Case syntax"
    ]
  },
  {
    "q": "How can you efficiently load only the 'title' and 'author' fields of a model while deferring the rest?",
    "c": null,
    "o": [
      "Use only('title', 'author') in the QuerySet",
      "Use select_related('title', 'author')",
      "Use defer('title', 'author')",
      "Use values() only"
    ]
  },
  {
    "q": "What is the effect of the following Django query?",
    "c": "from django.db.models import Count, Q\nBook.objects.annotate(\n    popular_reviews=Count('reviews', filter=Q(reviews__rating__gte=4))\n)",
    "o": [
      "Annotates each Book instance with the number of related reviews having rating >= 4",
      "Counts all reviews for each Book",
      "Filters books with at least one review >= 4",
      "Raises an error because filter is not allowed in Count"
    ]
  },
  {
    "q": "Which approach allows prefetching only active related objects for a model?",
    "c": null,
    "o": [
      "Use prefetch_related(Prefetch('related_name', queryset=RelatedModel.objects.filter(active=True)))",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will the following code snippet achieve?",
    "c": "from django.contrib.postgres.fields import HStoreField\nfrom django.db import models\n\nclass MyModel(models.Model):\n    data = HStoreField()\n\nMyModel.objects.filter(data__contains={'status':'active'})",
    "o": [
      "Filters MyModel objects where the HStoreField 'data' contains the key-value pair {'status':'active'}",
      "Filters all objects ignoring the HStoreField",
      "Raises an error since __contains is invalid",
      "Filters objects where data equals exactly {'status':'active'}"
    ]
  },
  {
    "q": "Which method ensures atomic updates across multiple related models?",
    "c": null,
    "o": [
      "Wrap the updates inside transaction.atomic()",
      "Use select_related() only",
      "Use annotate() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What is the purpose of using ExpressionIndex in a Django model?",
    "c": null,
    "o": [
      "To create an index based on an expression or function of a field for faster queries",
      "To set db_index=True on a normal field",
      "To enforce uniqueness",
      "To perform aggregation"
    ]
  },
  {
    "q": "What will the following code achieve?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.update(active_count=F('active_count') + KeyTextTransform('count', 'data'))",
    "o": [
      "It adds the integer value of the 'count' key from the HStoreField 'data' to the 'active_count' field",
      "It raises an error because KeyTextTransform cannot be used in update()",
      "It overwrites 'active_count' with the string value of 'count'",
      "It performs a select_related query"
    ]
  },
  {
    "q": "How can you enforce that a JSONField key is unique only for rows meeting a certain condition?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform expression and condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What is the purpose of using Prefetch with a filtered QuerySet?",
    "c": null,
    "o": [
      "To load only a subset of related objects efficiently, avoiding unnecessary queries",
      "To enforce database constraints",
      "To perform aggregation",
      "To update related objects in bulk"
    ]
  },
  {
    "q": "What will the following code do?",
    "c": "from django.db.models import Subquery\nlatest_review = Review.objects.filter(book=OuterRef('pk')).order_by('-created_at')[:1]\nBook.objects.annotate(latest_rating=Subquery(latest_review.values('rating')))",
    "o": [
      "Annotates each Book with the rating of its latest review",
      "Raises an error because Subquery cannot be used in annotate",
      "Filters books with a review rating",
      "Sets all latest_rating fields to None"
    ]
  },
  {
    "q": "What does the following code do?",
    "c": "from django.db import models\n\nclass Parent(models.Model):\n    value = models.IntegerField()\n\nclass Child(Parent):\n    extra = models.IntegerField()\n\nChild.objects.annotate(total=F('value') + F('extra'))",
    "o": [
      "Annotates each Child instance with the sum of 'value' from Parent and 'extra' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores the parent field and sums only 'extra'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "What is the result of this query?",
    "c": "from django.db.models import Count, Q\nAuthor.objects.annotate(\n    popular_books=Count('book', filter=Q(book__rating__gte=4))\n)",
    "o": [
      "Annotates each Author with the number of related books having rating >= 4",
      "Counts all books regardless of rating",
      "Filters authors with at least one popular book",
      "Raises an error due to filter usage in Count"
    ]
  },
  {
    "q": "What will this signal accomplish?",
    "c": "from django.db.models.signals import post_save\nfrom django.dispatch import receiver\nfrom django.db import transaction\n\n@receiver(post_save, sender=Order)\ndef update_inventory(sender, instance, **kwargs):\n    with transaction.atomic():\n        for item in instance.items.all():\n            item.product.stock -= item.quantity\n            item.product.save()",
    "o": [
      "Safely updates product stock for each item in the order within a transaction",
      "Updates stock without transaction safety",
      "Raises an error because signals cannot use transactions",
      "Does nothing if items exist"
    ]
  },
  {
    "q": "What will the following annotation achieve?",
    "c": "from django.db.models import Subquery, OuterRef\nlatest_order = Order.objects.filter(customer=OuterRef('pk')).order_by('-date')[:1]\nCustomer.objects.annotate(latest_order_total=Subquery(latest_order.values('total')))",
    "o": [
      "Annotates each Customer with the total of their most recent Order",
      "Filters customers with orders",
      "Raises an error because Subquery cannot be used in annotate",
      "Sets all latest_order_total fields to None"
    ]
  },
  {
    "q": "Which technique avoids N+1 queries when prefetching only active related objects?",
    "c": null,
    "o": [
      "Use prefetch_related(Prefetch('related_name', queryset=RelatedModel.objects.filter(active=True)))",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will the following update do?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.update(score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Adds the 'bonus' key from the HStoreField 'data' to the 'score' field for each row",
      "Raises an error because KeyTextTransform cannot be used in update()",
      "Sets all 'score' fields to the 'bonus' value",
      "Does nothing"
    ]
  },
  {
    "q": "How can you enforce uniqueness of a JSONField key only for active rows?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform expression and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What is the effect of this query?",
    "c": "Book.objects.annotate(\n    popular_reviews=Count('reviews', filter=Q(reviews__rating__gte=4))\n).filter(popular_reviews__gte=10)",
    "o": [
      "Filters Books that have at least 10 reviews with rating >= 4",
      "Counts all reviews regardless of rating",
      "Annotates without filtering",
      "Raises an error due to filter usage in Count"
    ]
  },
  {
    "q": "What will the following code accomplish?",
    "c": "from django.db.models import Case, When, F\nProduct.objects.update(\n    price=Case(\n        When(stock__lte=10, then=F('price') * 1.1),\n        default=F('price')\n    )\n)",
    "o": [
      "Increases the price by 10% for products with stock <= 10",
      "Increases the price for all products",
      "Sets price to 1.1 for products with stock <= 10",
      "Raises an error due to Case usage"
    ]
  },
  {
    "q": "How can you combine deferred fields and JSON/HStore functional annotations efficiently?",
    "c": null,
    "o": [
      "Use only()/defer() to select required fields and annotate() with F/KeyTextTransform expressions",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does the following query accomplish?",
    "c": "from django.db.models import Subquery, OuterRef\nlatest_review = Review.objects.filter(book=OuterRef('pk')).order_by('-created_at')[:1]\nBook.objects.annotate(latest_rating=Subquery(latest_review.values('rating')))",
    "o": [
      "Annotates each Book with the rating of its most recent Review",
      "Filters books with a review rating",
      "Raises an error because Subquery cannot be used in annotate",
      "Sets all latest_rating fields to None"
    ]
  },
  {
    "q": "What will this code do?",
    "c": "from django.contrib.postgres.fields import ArrayField\nfrom django.db import models\n\nclass Survey(models.Model):\n    answers = ArrayField(models.IntegerField())\n\nSurvey.objects.annotate(answer_count=Count('answers'))",
    "o": [
      "Annotates each Survey with the number of elements in the 'answers' ArrayField",
      "Counts all Survey rows",
      "Raises an error because Count cannot be used on ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "What does the following HStore update achieve?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.update(score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Adds the value of 'bonus' key from HStoreField 'data' to the 'score' field",
      "Sets all scores to the 'bonus' value",
      "Raises an error because KeyTextTransform cannot be used in update",
      "Does nothing"
    ]
  },
  {
    "q": "Which technique ensures atomic updates when modifying multiple related models within a signal?",
    "c": null,
    "o": [
      "Use transaction.atomic() inside the signal handler",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will this conditional update do?",
    "c": "from django.db.models import Case, When, F\nProduct.objects.update(\n    price=Case(\n        When(stock__lte=10, then=F('price') * 1.1),\n        default=F('price')\n    )\n)",
    "o": [
      "Increases the price by 10% for products with stock <= 10",
      "Increases the price for all products",
      "Sets price to 1.1 for products with stock <= 10",
      "Raises an error due to Case usage"
    ]
  },
  {
    "q": "What does this filtered annotation achieve?",
    "c": "Author.objects.annotate(\n    popular_books=Count('book', filter=Q(book__rating__gte=4))\n).filter(popular_books__gte=5)",
    "o": [
      "Filters Authors with at least 5 books having rating >= 4",
      "Counts all books for each author",
      "Annotates without filtering",
      "Raises an error due to filter usage in Count"
    ]
  },
  {
    "q": "What will this query do?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.annotate(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Annotates each instance with the sum of 'score' field and the 'bonus' key from HStoreField 'data'",
      "Overwrites 'score' with 'bonus' value",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "How can you prefetch only active related objects efficiently?",
    "c": null,
    "o": [
      "Use prefetch_related(Prefetch('related_name', queryset=RelatedModel.objects.filter(active=True)))",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What is the effect of combining deferred fields with functional JSON/HStore annotations?",
    "c": null,
    "o": [
      "Only required fields are loaded while computed annotations are evaluated in the database",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce a conditional unique constraint on a JSONField key for active rows?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "from django.db import models\nfrom django.db.models import F\n\nclass Parent(models.Model):\n    score = models.IntegerField()\n\nclass Child(Parent):\n    bonus = models.IntegerField()\n\nChild.objects.annotate(total_score=F('score') + F('bonus'))",
    "o": [
      "Annotates each Child with the sum of 'score' from Parent and 'bonus' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "What will this HStore annotation do?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.annotate(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Annotates each instance with the sum of 'score' field and 'bonus' key from HStoreField 'data'",
      "Overwrites 'score' with 'bonus'",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "What is the effect of this ArrayField annotation?",
    "c": "from django.contrib.postgres.fields import ArrayField\nfrom django.db.models import Count\nSurvey.objects.annotate(answer_count=Count('answers'))",
    "o": [
      "Annotates each Survey with the number of elements in the 'answers' ArrayField",
      "Counts all Survey rows",
      "Raises an error because Count cannot be used on ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "What does this conditional update accomplish?",
    "c": "from django.db.models import Case, When, F\nProduct.objects.update(\n    price=Case(\n        When(stock__lte=10, then=F('price') * 1.1),\n        default=F('price')\n    )\n)",
    "o": [
      "Increases price by 10% for products with stock <= 10",
      "Increases price for all products",
      "Sets price to 1.1 for low-stock products",
      "Raises an error due to Case usage"
    ]
  },
  {
    "q": "How can you prefetch only active related objects efficiently?",
    "c": null,
    "o": [
      "Use prefetch_related(Prefetch('related_name', queryset=RelatedModel.objects.filter(active=True)))",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will this Subquery annotation do?",
    "c": "from django.db.models import Subquery, OuterRef\nlatest_order = Order.objects.filter(customer=OuterRef('pk')).order_by('-date')[:1]\nCustomer.objects.annotate(latest_order_total=Subquery(latest_order.values('total')))",
    "o": [
      "Annotates each Customer with the total of their most recent Order",
      "Filters customers with orders",
      "Raises an error because Subquery cannot be used in annotate",
      "Sets all latest_order_total fields to None"
    ]
  },
  {
    "q": "Which technique ensures atomic updates in signals when modifying multiple related models?",
    "c": null,
    "o": [
      "Wrap the updates inside transaction.atomic()",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you enforce a JSONField key uniqueness only for active rows?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What will this filtered annotation accomplish?",
    "c": "Author.objects.annotate(\n    popular_books=Count('book', filter=Q(book__rating__gte=4))\n).filter(popular_books__gte=5)",
    "o": [
      "Filters Authors with at least 5 books having rating >= 4",
      "Counts all books for each author",
      "Annotates without filtering",
      "Raises an error due to filter usage in Count"
    ]
  },
  {
    "q": "How can you combine deferred fields with JSON/HStore/ArrayField annotations efficiently?",
    "c": null,
    "o": [
      "Use only()/defer() to select required fields and annotate() with F/KeyTextTransform/Subscript expressions",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does this ExpressionIndex accomplish?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' for faster queries",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "What will happen if a pre_save signal modifies a model instance and calls save() within the same signal?",
    "c": null,
    "o": [
      "It may trigger the same signal recursively unless handled carefully",
      "It will always succeed without any recursion",
      "It will raise an error immediately",
      "Signals are ignored during save()"
    ]
  },
  {
    "q": "How can you annotate a multi-table inherited child model with a computed value from both parent and child fields?",
    "c": null,
    "o": [
      "Use annotate() with F expressions referencing parent and child fields",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "Which approach allows creating a conditional functional index on a JSONField key in a child model?",
    "c": null,
    "o": [
      "Use ExpressionIndex with KeyTextTransform and a condition in Meta.indexes",
      "Set db_index=True only",
      "Use unique=True only",
      "Use annotate() without Meta.indexes"
    ]
  },
  {
    "q": "What is the purpose of using defer() on a model field?",
    "c": null,
    "o": [
      "To avoid loading the field’s value from the database until accessed",
      "To prevent the field from being saved",
      "To delete the field temporarily",
      "To index the field"
    ]
  },
  {
    "q": "Which technique ensures atomic updates across related models triggered in signals?",
    "c": null,
    "o": [
      "Wrap updates inside transaction.atomic() in the signal handler",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will the following filtered prefetch accomplish?",
    "c": "from django.db.models import Prefetch\nBook.objects.prefetch_related(Prefetch('reviews', queryset=Review.objects.filter(rating__gte=4)))",
    "o": [
      "Prefetches only reviews with rating >= 4 for each Book to avoid N+1 queries",
      "Fetches all reviews for each Book",
      "Filters Books with reviews >= 4",
      "Raises an error due to Prefetch usage"
    ]
  },
  {
    "q": "How can you enforce a unique constraint on an ArrayField element for rows meeting a condition?",
    "c": null,
    "o": [
      "Use UniqueConstraint with a Subscript expression and a condition in Meta.constraints",
      "Set unique=True on the ArrayField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What is the effect of combining only() with annotate() on deferred fields?",
    "c": null,
    "o": [
      "Only the selected fields are loaded from the database, while annotations are computed in SQL",
      "All fields are loaded regardless of only()",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you safely update multiple JSONField keys across parent and child models without triggering recursive signals?",
    "c": null,
    "o": [
      "Use update() with F/KeyTextTransform expressions inside transaction.atomic() and temporarily disconnect signals if necessary",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "Which technique allows performing conditional aggregation on HStore or JSONField keys efficiently?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) on KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will happen if you define a CheckConstraint on a model field that depends on another field?",
    "c": null,
    "o": [
      "The database enforces the condition during insert and update operations",
      "The ORM automatically validates it in Python",
      "It raises an error at model definition",
      "It is ignored unless manually checked"
    ]
  },
  {
    "q": "How can you perform a transactional update across multiple related models to ensure consistency?",
    "c": null,
    "o": [
      "Wrap the updates inside transaction.atomic()",
      "Use annotate() only",
      "Use select_related() only",
      "Iterate and call save() on each object without a transaction"
    ]
  },
  {
    "q": "What will the following code accomplish?",
    "c": "from django.contrib.postgres.fields import HStoreField\nfrom django.db.models import F, KeyTextTransform\nMyModel.objects.update(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Adds the integer value of 'bonus' from HStoreField 'data' to the 'score' field",
      "Overwrites 'score' with 'bonus'",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "Which approach allows creating an expression-based functional index on multiple JSONField keys?",
    "c": null,
    "o": [
      "Use ExpressionIndex with combined KeyTextTransform expressions in Meta.indexes",
      "Use db_index=True only",
      "Use unique=True only",
      "Use annotate() without indexes"
    ]
  },
  {
    "q": "How can you avoid recursive signal calls when updating related objects in bulk?",
    "c": null,
    "o": [
      "Temporarily disconnect signals or use update() inside transaction.atomic() without triggering signals",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will this filtered annotation do?",
    "c": "Author.objects.annotate(\n    top_books=Count('book', filter=Q(book__rating__gte=4))\n).filter(top_books__gte=3)",
    "o": [
      "Filters Authors having at least 3 books with rating >= 4",
      "Counts all books regardless of rating",
      "Annotates without filtering",
      "Raises an error due to filter usage in Count"
    ]
  },
  {
    "q": "How can you enforce that an ArrayField element is unique only for rows matching a condition?",
    "c": null,
    "o": [
      "Use UniqueConstraint with Subscript expression and a condition in Meta.constraints",
      "Set unique=True on the ArrayField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What is the effect of combining defer() with JSONField or HStoreField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded from the database until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "Which technique allows performing conditional aggregation on multiple HStore or JSONField keys efficiently?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will the following ExpressionIndex do?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to speed up queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "What does the following code achieve in a multi-table inheritance scenario?",
    "c": "from django.db import models\nfrom django.db.models import F\n\nclass Parent(models.Model):\n    score = models.IntegerField()\n\nclass Child(Parent):\n    bonus = models.IntegerField()\n\nChild.objects.annotate(total_score=F('score') + F('bonus'))",
    "o": [
      "Annotates each Child with the sum of 'score' from Parent and 'bonus' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "What will this JSONField filtered annotation do?",
    "c": "Book.objects.annotate(\n    top_chapter_count=Count('chapters', filter=Q(chapters__json_field__key='status', chapters__json_field__value='published'))\n)",
    "o": [
      "Counts the number of chapters per Book where the JSONField key 'status' equals 'published'",
      "Counts all chapters regardless of status",
      "Annotates without filtering",
      "Raises an error due to invalid JSONField filter"
    ]
  },
  {
    "q": "How can you safely update multiple HStoreField keys in parent and child models without triggering recursive signals?",
    "c": null,
    "o": [
      "Use update() with F/KeyTextTransform expressions inside transaction.atomic() and temporarily disconnect signals",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What is the purpose of using Subquery with OuterRef in annotations?",
    "c": null,
    "o": [
      "To annotate each row with a value computed from a related QuerySet dynamically",
      "To perform prefetch_related queries",
      "To enforce uniqueness in the database",
      "To defer field loading"
    ]
  },
  {
    "q": "What will the following code do for an ArrayField?",
    "c": "from django.contrib.postgres.fields import ArrayField\nfrom django.db.models import Count\nSurvey.objects.annotate(answer_count=Count('answers'))",
    "o": [
      "Annotates each Survey with the number of elements in the 'answers' ArrayField",
      "Counts all Survey rows",
      "Raises an error because Count cannot be used on ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "Which technique ensures atomic updates across related models triggered in a signal handler?",
    "c": null,
    "o": [
      "Wrap the updates inside transaction.atomic()",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does this ExpressionIndex accomplish?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to speed up queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce a conditional unique constraint on a JSONField key for active rows?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What is the effect of combining defer() with JSONField or HStoreField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded from the database until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "Which technique allows performing conditional aggregation on multiple HStore or JSONField keys efficiently?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "from django.db.models import Subquery, OuterRef\nlatest_order = Order.objects.filter(customer=OuterRef('pk')).order_by('-date')[:1]\nCustomer.objects.annotate(latest_order_total=Subquery(latest_order.values('total')))",
    "o": [
      "Annotates each Customer with the total of their most recent Order",
      "Filters customers with orders",
      "Raises an error because Subquery cannot be used in annotate",
      "Sets all latest_order_total fields to None"
    ]
  },
  {
    "q": "How can you safely update multiple ArrayField elements across parent and child models?",
    "c": null,
    "o": [
      "Use update() with Subscript expressions inside transaction.atomic() to avoid recursion or partial updates",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What will the following filtered annotation do?",
    "c": "Author.objects.annotate(\n    top_books=Count('book', filter=Q(book__rating__gte=4))\n).filter(top_books__gte=3)",
    "o": [
      "Filters Authors having at least 3 books with rating >= 4",
      "Counts all books for each author",
      "Annotates without filtering",
      "Raises an error due to filter usage in Count"
    ]
  },
  {
    "q": "How can you combine deferred fields with JSONField/HStoreField/ArrayField annotations?",
    "c": null,
    "o": [
      "Use only()/defer() to select required fields and annotate() with F/KeyTextTransform/Subscript expressions",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does this ExpressionIndex accomplish?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to speed up queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "What will the following HStoreField update do?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.update(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Adds the integer value of 'bonus' from HStoreField 'data' to the 'score' field",
      "Overwrites 'score' with 'bonus'",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "Which approach ensures atomic updates across related models triggered in a signal?",
    "c": null,
    "o": [
      "Wrap updates inside transaction.atomic() in the signal handler",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "How can you enforce a conditional unique constraint on a JSONField key only for active rows?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What is the effect of combining defer() with JSONField/HStoreField/ArrayField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded from the database until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you perform conditional aggregation on multiple JSONField/HStoreField keys efficiently?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does the following multi-table inheritance annotation achieve?",
    "c": "from django.db.models import F\n\nclass Parent(models.Model):\n    base_score = models.IntegerField()\n\nclass Child(Parent):\n    bonus_score = models.IntegerField()\n\nChild.objects.annotate(total_score=F('base_score') + F('bonus_score'))",
    "o": [
      "Annotates each Child with the sum of 'base_score' from Parent and 'bonus_score' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus_score'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "What will the following JSONField filtered aggregation do?",
    "c": "Book.objects.annotate(\n    published_chapters=Count('chapters', filter=Q(chapters__json_field__key='status', chapters__json_field__value='published'))\n)",
    "o": [
      "Counts the number of chapters per Book where the JSONField key 'status' equals 'published'",
      "Counts all chapters regardless of status",
      "Annotates without filtering",
      "Raises an error due to invalid JSONField filter"
    ]
  },
  {
    "q": "How can you safely perform bulk updates on HStoreField keys across parent and child models?",
    "c": null,
    "o": [
      "Use update() with F/KeyTextTransform expressions inside transaction.atomic() and temporarily disconnect signals if needed",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "Which technique allows annotating a QuerySet with a value derived from a related QuerySet dynamically?",
    "c": null,
    "o": [
      "Use Subquery with OuterRef in annotate()",
      "Use select_related() only",
      "Use prefetch_related() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will the following ArrayField annotation accomplish?",
    "c": "from django.contrib.postgres.fields import ArrayField\nfrom django.db.models import Count\nSurvey.objects.annotate(answer_count=Count('answers'))",
    "o": [
      "Annotates each Survey with the number of elements in the 'answers' ArrayField",
      "Counts all Survey rows",
      "Raises an error because Count cannot be used on ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "How can you ensure atomic updates across multiple related models in a signal handler?",
    "c": null,
    "o": [
      "Wrap all updates inside transaction.atomic()",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What is the purpose of this ExpressionIndex?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to optimize queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce uniqueness of a JSONField key only for rows meeting a condition?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What happens when you combine defer() with JSONField, HStoreField, or ArrayField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you efficiently perform conditional aggregation on multiple JSONField or HStoreField keys?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) on KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does this multi-table inheritance annotation accomplish?",
    "c": "from django.db.models import F\n\nclass Parent(models.Model):\n    base_score = models.IntegerField()\n\nclass Child(Parent):\n    bonus_score = models.IntegerField()\n\nChild.objects.annotate(total_score=F('base_score') + F('bonus_score'))",
    "o": [
      "Annotates each Child with the sum of 'base_score' from Parent and 'bonus_score' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus_score'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "What will this JSONField filtered Count achieve?",
    "c": "Book.objects.annotate(\n    approved_chapters=Count('chapters', filter=Q(chapters__json_field__key='status', chapters__json_field__value='approved'))\n)",
    "o": [
      "Counts the number of chapters per Book where JSONField key 'status' equals 'approved'",
      "Counts all chapters regardless of status",
      "Annotates without filtering",
      "Raises an error due to invalid JSONField filter"
    ]
  },
  {
    "q": "How can you perform safe bulk updates on HStoreField keys across parent and child models without triggering recursive signals?",
    "c": null,
    "o": [
      "Use update() with F/KeyTextTransform inside transaction.atomic() and temporarily disconnect signals",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What is the purpose of using Subquery with OuterRef in annotate()?",
    "c": null,
    "o": [
      "To dynamically annotate each row with a value computed from a related QuerySet",
      "To perform prefetch_related queries",
      "To enforce database uniqueness",
      "To defer field loading"
    ]
  },
  {
    "q": "What does this ArrayField annotation accomplish?",
    "c": "from django.contrib.postgres.fields import ArrayField\nfrom django.db.models import Count\nSurvey.objects.annotate(answer_count=Count('answers'))",
    "o": [
      "Annotates each Survey with the number of elements in the 'answers' ArrayField",
      "Counts all Survey rows",
      "Raises an error because Count cannot be used on ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "Which approach ensures atomic updates across multiple related models triggered in a signal?",
    "c": null,
    "o": [
      "Wrap all updates inside transaction.atomic() in the signal handler",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What is the purpose of this ExpressionIndex?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to optimize queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce uniqueness of a JSONField key only for rows that meet a condition?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What happens when you combine defer() with JSONField, HStoreField, or ArrayField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you efficiently perform conditional aggregation on multiple JSONField or HStoreField keys?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does the following multi-table inheritance update achieve?",
    "c": "from django.db.models import F\n\nclass Parent(models.Model):\n    base_score = models.IntegerField()\n\nclass Child(Parent):\n    bonus_score = models.IntegerField()\n\nChild.objects.update(total_score=F('base_score') + F('bonus_score'))",
    "o": [
      "Updates each Child with the sum of 'base_score' from Parent and 'bonus_score' from Child in the database directly",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus_score'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "How can you safely perform bulk updates on nested JSONField keys?",
    "c": null,
    "o": [
      "Use update() with KeyTextTransform expressions inside transaction.atomic() to avoid partial updates",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What will this HStoreField annotation do?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.annotate(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Annotates each instance with the sum of 'score' field and the integer 'bonus' key from HStoreField 'data'",
      "Overwrites 'score' with 'bonus'",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "Which approach ensures atomic updates across multiple related models triggered by a signal?",
    "c": null,
    "o": [
      "Wrap all updates inside transaction.atomic() in the signal handler",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will this filtered annotation on ArrayField achieve?",
    "c": "Survey.objects.annotate(answer_count=Count('answers', filter=Q(answers__contains=[1,2,3])))",
    "o": [
      "Counts elements in 'answers' containing 1, 2, or 3 for each Survey",
      "Counts all elements in 'answers' regardless",
      "Raises an error because Count cannot filter ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "What does the following ExpressionIndex do?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to optimize queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce a conditional unique constraint on a JSONField key for rows that meet a condition?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What is the effect of combining defer() with JSONField, HStoreField, or ArrayField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "Which technique allows conditional aggregation on multiple JSONField or HStoreField keys efficiently?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does using OuterRef in a Subquery annotation allow?",
    "c": null,
    "o": [
      "Dynamically referencing the outer QuerySet row to compute a value from a related QuerySet",
      "Prefetching related objects efficiently",
      "Enforcing uniqueness in the database",
      "Deferring fields until accessed"
    ]
  },
  {
    "q": "What will happen when you annotate a multi-table inherited child with a parent field using F()?",
    "c": "from django.db.models import F\n\nclass Parent(models.Model):\n    score = models.IntegerField()\n\nclass Child(Parent):\n    bonus = models.IntegerField()\n\nChild.objects.annotate(total_score=F('score') + F('bonus'))",
    "o": [
      "Annotates each Child with the sum of 'score' from Parent and 'bonus' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "How can you efficiently update multiple nested keys in JSONField without triggering recursive signals?",
    "c": null,
    "o": [
      "Use update() with KeyTextTransform expressions inside transaction.atomic() and temporarily disconnect signals",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What does the following HStoreField annotation achieve?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.annotate(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Annotates each instance with the sum of 'score' and the integer 'bonus' key from HStoreField 'data'",
      "Overwrites 'score' with 'bonus'",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "Which method ensures atomic updates across related models triggered by a signal?",
    "c": null,
    "o": [
      "Wrap all updates inside transaction.atomic() in the signal handler",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will this filtered ArrayField annotation do?",
    "c": "Survey.objects.annotate(answer_count=Count('answers', filter=Q(answers__contains=[1,2,3])))",
    "o": [
      "Counts elements in 'answers' containing 1, 2, or 3 for each Survey",
      "Counts all elements in 'answers' regardless",
      "Raises an error because Count cannot filter ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "What does this functional ExpressionIndex accomplish?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to optimize queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce uniqueness of a JSONField key conditionally?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What is the effect of combining defer() with JSONField, HStoreField, or ArrayField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you perform conditional aggregation on multiple JSONField/HStoreField keys efficiently?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does using OuterRef in a Subquery annotation allow?",
    "c": null,
    "o": [
      "Dynamically referencing the outer QuerySet row to compute a value from a related QuerySet",
      "Prefetching related objects efficiently",
      "Enforcing uniqueness in the database",
      "Deferring fields until accessed"
    ]
  },
  {
    "q": "What does this multi-table inheritance annotation do?",
    "c": "from django.db.models import F\n\nclass Parent(models.Model):\n    score = models.IntegerField()\n\nclass Child(Parent):\n    bonus = models.IntegerField()\n\nChild.objects.annotate(total_score=F('score') + F('bonus'))",
    "o": [
      "Annotates each Child with the sum of 'score' from Parent and 'bonus' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "How can you perform a safe bulk update on nested JSONField keys without triggering recursive signals?",
    "c": null,
    "o": [
      "Use update() with KeyTextTransform expressions inside transaction.atomic() and temporarily disconnect signals",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What will the following HStoreField annotation accomplish?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.annotate(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Annotates each instance with the sum of 'score' and the integer 'bonus' key from HStoreField 'data'",
      "Overwrites 'score' with 'bonus'",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "Which approach ensures atomic updates across multiple related models triggered in a signal?",
    "c": null,
    "o": [
      "Wrap all updates inside transaction.atomic() in the signal handler",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will this filtered ArrayField annotation do?",
    "c": "Survey.objects.annotate(answer_count=Count('answers', filter=Q(answers__contains=[1,2,3])))",
    "o": [
      "Counts elements in 'answers' containing 1, 2, or 3 for each Survey",
      "Counts all elements in 'answers' regardless",
      "Raises an error because Count cannot filter ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "What does this functional ExpressionIndex accomplish?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to optimize queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce a conditional unique constraint on a JSONField key?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What happens when you combine defer() with JSONField, HStoreField, or ArrayField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you efficiently perform conditional aggregation on multiple JSONField/HStoreField keys?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does using OuterRef in a Subquery annotation allow?",
    "c": null,
    "o": [
      "Dynamically referencing the outer QuerySet row to compute a value from a related QuerySet",
      "Prefetching related objects efficiently",
      "Enforcing uniqueness in the database",
      "Deferring fields until accessed"
    ]
  },
  {
    "q": "What happens when you use F() to combine parent and child fields in a multi-table inherited model?",
    "c": "from django.db.models import F\n\nclass Parent(models.Model):\n    score = models.IntegerField()\n\nclass Child(Parent):\n    bonus = models.IntegerField()\n\nChild.objects.annotate(total_score=F('score') + F('bonus'))",
    "o": [
      "Annotates each Child with the sum of 'score' from Parent and 'bonus' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "How can you perform a bulk update on nested JSONField keys safely without triggering signals?",
    "c": null,
    "o": [
      "Use update() with KeyTextTransform inside transaction.atomic() and optionally disconnect signals",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What does this HStoreField annotation do?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.annotate(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Annotates each instance with the sum of 'score' and the integer 'bonus' key from HStoreField 'data'",
      "Overwrites 'score' with 'bonus'",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "Which method ensures atomic updates across multiple related models in a signal handler?",
    "c": null,
    "o": [
      "Wrap all updates inside transaction.atomic()",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will this filtered ArrayField annotation accomplish?",
    "c": "Survey.objects.annotate(answer_count=Count('answers', filter=Q(answers__contains=[1,2,3])))",
    "o": [
      "Counts elements in 'answers' containing 1, 2, or 3 for each Survey",
      "Counts all elements in 'answers' regardless",
      "Raises an error because Count cannot filter ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "What is the purpose of this ExpressionIndex?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to optimize queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce conditional uniqueness on a JSONField key?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What happens when defer() is combined with JSONField, HStoreField, or ArrayField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you efficiently perform conditional aggregation on multiple JSONField or HStoreField keys?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does using OuterRef in a Subquery annotation allow?",
    "c": null,
    "o": [
      "Dynamically referencing the outer QuerySet row to compute a value from a related QuerySet",
      "Prefetching related objects efficiently",
      "Enforcing uniqueness in the database",
      "Deferring fields until accessed"
    ]
  },
  {
    "q": "What is the effect of annotating a child model with a field from its parent using F() in multi-table inheritance?",
    "c": "from django.db.models import F\n\nclass Parent(models.Model):\n    score = models.IntegerField()\n\nclass Child(Parent):\n    bonus = models.IntegerField()\n\nChild.objects.annotate(total_score=F('score') + F('bonus'))",
    "o": [
      "Annotates each Child with the sum of 'score' from Parent and 'bonus' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "How can you perform safe bulk updates on nested JSONField keys without triggering recursive signals?",
    "c": null,
    "o": [
      "Use update() with KeyTextTransform inside transaction.atomic() and temporarily disconnect signals",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What will the following HStoreField annotation do?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.annotate(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Annotates each instance with the sum of 'score' and the integer 'bonus' key from HStoreField 'data'",
      "Overwrites 'score' with 'bonus'",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "Which approach ensures atomic updates across multiple related models triggered by a signal?",
    "c": null,
    "o": [
      "Wrap all updates inside transaction.atomic() in the signal handler",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does this filtered ArrayField annotation accomplish?",
    "c": "Survey.objects.annotate(answer_count=Count('answers', filter=Q(answers__contains=[1,2,3])))",
    "o": [
      "Counts elements in 'answers' containing 1, 2, or 3 for each Survey",
      "Counts all elements in 'answers' regardless",
      "Raises an error because Count cannot filter ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "What is the purpose of this functional ExpressionIndex?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to optimize queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce a conditional unique constraint on a JSONField key?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What happens when combining defer() with JSONField, HStoreField, or ArrayField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you efficiently perform conditional aggregation on multiple JSONField or HStoreField keys?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does using OuterRef in a Subquery annotation allow?",
    "c": null,
    "o": [
      "Dynamically referencing the outer QuerySet row to compute a value from a related QuerySet",
      "Prefetching related objects efficiently",
      "Enforcing uniqueness in the database",
      "Deferring fields until accessed"
    ]
  },
  {
    "q": "What is the effect of annotating a multi-table inherited child with a field from its parent using F()?",
    "c": "from django.db.models import F\n\nclass Parent(models.Model):\n    score = models.IntegerField()\n\nclass Child(Parent):\n    bonus = models.IntegerField()\n\nChild.objects.annotate(total_score=F('score') + F('bonus'))",
    "o": [
      "Annotates each Child with the sum of 'score' from Parent and 'bonus' from Child",
      "Raises an error because F cannot reference parent fields",
      "Ignores parent fields and sums only 'bonus'",
      "Returns only Parent instances"
    ]
  },
  {
    "q": "How can you perform bulk updates on nested JSONField keys safely without triggering recursive signals?",
    "c": null,
    "o": [
      "Use update() with KeyTextTransform inside transaction.atomic() and temporarily disconnect signals",
      "Iterate and call save() on each object",
      "Use select_related() only",
      "Use prefetch_related() only"
    ]
  },
  {
    "q": "What does this HStoreField annotation accomplish?",
    "c": "from django.db.models import F, KeyTextTransform\nMyModel.objects.annotate(total_score=F('score') + KeyTextTransform('bonus', 'data'))",
    "o": [
      "Annotates each instance with the sum of 'score' and the integer 'bonus' key from HStoreField 'data'",
      "Overwrites 'score' with 'bonus'",
      "Raises an error due to KeyTextTransform usage",
      "Does nothing"
    ]
  },
  {
    "q": "Which method ensures atomic updates across multiple related models triggered in a signal?",
    "c": null,
    "o": [
      "Wrap all updates inside transaction.atomic() in the signal handler",
      "Use select_related() only",
      "Use annotate() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What will this filtered ArrayField annotation do?",
    "c": "Survey.objects.annotate(answer_count=Count('answers', filter=Q(answers__contains=[1,2,3])))",
    "o": [
      "Counts elements in 'answers' containing 1, 2, or 3 for each Survey",
      "Counts all elements in 'answers' regardless",
      "Raises an error because Count cannot filter ArrayField",
      "Sets answer_count to a default value"
    ]
  },
  {
    "q": "What is the purpose of this functional ExpressionIndex?",
    "c": "from django.contrib.postgres.indexes import ExpressionIndex\nfrom django.db.models import Lower\n\nclass MyModel(models.Model):\n    name = models.CharField(max_length=100)\n\n    class Meta:\n        indexes = [ExpressionIndex(Lower('name'), name='name_lower_idx')]",
    "o": [
      "Creates a functional index on the lowercase of 'name' to optimize queries using Lower()",
      "Sets db_index=True on 'name'",
      "Enforces uniqueness on 'name'",
      "Raises an error"
    ]
  },
  {
    "q": "How can you enforce conditional uniqueness on a JSONField key?",
    "c": null,
    "o": [
      "Use UniqueConstraint with KeyTextTransform and a condition in Meta.constraints",
      "Set unique=True on the JSONField",
      "Use db_index=True only",
      "Use CheckConstraint only"
    ]
  },
  {
    "q": "What happens when combining defer() with JSONField, HStoreField, or ArrayField annotations?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, while annotations are computed in SQL",
      "All fields are loaded regardless",
      "Annotations are ignored",
      "Raises an error"
    ]
  },
  {
    "q": "How can you efficiently perform conditional aggregation on multiple JSONField or HStoreField keys?",
    "c": null,
    "o": [
      "Use annotate() with Sum or Count and filter=Q(...) applied to KeyTextTransform expressions",
      "Use select_related() only",
      "Use values() only",
      "Iterate manually in Python"
    ]
  },
  {
    "q": "What does using OuterRef in a Subquery annotation allow?",
    "c": null,
    "o": [
      "Dynamically referencing the outer QuerySet row to compute a value from a related QuerySet",
      "Prefetching related objects efficiently",
      "Enforcing uniqueness in the database",
      "Deferring fields until accessed"
    ]
  },
  {
    "q": "What is the effect of using OneToOneField in Django models?",
    "c": null,
    "o": [
      "Creates a one-to-one relationship, ensuring each related object can only be associated with one instance",
      "Creates a one-to-many relationship",
      "Creates a many-to-many relationship",
      "Allows null values by default without any constraint"
    ]
  },
  {
    "q": "What will the following code do?",
    "c": "class Author(models.Model):\n    name = models.CharField(max_length=100)\n\nclass Book(models.Model):\n    author = models.ForeignKey(Author, on_delete=models.CASCADE)\n    title = models.CharField(max_length=200)",
    "o": [
      "Creates a one-to-many relationship: one Author can have multiple Books",
      "Creates a one-to-one relationship",
      "Creates a many-to-many relationship",
      "Raises an error because of missing related_name"
    ]
  },
  {
    "q": "Which option correctly describes on_delete=models.PROTECT?",
    "c": null,
    "o": [
      "Prevents deletion of the referenced object if it has related objects",
      "Deletes all related objects when the referenced object is deleted",
      "Sets the foreign key to null when the referenced object is deleted",
      "Allows the deletion without any constraint"
    ]
  },
  {
    "q": "What is the purpose of migrations in Django?",
    "c": null,
    "o": [
      "To propagate model changes to the database schema",
      "To populate data automatically",
      "To create templates for views",
      "To enforce admin permissions"
    ]
  },
  {
    "q": "What is the effect of using related_name in a ForeignKey?",
    "c": null,
    "o": [
      "Specifies the reverse name to access the related objects from the target model",
      "Renames the database column",
      "Overrides the model’s Meta verbose_name",
      "Automatically indexes the field"
    ]
  },
  {
    "q": "How can you define a many-to-many relationship in Django?",
    "c": null,
    "o": [
      "Using ManyToManyField in either of the two related models",
      "Using ForeignKey in both models",
      "Using OneToOneField in one model",
      "Using a JSONField to store IDs manually"
    ]
  },
  {
    "q": "What will the following migration command do?",
    "c": "python manage.py makemigrations",
    "o": [
      "Generates migration files reflecting changes in models",
      "Applies migrations to the database",
      "Rolls back applied migrations",
      "Deletes all migration files"
    ]
  },
  {
    "q": "Which relationship type is best for storing multiple categories for a blog post?",
    "c": null,
    "o": [
      "Many-to-many relationship",
      "One-to-many relationship",
      "One-to-one relationship",
      "ForeignKey with null=True"
    ]
  },
  {
    "q": "What does the following model do?",
    "c": "class Employee(models.Model):\n    manager = models.ForeignKey('self', null=True, blank=True, on_delete=models.SET_NULL)",
    "o": [
      "Creates a self-referential relationship allowing each Employee to optionally reference another Employee as manager",
      "Raises an error because ForeignKey cannot reference 'self'",
      "Creates a many-to-many relationship automatically",
      "Forces each Employee to have a manager"
    ]
  },
  {
    "q": "What is the purpose of unique=True in a model field?",
    "c": null,
    "o": [
      "Ensures that no two records have the same value for that field",
      "Automatically indexes the field",
      "Creates a foreign key relationship",
      "Allows null values to be duplicated"
    ]
  },
  {
    "q": "What will the following Meta option enforce?",
    "c": "class Product(models.Model):\n    name = models.CharField(max_length=100)\n    category = models.CharField(max_length=50)\n\n    class Meta:\n        constraints = [\n            models.UniqueConstraint(fields=['name', 'category'], name='unique_product_category')\n        ]",
    "o": [
      "No two Products can have the same name within the same category",
      "All product names must be globally unique",
      "Product names must not exceed 50 characters",
      "Category must be unique across all products"
    ]
  },
  {
    "q": "Which type of model inheritance creates an additional table for the child model, linking to the parent with an implicit OneToOneField?",
    "c": null,
    "o": [
      "Multi-table inheritance",
      "Abstract base class",
      "Proxy model",
      "Generic foreign key"
    ]
  },
  {
    "q": "What will the following query do?",
    "c": "Book.objects.select_related('author').all()",
    "o": [
      "Performs a SQL JOIN to fetch related Author objects in the same query",
      "Performs an additional query for each related Author",
      "Ignores Author relationships",
      "Raises an error if no related_name is set"
    ]
  },
  {
    "q": "Which migration operation is generated when you rename a model field?",
    "c": null,
    "o": [
      "RenameField",
      "AlterField",
      "AddField and RemoveField",
      "RunSQL"
    ]
  },
  {
    "q": "What does using abstract base classes in Django models accomplish?",
    "c": null,
    "o": [
      "Shares common fields and methods across multiple models without creating a separate table",
      "Creates a new table for the base class",
      "Forces all child classes to share the same table",
      "Restricts inheritance to only ForeignKey fields"
    ]
  },
  {
    "q": "What will the following do?",
    "c": "from django.db.models import Q\n\nBook.objects.filter(Q(title__icontains='django') | Q(title__icontains='python'))",
    "o": [
      "Returns all Books where the title contains 'django' or 'python'",
      "Returns all Books containing both 'django' and 'python'",
      "Raises a ValueError due to misuse of Q objects",
      "Returns only the first matching book"
    ]
  },
  {
    "q": "In migrations, what does the RunPython operation allow?",
    "c": null,
    "o": [
      "Running custom Python code during schema migration",
      "Generating SQL automatically",
      "Rolling back migrations only",
      "Creating fixtures for the admin site"
    ]
  },
  {
    "q": "What is the primary difference between prefetch_related and select_related?",
    "c": null,
    "o": [
      "select_related uses JOINs for single-valued relationships, prefetch_related uses separate queries for multi-valued relationships",
      "select_related always creates separate queries",
      "prefetch_related only works on ForeignKey",
      "They are identical in behavior"
    ]
  },
  {
    "q": "What will the following model design enforce?",
    "c": "class Profile(models.Model):\n    user = models.OneToOneField(User, on_delete=models.CASCADE)\n    bio = models.TextField()",
    "o": [
      "Each User can only have one Profile, and deleting the User deletes the Profile",
      "Each User can have multiple Profiles",
      "Deleting the User sets Profile.user to NULL",
      "Profile is a proxy for User"
    ]
  },
  {
    "q": "Which of the following is TRUE about proxy models?",
    "c": null,
    "o": [
      "They allow changing Python behavior of a model without creating a new table",
      "They always create a new database table",
      "They require abstract base classes",
      "They cannot override model managers"
    ]
  },
  {
    "q": "What does the Meta option ordering = ['-created_at'] enforce in a model?",
    "c": null,
    "o": [
      "All QuerySet results are ordered by created_at in descending order by default",
      "The database always creates an index on created_at",
      "Only the admin interface respects this ordering",
      "It prevents updates on created_at"
    ]
  },
  {
    "q": "What is the effect of setting db_index=True on a model field?",
    "c": null,
    "o": [
      "Creates a database index on that field to speed up lookups",
      "Automatically enforces uniqueness on that field",
      "Prevents NULL values from being stored",
      "Generates a default constraint"
    ]
  },
  {
    "q": "What does this migration snippet do?",
    "c": "migrations.AddConstraint(\n    model_name='employee',\n    constraint=models.CheckConstraint(check=Q(age__gte=18), name='min_age_check')\n)",
    "o": [
      "Ensures employees younger than 18 cannot be inserted into the table",
      "Deletes employees below age 18 automatically",
      "Raises an error during makemigrations if any record is < 18",
      "Applies only at the Python validation layer, not the database"
    ]
  },
  {
    "q": "Which of the following is TRUE about model managers?",
    "c": null,
    "o": [
      "They define the interface through which database query operations are provided to Django models",
      "They can only be used for abstract models",
      "They cannot be customized beyond default objects",
      "They only work in the Django admin"
    ]
  },
  {
    "q": "What happens if you set managed = False in a model’s Meta class?",
    "c": null,
    "o": [
      "Django will not create or modify the database table for this model",
      "The model cannot be used in QuerySets",
      "The model becomes abstract automatically",
      "Migrations will raise an error"
    ]
  },
  {
    "q": "What is the effect of this signal handler?",
    "c": "from django.db.models.signals import post_save\n\n@receiver(post_save, sender=User)\ndef create_profile(sender, instance, created, **kwargs):\n    if created:\n        Profile.objects.create(user=instance)",
    "o": [
      "Automatically creates a Profile whenever a new User is created",
      "Deletes the Profile when the User is deleted",
      "Prevents duplicate Users from being created",
      "Updates the Profile every time the User is saved"
    ]
  },
  {
    "q": "What does unique_together do in a model’s Meta class?",
    "c": null,
    "o": [
      "Prevents duplicate combinations of the given fields across records",
      "Makes each field individually unique",
      "Adds a default primary key to the model",
      "Forces all related ForeignKeys to be unique"
    ]
  },
  {
    "q": "Which migration operation would be generated if you change a CharField’s max_length from 100 to 200?",
    "c": null,
    "o": [
      "AlterField",
      "AddField",
      "RenameField",
      "RunSQL"
    ]
  },
  {
    "q": "What is the role of the Meta option db_table in a Django model?",
    "c": null,
    "o": [
      "Specifies the actual database table name to use for the model",
      "Creates a separate schema for the table",
      "Prevents migrations from altering the table",
      "Defines database engine configuration"
    ]
  },
  {
    "q": "What does this query do?",
    "c": "from django.db.models import F\n\nEmployee.objects.filter(salary__lt=F('bonus'))",
    "o": [
      "Fetches employees whose salary is less than their bonus",
      "Fetches employees with salary equal to bonus",
      "Raises an error because salary and bonus cannot be compared",
      "Always returns an empty queryset"
    ]
  },
  {
    "q": "What is a key difference between ArrayField and JSONField in Django with PostgreSQL?",
    "c": null,
    "o": [
      "ArrayField stores homogeneous lists of a single field type, while JSONField stores arbitrary JSON structures",
      "ArrayField can only be used with ForeignKeys, JSONField cannot",
      "JSONField enforces strict schema validation, ArrayField does not",
      "ArrayField is supported on all databases, JSONField only on PostgreSQL"
    ]
  },
  {
    "q": "What does the following query return?",
    "c": "Article.objects.filter(metadata__has_key='published')",
    "o": [
      "Articles whose JSONField metadata contains the key 'published'",
      "Articles where metadata['published'] is True",
      "Articles where metadata contains the value 'published'",
      "Raises FieldError because __has_key is invalid"
    ]
  },
  {
    "q": "Which Meta option enforces a specific database schema for a model?",
    "c": null,
    "o": [
      "db_tablespace",
      "db_table",
      "managed",
      "index_together"
    ]
  },
  {
    "q": "What does this migration operation do?",
    "c": "migrations.SeparateDatabaseAndState(\n    database_operations=[migrations.RunSQL(\"ALTER TABLE example DISABLE TRIGGER ALL;\")],\n    state_operations=[migrations.AlterModelTable('example', 'example_backup')]\n)",
    "o": [
      "Applies different operations to the database schema and Django’s model state",
      "Splits a table into two tables automatically",
      "Creates a backup before altering models",
      "Only applies state changes, never SQL"
    ]
  },
  {
    "q": "What happens if you use Meta option indexes with a fields argument?",
    "c": null,
    "o": [
      "Django creates a database index on the specified fields",
      "Django enforces uniqueness on those fields",
      "The fields are removed from migrations",
      "Only the Django admin uses the index"
    ]
  },
  {
    "q": "What does the following do?",
    "c": "from django.contrib.postgres.fields import ArrayField\n\nclass Survey(models.Model):\n    answers = ArrayField(models.IntegerField())",
    "o": [
      "Stores a list of integers in a single PostgreSQL column",
      "Creates a separate table for answers",
      "Forces answers to always contain exactly 2 integers",
      "Stores answers as a JSON object"
    ]
  },
  {
    "q": "In migrations, what does the RunSQL.noop attribute represent?",
    "c": null,
    "o": [
      "A placeholder operation that performs no database changes",
      "A rollback operation that drops a table",
      "An alias for SeparateDatabaseAndState",
      "A required dependency check before SQL runs"
    ]
  },
  {
    "q": "What will the following query return?",
    "c": "Book.objects.exclude(Q(title__icontains='django') & Q(title__icontains='python'))",
    "o": [
      "Books whose titles do not contain both 'django' and 'python' together",
      "Books whose titles do not contain either 'django' or 'python'",
      "Books containing 'django' but not 'python'",
      "All books, since exclude ignores complex conditions"
    ]
  },
  {
    "q": "Which of the following is TRUE about HStoreField?",
    "c": null,
    "o": [
      "It stores key-value pairs of strings in PostgreSQL",
      "It enforces strict JSON schema validation",
      "It stores unbounded binary data",
      "It is supported across all Django database backends"
    ]
  },
  {
    "q": "What does this migration snippet accomplish?",
    "c": "migrations.AlterUniqueTogether(\n    name='order',\n    unique_together={('customer', 'reference_code')},\n)",
    "o": [
      "Prevents duplicate (customer, reference_code) pairs in the Order model",
      "Enforces uniqueness only on reference_code",
      "Adds an index without enforcing uniqueness",
      "Applies uniqueness only at the ORM level, not database"
    ]
  },
  {
    "q": "What does the Meta option select_on_save = True enforce in a Django model?",
    "c": null,
    "o": [
      "Django performs a SELECT after every UPDATE to refresh the model instance with database values",
      "Django skips updating nullable fields",
      "The save() method only performs an INSERT, never an UPDATE",
      "The model cannot be saved if related objects are unsaved"
    ]
  },
  {
    "q": "What does this signal handler achieve?",
    "c": "from django.db.models.signals import pre_delete\n\n@receiver(pre_delete, sender=Order)\ndef archive_order(sender, instance, **kwargs):\n    ArchivedOrder.objects.create(data=str(instance))",
    "o": [
      "Creates an ArchivedOrder before deleting an Order",
      "Deletes an ArchivedOrder linked to the Order",
      "Cancels the deletion if the archive exists",
      "Raises an IntegrityError if no ArchivedOrder model exists"
    ]
  },
  {
    "q": "What happens if you define Meta option get_latest_by = 'created_at'?",
    "c": null,
    "o": [
      "Enables Model.latest() and Model.earliest() to use created_at field for ordering",
      "Forces all queries to order by created_at descending",
      "Automatically applies unique=True to created_at",
      "Creates an index on created_at by default"
    ]
  },
  {
    "q": "What does the following query return?",
    "c": "Employee.objects.aggregate(total=Sum(F('salary') + F('bonus')))",
    "o": [
      "The total sum of salary plus bonus across all employees",
      "A queryset of employees annotated with total",
      "Only employees where salary > bonus",
      "Raises TypeError because F objects cannot be added"
    ]
  },
  {
    "q": "What does migrations.RunPython.noop represent?",
    "c": null,
    "o": [
      "A no-operation placeholder for RunPython migrations",
      "Drops all Python-based migrations",
      "Forces state-only migration without SQL",
      "Skips all unapplied migrations"
    ]
  },
  {
    "q": "What does this constraint achieve?",
    "c": "models.UniqueConstraint(\n    fields=['user'],\n    condition=Q(is_active=True),\n    name='unique_active_user'\n)",
    "o": [
      "Ensures each user can only appear once where is_active=True",
      "Forces all users to have is_active=True",
      "Applies uniqueness across all fields of the model",
      "Raises error if a user is inactive"
    ]
  },
  {
    "q": "Which of the following is TRUE about OneToOneField?",
    "c": null,
    "o": [
      "It is essentially a ForeignKey with unique=True",
      "It creates a many-to-many relationship under the hood",
      "It cannot use on_delete argument",
      "It requires a through table like ManyToManyField"
    ]
  },
  {
    "q": "What does the Meta option required_db_features do?",
    "c": null,
    "o": [
      "Specifies database features that must be available for the model’s tests to run",
      "Forces a migration dependency on a specific database backend",
      "Defines required constraints on fields",
      "Automatically enables schema-level checks in PostgreSQL"
    ]
  },
  {
    "q": "What happens if you alter a model’s primary key field type in Django migrations?",
    "c": null,
    "o": [
      "Django generates an AlterField operation, which may involve creating a new column and copying data depending on backend",
      "It silently ignores the change since primary keys cannot be altered",
      "It forces a database reset automatically",
      "It only updates the ORM without touching the database schema"
    ]
  },
  {
    "q": "What does the following code do?",
    "c": "Book.objects.filter(Q(price__gte=50) | Q(discount__isnull=False))",
    "o": [
      "Fetches books priced at least 50 OR having a non-null discount",
      "Fetches books priced at least 50 AND having a discount",
      "Excludes all books with null discounts",
      "Always raises ValueError for combining Q objects"
    ]
  },
  {
    "q": "What is the primary purpose of an abstract base class in Django?",
    "c": null,
    "o": [
      "To define common fields and methods for child models without creating a database table",
      "To create a shared table for all child models",
      "To enforce uniqueness constraints across models",
      "To automatically generate proxy models"
    ]
  },
  {
    "q": "What does this proxy model do?",
    "c": "class EmployeeProxy(Employee):\n    class Meta:\n        proxy = True\n\n    def is_manager(self):\n        return self.role == 'Manager'",
    "o": [
      "Allows custom Python behavior on Employee instances without creating a new table",
      "Creates a separate database table for EmployeeProxy",
      "Enforces that only managers exist in the database",
      "Overrides the original Employee table schema"
    ]
  },
  {
    "q": "What is the effect of multi-table inheritance in Django?",
    "c": null,
    "o": [
      "Each child model gets its own table linked via an implicit OneToOneField to the parent",
      "All fields of parent and child are stored in a single table",
      "Child tables automatically inherit unique constraints from all siblings",
      "Child models cannot override parent methods"
    ]
  },
  {
    "q": "Which of the following is TRUE about polymorphic models in Django (using django-polymorphic)?",
    "c": null,
    "o": [
      "Querying the base class returns instances of the actual child class",
      "It merges all child tables into a single table automatically",
      "It does not support foreign key relationships",
      "It is fully supported natively without third-party packages"
    ]
  },
  {
    "q": "What does this query return?",
    "c": "Parent.objects.select_subclasses()",
    "o": [
      "Returns Parent instances as their actual child subclass types",
      "Only fetches Parent objects, ignoring child models",
      "Raises an error because select_subclasses is invalid in Django core",
      "Fetches all related objects for Parent via select_related"
    ]
  },
  {
    "q": "What happens if a child class in multi-table inheritance defines a field with the same name as a parent field?",
    "c": null,
    "o": [
      "It overrides the field at the ORM level, but parent column still exists in its table",
      "The database raises an error due to duplicate column",
      "The parent field is automatically dropped",
      "The child field is ignored"
    ]
  },
  {
    "q": "Why might you use an abstract base class with multiple child models?",
    "c": null,
    "o": [
      "To enforce consistent fields and behavior across several models without extra database tables",
      "To allow multiple models to share a single database table",
      "To automatically generate related ForeignKeys between children",
      "To prevent migrations from being created"
    ]
  },
  {
    "q": "Which statement is TRUE about proxy models and managers?",
    "c": null,
    "o": [
      "Proxy models can define custom managers without affecting the original model",
      "Proxy models cannot define managers",
      "Defining a manager on a proxy model changes the table schema",
      "Managers are ignored when querying proxy models"
    ]
  },
  {
    "q": "What is the main difference between abstract base classes and multi-table inheritance?",
    "c": null,
    "o": [
      "Abstract base classes don’t create a table, multi-table inheritance does",
      "Multi-table inheritance does not allow overriding methods",
      "Abstract base classes always enforce unique constraints",
      "Multi-table inheritance merges all fields into one table"
    ]
  },
  {
    "q": "What is a limitation of using proxy models in Django?",
    "c": null,
    "o": [
      "They cannot add new fields to the database table",
      "They cannot override methods of the original model",
      "They create extra database tables automatically",
      "They cannot be used with foreign keys"
    ]
  },
  {
    "q": "What will happen when you call save() on a model instance with a pre_save signal modifying a field?",
    "c": "from django.db.models.signals import pre_save\n\n@receiver(pre_save, sender=Order)\ndef update_total(sender, instance, **kwargs):\n    instance.total = instance.quantity * instance.price",
    "o": [
      "The pre_save signal updates the field before it is saved to the database",
      "The save() method ignores changes made by pre_save signals",
      "It raises a RuntimeError due to circular updates",
      "The database will always keep the original value"
    ]
  },
  {
    "q": "What does the following JSONField query accomplish?",
    "c": "Product.objects.filter(attributes__contains={'color': 'red'})",
    "o": [
      "Filters products whose JSONField 'attributes' contains the key-value pair color='red'",
      "Filters products with key 'color' regardless of value",
      "Filters products with any color value",
      "Raises FieldError because __contains is invalid"
    ]
  },
  {
    "q": "Which of the following statements is TRUE about ArrayField in Django with PostgreSQL?",
    "c": null,
    "o": [
      "You can perform lookups like contains, overlap, and exact on ArrayFields",
      "ArrayField automatically enforces uniqueness of elements",
      "ArrayField can only store integers",
      "ArrayField is supported on all database backends"
    ]
  },
  {
    "q": "What happens if a model with managed=False is included in migrations?",
    "c": null,
    "o": [
      "Django does not create or alter its table, but the model can still be used in ORM queries",
      "The table is created but not managed after creation",
      "It raises an error during migration",
      "Django creates a separate migration file automatically"
    ]
  },
  {
    "q": "What will the following HStoreField query do?",
    "c": "Book.objects.filter(details__has_key='isbn')",
    "o": [
      "Fetch books whose HStoreField 'details' contains the key 'isbn'",
      "Fetch books whose HStoreField 'details' contains the value 'isbn'",
      "Raises TypeError because has_key is invalid",
      "Updates all books to include the key 'isbn'"
    ]
  },
  {
    "q": "What does conditional uniqueness with UniqueConstraint and Q do?",
    "c": "models.UniqueConstraint(fields=['user'], condition=Q(is_active=True), name='unique_active_user')",
    "o": [
      "Ensures uniqueness only for records satisfying the condition is_active=True",
      "Forces all users to be active",
      "Applies uniqueness globally on the user field",
      "Ignores the condition and enforces uniqueness on all records"
    ]
  },
  {
    "q": "What happens if you annotate a child model in multi-table inheritance with F('parent_field')?",
    "c": null,
    "o": [
      "Django correctly resolves the parent field and allows annotation across tables",
      "It raises a FieldError because parent fields are inaccessible",
      "It ignores the annotation and returns child fields only",
      "It merges child and parent fields into a single column automatically"
    ]
  },
  {
    "q": "Which of the following is TRUE about running raw SQL in migrations using RunSQL?",
    "c": null,
    "o": [
      "You can provide separate SQL for forwards and backwards operations",
      "RunSQL always auto-generates reverse SQL",
      "It is not allowed to modify schema with RunSQL",
      "It cannot reference model tables"
    ]
  },
  {
    "q": "What will the following query return?",
    "c": "Survey.objects.filter(answers__overlap=[1,2,3])",
    "o": [
      "Surveys whose ArrayField 'answers' contains at least one of 1, 2, or 3",
      "Surveys whose answers contain exactly [1,2,3]",
      "Surveys whose answers do not contain 1, 2, or 3",
      "Raises FieldError because overlap is invalid"
    ]
  },
  {
    "q": "What is a potential issue when using signals inside migrations?",
    "c": null,
    "o": [
      "Signals can run before the model’s table exists or schema has changed, causing errors",
      "Signals always execute safely inside migrations",
      "Signals automatically get disabled during migration",
      "Using signals in migrations merges tables automatically"
    ]
  },
  {
    "q": "What happens if you try to use F() expressions inside a filter combined with an annotated field?",
    "c": "from django.db.models import F, Count\n\nBook.objects.annotate(review_count=Count('reviews')).filter(review_count__gt=F('pages'))",
    "o": [
      "Django allows comparing the annotated field with another field using F()",
      "It raises a TypeError because annotated fields cannot be used with F()",
      "The filter is ignored",
      "It always returns zero results"
    ]
  },
  {
    "q": "What does this migration operation achieve?",
    "c": "migrations.AlterUniqueTogether(\n    name='subscription',\n    unique_together={('user', 'plan')}\n)",
    "o": [
      "Ensures that a user cannot subscribe to the same plan more than once",
      "Automatically deletes duplicate subscriptions",
      "Forces all users to subscribe to all plans",
      "Applies uniqueness only at the ORM level"
    ]
  },
  {
    "q": "Which of the following is TRUE about deferred fields in Django models?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, saving memory and query time",
      "Deferred fields are ignored in all queries permanently",
      "Deferred fields cannot be updated once saved",
      "Deferred fields automatically trigger a full reload of the instance"
    ]
  },
  {
    "q": "What will the following query return?",
    "c": "Employee.objects.filter(manager__isnull=True)",
    "o": [
      "Employees who do not have a manager (top-level managers)",
      "Employees who are themselves managers",
      "All employees, ignoring the manager field",
      "Raises an error if manager is a OneToOneField"
    ]
  },
  {
    "q": "What does the following HStoreField query accomplish?",
    "c": "Book.objects.filter(details__contains={'language': 'EN'})",
    "o": [
      "Fetches books whose HStoreField 'details' contains the key-value pair language='EN'",
      "Fetches books where 'language' exists, ignoring the value",
      "Updates the HStoreField to include language='EN'",
      "Raises FieldError because contains is invalid for HStoreField"
    ]
  },
  {
    "q": "What happens when you add an index via Meta.indexes on a JSONField using KeyTextTransform?",
    "c": null,
    "o": [
      "Django creates a functional index on the JSON key for faster queries",
      "Django ignores the index because JSONField cannot be indexed",
      "The database stores a copy of the JSONField in a separate table",
      "It enforces uniqueness on the key"
    ]
  },
  {
    "q": "Which of the following is TRUE about multi-table inheritance and deletions?",
    "c": null,
    "o": [
      "Deleting a parent instance also deletes the linked child instances via cascading",
      "Child instances are unaffected by parent deletions",
      "Parent instances cannot be deleted",
      "Django automatically merges child data into the parent table before deletion"
    ]
  },
  {
    "q": "What will the following query return?",
    "c": "Survey.objects.filter(answers__len__gt=3)",
    "o": [
      "Surveys whose ArrayField 'answers' contains more than 3 elements",
      "Surveys whose answers include the number 3",
      "Raises FieldError because __len__ is not valid",
      "All surveys, ignoring the answers field"
    ]
  },
  {
    "q": "Which statement is TRUE about conditional constraints in Django?",
    "c": null,
    "o": [
      "You can enforce uniqueness or check constraints only when a Q condition is met",
      "They are enforced in Python but not at the database level",
      "Conditional constraints cannot use Q objects",
      "They automatically disable all other constraints"
    ]
  },
  {
    "q": "What is a potential pitfall of using signals to automatically create related models?",
    "c": null,
    "o": [
      "Signals may run during migrations when tables don’t exist, causing errors",
      "Signals always run in a separate database connection",
      "Signals bypass ORM validation automatically",
      "Signals cannot create multiple related models"
    ]
  },
  {
    "q": "What happens if you use a ForeignKey with on_delete=models.SET_DEFAULT but no default is set?",
    "c": null,
    "o": [
      "Django raises an IntegrityError when the referenced object is deleted",
      "The field is automatically set to NULL",
      "The object is deleted along with the referenced object",
      "Django uses the last object as default"
    ]
  },
  {
    "q": "What does this query return?",
    "c": "Book.objects.filter(metadata__jsonb_contains={'genre': 'fiction'})",
    "o": [
      "Books whose JSONField 'metadata' contains genre='fiction'",
      "Books with any genre",
      "Raises FieldError because __jsonb_contains is invalid",
      "Books with genre not equal to fiction"
    ]
  },
  {
    "q": "Which of the following is TRUE about deferred fields and select_related?",
    "c": null,
    "o": [
      "Deferred fields are not loaded until accessed, but select_related still fetches related objects in the same query",
      "Deferred fields are ignored by select_related",
      "Deferred fields automatically trigger prefetch_related",
      "select_related disables deferred loading for all fields"
    ]
  },
  {
    "q": "What is a potential issue when using post_save signals to create related objects?",
    "c": null,
    "o": [
      "Signals can cause recursive save calls if not handled carefully",
      "Signals always execute asynchronously",
      "Signals cannot access the saved instance",
      "Signals automatically bypass constraints"
    ]
  },
  {
    "q": "What does this ArrayField query accomplish?",
    "c": "Survey.objects.filter(answers__contains=[1,2])",
    "o": [
      "Fetches surveys whose ArrayField 'answers' contains both 1 and 2",
      "Fetches surveys containing either 1 or 2",
      "Fetches surveys with exactly [1,2]",
      "Raises FieldError because __contains is invalid"
    ]
  },
  {
    "q": "Which of the following statements is TRUE about proxy models?",
    "c": null,
    "o": [
      "Proxy models can override methods and managers without creating a new table",
      "Proxy models create a separate table in the database",
      "Proxy models cannot have custom managers",
      "Proxy models automatically inherit constraints from other proxy models"
    ]
  },
  {
    "q": "What does this migration operation do?",
    "c": "migrations.AlterModelOptions(\n    name='product',\n    options={'ordering': ['-created_at']}\n)",
    "o": [
      "Sets default ordering for Product queries by created_at descending",
      "Renames the Product table in the database",
      "Adds a unique constraint on created_at",
      "Deletes old records not matching the ordering"
    ]
  },
  {
    "q": "What is a limitation of multi-table inheritance when querying child models?",
    "c": null,
    "o": [
      "Queries may require joins between parent and child tables, potentially impacting performance",
      "Child models cannot override parent methods",
      "Parent fields are inaccessible from child instances",
      "Child models cannot have additional fields"
    ]
  },
  {
    "q": "What happens when using conditional UniqueConstraint with a Q object?",
    "c": null,
    "o": [
      "Uniqueness is enforced only for rows that satisfy the condition",
      "Uniqueness is ignored for all rows",
      "It raises an error if Q is used",
      "All rows are forced to satisfy the condition"
    ]
  },
  {
    "q": "What is the effect of select_for_update() in a query?",
    "c": null,
    "o": [
      "Locks selected rows until the end of the transaction, preventing concurrent updates",
      "Immediately commits all changes to the database",
      "Creates a copy of selected rows",
      "Prefetches related objects without locking"
    ]
  },
  {
    "q": "What happens if you define a ForeignKey with on_delete=models.SET_NULL but the field has null=False?",
    "c": null,
    "o": [
      "Django raises an IntegrityError when the referenced object is deleted",
      "The field is automatically set to a default value",
      "The related object is deleted instead",
      "Django ignores the on_delete option"
    ]
  },
  {
    "q": "What does this JSONField query do?",
    "c": "Product.objects.filter(details__has_key='weight')",
    "o": [
      "Fetches products whose JSONField 'details' contains the key 'weight'",
      "Fetches products whose 'weight' is greater than zero",
      "Raises FieldError because __has_key is invalid",
      "Updates all JSONFields to include 'weight'"
    ]
  },
  {
    "q": "Which of the following is TRUE about ArrayField lookups?",
    "c": null,
    "o": [
      "You can use contains, contained_by, overlap, and exact lookups",
      "ArrayField elements cannot be queried at all",
      "Only exact match is supported",
      "ArrayField automatically flattens arrays for queries"
    ]
  },
  {
    "q": "What is a potential pitfall of using pre_save signals to modify fields?",
    "c": null,
    "o": [
      "Recursive saves or unexpected updates if save() is called inside the signal",
      "Signals are ignored by the ORM",
      "Signals always run after the database commit",
      "Pre_save signals cannot access the instance"
    ]
  },
  {
    "q": "What does this query return?",
    "c": "Survey.objects.filter(answers__overlap=[4,5,6])",
    "o": [
      "Surveys whose ArrayField 'answers' contains at least one element from [4,5,6]",
      "Surveys whose answers contain exactly [4,5,6]",
      "Surveys whose answers do not contain 4,5,6",
      "Raises an error because overlap cannot be used"
    ]
  },
  {
    "q": "What happens if a child model in multi-table inheritance has a field with the same name as a parent field?",
    "c": null,
    "o": [
      "The child field overrides the parent field at the ORM level, but the parent column still exists in the database",
      "Django merges the columns automatically",
      "It raises a FieldError",
      "The parent field is deleted automatically"
    ]
  },
  {
    "q": "What does the following migration do?",
    "c": "migrations.AlterIndexTogether(\n    name='order',\n    index_together={('customer', 'created_at')}\n)",
    "o": [
      "Creates a composite index on the customer and created_at fields for faster lookups",
      "Enforces uniqueness on customer and created_at",
      "Drops existing indexes automatically",
      "Indexes only the first field"
    ]
  },
  {
    "q": "What does select_for_update(nowait=True) do?",
    "c": null,
    "o": [
      "Locks the selected rows and raises an error if another transaction holds a lock",
      "Prefetches related objects without locking",
      "Unlocks the rows immediately",
      "Performs an asynchronous query"
    ]
  },
  {
    "q": "Which of the following is TRUE about proxy models?",
    "c": null,
    "o": [
      "They allow custom behavior without creating a new table",
      "They automatically create a new database table",
      "They cannot override managers",
      "They require an abstract base class"
    ]
  },
  {
    "q": "What is the effect of using managed=False in a model’s Meta class?",
    "c": null,
    "o": [
      "Django does not create, modify, or delete the table in migrations",
      "The model cannot be queried",
      "It creates a table but does not manage indexes",
      "It only affects the admin interface"
    ]
  },
  {
    "q": "What will happen if you define a ForeignKey with on_delete=models.CASCADE in a model with existing related objects?",
    "c": null,
    "o": [
      "Deleting the referenced object also deletes all related objects automatically",
      "The deletion is blocked if related objects exist",
      "Django sets the foreign key to NULL",
      "Django raises a ValidationError when saving related objects"
    ]
  },
  {
    "q": "What does this query return?",
    "c": "Product.objects.filter(specs__contains={'color': 'blue'})",
    "o": [
      "Products whose JSONField 'specs' contains the key-value pair color='blue'",
      "Products where 'color' exists but with any value",
      "Products where specs contain the string 'blue' anywhere",
      "Raises FieldError because __contains is invalid"
    ]
  },
  {
    "q": "Which of the following is TRUE about HStoreField?",
    "c": null,
    "o": [
      "It stores key-value pairs of strings in PostgreSQL and supports lookups like contains and has_key",
      "It can store arbitrary Python objects",
      "It is supported in SQLite and MySQL",
      "It automatically enforces uniqueness on keys"
    ]
  },
  {
    "q": "What is a potential risk when using post_save signals to automatically create related models?",
    "c": null,
    "o": [
      "Recursive saves or infinite loops if the related model also triggers post_save",
      "Signals cannot access the saved instance",
      "The signal bypasses database constraints",
      "Signals are ignored if using bulk_create"
    ]
  },
  {
    "q": "What does this ArrayField query do?",
    "c": "Survey.objects.filter(answers__contained_by=[1,2,3,4])",
    "o": [
      "Fetches surveys whose ArrayField 'answers' is entirely contained within [1,2,3,4]",
      "Fetches surveys containing at least one element from [1,2,3,4]",
      "Fetches surveys whose answers exactly match [1,2,3,4]",
      "Raises FieldError because __contained_by is invalid"
    ]
  },
  {
    "q": "What happens when a child model in multi-table inheritance defines a new field with the same name as a parent field?",
    "c": null,
    "o": [
      "The child field overrides the parent field at the ORM level, but the parent column remains in the database",
      "The parent column is automatically dropped",
      "It raises a FieldError immediately",
      "Django merges both fields into a single column"
    ]
  },
  {
    "q": "What does migrations.AlterUniqueTogether accomplish?",
    "c": "migrations.AlterUniqueTogether(\n    name='subscription',\n    unique_together={('user', 'plan')}\n)",
    "o": [
      "Enforces uniqueness for the combination of user and plan in the Subscription model",
      "Creates separate unique constraints on user and plan individually",
      "Deletes duplicate rows automatically",
      "Applies uniqueness only in the ORM, not in the database"
    ]
  },
  {
    "q": "What is the effect of select_for_update(skip_locked=True)?",
    "c": null,
    "o": [
      "Locks selected rows but skips any rows that are already locked by another transaction",
      "Raises an error if a row is already locked",
      "Does not lock any rows",
      "Automatically retries locked rows until available"
    ]
  },
  {
    "q": "Which statement about proxy models is TRUE?",
    "c": null,
    "o": [
      "Proxy models allow you to change Python behavior, including custom methods and managers, without creating a new database table",
      "Proxy models create new tables by default",
      "Proxy models cannot override managers",
      "Proxy models require multi-table inheritance"
    ]
  },
  {
    "q": "What does managed=False in a model Meta class do?",
    "c": null,
    "o": [
      "Django does not create, alter, or delete the table in migrations, but the model can still be used in queries",
      "The model cannot be queried",
      "Django creates the table but does not manage indexes",
      "It only affects the admin interface"
    ]
  },
  {
    "q": "What happens if you use a OneToOneField with on_delete=models.SET_NULL but null=False?",
    "c": null,
    "o": [
      "Django raises an IntegrityError when the referenced object is deleted",
      "The field is automatically set to a default object",
      "The referenced object is deleted automatically",
      "Django ignores the on_delete option"
    ]
  },
  {
    "q": "What does this JSONField query do?",
    "c": "Product.objects.filter(specs__has_key='dimensions')",
    "o": [
      "Fetches products whose JSONField 'specs' contains the key 'dimensions'",
      "Fetches products where 'dimensions' has any value",
      "Fetches products whose specs contain the string 'dimensions' anywhere",
      "Raises FieldError because __has_key is invalid"
    ]
  },
  {
    "q": "Which of the following is TRUE about CheckConstraint with a Q condition?",
    "c": null,
    "o": [
      "The constraint only applies to rows that satisfy the Q condition",
      "The constraint applies globally to all rows regardless of the condition",
      "CheckConstraints cannot use Q objects",
      "The database ignores the constraint if any row violates it"
    ]
  },
  {
    "q": "What is a potential problem when using pre_save signals to modify related objects?",
    "c": null,
    "o": [
      "It may cause recursive saves or unintended updates if not carefully handled",
      "Pre_save signals run asynchronously and may be ignored",
      "The signal bypasses database constraints automatically",
      "The related objects are never saved"
    ]
  },
  {
    "q": "What does this ArrayField query return?",
    "c": "Survey.objects.filter(answers__contained_by=[1,2,3,4])",
    "o": [
      "Surveys whose ArrayField 'answers' is entirely contained within [1,2,3,4]",
      "Surveys containing at least one element from [1,2,3,4]",
      "Surveys whose answers exactly match [1,2,3,4]",
      "Raises FieldError because __contained_by is invalid"
    ]
  },
  {
    "q": "What happens when a child model in multi-table inheritance defines a new field with the same name as a parent field?",
    "c": null,
    "o": [
      "The child field overrides the parent field at the ORM level, but the parent column remains in the database",
      "The parent column is automatically dropped",
      "It raises a FieldError immediately",
      "Django merges both fields into a single column"
    ]
  },
  {
    "q": "What does migrations.AddIndex accomplish?",
    "c": "migrations.AddIndex(\n    model_name='order',\n    index=models.Index(fields=['customer', 'created_at'], name='order_customer_created_idx')\n)",
    "o": [
      "Creates a database index on the specified fields to speed up queries",
      "Enforces uniqueness across the fields",
      "Deletes duplicate rows automatically",
      "Indexes only the first field"
    ]
  },
  {
    "q": "What does select_for_update(nowait=True) do?",
    "c": null,
    "o": [
      "Locks selected rows and raises an error immediately if another transaction holds a lock",
      "Prefetches related objects without locking",
      "Unlocks the rows immediately",
      "Performs an asynchronous query"
    ]
  },
  {
    "q": "Which statement about proxy models is TRUE?",
    "c": null,
    "o": [
      "Proxy models allow you to customize Python behavior and managers without creating a new table",
      "Proxy models automatically create a new database table",
      "Proxy models cannot override managers",
      "Proxy models require multi-table inheritance"
    ]
  },
  {
    "q": "What is the effect of using managed=False in a model’s Meta class?",
    "c": null,
    "o": [
      "Django does not create, alter, or delete the table in migrations, but the model can still be queried",
      "The model cannot be queried",
      "Django creates the table but does not manage indexes",
      "It only affects the admin interface"
    ]
  },
  {
    "q": "What will happen if you define a ForeignKey with on_delete=models.PROTECT?",
    "c": null,
    "o": [
      "Django prevents deletion of the referenced object if related objects exist, raising a ProtectedError",
      "Django deletes the referenced object and all related objects automatically",
      "The foreign key is set to NULL automatically",
      "Django ignores the on_delete option"
    ]
  },
  {
    "q": "What does this JSONField query do?",
    "c": "Product.objects.filter(attributes__has_keys=['size','color'])",
    "o": [
      "Fetches products whose JSONField 'attributes' contains both keys 'size' and 'color'",
      "Fetches products containing either 'size' or 'color'",
      "Fetches products where attributes contain the string 'size' or 'color'",
      "Raises FieldError because __has_keys is invalid"
    ]
  },
  {
    "q": "Which of the following statements is TRUE about HStoreField?",
    "c": null,
    "o": [
      "HStoreField stores key-value pairs of strings in PostgreSQL and supports lookups like contains, has_key, and has_keys",
      "HStoreField can store arbitrary Python objects",
      "It is supported on all database backends",
      "It automatically enforces uniqueness on keys"
    ]
  },
  {
    "q": "What is a potential problem when using post_save signals to create related objects?",
    "c": null,
    "o": [
      "Signals may cause recursive saves or infinite loops if the related model also triggers post_save",
      "Signals cannot access the saved instance",
      "Signals bypass database constraints automatically",
      "Signals do not run when bulk_create is used"
    ]
  },
  {
    "q": "What does this ArrayField query do?",
    "c": "Survey.objects.filter(answers__overlap=[5,6,7])",
    "o": [
      "Fetches surveys whose ArrayField 'answers' contains at least one element from [5,6,7]",
      "Fetches surveys whose answers exactly match [5,6,7]",
      "Fetches surveys whose answers do not contain any of [5,6,7]",
      "Raises FieldError because __overlap is invalid"
    ]
  },
  {
    "q": "What happens when a child model in multi-table inheritance defines a field with the same name as a parent field?",
    "c": null,
    "o": [
      "The child field overrides the parent field at the ORM level, but the parent column still exists in the database",
      "The parent column is automatically dropped",
      "It raises a FieldError",
      "Django merges both fields into a single column"
    ]
  },
  {
    "q": "What does migrations.RemoveIndex accomplish?",
    "c": "migrations.RemoveIndex(\n    model_name='order',\n    name='order_customer_created_idx'\n)",
    "o": [
      "Removes the specified index from the database table",
      "Deletes all rows in the table",
      "Drops all indexes from the table",
      "Renames the index"
    ]
  },
  {
    "q": "What is the effect of select_for_update(nowait=True) in a query?",
    "c": null,
    "o": [
      "Locks the selected rows and raises an error immediately if another transaction holds a lock",
      "Prefetches related objects without locking",
      "Unlocks the rows immediately",
      "Performs an asynchronous query"
    ]
  },
  {
    "q": "Which statement about proxy models is TRUE?",
    "c": null,
    "o": [
      "Proxy models allow you to define custom Python behavior and managers without creating a new table",
      "Proxy models automatically create a new table in the database",
      "Proxy models cannot override managers",
      "Proxy models require multi-table inheritance"
    ]
  },
  {
    "q": "What is the effect of managed=False in a model Meta class?",
    "c": null,
    "o": [
      "Django does not create, alter, or delete the table in migrations, but the model can still be queried",
      "The model cannot be queried",
      "Django creates the table but does not manage indexes",
      "It only affects the admin interface"
    ]
  },
  {
    "q": "What happens if you use a ForeignKey with on_delete=models.SET_DEFAULT but no default value is defined?",
    "c": null,
    "o": [
      "Django raises an IntegrityError when the referenced object is deleted",
      "The field is automatically set to NULL",
      "The related object is deleted automatically",
      "Django ignores the on_delete option"
    ]
  },
  {
    "q": "What does this JSONField query return?",
    "c": "Product.objects.filter(config__contains={'enabled': true})",
    "o": [
      "Products whose JSONField 'config' contains the key-value pair enabled=true",
      "Products with any key named 'enabled'",
      "Products where 'enabled' has any value",
      "Raises FieldError because __contains is invalid"
    ]
  },
  {
    "q": "Which statement about HStoreField is TRUE?",
    "c": null,
    "o": [
      "HStoreField stores string key-value pairs in PostgreSQL and supports lookups like contains, has_key, and has_keys",
      "HStoreField can store arbitrary Python objects",
      "HStoreField works on all database backends",
      "HStoreField automatically enforces uniqueness on keys"
    ]
  },
  {
    "q": "What is a potential pitfall of using pre_save signals to modify related models?",
    "c": null,
    "o": [
      "It may trigger recursive saves or unintended updates if not handled carefully",
      "Pre_save signals run asynchronously and may be ignored",
      "The signal bypasses database constraints automatically",
      "Related objects are never updated"
    ]
  },
  {
    "q": "What does this ArrayField query do?",
    "c": "Survey.objects.filter(answers__contains=[2,3])",
    "o": [
      "Fetches surveys whose ArrayField 'answers' contains all of the elements 2 and 3",
      "Fetches surveys whose answers contain at least one element 2 or 3",
      "Fetches surveys whose answers exactly match [2,3]",
      "Raises FieldError because __contains is invalid"
    ]
  },
  {
    "q": "What happens if a child model in multi-table inheritance defines a field with the same name as a parent field?",
    "c": null,
    "o": [
      "The child field overrides the parent field at the ORM level, but the parent column remains in the database",
      "The parent column is automatically dropped",
      "It raises a FieldError immediately",
      "Django merges both fields into a single column"
    ]
  },
  {
    "q": "What does migrations.AddConstraint accomplish?",
    "c": "migrations.AddConstraint(\n    model_name='employee',\n    constraint=models.CheckConstraint(check=models.Q(salary__gte=0), name='salary_non_negative')\n)",
    "o": [
      "Adds a database-level constraint to enforce that salary is non-negative",
      "Automatically updates negative salaries to zero",
      "Drops existing constraints on the salary field",
      "Adds a Python-level validation but not a database constraint"
    ]
  },
  {
    "q": "What is the effect of select_for_update(skip_locked=True)?",
    "c": null,
    "o": [
      "Locks selected rows but skips rows already locked by other transactions",
      "Raises an error if a row is already locked",
      "Does not lock any rows",
      "Retries locked rows automatically until available"
    ]
  },
  {
    "q": "Which statement about proxy models is TRUE?",
    "c": null,
    "o": [
      "Proxy models allow you to change Python behavior and managers without creating a new database table",
      "Proxy models automatically create a new table",
      "Proxy models cannot override managers",
      "Proxy models require multi-table inheritance"
    ]
  },
  {
    "q": "What is the effect of managed=False in a model’s Meta class?",
    "c": null,
    "o": [
      "Django does not create, modify, or delete the table in migrations, but the model can still be queried",
      "The model cannot be queried",
      "Django creates the table but does not manage indexes",
      "It only affects the admin interface"
    ]
  },
  {
    "q": "What happens if you define a OneToOneField with on_delete=models.CASCADE?",
    "c": null,
    "o": [
      "Deleting the referenced object also deletes the related object automatically",
      "Django prevents deletion if the related object exists",
      "The foreign key is set to NULL automatically",
      "It raises a ValidationError when saving related objects"
    ]
  },
  {
    "q": "What does this JSONField query return?",
    "c": "Product.objects.filter(config__has_any_keys=['enabled','featured'])",
    "o": [
      "Fetches products whose JSONField 'config' contains at least one of the keys 'enabled' or 'featured'",
      "Fetches products containing all keys 'enabled' and 'featured'",
      "Fetches products where 'enabled' or 'featured' has any value",
      "Raises FieldError because __has_any_keys is invalid"
    ]
  },
  {
    "q": "Which of the following statements is TRUE about HStoreField lookups?",
    "c": null,
    "o": [
      "You can use contains, has_key, has_keys, and has_any_keys to query HStoreFields",
      "HStoreField lookups are only supported in SQLite",
      "HStoreField can store arbitrary Python objects",
      "It automatically enforces uniqueness on keys"
    ]
  },
  {
    "q": "What is a potential issue when using pre_save signals to modify related models?",
    "c": null,
    "o": [
      "It may trigger recursive saves or unintended updates if not handled carefully",
      "Pre_save signals run asynchronously and may be ignored",
      "The signal bypasses database constraints automatically",
      "Related objects are never saved"
    ]
  },
  {
    "q": "What does this ArrayField query accomplish?",
    "c": "Survey.objects.filter(answers__contained_by=[1,2,3,4])",
    "o": [
      "Fetches surveys whose ArrayField 'answers' is entirely contained within [1,2,3,4]",
      "Fetches surveys containing at least one element from [1,2,3,4]",
      "Fetches surveys whose answers exactly match [1,2,3,4]",
      "Raises FieldError because __contained_by is invalid"
    ]
  },
  {
    "q": "What happens if a child model in multi-table inheritance defines a field with the same name as a parent field?",
    "c": null,
    "o": [
      "The child field overrides the parent field at the ORM level, but the parent column remains in the database",
      "The parent column is automatically dropped",
      "It raises a FieldError immediately",
      "Django merges both fields into a single column"
    ]
  },
  {
    "q": "What does migrations.AddConstraint do?",
    "c": "migrations.AddConstraint(\n    model_name='employee',\n    constraint=models.CheckConstraint(check=models.Q(salary__gte=0), name='salary_non_negative')\n)",
    "o": [
      "Adds a database-level constraint to enforce that salary is non-negative",
      "Automatically updates negative salaries to zero",
      "Drops existing constraints on the salary field",
      "Adds a Python-level validation but not a database constraint"
    ]
  },
  {
    "q": "What is the effect of select_for_update(nowait=True) in a query?",
    "c": null,
    "o": [
      "Locks selected rows and raises an error immediately if another transaction holds a lock",
      "Prefetches related objects without locking",
      "Unlocks the rows immediately",
      "Performs an asynchronous query"
    ]
  },
  {
    "q": "Which statement about proxy models is TRUE?",
    "c": null,
    "o": [
      "Proxy models allow you to define custom Python behavior and managers without creating a new database table",
      "Proxy models automatically create a new table",
      "Proxy models cannot override managers",
      "Proxy models require multi-table inheritance"
    ]
  },
  {
    "q": "What is the effect of managed=False in a model’s Meta class?",
    "c": null,
    "o": [
      "Django does not create, modify, or delete the table in migrations, but the model can still be queried",
      "The model cannot be queried",
      "Django creates the table but does not manage indexes",
      "It only affects the admin interface"
    ]
  },
  {
    "q": "What happens if you define a ForeignKey with on_delete=models.SET_NULL but null=False?",
    "c": null,
    "o": [
      "Django raises an IntegrityError when the referenced object is deleted",
      "The field is automatically set to a default value",
      "The related object is deleted instead",
      "Django ignores the on_delete option"
    ]
  },
  {
    "q": "What does this JSONField query return?",
    "c": "Product.objects.filter(settings__has_key='active')",
    "o": [
      "Fetches products whose JSONField 'settings' contains the key 'active'",
      "Fetches products where 'active' exists with any value",
      "Fetches products containing the string 'active' anywhere",
      "Raises FieldError because __has_key is invalid"
    ]
  },
  {
    "q": "Which of the following statements is TRUE about HStoreField?",
    "c": null,
    "o": [
      "HStoreField stores string key-value pairs in PostgreSQL and supports lookups like contains, has_key, has_keys, and has_any_keys",
      "HStoreField can store arbitrary Python objects",
      "HStoreField works on all database backends",
      "HStoreField automatically enforces uniqueness on keys"
    ]
  },
  {
    "q": "What is a potential pitfall of using post_save signals to create related objects?",
    "c": null,
    "o": [
      "Signals may cause recursive saves or infinite loops if the related model also triggers post_save",
      "Signals cannot access the saved instance",
      "Signals bypass database constraints automatically",
      "Signals do not run when bulk_create is used"
    ]
  },
  {
    "q": "What does this ArrayField query accomplish?",
    "c": "Survey.objects.filter(answers__overlap=[3,4,5])",
    "o": [
      "Fetches surveys whose ArrayField 'answers' contains at least one element from [3,4,5]",
      "Fetches surveys whose answers exactly match [3,4,5]",
      "Fetches surveys whose answers do not contain any of [3,4,5]",
      "Raises FieldError because __overlap is invalid"
    ]
  },
  {
    "q": "What happens if a child model in multi-table inheritance defines a new field with the same name as a parent field?",
    "c": null,
    "o": [
      "The child field overrides the parent field at the ORM level, but the parent column remains in the database",
      "The parent column is automatically dropped",
      "It raises a FieldError immediately",
      "Django merges both fields into a single column"
    ]
  },
  {
    "q": "What does migrations.AlterField accomplish?",
    "c": "migrations.AlterField(\n    model_name='employee',\n    name='salary',\n    field=models.DecimalField(max_digits=10, decimal_places=2)\n)",
    "o": [
      "Changes the definition of the 'salary' field in the database and ORM",
      "Deletes the 'salary' field entirely",
      "Adds a new field named 'salary'",
      "Automatically populates all salaries with zeros"
    ]
  },
  {
    "q": "What is the effect of select_for_update(skip_locked=True)?",
    "c": null,
    "o": [
      "Locks selected rows but skips rows already locked by other transactions",
      "Raises an error if a row is already locked",
      "Does not lock any rows",
      "Retries locked rows automatically until available"
    ]
  },
  {
    "q": "Which statement about proxy models is TRUE?",
    "c": null,
    "o": [
      "Proxy models allow you to define custom Python behavior and managers without creating a new database table",
      "Proxy models automatically create a new table",
      "Proxy models cannot override managers",
      "Proxy models require multi-table inheritance"
    ]
  },
  {
    "q": "What is the effect of managed=False in a model’s Meta class?",
    "c": null,
    "o": [
      "Django does not create, modify, or delete the table in migrations, but the model can still be queried",
      "The model cannot be queried",
      "Django creates the table but does not manage indexes",
      "It only affects the admin interface"
    ]
  }
]