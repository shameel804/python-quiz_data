[
  {
    "q": "Which class in Django is commonly used to create a form?",
    "c": null,
    "o": [
      "forms.Form",
      "forms.Model",
      "forms.Input",
      "forms.TemplateForm"
    ]
  },
  {
    "q": "What does the following code represent in Django?",
    "c": "from django import forms\n\nclass ContactForm(forms.Form):\n    name = forms.CharField(max_length=100)\n    email = forms.EmailField()",
    "o": [
      "A simple form with name and email fields",
      "A model form that saves data directly to the database",
      "A form template for rendering in HTML only",
      "A serializer for API responses"
    ]
  },
  {
    "q": "Which method is used to check if a form’s data is valid?",
    "c": null,
    "o": [
      "is_valid()",
      "validate()",
      "check()",
      "is_clean()"
    ]
  },
  {
    "q": "What is the purpose of the 'cleaned_data' dictionary in Django forms?",
    "c": null,
    "o": [
      "It stores validated and converted form data",
      "It contains raw unprocessed data from the request",
      "It is used to store error messages for invalid fields",
      "It is used to render initial form field values"
    ]
  },
  {
    "q": "What does the following code define?",
    "c": "from django import forms\nfrom .models import Book\n\nclass BookForm(forms.ModelForm):\n    class Meta:\n        model = Book\n        fields = ['title', 'author']",
    "o": [
      "A ModelForm for the Book model",
      "A regular Django form unrelated to models",
      "A database migration form",
      "A form that validates only author field"
    ]
  },
  {
    "q": "In Django forms, which method is used to handle custom field-level validation?",
    "c": null,
    "o": [
      "clean_<fieldname>()",
      "validate_<fieldname>()",
      "check_<fieldname>()",
      "field_<fieldname>_validator()"
    ]
  },
  {
    "q": "What does the following code do in Django forms?",
    "c": "def clean_email(self):\n    email = self.cleaned_data.get('email')\n    if not email.endswith('.com'):\n        raise forms.ValidationError(\"Email must end with .com\")\n    return email",
    "o": [
      "Performs custom validation on the email field",
      "Automatically saves the email into the database",
      "Renders the email input field in a template",
      "Formats the email for display purposes"
    ]
  },
  {
    "q": "When using ModelForm, what does the 'fields' attribute inside Meta class specify?",
    "c": null,
    "o": [
      "The model fields to include in the form",
      "The default values of form fields",
      "The type of widgets used in the form",
      "The database table name to be used"
    ]
  },
  {
    "q": "Which argument is commonly used to change the HTML widget of a form field?",
    "c": null,
    "o": [
      "widget",
      "field_type",
      "renderer",
      "element"
    ]
  },
  {
    "q": "What does the following code snippet specify?",
    "c": "name = forms.CharField(widget=forms.Textarea)",
    "o": [
      "The 'name' field will be rendered as a textarea input",
      "The 'name' field will be validated as a textarea",
      "The 'name' field will accept only numeric input",
      "The 'name' field will be hidden from rendering"
    ]
  },
  {
    "q": "Which method of a Django form is typically used to save data when using a ModelForm?",
    "c": null,
    "o": [
      "save()",
      "commit()",
      "store()",
      "write()"
    ]
  },
  {
    "q": "What does the following form validation code do?",
    "c": "def clean(self):\n    cleaned_data = super().clean()\n    password = cleaned_data.get(\"password\")\n    confirm_password = cleaned_data.get(\"confirm_password\")\n\n    if password and confirm_password and password != confirm_password:\n        raise forms.ValidationError(\"Passwords do not match\")",
    "o": [
      "Validates that two password fields match",
      "Automatically hashes the password before saving",
      "Checks that the password is at least 8 characters long",
      "Ensures the password contains special characters"
    ]
  },
  {
    "q": "Which attribute of a Django form field allows you to display custom error messages?",
    "c": null,
    "o": [
      "error_messages",
      "help_text",
      "validators",
      "default"
    ]
  },
  {
    "q": "What does the following code represent in a form field?",
    "c": "email = forms.EmailField(error_messages={'invalid': 'Enter a valid email address'})",
    "o": [
      "A custom error message for invalid email input",
      "A default placeholder text for the email field",
      "A custom widget for rendering email input",
      "An instruction to save the email as lowercase"
    ]
  },
  {
    "q": "Which parameter inside the Meta class of ModelForm can be used to exclude certain fields?",
    "c": null,
    "o": [
      "exclude",
      "skip_fields",
      "omit",
      "ignore"
    ]
  },
  {
    "q": "What does the following ModelForm code do?",
    "c": "class ProfileForm(forms.ModelForm):\n    class Meta:\n        model = Profile\n        exclude = ['user']",
    "o": [
      "Creates a form for Profile model excluding the user field",
      "Creates a form with only the user field",
      "Validates only the user field from Profile",
      "Automatically assigns the user field from request"
    ]
  },
  {
    "q": "Which parameter can be used in a Django form field to display helper text below the input?",
    "c": null,
    "o": [
      "help_text",
      "description",
      "tooltip",
      "guide"
    ]
  },
  {
    "q": "What does the following code achieve in a form?",
    "c": "age = forms.IntegerField(min_value=18, max_value=60)",
    "o": [
      "Restricts the age input between 18 and 60",
      "Automatically saves the age in the database",
      "Renders a dropdown menu for age selection",
      "Marks the age field as optional"
    ]
  },
  {
    "q": "Which of the following is TRUE about Django's forms.Form and forms.ModelForm?",
    "c": null,
    "o": [
      "forms.ModelForm is tied to a database model, while forms.Form is not",
      "Both are tied to database models",
      "forms.Form automatically saves data to the database",
      "forms.ModelForm cannot include custom fields"
    ]
  },
  {
    "q": "What does the following snippet specify inside a ModelForm?",
    "c": "class Meta:\n    model = Article\n    fields = '__all__'",
    "o": [
      "All fields of the Article model are included in the form",
      "No fields will be included in the form",
      "Only the primary key field is included",
      "Only the fields defined manually in the form class are included"
    ]
  },
  {
    "q": "Which Django form method is used to get error messages for invalid fields?",
    "c": null,
    "o": [
      "errors",
      "get_errors()",
      "show_errors()",
      "list_invalid()"
    ]
  },
  {
    "q": "What does the following code indicate?",
    "c": "name = forms.CharField(required=False)",
    "o": [
      "The 'name' field is optional",
      "The 'name' field must always be filled",
      "The 'name' field is hidden",
      "The 'name' field will be validated as an integer"
    ]
  },
  {
    "q": "In Django forms, which method can be overridden to apply validation that involves multiple fields together?",
    "c": null,
    "o": [
      "clean()",
      "validate()",
      "is_valid()",
      "check_fields()"
    ]
  },
  {
    "q": "What does the following code do in a ModelForm?",
    "c": "class ProductForm(forms.ModelForm):\n    price = forms.DecimalField(max_digits=6, decimal_places=2)\n\n    class Meta:\n        model = Product\n        fields = ['name', 'price']",
    "o": [
      "Overrides the 'price' field with custom validation and formatting",
      "Removes the 'price' field from the form",
      "Automatically sets a default value for price",
      "Adds an unrelated extra field to the Product model"
    ]
  },
  {
    "q": "Which Django form field would you use to allow users to upload files?",
    "c": null,
    "o": [
      "FileField",
      "ImageField",
      "UploadField",
      "BinaryField"
    ]
  },
  {
    "q": "What does the following code represent?",
    "c": "profile_picture = forms.ImageField(required=False)",
    "o": [
      "An optional image upload field",
      "A required file upload field",
      "A hidden field for profile pictures",
      "A text field for entering image URL"
    ]
  },
  {
    "q": "Which attribute is used to assign a custom HTML class to a Django form widget?",
    "c": null,
    "o": [
      "attrs",
      "css_class",
      "class_name",
      "style"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "username = forms.CharField(widget=forms.TextInput(attrs={'class': 'form-control'}))",
    "o": [
      "Adds a CSS class to the username input field",
      "Sets the username as a hidden field",
      "Makes the username field read-only",
      "Marks the username as optional"
    ]
  },
  {
    "q": "In Django forms, which argument allows setting a default value for a field?",
    "c": null,
    "o": [
      "initial",
      "default",
      "preset",
      "start_value"
    ]
  },
  {
    "q": "What does the following code do?",
    "c": "country = forms.CharField(initial='USA')",
    "o": [
      "Sets 'USA' as the default value for the country field",
      "Restricts the input to only 'USA'",
      "Creates a dropdown for country selection",
      "Validates only 'USA' as a valid value"
    ]
  },
  {
    "q": "Which Django form field is best suited for boolean values?",
    "c": null,
    "o": [
      "BooleanField",
      "TrueFalseField",
      "ChoiceField",
      "CheckField"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "accept_terms = forms.BooleanField(error_messages={'required': 'You must accept the terms to continue.'})",
    "o": [
      "Displays a custom error message if the checkbox is not selected",
      "Automatically checks the checkbox by default",
      "Hides the checkbox field from the form",
      "Validates that the field value is always False"
    ]
  },
  {
    "q": "Which method is used to render a form as a series of <tr> elements in a table?",
    "c": null,
    "o": [
      "as_table()",
      "as_tr()",
      "render_table()",
      "to_table()"
    ]
  },
  {
    "q": "What does the following snippet do?",
    "c": "form = ContactForm()\nform.as_p()",
    "o": [
      "Renders the form fields wrapped in <p> tags",
      "Renders the form as a plain string",
      "Validates the form automatically",
      "Converts form data into a paragraph of text"
    ]
  },
  {
    "q": "Which Django form field is used for creating a drop-down selection?",
    "c": null,
    "o": [
      "ChoiceField",
      "SelectField",
      "DropdownField",
      "OptionsField"
    ]
  },
  {
    "q": "What does the following code represent?",
    "c": "COLOR_CHOICES = [('R', 'Red'), ('G', 'Green'), ('B', 'Blue')]\n\ncolor = forms.ChoiceField(choices=COLOR_CHOICES)",
    "o": [
      "A dropdown menu with Red, Green, and Blue options",
      "A multi-select field for choosing colors",
      "A text input field that accepts only R, G, or B",
      "A radio button group for selecting colors"
    ]
  },
  {
    "q": "Which attribute of a Django form field allows multiple selections?",
    "c": null,
    "o": [
      "MultipleChoiceField",
      "MultiField",
      "ChoiceListField",
      "CheckboxField"
    ]
  },
  {
    "q": "What does the following code snippet represent?",
    "c": "hobbies = forms.MultipleChoiceField(choices=[('R', 'Reading'), ('S', 'Sports')], widget=forms.CheckboxSelectMultiple)",
    "o": [
      "A set of checkboxes for selecting multiple hobbies",
      "A dropdown menu for multiple hobbies",
      "A text area for entering hobbies",
      "A single checkbox for each hobby category"
    ]
  },
  {
    "q": "Which form method returns True if the form contains errors?",
    "c": null,
    "o": [
      "is_valid() returns False",
      "has_errors() returns True",
      "check() returns True",
      "validate() returns False"
    ]
  },
  {
    "q": "What does the following validation code achieve?",
    "c": "def clean_age(self):\n    age = self.cleaned_data.get('age')\n    if age < 18:\n        raise forms.ValidationError('Must be at least 18 years old')\n    return age",
    "o": [
      "Ensures the user is at least 18 years old",
      "Automatically sets age to 18 if below",
      "Restricts input to only the number 18",
      "Stores the age without validation"
    ]
  },
  {
    "q": "Which Django form rendering method outputs the form fields without any surrounding HTML?",
    "c": null,
    "o": [
      "as_ul()",
      "as_p()",
      "as_table()",
      "plain()"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "form = LoginForm()\nform.as_ul()",
    "o": [
      "Renders the form fields as list items <li>",
      "Validates the form automatically",
      "Renders the form inside a <ul> block without <li>",
      "Converts the form into an unordered data list"
    ]
  },
  {
    "q": "Which attribute of a Django form field provides a short descriptive text displayed along with the field?",
    "c": null,
    "o": [
      "label",
      "title",
      "description",
      "caption"
    ]
  },
  {
    "q": "What does the following code define?",
    "c": "email = forms.EmailField(label='Your Email Address')",
    "o": [
      "A form field labeled 'Your Email Address'",
      "An email field that validates only Gmail addresses",
      "An email field with placeholder text",
      "An email field hidden from form rendering"
    ]
  },
  {
    "q": "Which Django form field is specifically designed for handling date input?",
    "c": null,
    "o": [
      "DateField",
      "DateTimeField",
      "TimeField",
      "CalendarField"
    ]
  },
  {
    "q": "What does the following form field specify?",
    "c": "birth_date = forms.DateField(widget=forms.SelectDateWidget)",
    "o": [
      "A dropdown-based widget for selecting dates",
      "A text input field for entering dates",
      "A datetime field including time selection",
      "A hidden field that stores a timestamp"
    ]
  },
  {
    "q": "Which argument allows pre-populating a form with existing data?",
    "c": null,
    "o": [
      "initial",
      "preset",
      "populate",
      "default"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "form = ProfileForm(initial={'username': 'guest'})",
    "o": [
      "Pre-fills the username field with 'guest'",
      "Restricts the username field to only 'guest'",
      "Validates only if username equals 'guest'",
      "Hides the username field from users"
    ]
  },
  {
    "q": "Which Django form field is best suited for large text input, like user comments?",
    "c": null,
    "o": [
      "CharField with Textarea widget",
      "TextField",
      "LargeTextField",
      "CommentField"
    ]
  },
  {
    "q": "What does the following code define?",
    "c": "message = forms.CharField(widget=forms.Textarea(attrs={'rows': 4, 'cols': 40}))",
    "o": [
      "A multi-line text area for entering a message",
      "A hidden input field for storing a message",
      "A single-line input field with max length",
      "A field that only accepts numbers"
    ]
  },
  {
    "q": "Which Django form field is used for selecting from a related model?",
    "c": null,
    "o": [
      "ModelChoiceField",
      "ForeignKeyField",
      "RelationField",
      "SelectModelField"
    ]
  },
  {
    "q": "What does the following code represent?",
    "c": "category = forms.ModelChoiceField(queryset=Category.objects.all())",
    "o": [
      "A dropdown list populated with Category model instances",
      "A text field for entering category names",
      "A multi-select widget for categories",
      "An integer field storing category IDs"
    ]
  },
  {
    "q": "Which field type allows selecting multiple related objects from a model?",
    "c": null,
    "o": [
      "ModelMultipleChoiceField",
      "MultiModelField",
      "ForeignKeyMultipleField",
      "RelationChoiceField"
    ]
  },
  {
    "q": "What does the following form field define?",
    "c": "tags = forms.ModelMultipleChoiceField(queryset=Tag.objects.all(), widget=forms.CheckboxSelectMultiple)",
    "o": [
      "A set of checkboxes for selecting multiple tags",
      "A dropdown for selecting one tag",
      "A text input for entering tag names",
      "A hidden field for storing tag IDs"
    ]
  },
  {
    "q": "Which parameter inside a Django form field can make the field mandatory by default?",
    "c": null,
    "o": [
      "required=True",
      "must=True",
      "mandatory=True",
      "validate=True"
    ]
  },
  {
    "q": "What does the following snippet achieve?",
    "c": "newsletter = forms.BooleanField(required=False, initial=True)",
    "o": [
      "Creates an optional checkbox pre-selected by default",
      "Forces the checkbox to always stay selected",
      "Hides the checkbox field",
      "Validates that checkbox must be unchecked"
    ]
  },
  {
    "q": "Which method is typically used to bind data from an HTTP POST request to a form?",
    "c": null,
    "o": [
      "form = MyForm(request.POST)",
      "form = MyForm(request.GET)",
      "form = MyForm(request.DATA)",
      "form = MyForm(request.BODY)"
    ]
  },
  {
    "q": "What does the following snippet do?",
    "c": "form = CommentForm(request.POST)\nif form.is_valid():\n    comment = form.save(commit=False)\n    comment.post = post\n    comment.save()",
    "o": [
      "Saves a form instance to the database after assigning a related object",
      "Saves the form automatically without validation",
      "Deletes an existing form record before saving",
      "Validates the form but skips saving"
    ]
  },
  {
    "q": "Which Django form method returns a dictionary of field errors?",
    "c": null,
    "o": [
      "errors",
      "error_dict()",
      "get_errors()",
      "list_errors()"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "if form.errors:\n    print(form.errors)",
    "o": [
      "Prints validation errors for the form fields",
      "Prints cleaned_data from the form",
      "Prints default field labels",
      "Prints initial data passed to the form"
    ]
  },
  {
    "q": "Which ModelForm option allows using all fields from the model except a few?",
    "c": null,
    "o": [
      "exclude",
      "skip",
      "ignore",
      "omit"
    ]
  },
  {
    "q": "What does the following code define?",
    "c": "class UserForm(forms.ModelForm):\n    class Meta:\n        model = User\n        exclude = ['last_login']",
    "o": [
      "A form for User model excluding the last_login field",
      "A form for User model including only last_login",
      "A form that validates only last_login",
      "A form that hides all User model fields"
    ]
  },
  {
    "q": "Which form field is best suited for uploading images?",
    "c": null,
    "o": [
      "ImageField",
      "FileField",
      "PictureField",
      "UploadField"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "photo = forms.ImageField(required=True, help_text='Upload a JPG or PNG image.')",
    "o": [
      "Creates a required image upload field with helper text",
      "Creates an optional image upload field",
      "Validates image size automatically",
      "Restricts upload to only JPG format"
    ]
  },
  {
    "q": "Which Django form field is used to handle true/false values with a checkbox?",
    "c": null,
    "o": [
      "BooleanField",
      "CheckField",
      "ChoiceField",
      "ToggleField"
    ]
  },
  {
    "q": "What does the following snippet achieve?",
    "c": "remember_me = forms.BooleanField(required=False, initial=False)",
    "o": [
      "Adds an optional checkbox defaulting to unchecked",
      "Forces the checkbox to always be checked",
      "Validates only if the checkbox is True",
      "Hides the checkbox from the form"
    ]
  },
  {
    "q": "Which method in Django forms processes raw input data into Python data types?",
    "c": null,
    "o": [
      "clean()",
      "is_valid()",
      "validate()",
      "process_data()"
    ]
  },
  {
    "q": "What does the following code snippet represent?",
    "c": "def clean_username(self):\n    username = self.cleaned_data.get('username')\n    if ' ' in username:\n        raise forms.ValidationError('Username should not contain spaces')\n    return username",
    "o": [
      "Custom validation for the username field",
      "Automatic saving of the username field",
      "Form rendering with spaces removed",
      "Adding a placeholder to username field"
    ]
  },
  {
    "q": "Which ModelForm Meta option is used to specify the model fields explicitly included in the form?",
    "c": null,
    "o": [
      "fields",
      "include",
      "allowed",
      "visible_fields"
    ]
  },
  {
    "q": "What does the following code define?",
    "c": "class ArticleForm(forms.ModelForm):\n    class Meta:\n        model = Article\n        fields = ['title', 'content']",
    "o": [
      "A ModelForm that includes only the title and content fields",
      "A ModelForm that excludes title and content",
      "A regular Django form without model binding",
      "A form that validates all fields of Article"
    ]
  },
  {
    "q": "Which argument in a Django form field can display instructions for users?",
    "c": null,
    "o": [
      "help_text",
      "tooltip",
      "label",
      "description"
    ]
  },
  {
    "q": "What does the following snippet achieve?",
    "c": "password = forms.CharField(widget=forms.PasswordInput, help_text='Enter a strong password')",
    "o": [
      "Renders a password input field with helper text",
      "Encrypts the password automatically",
      "Stores the password as plain text",
      "Validates password length only"
    ]
  },
  {
    "q": "Which Django class should be subclassed to create a custom form?",
    "c": null,
    "o": [
      "forms.Form",
      "forms.ModelForm",
      "forms.BaseForm",
      "forms.TemplateForm"
    ]
  },
  {
    "q": "What does the following snippet define?",
    "c": "class FeedbackForm(forms.Form):\n    email = forms.EmailField()\n    message = forms.CharField(widget=forms.Textarea)",
    "o": [
      "A custom form with email and message fields",
      "A model form tied to the Feedback model",
      "A serializer for API feedback data",
      "A template-only form with no validation"
    ]
  },
  {
    "q": "Which method of a Django form is responsible for checking validation and returning True or False?",
    "c": null,
    "o": [
      "is_valid()",
      "validate()",
      "check_valid()",
      "run_validation()"
    ]
  },
  {
    "q": "What does the following code do in a ModelForm?",
    "c": "form = BookForm(request.POST)\nif form.is_valid():\n    form.save()",
    "o": [
      "Saves a new Book instance to the database",
      "Prints the cleaned form data only",
      "Validates the form but skips saving",
      "Deletes old Book objects before saving"
    ]
  },
  {
    "q": "Which attribute is used in a form field to control how the field is displayed in HTML?",
    "c": null,
    "o": [
      "widget",
      "style",
      "html_type",
      "renderer"
    ]
  },
  {
    "q": "What does the following snippet represent?",
    "c": "title = forms.CharField(widget=forms.TextInput(attrs={'placeholder': 'Enter title'}))",
    "o": [
      "Adds a placeholder text 'Enter title' to the input field",
      "Sets the initial value of title to 'Enter title'",
      "Forces the field to always contain 'Enter title'",
      "Hides the title input field"
    ]
  },
  {
    "q": "Which Django form field would you use to allow users to select multiple items?",
    "c": null,
    "o": [
      "MultipleChoiceField",
      "ChoiceField",
      "CheckField",
      "SelectManyField"
    ]
  },
  {
    "q": "What does the following code specify?",
    "c": "interests = forms.MultipleChoiceField(choices=[('C', 'Coding'), ('M', 'Music')], widget=forms.CheckboxSelectMultiple)",
    "o": [
      "A group of checkboxes for selecting multiple interests",
      "A single dropdown list for choosing an interest",
      "A text area where users type interests",
      "A radio button for selecting one interest"
    ]
  },
  {
    "q": "Which Django form attribute is used to define human-readable text for a form field?",
    "c": null,
    "o": [
      "label",
      "caption",
      "title",
      "name"
    ]
  },
  {
    "q": "What does the following field definition achieve?",
    "c": "age = forms.IntegerField(label='Your Age', min_value=1)",
    "o": [
      "Creates an integer input field labeled 'Your Age' with a minimum value of 1",
      "Creates a text field labeled 'Your Age'",
      "Creates a hidden field for storing age",
      "Creates a decimal input field for age"
    ]
  },
  {
    "q": "Which argument in a Django form field can provide a prefilled value before user input?",
    "c": null,
    "o": [
      "initial",
      "default",
      "prefill",
      "start_value"
    ]
  },
  {
    "q": "What does the following snippet achieve?",
    "c": "username = forms.CharField(initial='guest_user')",
    "o": [
      "Sets 'guest_user' as the default value shown in the username field",
      "Restricts the username field to only 'guest_user'",
      "Validates that username must contain 'guest_user'",
      "Makes the username field hidden"
    ]
  },
  {
    "q": "Which Django form field should be used to capture decimal numbers with precision?",
    "c": null,
    "o": [
      "DecimalField",
      "FloatField",
      "NumberField",
      "PrecisionField"
    ]
  },
  {
    "q": "What does the following code represent?",
    "c": "price = forms.DecimalField(max_digits=5, decimal_places=2)",
    "o": [
      "A decimal field allowing up to 5 digits with 2 decimal places",
      "A float field rounded to 2 decimal places",
      "An integer-only input field",
      "A text field that stores numbers as strings"
    ]
  },
  {
    "q": "Which Django form method is used to return the form’s data after validation?",
    "c": null,
    "o": [
      "cleaned_data",
      "is_valid()",
      "validated_data",
      "form_data"
    ]
  },
  {
    "q": "What does the following snippet demonstrate?",
    "c": "if form.is_valid():\n    print(form.cleaned_data['email'])",
    "o": [
      "Accessing the validated email field from form data",
      "Printing raw email input from the request",
      "Accessing errors in the email field",
      "Fetching the email label from the form"
    ]
  },
  {
    "q": "Which Django utility can be used to write reusable field validators?",
    "c": null,
    "o": [
      "validators",
      "cleaners",
      "checks",
      "rules"
    ]
  },
  {
    "q": "What does the following snippet represent?",
    "c": "from django.core.validators import MinLengthValidator\n\nusername = forms.CharField(validators=[MinLengthValidator(5)])",
    "o": [
      "Applies a minimum length validator requiring at least 5 characters",
      "Automatically trims whitespace from username",
      "Rejects usernames containing digits",
      "Forces the username to be stored in lowercase"
    ]
  },
  {
    "q": "Which feature allows grouping multiple form instances together for validation and processing?",
    "c": null,
    "o": [
      "FormSet",
      "MultiForm",
      "FormGroup",
      "FormBundle"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "from django.forms import formset_factory\n\nBookFormSet = formset_factory(BookForm, extra=2)",
    "o": [
      "Creates a set of BookForm forms with 2 extra blank forms",
      "Creates exactly 2 BookForm instances and no more",
      "Validates BookForm twice before saving",
      "Bundles BookForm with two related models"
    ]
  },
  {
    "q": "Which ModelForm Meta option allows customizing widgets for form fields?",
    "c": null,
    "o": [
      "widgets",
      "renderers",
      "ui",
      "field_styles"
    ]
  },
  {
    "q": "What does the following snippet specify?",
    "c": "class ProductForm(forms.ModelForm):\n    class Meta:\n        model = Product\n        fields = ['name', 'price']\n        widgets = {\n            'price': forms.NumberInput(attrs={'class': 'form-control'})\n        }",
    "o": [
      "Customizes the HTML widget for the price field",
      "Excludes the price field from rendering",
      "Adds server-side validation for the price field",
      "Sets the price field as read-only"
    ]
  },
  {
    "q": "Which Django form attribute stores all non-field-specific errors?",
    "c": null,
    "o": [
      "non_field_errors()",
      "general_errors()",
      "form_errors()",
      "cleaned_errors()"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "form = SignupForm(request.POST)\nif not form.is_valid():\n    print(form.non_field_errors())",
    "o": [
      "Prints validation errors not tied to a specific field",
      "Prints all field-specific errors",
      "Prints cleaned_data from the form",
      "Prints only errors on required fields"
    ]
  },
  {
    "q": "Which argument must be included when instantiating a form that handles file uploads?",
    "c": null,
    "o": [
      "request.FILES",
      "request.DATA",
      "request.BODY",
      "request.INPUT"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "form = UploadForm(request.POST, request.FILES)\nif form.is_valid():\n    handle_uploaded_file(form.cleaned_data['file'])",
    "o": [
      "Processes a file uploaded via the form",
      "Validates file size automatically",
      "Stores the file in the database directly",
      "Rejects non-image files automatically"
    ]
  },
  {
    "q": "Which form field should be used to upload images with built-in validation?",
    "c": null,
    "o": [
      "ImageField",
      "FileField",
      "UploadField",
      "PictureField"
    ]
  },
  {
    "q": "What does the following field definition enforce?",
    "c": "photo = forms.ImageField(required=True, error_messages={'invalid': 'Only image files are allowed'})",
    "o": [
      "Validates that only image files are uploaded",
      "Stores images as plain text",
      "Automatically resizes the uploaded image",
      "Converts image to JPG format before saving"
    ]
  },
  {
    "q": "Which Django widget renders a password field as masked input?",
    "c": null,
    "o": [
      "PasswordInput",
      "HiddenInput",
      "MaskedInput",
      "SecureInput"
    ]
  },
  {
    "q": "What does the following code specify?",
    "c": "password = forms.CharField(widget=forms.PasswordInput(attrs={'placeholder': 'Enter Password'}))",
    "o": [
      "Renders a password input with placeholder text",
      "Encrypts the password automatically",
      "Saves the password as plain text",
      "Hides the password field entirely"
    ]
  },
  {
    "q": "Which method should be used to get all field-specific error messages in a Django form?",
    "c": null,
    "o": [
      "errors",
      "field_errors()",
      "get_errors()",
      "invalid_fields()"
    ]
  },
  {
    "q": "What does the following snippet print?",
    "c": "form = ProfileForm(data={})\nform.is_valid()\nprint(form.errors)",
    "o": [
      "A dictionary of validation errors for each field",
      "Cleaned data from the form",
      "Default values of each field",
      "The rendered form as HTML"
    ]
  },
  {
    "q": "Which hidden form is automatically added to every formset to manage metadata like total forms?",
    "c": null,
    "o": [
      "ManagementForm",
      "AdminForm",
      "MetaForm",
      "HiddenForm"
    ]
  },
  {
    "q": "What does the following snippet ensure in a formset?",
    "c": "print(formset.management_form)",
    "o": [
      "Renders the hidden management form with TOTAL_FORMS and INITIAL_FORMS",
      "Prints all cleaned data from the formset",
      "Shows only validation errors across forms",
      "Displays the first form in the formset"
    ]
  },
  {
    "q": "Which key in ManagementForm data specifies the total number of forms in the formset?",
    "c": null,
    "o": [
      "TOTAL_FORMS",
      "FORM_COUNT",
      "ALL_FORMS",
      "FORM_TOTAL"
    ]
  },
  {
    "q": "What does the following POST data field represent?",
    "c": "'myformset-TOTAL_FORMS': '3'",
    "o": [
      "The formset contains 3 forms in total",
      "The formset allows a maximum of 3 forms",
      "The formset contains 3 initial forms only",
      "Validation will run only 3 times"
    ]
  },
  {
    "q": "Which key in ManagementForm data specifies how many forms were initially provided?",
    "c": null,
    "o": [
      "INITIAL_FORMS",
      "START_FORMS",
      "BASE_FORMS",
      "OLD_FORMS"
    ]
  },
  {
    "q": "What does the following snippet achieve?",
    "c": "BookFormSet = formset_factory(BookForm, extra=0)\nformset = BookFormSet(initial=[{'title': 'Book A'}, {'title': 'Book B'}])",
    "o": [
      "Creates a formset prefilled with two initial forms",
      "Creates a formset with no forms at all",
      "Always creates only 1 form regardless of initial data",
      "Raises an error because extra=0 is invalid"
    ]
  },
  {
    "q": "Which argument enables reordering of forms inside a formset?",
    "c": null,
    "o": [
      "can_order=True",
      "allow_sort=True",
      "orderable=True",
      "enable_reorder=True"
    ]
  },
  {
    "q": "What does the following code snippet enable?",
    "c": "BookFormSet = formset_factory(BookForm, can_delete=True)\nformset = BookFormSet()",
    "o": [
      "Allows marking individual forms in the formset for deletion",
      "Deletes the formset after saving",
      "Deletes all fields marked as optional",
      "Automatically deletes duplicate data entries"
    ]
  },
  {
    "q": "Which built-in method renders a Django form’s fields wrapped in `<p>` tags?",
    "c": null,
    "o": [
      "as_p",
      "as_paragraphs",
      "to_p",
      "render_p"
    ]
  },
  {
    "q": "What does the following template code output?",
    "c": "{{ form.as_table }}",
    "o": [
      "Renders the form fields inside an HTML table",
      "Renders the form fields inside paragraph tags",
      "Outputs only field labels",
      "Displays only non-field errors"
    ]
  },
  {
    "q": "Which method renders a form with fields as list items (`<li>`)?",
    "c": null,
    "o": [
      "as_ul",
      "as_list",
      "to_li",
      "render_ul"
    ]
  },
  {
    "q": "What does the following field definition achieve?",
    "c": "email = forms.EmailField(help_text='Enter a valid email address')",
    "o": [
      "Displays helper text under the email field",
      "Validates that the email must end with .com",
      "Adds placeholder text to the input",
      "Shows email as read-only"
    ]
  },
  {
    "q": "Which parameter in a field allows customizing the error messages?",
    "c": null,
    "o": [
      "error_messages",
      "errors",
      "validation_text",
      "messages"
    ]
  },
  {
    "q": "What does the following definition enforce?",
    "c": "username = forms.CharField(max_length=30, error_messages={'required': 'Username cannot be blank'})",
    "o": [
      "Shows a custom error if username is missing",
      "Validates that username has no spaces",
      "Restricts username to lowercase only",
      "Displays default error messages always"
    ]
  },
  {
    "q": "Which argument in a form field overrides the label displayed in the template?",
    "c": null,
    "o": [
      "label",
      "name",
      "display",
      "caption"
    ]
  },
  {
    "q": "What does the following field definition achieve?",
    "c": "age = forms.IntegerField(label='Your Age')",
    "o": [
      "Displays the field with a custom label 'Your Age'",
      "Validates that age must be positive",
      "Restricts the field to under 100",
      "Automatically sets default age value"
    ]
  },
  {
    "q": "Which method of a custom Django form field is responsible for converting input data into the correct Python type?",
    "c": null,
    "o": [
      "to_python",
      "clean",
      "validate",
      "convert"
    ]
  },
  {
    "q": "What does the following method inside a custom field do?",
    "c": "def validate(self, value):\n    if not value.startswith('X'):\n        raise forms.ValidationError('Value must start with X')",
    "o": [
      "Adds custom validation after type conversion",
      "Converts the input value into Python type",
      "Cleans whitespace from input",
      "Automatically sets an initial value"
    ]
  },
  {
    "q": "Which method of a custom field combines both type conversion and validation?",
    "c": null,
    "o": [
      "clean",
      "validate",
      "to_python",
      "process"
    ]
  },
  {
    "q": "What does the following custom field achieve?",
    "c": "class EvenIntegerField(forms.IntegerField):\n    def validate(self, value):\n        super().validate(value)\n        if value % 2 != 0:\n            raise forms.ValidationError('Only even numbers are allowed')",
    "o": [
      "Ensures only even integers are accepted",
      "Rejects all odd integers but allows floats",
      "Converts odd integers into even ones automatically",
      "Rejects all integers above 100"
    ]
  },
  {
    "q": "Which base class should be extended to define a new form field in Django?",
    "c": null,
    "o": [
      "forms.Field",
      "forms.BaseField",
      "forms.AbstractField",
      "forms.CustomField"
    ]
  },
  {
    "q": "What does the following code represent?",
    "c": "class UppercaseCharField(forms.CharField):\n    def to_python(self, value):\n        value = super().to_python(value)\n        return value.upper() if value else value",
    "o": [
      "Automatically converts input text to uppercase",
      "Validates that input must be uppercase",
      "Rejects lowercase input values",
      "Displays text in uppercase in templates only"
    ]
  },
  {
    "q": "In a custom field, when is the `to_python` method executed?",
    "c": null,
    "o": [
      "Before validation to normalize data",
      "After validation has succeeded",
      "Only when rendering the field",
      "Only if errors occur in validation"
    ]
  },
  {
    "q": "What does the following snippet ensure in a custom field?",
    "c": "def clean(self, value):\n    value = self.to_python(value)\n    self.validate(value)\n    self.run_validators(value)\n    return value",
    "o": [
      "Performs conversion, validation, and runs custom validators",
      "Skips built-in validation and uses only custom rules",
      "Only converts input without validation",
      "Only checks required fields"
    ]
  },
  {
    "q": "Which class provides the core implementation that all Django forms inherit from?",
    "c": null,
    "o": [
      "BaseForm",
      "FormMixin",
      "FormView",
      "AbstractForm"
    ]
  },
  {
    "q": "Which mixin is commonly used in class-based views to handle forms?",
    "c": null,
    "o": [
      "FormMixin",
      "FormHandlerMixin",
      "FormManager",
      "FormController"
    ]
  },
  {
    "q": "What does the following generic view achieve?",
    "c": "class ContactView(FormView):\n    template_name = 'contact.html'\n    form_class = ContactForm\n    success_url = '/thanks/'\n\n    def form_valid(self, form):\n        form.send_email()\n        return super().form_valid(form)",
    "o": [
      "Renders a form, validates it, and calls send_email() on success",
      "Automatically saves form data to a database model",
      "Always redirects to '/thanks/' without validation",
      "Displays the form but never validates it"
    ]
  },
  {
    "q": "Which method of FormMixin provides the URL to redirect to after successful form submission?",
    "c": null,
    "o": [
      "get_success_url",
      "redirect_url",
      "on_success",
      "success_redirect"
    ]
  },
  {
    "q": "What does the following method override accomplish in a FormView?",
    "c": "def form_invalid(self, form):\n    return self.render_to_response(self.get_context_data(form=form))",
    "o": [
      "Re-renders the form with errors displayed",
      "Redirects to success_url regardless of errors",
      "Deletes the invalid form instance",
      "Ignores errors and resubmits the form"
    ]
  },
  {
    "q": "Which generic class-based view is used to create new model objects using a ModelForm?",
    "c": null,
    "o": [
      "CreateView",
      "FormView",
      "ModelFormView",
      "AddView"
    ]
  },
  {
    "q": "Which method in FormMixin is responsible for processing a POST request with form data?",
    "c": null,
    "o": [
      "post",
      "process",
      "submit",
      "handle_post"
    ]
  },
  {
    "q": "What does the following `post` method inside a FormView do?",
    "c": "def post(self, request, *args, **kwargs):\n    form = self.get_form()\n    if form.is_valid():\n        return self.form_valid(form)\n    else:\n        return self.form_invalid(form)",
    "o": [
      "Handles POST request by validating the form and delegating to form_valid or form_invalid",
      "Ignores POST data and reloads the form",
      "Saves the form data unconditionally",
      "Always raises a validation error"
    ]
  },
  {
    "q": "Which generic class-based view automatically uses a ModelForm to create new model instances?",
    "c": null,
    "o": [
      "CreateView",
      "FormView",
      "ModelFormView",
      "AddView"
    ]
  },
  {
    "q": "What does the following view achieve?",
    "c": "class BookUpdateView(UpdateView):\n    model = Book\n    fields = ['title', 'author']\n    template_name = 'book_form.html'\n    success_url = '/books/'",
    "o": [
      "Displays a form for editing an existing Book instance",
      "Always creates a new Book instance",
      "Deletes a Book instance",
      "Lists all Book objects"
    ]
  },
  {
    "q": "Which generic class-based view combines a ModelForm with object deletion?",
    "c": null,
    "o": [
      "DeleteView",
      "RemoveView",
      "DestroyView",
      "DropView"
    ]
  },
  {
    "q": "What does the following view accomplish?",
    "c": "class AuthorCreateView(CreateView):\n    model = Author\n    fields = ['name', 'email']\n    template_name = 'author_form.html'\n    success_url = '/authors/'",
    "o": [
      "Displays a form to add a new Author and saves it on submit",
      "Displays a list of authors",
      "Deletes an author from the database",
      "Edits an existing author only"
    ]
  },
  {
    "q": "Which method is responsible for saving a valid ModelForm in CreateView or UpdateView?",
    "c": null,
    "o": [
      "form_valid",
      "form_save",
      "save_form",
      "post"
    ]
  },
  {
    "q": "What does overriding `get_form_class()` in a CreateView allow you to do?",
    "c": null,
    "o": [
      "Provide a custom ModelForm class dynamically",
      "Change the template name used",
      "Override the redirect URL",
      "Specify the database table explicitly"
    ]
  },
  {
    "q": "What does the following `form_valid` override achieve?",
    "c": "def form_valid(self, form):\n    form.instance.created_by = self.request.user\n    return super().form_valid(form)",
    "o": [
      "Automatically assigns the logged-in user to the created object before saving",
      "Validates that the logged-in user is unique",
      "Saves the form twice to ensure persistence",
      "Rejects form submissions from anonymous users"
    ]
  },
  {
    "q": "Which method in UpdateView retrieves the specific object that should be updated?",
    "c": null,
    "o": [
      "get_object",
      "fetch_instance",
      "load_object",
      "get_instance"
    ]
  },
  {
    "q": "Which field type in Django forms is typically used to represent a ForeignKey relation?",
    "c": null,
    "o": [
      "ModelChoiceField",
      "ForeignKeyField",
      "ChoiceField",
      "RelationField"
    ]
  },
  {
    "q": "What does the following code achieve?",
    "c": "class BookForm(forms.ModelForm):\n    class Meta:\n        model = Book\n        fields = ['title', 'author']\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.fields['author'].queryset = Author.objects.filter(active=True)",
    "o": [
      "Limits the author choices to only active authors",
      "Ensures only one author can be selected",
      "Automatically creates a new Author if none exists",
      "Rejects all inactive authors from the database"
    ]
  },
  {
    "q": "Which method in Django admin forms is often overridden to optimize querysets for foreign key fields?",
    "c": null,
    "o": [
      "formfield_for_foreignkey",
      "optimize_queryset",
      "foreignkey_queryset",
      "field_queryset"
    ]
  },
  {
    "q": "What does the following code in a form save?",
    "c": "class OrderForm(forms.ModelForm):\n    class Meta:\n        model = Order\n        fields = ['product']\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.fields['product'].queryset = Product.objects.select_related('category')",
    "o": [
      "Optimizes product queries by joining with category in one query",
      "Restricts products only to those with categories",
      "Prevents products without categories from being saved",
      "Forces lazy loading of categories"
    ]
  },
  {
    "q": "Which optimization technique reduces the number of queries when accessing reverse relationships in ModelForms?",
    "c": null,
    "o": [
      "prefetch_related",
      "select_related",
      "defer",
      "only"
    ]
  },
  {
    "q": "What does the following form initialization achieve?",
    "c": "self.fields['tags'].queryset = Tag.objects.prefetch_related('articles')",
    "o": [
      "Reduces queries when accessing related articles for tags",
      "Restricts tags to those with related articles only",
      "Deletes unrelated tags automatically",
      "Forces each tag to load only one article"
    ]
  },
  {
    "q": "Which ModelForm Meta option is used to include all model fields without listing them explicitly?",
    "c": null,
    "o": [
      "fields = '__all__'",
      "include_all = True",
      "all_fields = True",
      "use_all = True"
    ]
  },
  {
    "q": "What does the following Meta definition enforce?",
    "c": "class Meta:\n    model = Profile\n    exclude = ['user']",
    "o": [
      "Includes all Profile fields except user",
      "Includes only the user field",
      "Validates that user is required",
      "Ignores validation for all fields"
    ]
  },
  {
    "q": "Which Meta option in a Django ModelForm allows you to customize how fields are rendered with widgets?",
    "c": null,
    "o": [
      "widgets",
      "render",
      "form_fields",
      "display"
    ]
  },
  {
    "q": "What does the following code do?",
    "c": "class CommentForm(forms.ModelForm):\n    class Meta:\n        model = Comment\n        fields = ['text']\n        widgets = {\n            'text': forms.Textarea(attrs={'rows': 5, 'cols': 40})\n        }",
    "o": [
      "Renders the text field as a textarea with 5 rows and 40 columns",
      "Adds default validation rules for the text field",
      "Limits the text field to 40 characters",
      "Converts the text field into a hidden field"
    ]
  },
  {
    "q": "Which widget would you use for a BooleanField to render as a checkbox?",
    "c": null,
    "o": [
      "CheckboxInput",
      "BooleanInput",
      "RadioSelect",
      "ToggleInput"
    ]
  },
  {
    "q": "What does the following widget customization achieve?",
    "c": "forms.DateField(widget=forms.DateInput(attrs={'type': 'date'}))",
    "o": [
      "Renders an HTML5 date picker input",
      "Validates that input is a date",
      "Automatically stores the current date",
      "Converts date into string only"
    ]
  },
  {
    "q": "Which argument is passed to a widget to set HTML attributes like CSS classes or placeholders?",
    "c": null,
    "o": [
      "attrs",
      "options",
      "properties",
      "styles"
    ]
  },
  {
    "q": "What does the following widget declaration do?",
    "c": "forms.CharField(widget=forms.TextInput(attrs={'placeholder': 'Enter your name'}))",
    "o": [
      "Displays a text input with placeholder 'Enter your name'",
      "Automatically validates non-empty input",
      "Converts input into uppercase",
      "Sets a default value for the field"
    ]
  },
  {
    "q": "Which widget renders a dropdown select field populated from a queryset?",
    "c": null,
    "o": [
      "Select",
      "ChoiceField",
      "Dropdown",
      "ModelDropdown"
    ]
  },
  {
    "q": "What does the following snippet achieve?",
    "c": "class LoginForm(forms.Form):\n    username = forms.CharField(widget=forms.TextInput(attrs={'class': 'form-control'}))\n    password = forms.CharField(widget=forms.PasswordInput(attrs={'class': 'form-control'}))",
    "o": [
      "Adds Bootstrap CSS styling to the username and password inputs",
      "Encrypts the password automatically before saving",
      "Makes both fields required by default",
      "Displays both fields as hidden inputs"
    ]
  },
  {
    "q": "Which base class should you subclass to create a fully custom form widget in Django?",
    "c": null,
    "o": [
      "forms.Widget",
      "forms.BaseField",
      "forms.Input",
      "forms.FormWidget"
    ]
  },
  {
    "q": "What does overriding the `render` method in a custom widget allow you to do?",
    "c": null,
    "o": [
      "Control how the widget’s HTML is output",
      "Define how form validation works",
      "Save the widget data to the database",
      "Determine which model field is used"
    ]
  },
  {
    "q": "What does the following custom widget render?",
    "c": "class ColorPickerWidget(forms.TextInput):\n    def __init__(self, attrs=None):\n        default_attrs = {'type': 'color'}\n        if attrs:\n            default_attrs.update(attrs)\n        super().__init__(attrs=default_attrs)",
    "o": [
      "An HTML5 color picker input",
      "A dropdown list of colors",
      "A text area for color codes",
      "A select input with color names"
    ]
  },
  {
    "q": "Which method in a widget is responsible for extracting the submitted value from request.POST data?",
    "c": null,
    "o": [
      "value_from_datadict",
      "from_request",
      "get_value",
      "parse_input"
    ]
  },
  {
    "q": "What does the following custom widget achieve?",
    "c": "class ListTextWidget(forms.TextInput):\n    def __init__(self, data_list, name, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self._name = name\n        self._list = data_list\n\n    def render(self, name, value, attrs=None, renderer=None):\n        text_html = super().render(name, value, attrs, renderer)\n        data_list = '<datalist id=\"%s\">%s</datalist>' % (\n            self._name,\n            ''.join(['<option value=\"%s\">' % item for item in self._list])\n        )\n        return text_html + data_list",
    "o": [
      "Adds an HTML5 datalist autocomplete feature to a text input",
      "Validates user input against a list of options",
      "Converts list input into multiple fields",
      "Restricts text input to integers only"
    ]
  },
  {
    "q": "Which widget subclass is best suited when you want to wrap an existing input type with extra HTML or JS behavior?",
    "c": null,
    "o": [
      "forms.Widget",
      "forms.MultiWidget",
      "forms.CustomInput",
      "forms.BaseWidget"
    ]
  },
  {
    "q": "What is the purpose of `decompress` in a MultiWidget subclass?",
    "c": null,
    "o": [
      "It splits a single field value into multiple widget values",
      "It validates input before saving",
      "It compresses multiple fields into one string",
      "It renders JavaScript for the widget"
    ]
  },
  {
    "q": "What does the following MultiWidget handle?",
    "c": "class SplitDateTimeWidget(forms.MultiWidget):\n    def __init__(self, attrs=None):\n        widgets = [\n            forms.DateInput(attrs={'type': 'date'}),\n            forms.TimeInput(attrs={'type': 'time'})\n        ]\n        super().__init__(widgets, attrs)\n\n    def decompress(self, value):\n        if value:\n            return [value.date(), value.time()]\n        return [None, None]",
    "o": [
      "Renders a datetime field as separate date and time inputs",
      "Forces datetime input into UTC only",
      "Validates only date values but ignores time",
      "Combines multiple models into one field"
    ]
  },
  {
    "q": "Which function is used to create a basic formset from a form class?",
    "c": null,
    "o": [
      "formset_factory",
      "create_formset",
      "FormSet",
      "make_formset"
    ]
  },
  {
    "q": "What does the following code produce?",
    "c": "BookFormSet = formset_factory(BookForm, extra=2)",
    "o": [
      "A formset with 2 extra empty forms in addition to existing ones",
      "A formset with only 2 forms total",
      "A formset that accepts 2 model instances only",
      "A formset that validates only 2 fields"
    ]
  },
  {
    "q": "Which parameter in `formset_factory` specifies the maximum number of forms allowed in the formset?",
    "c": null,
    "o": [
      "max_num",
      "limit",
      "maximum",
      "form_limit"
    ]
  },
  {
    "q": "What does the `can_delete=True` option in a formset allow?",
    "c": null,
    "o": [
      "Enables users to mark forms for deletion",
      "Prevents deletion of any form instance",
      "Deletes the formset automatically on submit",
      "Limits the formset to one form only"
    ]
  },
  {
    "q": "Which function is used to create a ModelFormSet for working directly with models?",
    "c": null,
    "o": [
      "modelformset_factory",
      "model_formset",
      "create_modelformset",
      "ModelFormSet"
    ]
  },
  {
    "q": "What does the following code define?",
    "c": "AuthorFormSet = modelformset_factory(Author, fields=['name'], extra=1)",
    "o": [
      "A ModelFormSet for Author with one extra empty form",
      "A single ModelForm for Author",
      "A formset that validates only unique author names",
      "A formset that excludes the name field"
    ]
  },
  {
    "q": "Which method must be called on a formset instance to validate all forms within it?",
    "c": null,
    "o": [
      "is_valid()",
      "validate_all()",
      "check_valid()",
      "full_clean()"
    ]
  },
  {
    "q": "What does the following formset snippet achieve?",
    "c": "formset = AuthorFormSet(queryset=Author.objects.none())",
    "o": [
      "Creates a formset with no pre-populated authors",
      "Deletes all authors before rendering",
      "Prevents saving any new authors",
      "Throws an error since queryset cannot be empty"
    ]
  },
  {
    "q": "Which special formset is used for handling inline related objects in Django admin?",
    "c": null,
    "o": [
      "inlineformset_factory",
      "related_formset",
      "foreignkey_formset",
      "childformset_factory"
    ]
  },
  {
    "q": "What does the following inline formset definition allow?",
    "c": "BookFormSet = inlineformset_factory(Author, Book, fields=['title'], extra=1)",
    "o": [
      "Allows editing multiple Book objects related to an Author",
      "Restricts an Author to only one Book",
      "Deletes all Books when an Author is deleted",
      "Validates that every Author has exactly one Book"
    ]
  },
  {
    "q": "Which method should be overridden in a custom FormSet to add validation across multiple forms?",
    "c": null,
    "o": [
      "clean",
      "validate",
      "check_forms",
      "full_clean"
    ]
  },
  {
    "q": "What does the following custom FormSet do?",
    "c": "class BaseAuthorFormSet(BaseFormSet):\n    def clean(self):\n        super().clean()\n        names = []\n        for form in self.forms:\n            if self.can_delete and self._should_delete_form(form):\n                continue\n            name = form.cleaned_data.get('name')\n            if name in names:\n                raise forms.ValidationError('Authors must have unique names')\n            names.append(name)",
    "o": [
      "Ensures that no two authors in the formset share the same name",
      "Deletes duplicate authors from the database",
      "Automatically renames duplicate authors",
      "Prevents saving any empty forms"
    ]
  },
  {
    "q": "Which parameter in a formset enables the ability to change the order of forms?",
    "c": null,
    "o": [
      "can_order",
      "allow_reorder",
      "order_enabled",
      "sort_forms"
    ]
  },
  {
    "q": "What does the following snippet allow?",
    "c": "BookFormSet = modelformset_factory(Book, fields=['title'], can_order=True)\nformset = BookFormSet(queryset=Book.objects.all())",
    "o": [
      "Users can reorder book forms before saving",
      "Books are automatically ordered by title",
      "Books must be entered in alphabetical order",
      "Books cannot be deleted in this formset"
    ]
  },
  {
    "q": "Which attribute is added to a form when `can_delete=True` is enabled on a formset?",
    "c": null,
    "o": [
      "DELETE",
      "REMOVE",
      "CANCEL",
      "DISCARD"
    ]
  },
  {
    "q": "What does the following formset behavior represent?",
    "c": "if formset.is_valid():\n    formset.save(commit=False)\n    for form in formset.forms:\n        obj = form.save(commit=False)\n        obj.added_by = request.user\n        obj.save()",
    "o": [
      "Associates each saved formset object with the current user before saving",
      "Automatically prevents duplicate form entries",
      "Deletes all objects not modified in the formset",
      "Forces all objects to save even if invalid"
    ]
  },
  {
    "q": "Which method is called to persist changes from a valid ModelFormSet to the database?",
    "c": null,
    "o": [
      "save",
      "commit",
      "persist",
      "store"
    ]
  },
  {
    "q": "What does the following inline formset ensure?",
    "c": "ChapterFormSet = inlineformset_factory(Book, Chapter, fields=['title'], validate_min=True, min_num=1)",
    "o": [
      "Each Book must have at least one Chapter",
      "Chapters must have unique titles",
      "Chapters are limited to one per Book",
      "Chapter deletion is disabled"
    ]
  },
  {
    "q": "Which parameter in a formset ensures that at least a certain number of forms are submitted?",
    "c": null,
    "o": [
      "min_num",
      "required",
      "minimum",
      "must_have"
    ]
  },
  {
    "q": "What does the `queryset` argument in a ModelFormSet control?",
    "c": null,
    "o": [
      "Which objects are displayed and editable in the formset",
      "How many extra forms are generated",
      "Whether ordering is applied to the formset",
      "If deletion is enabled on forms"
    ]
  },
  {
    "q": "What does `inlineformset_factory` primarily simplify in Django?",
    "c": null,
    "o": [
      "Editing related objects for a parent model in a single form",
      "Creating unique constraints between fields",
      "Generating validation for custom fields",
      "Rendering custom widgets automatically"
    ]
  },
  {
    "q": "What does the following code allow?",
    "c": "ProfileFormSet = inlineformset_factory(User, Profile, fields=['bio', 'location'], can_delete=False)",
    "o": [
      "Edits Profile objects directly when editing a User",
      "Automatically creates a Profile when a User is created",
      "Deletes Profile objects if User is deleted",
      "Restricts a User to multiple Profile objects"
    ]
  },
  {
    "q": "In a OneToOne relationship, how can an inline formset enforce only a single related object?",
    "c": null,
    "o": [
      "By using `max_num=1`",
      "By setting `unique=True` on the field",
      "By overriding `clean` in the formset",
      "By setting `can_delete=False`"
    ]
  },
  {
    "q": "What does the following inline formset achieve?",
    "c": "ChapterFormSet = inlineformset_factory(Book, Chapter, fields=['title'], validate_max=True, max_num=5)",
    "o": [
      "Ensures that no Book has more than 5 Chapters",
      "Prevents duplicate Chapter titles in a Book",
      "Deletes extra chapters beyond 5 automatically",
      "Orders Chapters alphabetically by default"
    ]
  },
  {
    "q": "Which error is raised when an inline formset violates a `unique_together` constraint?",
    "c": null,
    "o": [
      "ValidationError",
      "IntegrityError",
      "UniqueConstraintError",
      "DatabaseError"
    ]
  },
  {
    "q": "What does the following validation enforce?",
    "c": "class BaseChapterFormSet(BaseInlineFormSet):\n    def clean(self):\n        super().clean()\n        titles = []\n        for form in self.forms:\n            if form.cleaned_data:\n                title = form.cleaned_data.get('title')\n                if title in titles:\n                    raise forms.ValidationError('Chapters must have unique titles per book')\n                titles.append(title)",
    "o": [
      "Chapters under the same Book must have unique titles",
      "Every Book must contain at least one Chapter",
      "Chapter titles cannot exceed 50 characters",
      "Chapters cannot be deleted once created"
    ]
  },
  {
    "q": "Which parameter combination ensures at least one related object exists in an inline formset?",
    "c": null,
    "o": [
      "min_num=1, validate_min=True",
      "extra=1, can_delete=False",
      "required=True, validate=True",
      "default=1, ensure_one=True"
    ]
  },
  {
    "q": "What does this inline formset configuration enforce?",
    "c": "ImageFormSet = inlineformset_factory(Product, ProductImage, fields=['image'], extra=3)",
    "o": [
      "Allows uploading up to 3 additional images per Product",
      "Restricts a Product to exactly 3 images",
      "Deletes all images when Product is deleted",
      "Validates that at least 3 images exist"
    ]
  },
  {
    "q": "Which parameter ensures that a related object inline formset will not allow deletions?",
    "c": null,
    "o": [
      "can_delete=False",
      "prevent_delete=True",
      "delete=False",
      "readonly=True"
    ]
  },
  {
    "q": "What is the default behavior of an inline formset when the parent object is deleted?",
    "c": null,
    "o": [
      "All related objects in the inline formset are deleted as well",
      "The inline objects are preserved but detached",
      "Deletion is prevented if inlines exist",
      "An error is raised before deletion"
    ]
  },
  {
    "q": "Which attribute inside a ModelForm’s Meta class specifies the fields to include?",
    "c": null,
    "o": [
      "fields",
      "include",
      "only",
      "attributes"
    ]
  },
  {
    "q": "What does the following ModelForm Meta definition do?",
    "c": "class BookForm(forms.ModelForm):\n    class Meta:\n        model = Book\n        fields = ['title', 'author']",
    "o": [
      "Includes only 'title' and 'author' fields in the form",
      "Excludes 'title' and 'author' fields from the form",
      "Automatically validates 'title' and 'author' uniqueness",
      "Generates all model fields except 'title' and 'author'"
    ]
  },
  {
    "q": "Which Meta option in a ModelForm excludes specific fields?",
    "c": null,
    "o": [
      "exclude",
      "omit",
      "ignore",
      "remove"
    ]
  },
  {
    "q": "What happens if both `fields` and `exclude` are defined in a ModelForm Meta class?",
    "c": null,
    "o": [
      "Django raises an ImproperlyConfigured error",
      "The exclude list takes priority",
      "The fields list takes priority",
      "All fields are ignored"
    ]
  },
  {
    "q": "Which Meta option is used to customize widgets in a ModelForm?",
    "c": null,
    "o": [
      "widgets",
      "custom",
      "inputs",
      "ui"
    ]
  },
  {
    "q": "What does the following Meta configuration achieve?",
    "c": "class AuthorForm(forms.ModelForm):\n    class Meta:\n        model = Author\n        fields = ['name']\n        widgets = {\n            'name': forms.TextInput(attrs={'placeholder': 'Enter author name'})\n        }",
    "o": [
      "Adds a placeholder to the name input field",
      "Marks the name field as required",
      "Validates author names for uniqueness",
      "Hides the name field from the form"
    ]
  },
  {
    "q": "Which method is typically overridden to add custom save logic in a ModelForm?",
    "c": null,
    "o": [
      "save",
      "commit",
      "store",
      "persist"
    ]
  },
  {
    "q": "What does this custom save method accomplish?",
    "c": "class ProfileForm(forms.ModelForm):\n    def save(self, commit=True):\n        profile = super().save(commit=False)\n        profile.updated_by = self.request.user\n        if commit:\n            profile.save()\n        return profile",
    "o": [
      "Adds the current user as the updater before saving the Profile",
      "Prevents saving unless commit=False",
      "Deletes old profiles before saving new ones",
      "Disables form validation"
    ]
  },
  {
    "q": "Which attribute allows overriding labels for fields in a ModelForm?",
    "c": null,
    "o": [
      "labels",
      "titles",
      "names",
      "captions"
    ]
  },
  {
    "q": "What does the following Meta option achieve?",
    "c": "class BookForm(forms.ModelForm):\n    class Meta:\n        model = Book\n        fields = ['title']\n        labels = {'title': 'Book Title'}",
    "o": [
      "Displays 'Book Title' instead of 'title' in the form label",
      "Sets 'title' as a required field",
      "Excludes 'title' from the form",
      "Orders 'title' first in the form fields"
    ]
  },
  {
    "q": "Which method in a Django Form is used for field-specific validation?",
    "c": null,
    "o": [
      "clean_<fieldname>()",
      "validate_field()",
      "check_<fieldname>()",
      "full_clean_field()"
    ]
  },
  {
    "q": "What does this form method do?",
    "c": "class UserForm(forms.Form):\n    username = forms.CharField()\n    def clean_username(self):\n        data = self.cleaned_data['username']\n        if ' ' in data:\n            raise forms.ValidationError('Username cannot contain spaces')\n        return data",
    "o": [
      "Prevents usernames with spaces from being valid",
      "Automatically strips spaces from usernames",
      "Converts spaces in usernames to underscores",
      "Raises an error for all usernames"
    ]
  },
  {
    "q": "Which method in a Django Form is used for cross-field validation?",
    "c": null,
    "o": [
      "clean()",
      "validate_all()",
      "full_clean()",
      "check_fields()"
    ]
  },
  {
    "q": "What does this validation logic enforce?",
    "c": "class PasswordForm(forms.Form):\n    password = forms.CharField()\n    confirm_password = forms.CharField()\n\n    def clean(self):\n        cleaned_data = super().clean()\n        p1 = cleaned_data.get('password')\n        p2 = cleaned_data.get('confirm_password')\n        if p1 and p2 and p1 != p2:\n            raise forms.ValidationError('Passwords do not match')",
    "o": [
      "Ensures password and confirm_password fields match",
      "Forces passwords to be at least 8 characters long",
      "Hashes passwords before saving",
      "Rejects empty password fields"
    ]
  },
  {
    "q": "Which exception class must be raised during form validation errors?",
    "c": null,
    "o": [
      "ValidationError",
      "FormError",
      "FieldError",
      "InvalidFormError"
    ]
  },
  {
    "q": "What happens when `ValidationError` is raised in a `clean_<field>` method?",
    "c": null,
    "o": [
      "The error is attached to that specific field",
      "The entire form is marked invalid with no specific field",
      "The form ignores the field value and continues",
      "The form raises a runtime exception"
    ]
  },
  {
    "q": "What does `form.non_field_errors()` return?",
    "c": null,
    "o": [
      "Errors raised in the form’s clean() method",
      "Errors related to missing required fields",
      "Database-related validation errors",
      "Warnings about deprecated fields"
    ]
  },
  {
    "q": "What does this code validate?",
    "c": "class RegistrationForm(forms.Form):\n    email = forms.EmailField()\n    def clean_email(self):\n        email = self.cleaned_data['email']\n        if not email.endswith('@example.com'):\n            raise forms.ValidationError('Email must be from example.com domain')\n        return email",
    "o": [
      "Restricts valid emails to the example.com domain",
      "Automatically appends @example.com to all emails",
      "Rejects all email addresses",
      "Strips domain names from emails"
    ]
  },
  {
    "q": "Which method is automatically called before accessing `cleaned_data`?",
    "c": null,
    "o": [
      "full_clean()",
      "run_validation()",
      "validate()",
      "is_validated()"
    ]
  },
  {
    "q": "What does `form.errors` return in Django?",
    "c": null,
    "o": [
      "A dictionary mapping fields to lists of error messages",
      "Only a list of non-field errors",
      "A queryset of failed objects",
      "Boolean value indicating form validity"
    ]
  },
  {
    "q": "Which argument of a Django Form field allows attaching custom validator functions?",
    "c": null,
    "o": [
      "validators",
      "cleaners",
      "checks",
      "rules"
    ]
  },
  {
    "q": "What does the following field definition achieve?",
    "c": "def validate_even(value):\n    if value % 2 != 0:\n        raise forms.ValidationError('Value must be even')\n\nnumber = forms.IntegerField(validators=[validate_even])",
    "o": [
      "Ensures only even integers are accepted",
      "Rounds values to the nearest even number",
      "Rejects all integers",
      "Allows only positive integers"
    ]
  },
  {
    "q": "Which method in a Django model is used for model-level validation?",
    "c": null,
    "o": [
      "clean()",
      "validate()",
      "check()",
      "full_clean()"
    ]
  },
  {
    "q": "What does this model validation enforce?",
    "c": "class Event(models.Model):\n    start_date = models.DateField()\n    end_date = models.DateField()\n\n    def clean(self):\n        if self.end_date < self.start_date:\n            raise ValidationError('End date cannot be before start date')",
    "o": [
      "Ensures end_date is not earlier than start_date",
      "Automatically swaps start_date and end_date if reversed",
      "Rejects events without dates",
      "Restricts dates to current year only"
    ]
  },
  {
    "q": "How does Django combine Model.clean() with ModelForm validation?",
    "c": null,
    "o": [
      "Model.clean() runs after form validation and adds errors if necessary",
      "Model.clean() runs before form validation",
      "They run in parallel and return merged errors",
      "Model.clean() is ignored when using ModelForms"
    ]
  },
  {
    "q": "Which method validates all fields and also calls `clean()` in a Form?",
    "c": null,
    "o": [
      "full_clean()",
      "is_valid()",
      "validate_all()",
      "check_form()"
    ]
  },
  {
    "q": "What does the following form method achieve?",
    "c": "class AgeForm(forms.Form):\n    age = forms.IntegerField()\n\n    def clean_age(self):\n        value = self.cleaned_data['age']\n        if value < 18:\n            raise forms.ValidationError('Must be at least 18')\n        return value",
    "o": [
      "Restricts the field to ages 18 and above",
      "Automatically adjusts age to 18 if smaller",
      "Rejects non-integer values",
      "Requires exact age of 18"
    ]
  },
  {
    "q": "What does `form.add_error('field', 'message')` do?",
    "c": null,
    "o": [
      "Adds a validation error to a specific field",
      "Raises a ValidationError immediately",
      "Adds an error not tied to any field",
      "Deletes a field from the form"
    ]
  },
  {
    "q": "What does this snippet do?",
    "c": "class CustomForm(forms.Form):\n    def clean(self):\n        cleaned_data = super().clean()\n        if not cleaned_data.get('field1') and not cleaned_data.get('field2'):\n            raise forms.ValidationError('At least one of field1 or field2 is required')",
    "o": [
      "Enforces that at least one of field1 or field2 is filled",
      "Marks both fields as required always",
      "Rejects forms with both fields filled",
      "Ignores empty fields during validation"
    ]
  },
  {
    "q": "Which method in Django forms collects all validation errors into `form.errors`?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "validate_all()",
      "check()"
    ]
  },
  {
    "q": "Which class in Django is used to create a formset from a given Form class?",
    "c": null,
    "o": [
      "formset_factory",
      "FormSetBuilder",
      "create_formset",
      "FormSetFactory"
    ]
  },
  {
    "q": "What does this code snippet create?",
    "c": "BookFormSet = formset_factory(BookForm, extra=2)",
    "o": [
      "A formset with 2 extra empty BookForm instances",
      "A formset limited to only 2 total forms",
      "A formset that always validates 2 forms only",
      "A formset with exactly 2 pre-filled forms"
    ]
  },
  {
    "q": "Which parameter in formset_factory enforces a minimum number of forms?",
    "c": null,
    "o": [
      "min_num",
      "minimum",
      "at_least",
      "min_forms"
    ]
  },
  {
    "q": "What happens if `validate_min=True` is passed along with `min_num`?",
    "c": null,
    "o": [
      "Validation fails if fewer than min_num forms are submitted",
      "Validation is skipped for empty forms",
      "Only extra forms are validated",
      "min_num is ignored"
    ]
  },
  {
    "q": "Which parameter enforces a maximum number of forms in a formset?",
    "c": null,
    "o": [
      "max_num",
      "maximum",
      "limit",
      "max_forms"
    ]
  },
  {
    "q": "What does this formset clean method achieve?",
    "c": "class BaseBookFormSet(BaseFormSet):\n    def clean(self):\n        if any(self.errors):\n            return\n        titles = []\n        for form in self.forms:\n            title = form.cleaned_data.get('title')\n            if title in titles:\n                raise forms.ValidationError('Books must have unique titles')\n            titles.append(title)",
    "o": [
      "Ensures all book titles in the formset are unique",
      "Prevents empty book titles from being saved",
      "Automatically capitalizes duplicate book titles",
      "Excludes duplicate book forms from saving"
    ]
  },
  {
    "q": "Which attribute of a formset holds all the form instances?",
    "c": null,
    "o": [
      "forms",
      "instances",
      "form_list",
      "children"
    ]
  },
  {
    "q": "What does this configuration enforce?",
    "c": "AuthorFormSet = formset_factory(AuthorForm, min_num=1, validate_min=True)",
    "o": [
      "At least one author form must be submitted",
      "Exactly one author form must be submitted",
      "Validation ignores empty author forms",
      "At least two author forms must be submitted"
    ]
  },
  {
    "q": "Which function is used to create a formset tied to a model?",
    "c": null,
    "o": [
      "modelformset_factory",
      "ModelFormSetBuilder",
      "create_modelformset",
      "modelform_factory"
    ]
  },
  {
    "q": "What does the following code create?",
    "c": "BookFormSet = modelformset_factory(Book, fields=['title'], extra=1)",
    "o": [
      "A ModelFormSet for the Book model with one extra empty form",
      "A single ModelForm for the Book model",
      "A formset that excludes the title field",
      "A formset that requires all Book fields"
    ]
  },
  {
    "q": "What does the `queryset` argument in a ModelFormSet control?",
    "c": null,
    "o": [
      "Which model instances are included in the formset",
      "How many empty forms are generated",
      "Whether the formset validates fields",
      "The database model used by the formset"
    ]
  },
  {
    "q": "What does this ModelFormSet configuration do?",
    "c": "BookFormSet = modelformset_factory(Book, fields=['title'], queryset=Book.objects.filter(published=True))",
    "o": [
      "Includes only published books in the formset",
      "Validates only books without a title",
      "Restricts users from adding new books",
      "Automatically publishes books upon save"
    ]
  },
  {
    "q": "What is the effect of setting `can_delete=True` in a ModelFormSet?",
    "c": null,
    "o": [
      "Allows users to mark existing objects for deletion via a checkbox",
      "Deletes objects immediately upon form rendering",
      "Prevents any form from being deleted",
      "Automatically deletes invalid forms"
    ]
  },
  {
    "q": "What does the following code do?",
    "c": "formset = BookFormSet(request.POST, queryset=Book.objects.none())",
    "o": [
      "Creates a formset with no initial model instances (for new objects)",
      "Deletes all books from the database",
      "Validates only empty forms",
      "Loads all books from the database"
    ]
  },
  {
    "q": "What does overriding `clean()` in a ModelFormSet allow?",
    "c": null,
    "o": [
      "Perform validation across multiple model forms in the set",
      "Override default widgets for the forms",
      "Change model field values before saving",
      "Customize the queryset used in the formset"
    ]
  },
  {
    "q": "What does this custom clean method ensure?",
    "c": "class BaseAuthorFormSet(BaseModelFormSet):\n    def clean(self):\n        if any(self.errors):\n            return\n        emails = []\n        for form in self.forms:\n            email = form.cleaned_data.get('email')\n            if email in emails:\n                raise forms.ValidationError('Duplicate email addresses are not allowed')\n            emails.append(email)",
    "o": [
      "Prevents duplicate email addresses in the formset",
      "Ensures at least one email is filled",
      "Automatically corrects duplicate emails",
      "Marks all duplicate emails as valid"
    ]
  },
  {
    "q": "What does the `instance` argument in an inline formset bind to?",
    "c": null,
    "o": [
      "The parent model object whose related objects are being edited",
      "The model class being edited",
      "The queryset of related objects",
      "The formset’s base class"
    ]
  },
  {
    "q": "What does this inline formset factory produce?",
    "c": "ChapterFormSet = inlineformset_factory(Book, Chapter, fields=['title'], extra=2)",
    "o": [
      "A formset to edit Chapter objects related to a Book, with 2 extra empty forms",
      "A formset that edits only Book objects",
      "A formset with 2 Chapter forms only",
      "A single form to edit a Book and its Chapters"
    ]
  },
  {
    "q": "Which parameter in an inline formset ensures that at least one related object is submitted?",
    "c": null,
    "o": [
      "min_num",
      "required",
      "must_exist",
      "ensure_one"
    ]
  },
  {
    "q": "What is the effect of setting `validate_max=True` with `max_num` in a formset?",
    "c": null,
    "o": [
      "Raises a validation error if more than `max_num` forms are submitted",
      "Automatically deletes extra forms beyond max_num",
      "Limits rendering to max_num forms only",
      "Ignores any form beyond max_num without error"
    ]
  },
  {
    "q": "What is the main purpose of `inlineformset_factory` in Django?",
    "c": null,
    "o": [
      "To create a formset for managing related objects of a parent model",
      "To generate a formset for unrelated models",
      "To replace `modelformset_factory` entirely",
      "To automatically save data without validation"
    ]
  },
  {
    "q": "What does this inline formset configuration achieve?",
    "c": "ChapterFormSet = inlineformset_factory(Book, Chapter, fields=['title'], can_delete=True)",
    "o": [
      "Allows adding, editing, and deleting Chapter objects related to a Book",
      "Restricts deletion of related Chapter objects",
      "Creates an inline formset for unrelated models",
      "Automatically deletes all Chapter objects on form submission"
    ]
  },
  {
    "q": "What happens if you pass `instance=book` when initializing an inline formset?",
    "c": null,
    "o": [
      "The formset is bound to the given Book and its related objects",
      "The formset ignores the parent model and acts independently",
      "Only empty forms are created",
      "The formset becomes read-only"
    ]
  },
  {
    "q": "What does the following code ensure?",
    "c": "formset = ChapterFormSet(request.POST, instance=book)\nif formset.is_valid():\n    formset.save()",
    "o": [
      "Saves all Chapter objects related to the given Book instance",
      "Deletes all existing chapters before saving new ones",
      "Validates chapters without saving",
      "Updates only the Book instance, not chapters"
    ]
  },
  {
    "q": "What happens if `commit=False` is passed to `formset.save()`?",
    "c": null,
    "o": [
      "Returns unsaved model instances, allowing custom modifications before saving",
      "Immediately saves the instances without validation",
      "Skips creating model instances entirely",
      "Deletes unsaved instances automatically"
    ]
  },
  {
    "q": "What does this snippet do before saving?",
    "c": "instances = formset.save(commit=False)\nfor obj in instances:\n    obj.created_by = request.user\n    obj.save()",
    "o": [
      "Adds custom logic to each unsaved instance before committing to the database",
      "Skips saving the instances entirely",
      "Deletes all related objects",
      "Creates duplicates of the same instances"
    ]
  },
  {
    "q": "Which inline formset parameter enables ordering of related objects?",
    "c": null,
    "o": [
      "can_order",
      "ordering",
      "sort_by",
      "order_fields"
    ]
  },
  {
    "q": "What is stored in the `ORDER` field when `can_order=True`?",
    "c": null,
    "o": [
      "The integer value indicating the display order of the form",
      "The primary key of the model instance",
      "A boolean flag for ordering",
      "The timestamp of when the form was created"
    ]
  },
  {
    "q": "What does this code enforce?",
    "c": "class BaseChapterFormSet(BaseInlineFormSet):\n    def clean(self):\n        if any(self.errors):\n            return\n        if not any(form.cleaned_data and not form.cleaned_data.get('DELETE', False) for form in self.forms):\n            raise forms.ValidationError('At least one chapter is required')",
    "o": [
      "Requires at least one Chapter form to be submitted",
      "Prevents all Chapter forms from being deleted",
      "Rejects duplicate Chapter titles",
      "Marks all Chapter forms as optional"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` return in the case of an inline formset?",
    "c": null,
    "o": [
      "A list of unsaved child model instances",
      "The parent instance only",
      "A queryset of all related objects",
      "Nothing, since inline formsets must save immediately"
    ]
  },
  {
    "q": "Why would you wrap formset saving in a transaction?",
    "c": null,
    "o": [
      "To ensure all forms save successfully or none do",
      "To improve form rendering speed",
      "To automatically delete duplicate forms",
      "To prevent validation errors from showing"
    ]
  },
  {
    "q": "What does this code achieve?",
    "c": "from django.db import transaction\n\nwith transaction.atomic():\n    parent_form.save()\n    child_formset.save()",
    "o": [
      "Ensures parent and child objects are saved together atomically",
      "Skips validation for parent and child objects",
      "Forces all forms to save regardless of errors",
      "Deletes all child objects before saving"
    ]
  },
  {
    "q": "What is a common limitation of Django inline formsets?",
    "c": null,
    "o": [
      "They do not natively support nested relationships (e.g., grandchild models)",
      "They cannot be used with ForeignKey relations",
      "They do not allow adding extra forms",
      "They cannot validate uniqueness"
    ]
  },
  {
    "q": "What technique is often used to implement nested formsets?",
    "c": null,
    "o": [
      "Custom management forms and manually handling child formsets",
      "Using inlineformset_factory with depth=2",
      "Overriding Django’s default template loader",
      "Registering nested formsets in urls.py"
    ]
  },
  {
    "q": "What does this code illustrate?",
    "c": "def save_all(parent_form, child_formset, grandchild_formsets):\n    with transaction.atomic():\n        parent = parent_form.save()\n        children = child_formset.save(commit=False)\n        for child, gc_formset in zip(children, grandchild_formsets):\n            child.parent = parent\n            child.save()\n            gc_formset.instance = child\n            gc_formset.save()",
    "o": [
      "A custom approach to saving nested formsets with parent-child-grandchild relationships",
      "Automatic creation of inline formsets for all relationships",
      "Validation bypass for nested models",
      "Built-in Django support for 3-level inline formsets"
    ]
  },
  {
    "q": "Which Django feature helps manage formsets across multiple models in a single transaction?",
    "c": null,
    "o": [
      "transaction.atomic",
      "formset_factory",
      "validate_all",
      "context_processors"
    ]
  },
  {
    "q": "What is the role of the management form in Django formsets?",
    "c": null,
    "o": [
      "It keeps track of total forms, initial forms, and deleted forms",
      "It validates model uniqueness constraints",
      "It automatically saves forms to the database",
      "It defines which fields appear in the form"
    ]
  },
  {
    "q": "What happens if the management form in a formset is missing or tampered with?",
    "c": null,
    "o": [
      "Django raises a ManagementForm data error",
      "The formset still processes normally",
      "Only the first form in the set is validated",
      "All forms are silently discarded"
    ]
  },
  {
    "q": "What does this code enforce?",
    "c": "class BaseOrderFormSet(BaseInlineFormSet):\n    def clean(self):\n        if any(self.errors):\n            return\n        total = sum(form.cleaned_data.get('quantity', 0) for form in self.forms if not form.cleaned_data.get('DELETE', False))\n        if total < 1:\n            raise forms.ValidationError('At least one item must be ordered')",
    "o": [
      "Ensures that the total quantity across all forms is at least 1",
      "Prevents duplicate order quantities",
      "Limits maximum quantity to 1",
      "Disables deletion of order forms"
    ]
  },
  {
    "q": "What is the significance of `formset.is_valid()` before calling `formset.save()`?",
    "c": null,
    "o": [
      "Ensures that all forms in the formset pass validation before saving",
      "Automatically saves all forms without errors",
      "Skips the management form validation",
      "Guarantees commit=False saving"
    ]
  },
  {
    "q": "What is the difference between `clean_<fieldname>()` and `clean()` in a Django Form?",
    "c": null,
    "o": [
      "`clean_<fieldname>()` validates a single field, while `clean()` validates across multiple fields",
      "Both perform the same function",
      "`clean()` runs before field cleaning, `clean_<fieldname>()` runs after",
      "`clean_<fieldname>()` skips validation if `clean()` is defined"
    ]
  },
  {
    "q": "What does this code accomplish?",
    "c": "class ContactForm(forms.Form):\n    email = forms.EmailField()\n\n    def clean_email(self):\n        email = self.cleaned_data['email']\n        if not email.endswith('@example.com'):\n            raise forms.ValidationError('Only @example.com emails allowed')\n        return email",
    "o": [
      "Restricts the email field to addresses ending with @example.com",
      "Rejects all valid emails",
      "Automatically converts all emails to @example.com",
      "Performs no validation at all"
    ]
  },
  {
    "q": "When overriding `clean()` in a Form, what should you always do?",
    "c": null,
    "o": [
      "Call `super().clean()` to preserve existing validation logic",
      "Skip returning cleaned data",
      "Delete `self.cleaned_data` manually",
      "Return `None` explicitly"
    ]
  },
  {
    "q": "What does this `clean()` method enforce?",
    "c": "def clean(self):\n    cleaned_data = super().clean()\n    start = cleaned_data.get('start_date')\n    end = cleaned_data.get('end_date')\n    if start and end and start > end:\n        raise forms.ValidationError('Start date cannot be after end date')\n    return cleaned_data",
    "o": [
      "Validates that start_date is not later than end_date",
      "Prevents start_date from being empty",
      "Automatically swaps start and end dates",
      "Rejects all dates provided"
    ]
  },
  {
    "q": "Which argument allows you to attach a custom widget to a field in a ModelForm?",
    "c": null,
    "o": [
      "widgets in the form’s Meta class",
      "fields in the form’s Meta class",
      "labels in the form’s Meta class",
      "validators in the form’s Meta class"
    ]
  },
  {
    "q": "What does this ModelForm customization do?",
    "c": "class ProfileForm(forms.ModelForm):\n    class Meta:\n        model = Profile\n        fields = ['bio']\n        widgets = {\n            'bio': forms.Textarea(attrs={'rows':4, 'cols':40})\n        }",
    "o": [
      "Uses a Textarea widget for the bio field with custom size",
      "Removes the bio field from the form",
      "Validates bio length automatically",
      "Converts the bio field into a hidden field"
    ]
  },
  {
    "q": "Which option allows adding extra validators to a ModelForm field?",
    "c": null,
    "o": [
      "Defining the field explicitly in the form and adding validators",
      "Using the Meta class `extra_validators` attribute",
      "Overriding the model’s `save()` method",
      "Editing the formset management form"
    ]
  },
  {
    "q": "What does this code enforce?",
    "c": "class UserForm(forms.ModelForm):\n    class Meta:\n        model = User\n        fields = ['username']\n\n    def clean_username(self):\n        username = self.cleaned_data['username']\n        if User.objects.filter(username=username).exists():\n            raise forms.ValidationError('Username already taken')\n        return username",
    "o": [
      "Prevents duplicate usernames from being submitted",
      "Automatically appends a number to duplicate usernames",
      "Converts usernames to lowercase",
      "Allows only admin usernames"
    ]
  },
  {
    "q": "What does overriding a field in a ModelForm do?",
    "c": null,
    "o": [
      "It replaces the automatically generated field with the custom definition",
      "It disables validation for that field",
      "It removes the field from the form completely",
      "It makes the field read-only by default"
    ]
  },
  {
    "q": "What happens if you exclude a field in ModelForm’s Meta but still use it in the template?",
    "c": null,
    "o": [
      "The field won’t render because it isn’t part of the form",
      "It will render as a hidden field",
      "It will automatically pull from the model",
      "It raises an error at server startup"
    ]
  },
  {
    "q": "Which Django app provides functionality for multi-step form handling?",
    "c": null,
    "o": [
      "django-formtools",
      "django-wizard",
      "django-multiform",
      "django-steps"
    ]
  },
  {
    "q": "What does this snippet represent?",
    "c": "from formtools.wizard.views import SessionWizardView\n\nclass ContactWizard(SessionWizardView):\n    form_list = [ContactForm1, ContactForm2]",
    "o": [
      "A multi-step form wizard storing data in session",
      "A single form with two models",
      "A ModelFormSet with two forms",
      "A nested inline formset"
    ]
  },
  {
    "q": "What is the difference between SessionWizardView and CookieWizardView?",
    "c": null,
    "o": [
      "SessionWizardView stores progress in the session, CookieWizardView stores it in browser cookies",
      "SessionWizardView works only with POST, CookieWizardView only with GET",
      "CookieWizardView supports CSRF, SessionWizardView does not",
      "CookieWizardView can only be used for file uploads"
    ]
  },
  {
    "q": "What does this do inside a FormWizard step?",
    "c": "def done(self, form_list, **kwargs):\n    data = [form.cleaned_data for form in form_list]\n    return render(self.request, 'done.html', {'data': data})",
    "o": [
      "Processes all cleaned form data after the wizard completes",
      "Renders only the last form step",
      "Deletes all wizard session data",
      "Automatically saves data to the database"
    ]
  },
  {
    "q": "Which Django form field is typically used for file uploads?",
    "c": null,
    "o": [
      "FileField",
      "UploadField",
      "AttachmentField",
      "DocumentField"
    ]
  },
  {
    "q": "What does this form definition enable?",
    "c": "class UploadForm(forms.Form):\n    file = forms.FileField()",
    "o": [
      "Allows uploading a file via the form",
      "Restricts submission to text files only",
      "Creates a hidden input field",
      "Auto-saves files to the model"
    ]
  },
  {
    "q": "Which attribute must be added to an HTML form tag to support file uploads?",
    "c": null,
    "o": [
      "enctype=\"multipart/form-data\"",
      "method=\"file\"",
      "type=\"upload\"",
      "accept=\"all\""
    ]
  },
  {
    "q": "What does this do in a form view?",
    "c": "if request.method == 'POST':\n    form = UploadForm(request.POST, request.FILES)\n    if form.is_valid():\n        handle_uploaded_file(request.FILES['file'])",
    "o": [
      "Processes uploaded files along with other form data",
      "Ignores the uploaded file",
      "Forces validation to skip",
      "Deletes uploaded files after save"
    ]
  },
  {
    "q": "How can you dynamically add a field to a form in Django?",
    "c": null,
    "o": [
      "By adding it to `self.fields` in the form’s __init__ method",
      "By editing the Meta class at runtime",
      "By modifying the model definition directly",
      "By changing the form template"
    ]
  },
  {
    "q": "What does this dynamic form code do?",
    "c": "class SurveyForm(forms.Form):\n    def __init__(self, *args, questions=None, **kwargs):\n        super().__init__(*args, **kwargs)\n        for q in questions or []:\n            self.fields[q.slug] = forms.CharField(label=q.text)",
    "o": [
      "Generates form fields dynamically from a list of questions",
      "Hardcodes only one text field",
      "Creates duplicate form fields",
      "Ignores the given question list"
    ]
  },
  {
    "q": "Which Django form field is specifically used for handling images?",
    "c": null,
    "o": [
      "ImageField",
      "FileField",
      "PictureField",
      "MediaField"
    ]
  },
  {
    "q": "What extra validation does Django’s ImageField provide compared to FileField?",
    "c": null,
    "o": [
      "Checks that the uploaded file is a valid image",
      "Automatically resizes images",
      "Converts all images to JPEG",
      "Prevents uploading large files"
    ]
  },
  {
    "q": "What widget does Django use by default for FileField and ImageField?",
    "c": null,
    "o": [
      "ClearableFileInput",
      "FileUploadWidget",
      "InputFile",
      "UploadFieldWidget"
    ]
  },
  {
    "q": "What does this form definition allow?",
    "c": "class PhotoForm(forms.Form):\n    image = forms.ImageField(widget=forms.ClearableFileInput(attrs={'multiple': True}))",
    "o": [
      "Uploading multiple images in a single form field",
      "Restricting uploads to only one image",
      "Forcing images to be stored as text",
      "Automatically validating image dimensions"
    ]
  },
  {
    "q": "What must be installed for ImageField to work properly?",
    "c": null,
    "o": [
      "Pillow",
      "OpenCV",
      "NumPy",
      "Requests"
    ]
  },
  {
    "q": "What does this code accomplish?",
    "c": "def clean_file(self):\n    file = self.cleaned_data['file']\n    if file.size > 5*1024*1024:\n        raise forms.ValidationError('File too large (max 5MB)')\n    return file",
    "o": [
      "Validates that the uploaded file size does not exceed 5MB",
      "Automatically compresses large files",
      "Rejects all files under 5MB",
      "Deletes large files after upload"
    ]
  },
  {
    "q": "How can you restrict file uploads to only PDF files in a FileField?",
    "c": null,
    "o": [
      "By using `FileExtensionValidator(['pdf'])`",
      "By setting `max_length=pdf`",
      "By adding `accept='pdf'` in the widget",
      "By overriding the model’s save method"
    ]
  },
  {
    "q": "What does this widget configuration enforce?",
    "c": "file = forms.FileField(widget=forms.ClearableFileInput(attrs={'accept':'.pdf'}))",
    "o": [
      "Restricts file selection in the browser to PDF files",
      "Forces all uploads to be renamed .pdf",
      "Validates PDF files on the server",
      "Automatically converts files to PDF"
    ]
  },
  {
    "q": "Which attribute is used to access uploaded files in the request?",
    "c": null,
    "o": [
      "request.FILES",
      "request.uploads",
      "request.data.files",
      "request.MEDIA"
    ]
  },
  {
    "q": "What does this upload handler do?",
    "c": "if form.is_valid():\n    for f in request.FILES.getlist('images'):\n        handle_uploaded_file(f)",
    "o": [
      "Processes multiple uploaded files from a single input field",
      "Deletes duplicate files before saving",
      "Stores files directly in the session",
      "Validates image resolution automatically"
    ]
  },
  {
    "q": "What is the purpose of a FormSet in Django?",
    "c": null,
    "o": [
      "To manage multiple instances of the same form together",
      "To validate forms across different models",
      "To convert forms into REST APIs",
      "To handle form validation without HTML templates"
    ]
  },
  {
    "q": "Which helper function is used to create a FormSet from a form class?",
    "c": null,
    "o": [
      "formset_factory",
      "make_formset",
      "create_formset",
      "build_formset"
    ]
  },
  {
    "q": "What does this code do?",
    "c": "BookFormSet = formset_factory(BookForm, extra=2)",
    "o": [
      "Creates a formset with two extra empty BookForm instances",
      "Creates a formset with exactly two forms allowed",
      "Restricts the formset to two submitted forms",
      "Prevents submission of empty BookForms"
    ]
  },
  {
    "q": "What is the difference between a FormSet and a ModelFormSet?",
    "c": null,
    "o": [
      "FormSet works with plain forms, ModelFormSet works with model-backed forms",
      "FormSet automatically saves data, ModelFormSet does not",
      "ModelFormSet cannot be validated",
      "FormSet supports file uploads, ModelFormSet does not"
    ]
  },
  {
    "q": "Which function creates an inline formset for related models?",
    "c": null,
    "o": [
      "inlineformset_factory",
      "related_formset_factory",
      "child_formset_factory",
      "foreignkey_formset_factory"
    ]
  },
  {
    "q": "What does this inline formset definition do?",
    "c": "AuthorBookFormSet = inlineformset_factory(Author, Book, fields=['title'], extra=1)",
    "o": [
      "Creates a formset to manage Book objects related to an Author",
      "Creates one form to edit both Author and Book models",
      "Restricts the Book model to one author only",
      "Deletes all related Book objects automatically"
    ]
  },
  {
    "q": "What does the `extra` parameter in formsets control?",
    "c": null,
    "o": [
      "The number of additional empty forms displayed",
      "The maximum number of forms allowed",
      "Whether the formset is saved automatically",
      "The order of form validation"
    ]
  },
  {
    "q": "What does this formset configuration enforce?",
    "c": "BookFormSet = modelformset_factory(Book, fields=['title'], can_delete=True)",
    "o": [
      "Allows deletion of Book instances directly from the formset",
      "Forbids creating new Book instances",
      "Locks all Book fields as read-only",
      "Deletes all Books automatically on save"
    ]
  },
  {
    "q": "Which management fields are automatically added to a Django formset?",
    "c": null,
    "o": [
      "TOTAL_FORMS, INITIAL_FORMS, MIN_NUM_FORMS, MAX_NUM_FORMS",
      "FORM_COUNT, MAX_FORMS, MIN_FORMS",
      "TOTAL_ENTRIES, INITIAL_ENTRIES",
      "FORM_ID, FORM_KEY"
    ]
  },
  {
    "q": "What does this enforce in a formset?",
    "c": "BookFormSet = modelformset_factory(Book, fields=['title'], min_num=2, validate_min=True)",
    "o": [
      "Requires at least 2 valid Book forms to be submitted",
      "Limits the formset to 2 forms maximum",
      "Creates exactly 2 empty Book forms",
      "Skips validation for the first 2 forms"
    ]
  },
  {
    "q": "How can you restrict the queryset used in a ModelFormSet?",
    "c": null,
    "o": [
      "By passing a queryset argument when instantiating the formset",
      "By overriding the form’s __init__ method",
      "By filtering inside the template",
      "By modifying the model’s Meta class"
    ]
  },
  {
    "q": "What does this configuration achieve?",
    "c": "BookFormSet = modelformset_factory(Book, fields=['title'])\nformset = BookFormSet(queryset=Book.objects.filter(published=True))",
    "o": [
      "Only includes published books in the formset",
      "Forces all new books to be published",
      "Deletes unpublished books automatically",
      "Adds a published field to every form"
    ]
  },
  {
    "q": "How do you prevent users from adding extra objects in an inline formset?",
    "c": null,
    "o": [
      "Set extra=0 in inlineformset_factory",
      "Set max_num=0",
      "Set validate_max=True",
      "Remove management form fields"
    ]
  },
  {
    "q": "What does this code do in a CBV?",
    "c": "def form_valid(self, form):\n    context = self.get_context_data()\n    formset = context['formset']\n    if formset.is_valid():\n        self.object = form.save()\n        formset.instance = self.object\n        formset.save()\n        return super().form_valid(form)\n    else:\n        return self.form_invalid(form)",
    "o": [
      "Saves both the main form and related inline formset together",
      "Validates only the form and ignores the formset",
      "Deletes all related inline objects",
      "Skips saving the parent form"
    ]
  },
  {
    "q": "How do you enforce a maximum number of related objects in an inline formset?",
    "c": null,
    "o": [
      "Use max_num with validate_max=True",
      "Use limit= in the formset factory",
      "Set MAX_FORMS in Meta",
      "Override the model’s clean()"
    ]
  },
  {
    "q": "What happens if can_delete=True is set in a formset?",
    "c": null,
    "o": [
      "A checkbox is added to each form to allow marking objects for deletion",
      "All objects are deleted automatically on save",
      "The formset cannot create new objects",
      "The formset skips validation"
    ]
  },
  {
    "q": "What does this inline formset restrict?",
    "c": "AuthorBookFormSet = inlineformset_factory(\n    Author, Book, fields=['title'], extra=0, can_delete=True, max_num=5, validate_max=True\n)",
    "o": [
      "Allows deleting books but enforces a maximum of 5 per author",
      "Prevents deletion but allows up to 5 authors",
      "Restricts all authors to exactly 5 books",
      "Deletes any book beyond 5 automatically"
    ]
  },
  {
    "q": "How can you perform custom validation across multiple forms in a formset?",
    "c": null,
    "o": [
      "Override the formset’s clean() method",
      "Override the Meta class in the model",
      "Write a custom validator on each field",
      "Change the management form"
    ]
  },
  {
    "q": "What does this clean() method do?",
    "c": "def clean(self):\n    super().clean()\n    titles = []\n    for form in self.forms:\n        if form.cleaned_data:\n            title = form.cleaned_data.get('title')\n            if title in titles:\n                raise forms.ValidationError('Duplicate titles are not allowed')\n            titles.append(title)",
    "o": [
      "Prevents duplicate book titles across all forms in the formset",
      "Ensures every form has a title field",
      "Auto-saves duplicate titles",
      "Limits the title length"
    ]
  },
  {
    "q": "How do you associate an inline formset with its parent instance in a view?",
    "c": null,
    "o": [
      "By setting formset.instance to the parent object",
      "By passing the parent object in the template",
      "By overriding the model’s save()",
      "By using a ForeignKey with null=True"
    ]
  },
  {
    "q": "Which method renders form fields wrapped in `<p>` tags?",
    "c": null,
    "o": [
      "form.as_p",
      "form.as_table",
      "form.as_ul",
      "form.render_p"
    ]
  },
  {
    "q": "What does calling `form.as_table` do?",
    "c": null,
    "o": [
      "Renders the form fields as table rows",
      "Converts the form to a dictionary",
      "Displays the form without labels",
      "Saves the form in the database"
    ]
  },
  {
    "q": "How can you add a CSS class to a form field widget?",
    "c": "name = forms.CharField(widget=forms.TextInput(attrs={'class': 'form-control'}))",
    "o": [
      "Adds the 'form-control' CSS class to the input field",
      "Changes the field type to 'form-control'",
      "Validates input with 'form-control' regex",
      "Automatically makes the field required"
    ]
  },
  {
    "q": "Which widget is used by default for a BooleanField in Django forms?",
    "c": null,
    "o": [
      "CheckboxInput",
      "RadioSelect",
      "TextInput",
      "Select"
    ]
  },
  {
    "q": "What does this widget configuration enforce?",
    "c": "birthdate = forms.DateField(widget=forms.DateInput(attrs={'type':'date'}))",
    "o": [
      "Renders the field as a date picker in HTML5 browsers",
      "Validates that the date is in ISO format",
      "Automatically sets today’s date as default",
      "Restricts the field to only past dates"
    ]
  },
  {
    "q": "Which widget would you use for selecting multiple options?",
    "c": null,
    "o": [
      "SelectMultiple",
      "RadioSelect",
      "Select",
      "TextInput"
    ]
  },
  {
    "q": "What does this widget enforce in a form?",
    "c": "color = forms.CharField(widget=forms.TextInput(attrs={'type':'color'}))",
    "o": [
      "Renders an HTML5 color picker input",
      "Restricts input to text only",
      "Automatically validates hexadecimal colors",
      "Converts all values to lowercase"
    ]
  },
  {
    "q": "Which widget would you use to allow file uploads?",
    "c": null,
    "o": [
      "FileInput",
      "ClearableFileInput",
      "TextInput",
      "SelectFile"
    ]
  },
  {
    "q": "What does `form.non_field_errors()` return?",
    "c": null,
    "o": [
      "Errors not tied to a specific field but raised at form level",
      "All validation errors in the form",
      "Widget rendering issues",
      "Field errors for hidden fields only"
    ]
  },
  {
    "q": "What does this widget configuration enforce?",
    "c": "agree = forms.BooleanField(widget=forms.CheckboxInput(attrs={'required': True}))",
    "o": [
      "Ensures the checkbox must be ticked before submitting",
      "Hides the checkbox from the form",
      "Validates checkbox value on the server only",
      "Converts checkbox into a toggle switch"
    ]
  },
  {
    "q": "Which method is called to perform validation on a single field in a Django form?",
    "c": null,
    "o": [
      "clean_<fieldname>()",
      "clean()",
      "is_valid()",
      "full_clean()"
    ]
  },
  {
    "q": "What does this code achieve?",
    "c": "def clean_email(self):\n    email = self.cleaned_data['email']\n    if not email.endswith('@example.com'):\n        raise forms.ValidationError('Email must be from example.com')\n    return email",
    "o": [
      "Ensures the email ends with @example.com",
      "Rejects all emails by default",
      "Automatically converts email to lowercase",
      "Replaces the email with a dummy value"
    ]
  },
  {
    "q": "Which method validates the entire form including cross-field checks?",
    "c": null,
    "o": [
      "clean()",
      "clean_<field>()",
      "is_valid()",
      "save()"
    ]
  },
  {
    "q": "What does calling `form.is_valid()` do?",
    "c": null,
    "o": [
      "Runs validation and populates form.cleaned_data if valid",
      "Saves the form to the database",
      "Resets all form errors",
      "Clears form.cleaned_data"
    ]
  },
  {
    "q": "What does this form method enforce?",
    "c": "def clean(self):\n    cleaned_data = super().clean()\n    start = cleaned_data.get('start_date')\n    end = cleaned_data.get('end_date')\n    if start and end and start > end:\n        raise forms.ValidationError('Start date must be before end date')",
    "o": [
      "Validates cross-field relationship between start_date and end_date",
      "Forces both fields to be required",
      "Auto-swaps start and end dates",
      "Ignores empty values in both fields"
    ]
  },
  {
    "q": "Which method is responsible for running all validation steps on a form?",
    "c": null,
    "o": [
      "full_clean()",
      "is_valid()",
      "clean()",
      "validate()"
    ]
  },
  {
    "q": "What happens if a ValidationError is raised inside clean_<field>()?",
    "c": null,
    "o": [
      "The error is attached to that specific field",
      "The form is marked invalid without specifying the field",
      "The form skips validation",
      "The form still passes validation"
    ]
  },
  {
    "q": "What does this clean() method achieve?",
    "c": "def clean(self):\n    cleaned_data = super().clean()\n    password = cleaned_data.get('password')\n    confirm = cleaned_data.get('confirm_password')\n    if password and confirm and password != confirm:\n        raise forms.ValidationError('Passwords do not match')",
    "o": [
      "Ensures password and confirm_password fields match",
      "Encrypts the password automatically",
      "Makes confirm_password optional",
      "Skips saving if password is empty"
    ]
  },
  {
    "q": "When does Django automatically call full_clean() on a form?",
    "c": null,
    "o": [
      "When calling is_valid()",
      "When instantiating the form",
      "When saving the form",
      "When rendering the form"
    ]
  },
  {
    "q": "What does this code snippet enforce?",
    "c": "class UserForm(forms.Form):\n    age = forms.IntegerField()\n\n    def clean_age(self):\n        age = self.cleaned_data['age']\n        if age < 18:\n            raise forms.ValidationError('Must be at least 18 years old')\n        return age",
    "o": [
      "Validates that age must be at least 18",
      "Converts age to a string",
      "Sets a default age of 18",
      "Skips validation for age field"
    ]
  },
  {
    "q": "What does `form.save(commit=False)` do in a ModelForm?",
    "c": null,
    "o": [
      "Returns an unsaved model instance",
      "Saves the instance immediately",
      "Deletes the instance from the database",
      "Rolls back all changes"
    ]
  },
  {
    "q": "What does this ModelForm code achieve?",
    "c": "class BookForm(forms.ModelForm):\n    class Meta:\n        model = Book\n        fields = ['title']\n\n    def save(self, commit=True):\n        instance = super().save(commit=False)\n        instance.title = instance.title.upper()\n        if commit:\n            instance.save()\n        return instance",
    "o": [
      "Converts the book title to uppercase before saving",
      "Prevents saving unless commit=False",
      "Validates that the title is unique",
      "Creates a dummy title if empty"
    ]
  },
  {
    "q": "When is model.clean() called during ModelForm validation?",
    "c": null,
    "o": [
      "Automatically as part of form.full_clean()",
      "Only when manually invoked",
      "After the model is saved",
      "Only for ForeignKey fields"
    ]
  },
  {
    "q": "What does this code enforce?",
    "c": "class RegistrationForm(forms.ModelForm):\n    class Meta:\n        model = User\n        fields = ['username', 'email']\n\n    def clean_email(self):\n        email = self.cleaned_data['email']\n        if User.objects.filter(email=email).exists():\n            raise forms.ValidationError('Email already exists')\n        return email",
    "o": [
      "Prevents duplicate emails during registration",
      "Converts emails to lowercase automatically",
      "Allows only @gmail.com emails",
      "Sets email as optional"
    ]
  },
  {
    "q": "Which Meta option in a ModelForm ensures all model fields are included?",
    "c": null,
    "o": [
      "fields = '__all__'",
      "include = '*'",
      "all_fields = True",
      "model_fields = 'all'"
    ]
  },
  {
    "q": "What does this save(commit=False) usage allow?",
    "c": "form = BookForm(request.POST)\nif form.is_valid():\n    book = form.save(commit=False)\n    book.author = request.user\n    book.save()",
    "o": [
      "Assigns the logged-in user as the book’s author before saving",
      "Skips validation of the book form",
      "Deletes the existing book instance",
      "Ignores the author field completely"
    ]
  },
  {
    "q": "What happens if exclude is used in a ModelForm Meta class?",
    "c": null,
    "o": [
      "All model fields are included except those listed in exclude",
      "Only excluded fields are shown in the form",
      "Excluded fields are still validated",
      "Excluded fields must be set manually"
    ]
  },
  {
    "q": "What does this custom clean() enforce?",
    "c": "class EventForm(forms.ModelForm):\n    class Meta:\n        model = Event\n        fields = ['name', 'start', 'end']\n\n    def clean(self):\n        cleaned_data = super().clean()\n        if cleaned_data['start'] >= cleaned_data['end']:\n            raise forms.ValidationError('End must be after start')",
    "o": [
      "Ensures event start is before end",
      "Forces both fields to be required",
      "Auto-swaps start and end times",
      "Assigns default start time"
    ]
  },
  {
    "q": "What does `instance=form.save(commit=False)` allow in ModelForms?",
    "c": null,
    "o": [
      "Modify the instance before final save",
      "Skip validation errors",
      "Automatically rollback on error",
      "Delete the model instance"
    ]
  },
  {
    "q": "What happens if both fields and exclude are defined in a ModelForm Meta class?",
    "c": null,
    "o": [
      "fields takes precedence and exclude is ignored",
      "exclude takes precedence and fields is ignored",
      "Both must match exactly or error is raised",
      "It automatically includes all fields"
    ]
  },
  {
    "q": "What is the main purpose of a FormSet in Django?",
    "c": null,
    "o": [
      "To manage multiple instances of the same form at once",
      "To render a form with custom widgets",
      "To validate only model forms",
      "To group unrelated forms together"
    ]
  },
  {
    "q": "Which factory function is used to create a FormSet from a custom form class?",
    "c": null,
    "o": [
      "formset_factory()",
      "FormSet()",
      "make_formset()",
      "generate_formset()"
    ]
  },
  {
    "q": "What does this code create?",
    "c": "BookFormSet = modelformset_factory(Book, fields=['title', 'author'], extra=2)",
    "o": [
      "A ModelFormSet for Book with 2 extra empty forms",
      "A single form for Book with 2 fields",
      "Two separate ModelForms for Book",
      "A FormSet without model binding"
    ]
  },
  {
    "q": "Which attribute in a FormSet ensures at least one form must be submitted?",
    "c": null,
    "o": [
      "min_num",
      "extra",
      "max_num",
      "required"
    ]
  },
  {
    "q": "What does this inline formset creation do?",
    "c": "AuthorBookFormSet = inlineformset_factory(Author, Book, fields=['title'], extra=1)",
    "o": [
      "Creates a FormSet to manage Book objects related to an Author",
      "Creates a FormSet that validates unrelated models",
      "Ensures each Author has exactly one Book",
      "Automatically deletes orphaned Book objects"
    ]
  },
  {
    "q": "Which FormSet property contains all validation errors?",
    "c": null,
    "o": [
      "non_form_errors()",
      "errors",
      "form_errors",
      "all_errors"
    ]
  },
  {
    "q": "What does the parameter `extra=0` do in a FormSet?",
    "c": null,
    "o": [
      "Prevents any extra empty forms from being displayed",
      "Forces all forms to be empty",
      "Disables validation",
      "Deletes existing forms automatically"
    ]
  },
  {
    "q": "What does this code enforce?",
    "c": "class BaseBookFormSet(BaseModelFormSet):\n    def clean(self):\n        super().clean()\n        titles = []\n        for form in self.forms:\n            if form.cleaned_data:\n                title = form.cleaned_data['title']\n                if title in titles:\n                    raise forms.ValidationError('Books must have unique titles')\n                titles.append(title)",
    "o": [
      "Ensures no duplicate book titles are submitted in the FormSet",
      "Sets all titles to lowercase",
      "Limits title length to unique values only",
      "Automatically merges duplicate book objects"
    ]
  },
  {
    "q": "What is the default maximum number of forms in a FormSet if max_num is not specified?",
    "c": null,
    "o": [
      "1000",
      "100",
      "10",
      "Unlimited"
    ]
  },
  {
    "q": "What does the `can_delete=True` option in a FormSet allow?",
    "c": null,
    "o": [
      "Users can mark individual forms for deletion",
      "All forms are deleted after submission",
      "Validation skips deleted forms",
      "The entire FormSet can be removed"
    ]
  },
  {
    "q": "Which factory function is used to create a ModelFormSet for a specific model?",
    "c": null,
    "o": [
      "modelformset_factory()",
      "formset_factory()",
      "inlineformset_factory()",
      "ModelSet()"
    ]
  },
  {
    "q": "What does `can_order=True` do in a FormSet?",
    "c": null,
    "o": [
      "Adds an ORDER field to each form so users can control form ordering",
      "Sorts all forms alphabetically",
      "Forces forms to be submitted in a fixed order",
      "Limits the number of forms in the set"
    ]
  },
  {
    "q": "What does this inline formset allow?",
    "c": "BookFormSet = inlineformset_factory(Author, Book, fields=['title'], extra=2, can_delete=True)",
    "o": [
      "Manage Book objects related to an Author, with ability to delete books",
      "Ensure each Author has exactly 2 books",
      "Delete the Author if any Book is deleted",
      "Prevent duplicate book titles across all Authors"
    ]
  },
  {
    "q": "Which method validates all forms in a FormSet, including cross-form checks?",
    "c": null,
    "o": [
      "clean() in the BaseFormSet subclass",
      "is_valid() on each form individually",
      "save(commit=False)",
      "validate_all()"
    ]
  },
  {
    "q": "What does `max_num=5` do when passed to formset_factory?",
    "c": null,
    "o": [
      "Limits the total number of forms in the FormSet to 5",
      "Displays exactly 5 extra blank forms",
      "Forces at least 5 forms to be submitted",
      "Deletes forms beyond the fifth one"
    ]
  },
  {
    "q": "What does this custom BaseFormSet clean() enforce?",
    "c": "class BaseTeamFormSet(BaseFormSet):\n    def clean(self):\n        super().clean()\n        players = [form.cleaned_data.get('player') for form in self.forms if form.cleaned_data]\n        if len(players) != len(set(players)):\n            raise forms.ValidationError('Duplicate players are not allowed')",
    "o": [
      "Prevents duplicate players from being added across the FormSet",
      "Forces exactly one player per form",
      "Deletes duplicate players automatically",
      "Validates player age for duplicates"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` return for a ModelFormSet?",
    "c": null,
    "o": [
      "A list of unsaved model instances",
      "A single unsaved model instance",
      "Nothing until commit=True",
      "A QuerySet of saved objects"
    ]
  },
  {
    "q": "Which argument ensures at least 3 valid forms must be submitted in a FormSet?",
    "c": null,
    "o": [
      "min_num=3",
      "extra=3",
      "required=3",
      "force_num=3"
    ]
  },
  {
    "q": "What does this code snippet enforce?",
    "c": "AuthorBookFormSet = inlineformset_factory(Author, Book, fields=['title'], extra=1, validate_max=True, max_num=3)",
    "o": [
      "Prevents an Author from having more than 3 related Book forms",
      "Forces exactly 3 books for each Author",
      "Automatically deletes extra books beyond 3",
      "Allows unlimited books per Author"
    ]
  },
  {
    "q": "What does `formset.non_form_errors()` return?",
    "c": null,
    "o": [
      "Validation errors that apply to the entire FormSet, not individual forms",
      "Errors for each specific field in the FormSet",
      "A dictionary of all field errors",
      "System-level Django errors"
    ]
  },
  {
    "q": "Which argument is used in a form field to customize its HTML widget?",
    "c": null,
    "o": [
      "widget",
      "render",
      "input_type",
      "ui"
    ]
  },
  {
    "q": "What will this field render as?",
    "c": "email = forms.EmailField(widget=forms.TextInput(attrs={'placeholder': 'Enter email'}))",
    "o": [
      "A text input field with 'Enter email' placeholder",
      "An email input that validates format automatically",
      "A password input with placeholder",
      "A hidden input field"
    ]
  },
  {
    "q": "Which widget is the default for a BooleanField?",
    "c": null,
    "o": [
      "CheckboxInput",
      "RadioSelect",
      "Select",
      "TextInput"
    ]
  },
  {
    "q": "What does this form field configuration achieve?",
    "c": "age = forms.IntegerField(label='Your Age', help_text='Enter in years', widget=forms.NumberInput)",
    "o": [
      "Displays an integer input with custom label and help text",
      "Validates only positive integers",
      "Forces minimum age of 18",
      "Hides the field from rendering"
    ]
  },
  {
    "q": "Which widget is typically used for ChoiceField?",
    "c": null,
    "o": [
      "Select",
      "TextInput",
      "RadioSelect",
      "CheckboxInput"
    ]
  },
  {
    "q": "What does this form field render as?",
    "c": "password = forms.CharField(widget=forms.PasswordInput)",
    "o": [
      "A password input that hides typed characters",
      "A text field with max length 8",
      "A hidden field storing password hash",
      "A text area for password entry"
    ]
  },
  {
    "q": "How do you render a form in a Django template with all fields and labels?",
    "c": null,
    "o": [
      "{{ form.as_p }}",
      "{{ form.as_html }}",
      "{{ form.render }}",
      "{{ form.output }}"
    ]
  },
  {
    "q": "What is the default widget for DateField?",
    "c": null,
    "o": [
      "TextInput",
      "DateInput",
      "DateTimeInput",
      "NumberInput"
    ]
  },
  {
    "q": "What does this code achieve?",
    "c": "class ContactForm(forms.Form):\n    message = forms.CharField(widget=forms.Textarea(attrs={'rows': 5, 'cols': 40}))",
    "o": [
      "Renders a multi-line text area with 5 rows and 40 columns",
      "Limits message length to 40 characters",
      "Converts message to uppercase automatically",
      "Displays message as read-only"
    ]
  },
  {
    "q": "Which parameter in a form field allows setting an initial value?",
    "c": null,
    "o": [
      "initial",
      "default",
      "value",
      "preset"
    ]
  },
  {
    "q": "Which widget is typically used for a MultipleChoiceField?",
    "c": null,
    "o": [
      "CheckboxSelectMultiple",
      "Select",
      "RadioSelect",
      "TextInput"
    ]
  },
  {
    "q": "What does this field render as?",
    "c": "choices = forms.ChoiceField(choices=[('m','Male'),('f','Female')], widget=forms.RadioSelect)",
    "o": [
      "A group of radio buttons for gender selection",
      "A dropdown select for gender",
      "Two checkboxes for gender selection",
      "A text input that accepts 'm' or 'f'"
    ]
  },
  {
    "q": "Which widget is most appropriate for FileField?",
    "c": null,
    "o": [
      "ClearableFileInput",
      "TextInput",
      "CheckboxInput",
      "HiddenInput"
    ]
  },
  {
    "q": "What does this field configuration do?",
    "c": "secret = forms.CharField(widget=forms.HiddenInput())",
    "o": [
      "Renders a hidden input field not visible to users",
      "Renders a password field that hides characters",
      "Encrypts the input automatically",
      "Displays the field in read-only mode"
    ]
  },
  {
    "q": "Which method renders a form’s fields wrapped in <tr> tags?",
    "c": null,
    "o": [
      "{{ form.as_table }}",
      "{{ form.as_p }}",
      "{{ form.as_ul }}",
      "{{ form.as_div }}"
    ]
  },
  {
    "q": "What does this snippet achieve in a form?",
    "c": "photo = forms.ImageField(widget=forms.ClearableFileInput(attrs={'multiple': True}))",
    "o": [
      "Allows uploading multiple image files with clear option",
      "Forces uploading exactly one image",
      "Hides uploaded images automatically",
      "Converts images to base64"
    ]
  },
  {
    "q": "Which widget is the default for a TextField in a ModelForm?",
    "c": null,
    "o": [
      "Textarea",
      "TextInput",
      "Select",
      "HiddenInput"
    ]
  },
  {
    "q": "What does the `attrs` dictionary in a widget control?",
    "c": null,
    "o": [
      "HTML attributes of the rendered input element",
      "Validation rules for the form",
      "Database field constraints",
      "Django ORM query behavior"
    ]
  },
  {
    "q": "What does this widget customization do?",
    "c": "name = forms.CharField(widget=forms.TextInput(attrs={'class': 'form-control', 'maxlength': '50'}))",
    "o": [
      "Applies CSS class and HTML maxlength to the text input",
      "Forces server-side validation of max length",
      "Limits name length only in the database",
      "Applies JavaScript validation automatically"
    ]
  },
  {
    "q": "Which widget should be used for BooleanField if you want Yes/No radio buttons?",
    "c": null,
    "o": [
      "RadioSelect",
      "CheckboxInput",
      "Select",
      "ToggleInput"
    ]
  },
  {
    "q": "How do you override a model field’s default widget in a ModelForm?",
    "c": "class UserForm(forms.ModelForm):\n    class Meta:\n        model = User\n        fields = ['username']\n        widgets = {\n            'username': forms.TextInput(attrs={'class': 'custom-class'})\n        }",
    "o": [
      "By defining a widgets dictionary in Meta class",
      "By subclassing the model field directly",
      "By using form.override_widget()",
      "By editing the model’s database schema"
    ]
  },
  {
    "q": "Which widget is most suitable for a field where multiple files can be uploaded?",
    "c": null,
    "o": [
      "ClearableFileInput(attrs={'multiple': True})",
      "FilePathInput",
      "FileUploadSelect",
      "MultiFileTextInput"
    ]
  },
  {
    "q": "What does this custom widget class achieve?",
    "c": "class ColorPicker(forms.TextInput):\n    input_type = 'color'\n\ncolor = forms.CharField(widget=ColorPicker)",
    "o": [
      "Renders a color picker input in the form",
      "Renders a text area for color names",
      "Validates that input is a CSS color string",
      "Converts color to RGB automatically"
    ]
  },
  {
    "q": "Which widget argument is used to display an empty option in a Select field?",
    "c": null,
    "o": [
      "empty_label",
      "initial",
      "blank_choice",
      "default_option"
    ]
  },
  {
    "q": "What does this code snippet achieve?",
    "c": "agree = forms.BooleanField(widget=forms.CheckboxInput(attrs={'class': 'form-check'}))",
    "o": [
      "Renders a checkbox with custom CSS class",
      "Forces agree to always be checked",
      "Hides the checkbox input",
      "Validates only True values"
    ]
  },
  {
    "q": "Which widget would you use to allow selection of multiple options from a dropdown?",
    "c": null,
    "o": [
      "SelectMultiple",
      "CheckboxSelectMultiple",
      "MultiSelectTextInput",
      "Select"
    ]
  },
  {
    "q": "What does this customization achieve?",
    "c": "dob = forms.DateField(widget=forms.DateInput(attrs={'type': 'date'}))",
    "o": [
      "Renders an HTML5 date picker input",
      "Forces date in YYYY-MM-DD format only",
      "Converts all dates to datetime objects",
      "Stores date in database automatically"
    ]
  },
  {
    "q": "Which widget is most appropriate for a form field that should not be shown but must carry a value?",
    "c": null,
    "o": [
      "HiddenInput",
      "TextInput(attrs={'hidden':True})",
      "InvisibleField",
      "NoneWidget"
    ]
  },
  {
    "q": "What does this snippet achieve?",
    "c": "rating = forms.IntegerField(widget=forms.NumberInput(attrs={'min':1, 'max':5}))",
    "o": [
      "Renders a number input restricted between 1 and 5",
      "Forces server-side validation of numbers only",
      "Creates a dropdown with numbers 1–5",
      "Rejects any negative number automatically"
    ]
  },
  {
    "q": "What happens if you use `form.as_ul` in a template?",
    "c": null,
    "o": [
      "Each form field is rendered as a list item in <ul>",
      "The form renders with unordered styling",
      "It applies Bootstrap styling automatically",
      "The form renders as <ul> without field labels"
    ]
  },
  {
    "q": "Which argument allows customizing the error text for a specific validation failure in a form field?",
    "c": null,
    "o": [
      "error_messages",
      "validators",
      "help_text",
      "invalid_text"
    ]
  },
  {
    "q": "What does this code achieve?",
    "c": "username = forms.CharField(\n    max_length=20,\n    error_messages={'required': 'Please enter a username'}\n)",
    "o": [
      "Displays a custom message if the username is left empty",
      "Forces username to always be unique",
      "Validates only alphabetic usernames",
      "Prevents empty usernames at the database level"
    ]
  },
  {
    "q": "Which method on a form returns errors not associated with a specific field?",
    "c": null,
    "o": [
      "non_field_errors()",
      "all_errors()",
      "general_errors()",
      "form_errors()"
    ]
  },
  {
    "q": "What does this form configuration do?",
    "c": "email = forms.EmailField(error_messages={'invalid': 'Enter a valid email address'})",
    "o": [
      "Overrides the default invalid email message",
      "Validates only Gmail addresses",
      "Marks email as optional",
      "Hides the email field when invalid"
    ]
  },
  {
    "q": "Which attribute of a Form instance contains all validation errors after is_valid() is called?",
    "c": null,
    "o": [
      "errors",
      "cleaned_data",
      "invalid_fields",
      "messages"
    ]
  },
  {
    "q": "What does this do in a form?",
    "c": "def clean(self):\n    cleaned_data = super().clean()\n    password = cleaned_data.get('password')\n    confirm = cleaned_data.get('confirm_password')\n    if password and confirm and password != confirm:\n        raise forms.ValidationError('Passwords do not match')",
    "o": [
      "Adds a non-field error if passwords differ",
      "Marks the password field as optional",
      "Encrypts the password automatically",
      "Hides confirm_password from rendering"
    ]
  },
  {
    "q": "Which key in error_messages allows customization for exceeding max_length?",
    "c": null,
    "o": [
      "max_length",
      "length_error",
      "overflow",
      "too_long"
    ]
  },
  {
    "q": "What does this field definition achieve?",
    "c": "age = forms.IntegerField(error_messages={'invalid': 'Please enter a number'})",
    "o": [
      "Shows a custom error message when input is not an integer",
      "Restricts input to only positive numbers",
      "Forces age between 18 and 99",
      "Hides the age field if invalid"
    ]
  },
  {
    "q": "Which method on a form field provides access to error messages for that field?",
    "c": null,
    "o": [
      "field.errors",
      "field.get_errors()",
      "field.error_messages",
      "field.messages"
    ]
  },
  {
    "q": "What is the role of ValidationError in Django forms?",
    "c": null,
    "o": [
      "To raise and store error messages during validation",
      "To stop the form from saving to the database",
      "To enforce uniqueness constraints",
      "To hide invalid fields from rendering"
    ]
  },
  {
    "q": "Which FormSet parameter ensures at least a minimum number of forms must be submitted?",
    "c": null,
    "o": [
      "min_num",
      "extra",
      "max_num",
      "validate_min"
    ]
  },
  {
    "q": "What does this inline formset configuration enforce?",
    "c": "BookFormSet = inlineformset_factory(Author, Book, fields=['title'], extra=1, min_num=1, validate_min=True)",
    "o": [
      "Requires at least one Book form per Author",
      "Allows zero books per Author",
      "Forces exactly one book per Author",
      "Prevents deletion of books"
    ]
  },
  {
    "q": "Which method allows you to add validation across all forms in a FormSet?",
    "c": null,
    "o": [
      "clean() in a BaseFormSet subclass",
      "clean_<field>() in each form",
      "validate_all_forms()",
      "full_clean()"
    ]
  },
  {
    "q": "What does `can_delete=True` do in a ModelFormSet?",
    "c": null,
    "o": [
      "Allows marking forms for deletion using a checkbox",
      "Deletes all forms automatically on save",
      "Disables validation for deleted forms",
      "Removes the FormSet entirely"
    ]
  },
  {
    "q": "What happens if `validate_max=True` and max_num is set in a FormSet?",
    "c": null,
    "o": [
      "Prevents submitting more forms than max_num",
      "Automatically trims excess forms",
      "Ignores forms beyond max_num",
      "Raises an error if min_num is not met"
    ]
  },
  {
    "q": "How do you restrict a ModelFormSet to a subset of model instances?",
    "c": null,
    "o": [
      "Pass a queryset argument when instantiating the formset",
      "Set fields=[] in Meta class",
      "Use exclude in Meta class",
      "Override form.save() method"
    ]
  },
  {
    "q": "Which attribute contains validation errors for the entire FormSet, not specific forms?",
    "c": null,
    "o": [
      "non_form_errors()",
      "form_errors",
      "all_errors",
      "base_errors"
    ]
  },
  {
    "q": "What does this BaseFormSet clean() enforce?",
    "c": "class BaseTeamFormSet(BaseFormSet):\n    def clean(self):\n        super().clean()\n        names = [form.cleaned_data.get('player') for form in self.forms if form.cleaned_data]\n        if len(names) != len(set(names)):\n            raise forms.ValidationError('Duplicate players are not allowed')",
    "o": [
      "Prevents duplicate players across the FormSet",
      "Forces exactly one player per form",
      "Deletes duplicate players automatically",
      "Validates only the first form"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` return in a ModelFormSet?",
    "c": null,
    "o": [
      "A list of unsaved model instances",
      "A single unsaved instance",
      "Nothing until commit=True",
      "A QuerySet of saved instances"
    ]
  },
  {
    "q": "Which parameter controls the number of extra empty forms displayed in a FormSet?",
    "c": null,
    "o": [
      "extra",
      "min_num",
      "max_num",
      "validate_extra"
    ]
  },
  {
    "q": "What does the `can_order=True` parameter do in a FormSet?",
    "c": null,
    "o": [
      "Adds an ORDER field to let users control form ordering",
      "Sorts all forms alphabetically",
      "Locks the form submission order",
      "Validates order of fields automatically"
    ]
  },
  {
    "q": "Which method is used to enforce validation across all forms in a FormSet?",
    "c": null,
    "o": [
      "clean() in BaseFormSet subclass",
      "clean_<field>() in each form",
      "validate_all_forms()",
      "formset_full_clean()"
    ]
  },
  {
    "q": "What happens if a form in a ModelFormSet is marked for deletion and commit=True is used?",
    "c": null,
    "o": [
      "The corresponding model instance is deleted from the database",
      "The form is ignored but saved",
      "It raises a validation error",
      "Only the first form is deleted"
    ]
  },
  {
    "q": "How can you limit an inline formset to only certain related objects?",
    "c": null,
    "o": [
      "Pass a filtered queryset when instantiating the formset",
      "Use fields=[] in the formset factory",
      "Set extra=0",
      "Override form.save()"
    ]
  },
  {
    "q": "What does `validate_min=True` enforce in a FormSet?",
    "c": null,
    "o": [
      "Requires at least min_num forms to be submitted",
      "Automatically fills missing forms",
      "Skips validation for empty forms",
      "Limits maximum number of forms"
    ]
  },
  {
    "q": "What does `formset.non_form_errors()` return?",
    "c": null,
    "o": [
      "Errors that are related to the FormSet as a whole, not specific forms",
      "Errors for each individual form",
      "All field errors combined",
      "System-level Django errors"
    ]
  },
  {
    "q": "Which FormSet parameter specifies the maximum allowed forms?",
    "c": null,
    "o": [
      "max_num",
      "extra",
      "min_num",
      "validate_max"
    ]
  },
  {
    "q": "How can you customize error messages for FormSet-wide validation?",
    "c": null,
    "o": [
      "Raise forms.ValidationError with a custom message in clean()",
      "Use error_messages argument in Meta class",
      "Set custom_message in formset_factory",
      "Override is_valid() method"
    ]
  },
  {
    "q": "What does this code enforce in an inline formset?",
    "c": "class BaseBookFormSet(BaseInlineFormSet):\n    def clean(self):\n        super().clean()\n        titles = [form.cleaned_data.get('title') for form in self.forms if form.cleaned_data]\n        if len(titles) != len(set(titles)):\n            raise forms.ValidationError('Book titles must be unique per author')",
    "o": [
      "Ensures no duplicate book titles for the same author",
      "Forces each author to have one book",
      "Deletes duplicate book objects automatically",
      "Validates only the first form in the set"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` allow in a ModelFormSet?",
    "c": null,
    "o": [
      "Modify instances before saving them to the database",
      "Skip validation entirely",
      "Delete all instances automatically",
      "Returns a QuerySet of saved instances"
    ]
  },
  {
    "q": "What does `empty_permitted=True` do in a FormSet form?",
    "c": null,
    "o": [
      "Allows the form to be submitted without data",
      "Marks the form as read-only",
      "Prevents deletion of the form",
      "Requires all fields to be filled"
    ]
  },
  {
    "q": "Which attribute in a FormSet form indicates if it is marked for deletion?",
    "c": null,
    "o": [
      "DELETE",
      "is_deleted",
      "marked_for_delete",
      "remove"
    ]
  },
  {
    "q": "What happens if a form in a FormSet is empty and empty_permitted=False?",
    "c": null,
    "o": [
      "Raises a validation error",
      "Automatically fills default values",
      "Skips the form silently",
      "Marks it for deletion"
    ]
  },
  {
    "q": "Which FormSet method can be used to access forms in order according to can_order=True?",
    "c": null,
    "o": [
      "ordered_forms",
      "sorted_forms",
      "form_order",
      "get_ordered()"
    ]
  },
  {
    "q": "What does setting `extra=3` in an inline formset do?",
    "c": null,
    "o": [
      "Adds three extra blank forms for new objects",
      "Limits total forms to 3",
      "Automatically deletes three forms",
      "Forces exactly three related objects"
    ]
  },
  {
    "q": "How can you enforce unique relationships in an inline formset?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and raise ValidationError for duplicates",
      "Set unique=True in the model",
      "Use unique_together in the form Meta",
      "Disable extra forms"
    ]
  },
  {
    "q": "Which method returns only the forms that are not marked for deletion?",
    "c": null,
    "o": [
      "forms",
      "active_forms",
      "remaining_forms",
      "live_forms"
    ]
  },
  {
    "q": "What happens if a user tries to submit more forms than max_num in a FormSet with validate_max=True?",
    "c": null,
    "o": [
      "Raises a ValidationError",
      "Skips extra forms silently",
      "Deletes forms beyond max_num automatically",
      "Automatically increases max_num"
    ]
  },
  {
    "q": "Which attribute in a BaseInlineFormSet contains the parent instance?",
    "c": null,
    "o": [
      "instance",
      "parent",
      "related_instance",
      "object"
    ]
  },
  {
    "q": "What does `formset.is_valid()` return if one of the forms marked for deletion is invalid?",
    "c": null,
    "o": [
      "Still returns True, since deleted forms are ignored",
      "Always returns False",
      "Raises a ValidationError immediately",
      "Skips validation for the FormSet entirely"
    ]
  },
  {
    "q": "Which method can be overridden to customize saving behavior of an inline formset?",
    "c": null,
    "o": [
      "save()",
      "full_clean()",
      "is_valid()",
      "clean()"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` allow when working with an inline formset?",
    "c": null,
    "o": [
      "Modify the related instances before saving to the database",
      "Skip validation entirely",
      "Delete all related objects automatically",
      "Return a QuerySet of saved instances"
    ]
  },
  {
    "q": "Which property in a BaseInlineFormSet contains only the forms with data?",
    "c": null,
    "o": [
      "forms",
      "empty_forms",
      "active_forms",
      "non_deleted_forms"
    ]
  },
  {
    "q": "What happens if `ordered_forms` is used in a FormSet with can_order=True?",
    "c": null,
    "o": [
      "Returns forms sorted according to the ORDER field",
      "Ignores ORDER values",
      "Returns forms in the original database order",
      "Deletes unordered forms"
    ]
  },
  {
    "q": "How can you validate that no two forms in an inline formset have the same value for a field?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and check for duplicates",
      "Set unique=True in the model field",
      "Use extra=0 in the formset",
      "Set can_delete=True"
    ]
  },
  {
    "q": "Which attribute in an inline formset points to the parent object instance?",
    "c": null,
    "o": [
      "instance",
      "parent",
      "related_instance",
      "owner"
    ]
  },
  {
    "q": "What happens when a form marked for deletion contains invalid data?",
    "c": null,
    "o": [
      "It is ignored during formset validation",
      "Raises a ValidationError",
      "Prevents the entire formset from saving",
      "Triggers clean_<field>() on the deleted form"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` allows adding extra blank forms?",
    "c": null,
    "o": [
      "extra",
      "min_num",
      "max_num",
      "validate_extra"
    ]
  },
  {
    "q": "How do you enforce a maximum number of forms in an inline formset?",
    "c": null,
    "o": [
      "Use max_num with validate_max=True",
      "Use extra=0",
      "Override save() method",
      "Set can_delete=False"
    ]
  },
  {
    "q": "What does this BaseInlineFormSet clean() snippet enforce?",
    "c": "class BaseItemFormSet(BaseInlineFormSet):\n    def clean(self):\n        super().clean()\n        codes = [form.cleaned_data.get('code') for form in self.forms if form.cleaned_data and not form.cleaned_data.get('DELETE', False)]\n        if len(codes) != len(set(codes)):\n            raise forms.ValidationError('Codes must be unique')",
    "o": [
      "Ensures all non-deleted forms have unique 'code' values",
      "Prevents any form from being deleted",
      "Requires at least one form",
      "Automatically assigns unique codes"
    ]
  },
  {
    "q": "What happens if a form in an inline formset is empty and `empty_permitted=False`?",
    "c": null,
    "o": [
      "A validation error is raised",
      "The form is skipped silently",
      "It is marked for deletion automatically",
      "The form is saved with default values"
    ]
  },
  {
    "q": "Which attribute in a BaseInlineFormSet can be used to access forms marked for deletion?",
    "c": null,
    "o": [
      "deleted_forms",
      "DELETE",
      "forms_to_delete",
      "removed_forms"
    ]
  },
  {
    "q": "What does the `queryset` argument do when instantiating a ModelFormSet?",
    "c": null,
    "o": [
      "Limits the forms to a subset of model instances",
      "Specifies which fields are required",
      "Overrides widget attributes",
      "Determines extra forms to display"
    ]
  },
  {
    "q": "How can you enforce that a field value is unique across all forms in an inline formset?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and check for duplicates",
      "Set unique=True on the model field",
      "Use min_num=1",
      "Use can_delete=False"
    ]
  },
  {
    "q": "Which property returns forms sorted according to the ORDER field when `can_order=True`?",
    "c": null,
    "o": [
      "ordered_forms",
      "sorted_forms",
      "forms",
      "active_forms"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` return for an inline formset?",
    "c": null,
    "o": [
      "A list of unsaved related model instances",
      "A single unsaved instance",
      "Nothing until commit=True",
      "A QuerySet of saved instances"
    ]
  },
  {
    "q": "What happens if `validate_min=True` and fewer than `min_num` forms are submitted?",
    "c": null,
    "o": [
      "A ValidationError is raised",
      "Extra blank forms are automatically added",
      "The formset is ignored",
      "Forms are deleted automatically"
    ]
  },
  {
    "q": "How can you ensure non-deleted forms in an inline formset have unique values for a specific field?",
    "c": null,
    "o": [
      "Check uniqueness in clean() and exclude forms marked for DELETE",
      "Use unique=True in the model field",
      "Set max_num=number of forms",
      "Use extra=0"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet points to the parent instance?",
    "c": null,
    "o": [
      "instance",
      "parent",
      "related_instance",
      "object"
    ]
  },
  {
    "q": "What happens if a form marked for deletion contains invalid data in a ModelFormSet?",
    "c": null,
    "o": [
      "It is ignored during validation",
      "Raises a ValidationError",
      "Prevents the entire formset from saving",
      "Triggers clean_<field>() anyway"
    ]
  },
  {
    "q": "What does `extra=0` do when used in an inline formset?",
    "c": null,
    "o": [
      "Prevents any additional blank forms from being displayed",
      "Forces all forms to be blank",
      "Automatically deletes extra instances",
      "Limits the total forms to 0 permanently"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` allows forms to be marked for deletion?",
    "c": null,
    "o": [
      "can_delete=True",
      "extra=1",
      "validate_min=True",
      "min_num=1"
    ]
  },
  {
    "q": "What does `formset.is_valid()` return if all forms marked for deletion are invalid?",
    "c": null,
    "o": [
      "True, because deleted forms are ignored during validation",
      "False, validation fails",
      "Raises a ValidationError",
      "Depends on commit=False"
    ]
  },
  {
    "q": "How can you limit an inline formset to only related objects that meet a condition?",
    "c": null,
    "o": [
      "Pass a filtered queryset when instantiating the formset",
      "Set extra=0",
      "Override save() method",
      "Use can_delete=False"
    ]
  },
  {
    "q": "Which attribute contains the forms that are not marked for deletion?",
    "c": null,
    "o": [
      "forms",
      "active_forms",
      "remaining_forms",
      "non_deleted_forms"
    ]
  },
  {
    "q": "What happens if `validate_max=True` and the user submits more than `max_num` forms?",
    "c": null,
    "o": [
      "Raises a ValidationError",
      "Truncates extra forms automatically",
      "Skips forms beyond max_num",
      "Increases max_num automatically"
    ]
  },
  {
    "q": "Which method in a BaseInlineFormSet is used to enforce validation across all forms?",
    "c": null,
    "o": [
      "clean()",
      "full_clean()",
      "validate_all()",
      "save(commit=False)"
    ]
  },
  {
    "q": "How do you enforce that a particular field is unique across all non-deleted forms?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and check for duplicates excluding DELETE forms",
      "Use unique=True in the model",
      "Set max_num=number of forms",
      "Use extra=0"
    ]
  },
  {
    "q": "Which property returns forms sorted by the ORDER field when `can_order=True`?",
    "c": null,
    "o": [
      "ordered_forms",
      "forms",
      "active_forms",
      "sorted_forms"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` allow in an inline formset?",
    "c": null,
    "o": [
      "Modify related instances before saving to the database",
      "Skip validation entirely",
      "Delete all instances automatically",
      "Return a QuerySet of saved instances"
    ]
  },
  {
    "q": "What does `empty_permitted=True` allow in a FormSet form?",
    "c": null,
    "o": [
      "The form can be submitted without any data",
      "The form is always required",
      "It marks the form for deletion automatically",
      "Validation is skipped entirely"
    ]
  },
  {
    "q": "Which attribute indicates if a form in a FormSet is marked for deletion?",
    "c": null,
    "o": [
      "DELETE",
      "is_deleted",
      "marked_for_delete",
      "remove"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` specifies how many blank forms to display for new objects?",
    "c": null,
    "o": [
      "extra",
      "min_num",
      "max_num",
      "validate_min"
    ]
  },
  {
    "q": "What happens if `validate_min=True` and fewer than `min_num` forms are submitted?",
    "c": null,
    "o": [
      "A ValidationError is raised",
      "Extra blank forms are automatically added",
      "The formset is ignored",
      "Forms are deleted automatically"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet points to the parent instance?",
    "c": null,
    "o": [
      "instance",
      "parent",
      "related_instance",
      "object"
    ]
  },
  {
    "q": "How can you enforce that no two forms have the same value for a field, excluding deleted forms?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and check for duplicates excluding DELETE forms",
      "Set unique=True on the model field",
      "Use min_num=number of forms",
      "Use extra=0"
    ]
  },
  {
    "q": "Which property returns only forms not marked for deletion?",
    "c": null,
    "o": [
      "forms",
      "active_forms",
      "remaining_forms",
      "non_deleted_forms"
    ]
  },
  {
    "q": "What happens when `can_order=True` is set in a FormSet?",
    "c": null,
    "o": [
      "An ORDER field is added to let users control form order",
      "Forms are automatically sorted alphabetically",
      "ORDER field is ignored",
      "Forms are deleted if unordered"
    ]
  },
  {
    "q": "Which method should be overridden to enforce cross-form validation in a FormSet?",
    "c": null,
    "o": [
      "clean()",
      "full_clean()",
      "validate_all()",
      "save(commit=False)"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` return in a ModelFormSet?",
    "c": null,
    "o": [
      "A list of unsaved model instances",
      "A single unsaved instance",
      "Nothing until commit=True",
      "A QuerySet of saved instances"
    ]
  },
  {
    "q": "What happens if `extra` is set to 0 in a FormSet?",
    "c": null,
    "o": [
      "No extra blank forms are displayed",
      "All forms are required",
      "Extra forms are deleted automatically",
      "FormSet cannot be submitted"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` allows forms to be deleted by the user?",
    "c": null,
    "o": [
      "can_delete=True",
      "extra=1",
      "validate_min=True",
      "min_num=1"
    ]
  },
  {
    "q": "What does the `ordered_forms` property return in a FormSet with `can_order=True`?",
    "c": null,
    "o": [
      "Forms sorted according to the ORDER field",
      "Forms in the original database order",
      "Only forms marked for deletion",
      "Forms with data only"
    ]
  },
  {
    "q": "What happens if a form marked for deletion contains invalid data?",
    "c": null,
    "o": [
      "It is ignored during formset validation",
      "A ValidationError is raised",
      "Prevents the formset from saving",
      "Triggers clean_<field>() anyway"
    ]
  },
  {
    "q": "Which method is used to enforce validation across all forms in a FormSet?",
    "c": null,
    "o": [
      "clean()",
      "full_clean()",
      "validate_all()",
      "save(commit=False)"
    ]
  },
  {
    "q": "How can you ensure that a field value is unique across all non-deleted forms in an inline formset?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and check for duplicates excluding DELETE forms",
      "Set unique=True on the model field",
      "Use max_num to limit forms",
      "Set extra=0"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet points to the parent object instance?",
    "c": null,
    "o": [
      "instance",
      "parent",
      "related_instance",
      "owner"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` allow in a ModelFormSet?",
    "c": null,
    "o": [
      "Modify instances before saving them to the database",
      "Skip validation entirely",
      "Delete all instances automatically",
      "Return a QuerySet of saved instances"
    ]
  },
  {
    "q": "What happens if `validate_max=True` and more than `max_num` forms are submitted?",
    "c": null,
    "o": [
      "A ValidationError is raised",
      "Extra forms are truncated automatically",
      "Forms beyond max_num are ignored",
      "max_num is increased automatically"
    ]
  },
  {
    "q": "Which attribute returns forms that are not marked for deletion?",
    "c": null,
    "o": [
      "forms",
      "active_forms",
      "remaining_forms",
      "non_deleted_forms"
    ]
  },
  {
    "q": "What does setting `can_delete=True` in a ModelFormSet do?",
    "c": null,
    "o": [
      "Allows forms to be marked for deletion using a checkbox",
      "Deletes all instances automatically on save",
      "Prevents validation on deleted forms",
      "Disables saving of the FormSet"
    ]
  },
  {
    "q": "Which property returns only forms with data in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "forms",
      "empty_forms",
      "active_forms",
      "non_deleted_forms"
    ]
  },
  {
    "q": "What happens if a form marked for deletion contains invalid data?",
    "c": null,
    "o": [
      "It is ignored during validation",
      "Raises a ValidationError",
      "Prevents the formset from saving",
      "Triggers field-level clean methods"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` controls the number of extra blank forms?",
    "c": null,
    "o": [
      "extra",
      "min_num",
      "max_num",
      "validate_min"
    ]
  },
  {
    "q": "What does `empty_permitted=True` allow for a FormSet form?",
    "c": null,
    "o": [
      "The form can be submitted without data",
      "The form is required",
      "Marks the form for deletion",
      "Skips validation entirely"
    ]
  },
  {
    "q": "How can you ensure a field value is unique across all non-deleted forms in an inline formset?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and check for duplicates excluding DELETE forms",
      "Set unique=True on the model field",
      "Limit max_num to one",
      "Use extra=0"
    ]
  },
  {
    "q": "Which method should be overridden to validate all forms together in a FormSet?",
    "c": null,
    "o": [
      "clean()",
      "full_clean()",
      "validate_all()",
      "save(commit=False)"
    ]
  },
  {
    "q": "Which property returns forms sorted according to ORDER field when `can_order=True`?",
    "c": null,
    "o": [
      "ordered_forms",
      "forms",
      "active_forms",
      "sorted_forms"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` return in a ModelFormSet?",
    "c": null,
    "o": [
      "A list of unsaved related model instances",
      "A single unsaved instance",
      "Nothing until commit=True",
      "A QuerySet of saved instances"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet points to the parent instance?",
    "c": null,
    "o": [
      "instance",
      "parent",
      "related_instance",
      "owner"
    ]
  },
  {
    "q": "What does `min_num` in a FormSet enforce?",
    "c": null,
    "o": [
      "Requires at least the specified number of forms to be submitted",
      "Limits the maximum number of forms",
      "Automatically deletes extra forms",
      "Marks forms as optional"
    ]
  },
  {
    "q": "What happens if a FormSet has `validate_min=True` and fewer forms than `min_num` are submitted?",
    "c": null,
    "o": [
      "A ValidationError is raised",
      "Extra forms are automatically added",
      "The FormSet is ignored",
      "Forms are deleted automatically"
    ]
  },
  {
    "q": "Which argument controls the maximum number of forms allowed in a FormSet?",
    "c": null,
    "o": [
      "max_num",
      "extra",
      "min_num",
      "validate_max"
    ]
  },
  {
    "q": "What happens if `validate_max=True` and more than `max_num` forms are submitted?",
    "c": null,
    "o": [
      "A ValidationError is raised",
      "Extra forms are ignored silently",
      "Forms beyond max_num are deleted automatically",
      "max_num is increased automatically"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet contains only forms that are not marked for deletion?",
    "c": null,
    "o": [
      "forms",
      "active_forms",
      "remaining_forms",
      "non_deleted_forms"
    ]
  },
  {
    "q": "How can you enforce uniqueness of a field across all non-deleted forms in an inline formset?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and check duplicates excluding DELETE forms",
      "Use unique=True in the model field",
      "Set max_num=1",
      "Use extra=0"
    ]
  },
  {
    "q": "Which property returns forms sorted by the ORDER field when `can_order=True`?",
    "c": null,
    "o": [
      "ordered_forms",
      "forms",
      "active_forms",
      "sorted_forms"
    ]
  },
  {
    "q": "What does `empty_permitted=True` allow for a FormSet form?",
    "c": null,
    "o": [
      "The form can be submitted without any data",
      "The form is required",
      "It marks the form for deletion",
      "Skips validation entirely"
    ]
  },
  {
    "q": "Which method should be overridden to validate all forms together in a FormSet?",
    "c": null,
    "o": [
      "clean()",
      "full_clean()",
      "validate_all()",
      "save(commit=False)"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` return?",
    "c": null,
    "o": [
      "A list of unsaved model instances",
      "A single unsaved instance",
      "Nothing until commit=True",
      "A QuerySet of saved instances"
    ]
  },
  {
    "q": "What does the `can_order` argument do in a FormSet?",
    "c": null,
    "o": [
      "Adds an ORDER field to allow users to reorder forms",
      "Automatically sorts forms alphabetically",
      "Prevents forms from being reordered",
      "Deletes unordered forms automatically"
    ]
  },
  {
    "q": "Which attribute in a BaseInlineFormSet allows you to access forms that have been marked for deletion?",
    "c": null,
    "o": [
      "deleted_forms",
      "DELETE",
      "forms_to_delete",
      "removed_forms"
    ]
  },
  {
    "q": "If a form in a FormSet is empty and `empty_permitted=False`, what happens?",
    "c": null,
    "o": [
      "A ValidationError is raised",
      "The form is skipped silently",
      "It is marked for deletion automatically",
      "The form is saved with default values"
    ]
  },
  {
    "q": "Which method should be overridden to enforce validation across all forms in a FormSet?",
    "c": null,
    "o": [
      "clean()",
      "full_clean()",
      "validate_all_forms()",
      "save(commit=False)"
    ]
  },
  {
    "q": "How can you enforce that a particular field is unique across all non-deleted forms in an inline formset?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and check for duplicates excluding DELETE forms",
      "Use unique=True in the model field",
      "Set max_num=number of forms",
      "Use extra=0"
    ]
  },
  {
    "q": "Which property returns only forms that are not marked for deletion?",
    "c": null,
    "o": [
      "forms",
      "active_forms",
      "remaining_forms",
      "non_deleted_forms"
    ]
  },
  {
    "q": "What happens if `validate_max=True` and more than `max_num` forms are submitted?",
    "c": null,
    "o": [
      "Raises a ValidationError",
      "Truncates extra forms automatically",
      "Skips forms beyond max_num",
      "Increases max_num automatically"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` allow you to do in a ModelFormSet?",
    "c": null,
    "o": [
      "Modify instances before saving them to the database",
      "Skip validation entirely",
      "Delete all instances automatically",
      "Return a QuerySet of saved instances"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet points to the parent object instance?",
    "c": null,
    "o": [
      "instance",
      "parent",
      "related_instance",
      "owner"
    ]
  },
  {
    "q": "What is the purpose of the `extra` argument in a FormSet?",
    "c": null,
    "o": [
      "Specifies how many extra blank forms to display",
      "Specifies the minimum number of forms",
      "Specifies the maximum number of forms",
      "Specifies forms to be deleted automatically"
    ]
  },
  {
    "q": "How can you perform cross-form validation in an inline formset?",
    "c": null,
    "o": [
      "Override the clean() method in the BaseInlineFormSet",
      "Use validate_unique on the model field",
      "Set can_delete=False",
      "Use extra=0"
    ]
  },
  {
    "q": "Which method allows you to customize saving behavior of all forms in a ModelFormSet?",
    "c": null,
    "o": [
      "save()",
      "full_clean()",
      "is_valid()",
      "clean()"
    ]
  },
  {
    "q": "How can you conditionally prevent deletion of a form in an inline formset?",
    "c": null,
    "o": [
      "Override clean() or save() and check DELETE field",
      "Set can_delete=False in the factory",
      "Use max_num to limit forms",
      "Use extra=0"
    ]
  },
  {
    "q": "Which field type can you use to enforce custom validation in a ModelForm?",
    "c": null,
    "o": [
      "CharField with clean_<field>() method",
      "ForeignKey",
      "ManyToManyField",
      "BooleanField"
    ]
  },
  {
    "q": "What happens when `formset.save(commit=False)` is used with an inline formset?",
    "c": null,
    "o": [
      "Returns unsaved model instances that can be modified before saving",
      "Skips validation entirely",
      "Deletes all related objects",
      "Returns a QuerySet of saved instances"
    ]
  },
  {
    "q": "Which attribute can be used to access only forms with valid data in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "forms",
      "empty_forms",
      "deleted_forms",
      "ordered_forms"
    ]
  },
  {
    "q": "How can you enforce that a numeric field is positive across all forms in a FormSet?",
    "c": null,
    "o": [
      "Implement clean_<field>() in the form class",
      "Set max_value=0",
      "Use extra=0",
      "Override save() method"
    ]
  },
  {
    "q": "How do you validate that related forms in an inline formset have consistent data?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and compare fields across forms",
      "Use unique=True on model fields",
      "Set min_num=1",
      "Use can_delete=False"
    ]
  },
  {
    "q": "Which method is called automatically when formset.is_valid() is called?",
    "c": null,
    "o": [
      "full_clean()",
      "save()",
      "clean()",
      "validate_max()"
    ]
  },
  {
    "q": "How can you add custom validation to a field in a ModelForm?",
    "c": null,
    "o": [
      "Define a clean_<field>() method in the form class",
      "Set unique=True in the model",
      "Use extra=0 in the formset",
      "Override save() method"
    ]
  },
  {
    "q": "How can you skip saving certain forms in a ModelFormSet based on a condition?",
    "c": null,
    "o": [
      "Use formset.save(commit=False) and selectively save instances",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "Which attribute allows access to only forms that have changed data in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "changed_forms",
      "forms",
      "active_forms",
      "ordered_forms"
    ]
  },
  {
    "q": "How can you enforce that a field in one form must match a field in another form in the same FormSet?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and compare fields",
      "Use clean_<field>() in the form class",
      "Set extra=0",
      "Use min_num=1"
    ]
  },
  {
    "q": "What happens if a form marked for deletion has invalid data and `can_delete=True`?",
    "c": null,
    "o": [
      "It is ignored during validation",
      "Raises a ValidationError",
      "Prevents saving of other forms",
      "Triggers field clean methods"
    ]
  },
  {
    "q": "How can you dynamically set the queryset for a field in a ModelForm based on the parent instance?",
    "c": null,
    "o": [
      "Override __init__() in the form class and filter the queryset",
      "Set extra=0",
      "Use min_num=1",
      "Override save() method"
    ]
  },
  {
    "q": "Which method in a BaseInlineFormSet is called automatically when formset.is_valid() is invoked?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_max()"
    ]
  },
  {
    "q": "How can you enforce a custom validator across all forms in an inline formset?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet and apply the validator",
      "Set unique=True on the model field",
      "Use extra=0",
      "Use min_num=1"
    ]
  },
  {
    "q": "Which attribute contains all forms including those marked for deletion in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "forms",
      "deleted_forms",
      "active_forms",
      "ordered_forms"
    ]
  },
  {
    "q": "How can you enforce that numeric fields are positive across all forms in a ModelFormSet?",
    "c": null,
    "o": [
      "Implement clean_<field>() in the form class or override FormSet clean()",
      "Use max_num to limit forms",
      "Set extra=0",
      "Use can_delete=False"
    ]
  },
  {
    "q": "What happens if you call formset.save(commit=False) on an inline formset?",
    "c": null,
    "o": [
      "Returns unsaved model instances for manual processing before saving",
      "Skips validation entirely",
      "Deletes all instances automatically",
      "Returns a QuerySet of saved instances"
    ]
  },
  {
    "q": "Which attribute in a BaseInlineFormSet contains forms that are valid and not marked for deletion, ordered by the model’s ordering?",
    "c": null,
    "o": [
      "ordered_forms",
      "forms",
      "changed_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "What is the role of the management form in Django formsets?",
    "c": null,
    "o": [
      "It keeps track of the number of forms and form indices",
      "It stores CSRF tokens",
      "It validates foreign key relationships",
      "It handles pagination of forms"
    ]
  },
  {
    "q": "What happens if the management form of a formset is missing in the POST data?",
    "c": null,
    "o": [
      "A ManagementForm data is missing error is raised",
      "Formset validates as empty",
      "All forms are treated as deleted",
      "Validation silently fails"
    ]
  },
  {
    "q": "Which property of a BaseInlineFormSet provides a form template for adding new forms dynamically in JavaScript?",
    "c": null,
    "o": [
      "empty_form",
      "forms",
      "initial_forms",
      "management_form"
    ]
  },
  {
    "q": "How can you enforce that a parent instance must be saved before saving its inline formset?",
    "c": null,
    "o": [
      "Ensure the parent instance is saved before calling formset.save()",
      "Use commit=False on the formset",
      "Override clean() in BaseInlineFormSet",
      "Pass save_as_new=True"
    ]
  },
  {
    "q": "If max_num is set in a formset, what happens when more forms than max_num are submitted?",
    "c": null,
    "o": [
      "Extra forms beyond max_num are ignored",
      "Raises ValidationError",
      "Formset fails completely",
      "Excess forms are automatically deleted"
    ]
  },
  {
    "q": "How can you ensure only changed forms are saved in a ModelFormSet?",
    "c": null,
    "o": [
      "Check form.has_changed() before saving",
      "Set extra=0",
      "Use min_num=1",
      "Set validate_max=True"
    ]
  },
  {
    "q": "What does formset.initial_forms return?",
    "c": null,
    "o": [
      "Forms that are bound to existing objects",
      "All submitted forms",
      "Only new extra forms",
      "Only forms marked for deletion"
    ]
  },
  {
    "q": "How can you ensure atomic saving of a parent model and its inline formset?",
    "c": null,
    "o": [
      "Wrap save() calls in a transaction.atomic() block",
      "Set can_delete=False",
      "Use min_num=1",
      "Override __init__()"
    ]
  },
  {
    "q": "Which argument ensures that at least one form in a formset must be submitted?",
    "c": null,
    "o": [
      "min_num",
      "extra",
      "max_num",
      "can_delete"
    ]
  },
  {
    "q": "Which property of a BaseInlineFormSet provides the forms that are bound to existing model instances?",
    "c": null,
    "o": [
      "initial_forms",
      "forms",
      "empty_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "What does `formset.extra` control?",
    "c": null,
    "o": [
      "The number of extra blank forms displayed for adding new objects",
      "The minimum number of required forms",
      "The maximum number of forms allowed",
      "Whether deleted forms are saved"
    ]
  },
  {
    "q": "Which attribute contains forms that are not bound to any model instance in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "empty_forms",
      "forms",
      "initial_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "What is the purpose of `formset.has_changed()`?",
    "c": null,
    "o": [
      "Checks if any form in the formset has changed data",
      "Validates all forms in the formset",
      "Checks if any forms are marked for deletion",
      "Determines the total number of forms"
    ]
  },
  {
    "q": "What happens if `can_order=True` is set in an inline formset?",
    "c": null,
    "o": [
      "An ORDER field is added to allow reordering of forms",
      "Forms are automatically sorted alphabetically",
      "Forms cannot be deleted",
      "Forms are saved in reverse order"
    ]
  },
  {
    "q": "Which attribute contains only the forms that have been marked for deletion?",
    "c": null,
    "o": [
      "deleted_forms",
      "forms",
      "initial_forms",
      "empty_forms"
    ]
  },
  {
    "q": "How can you access the parent instance from a BaseInlineFormSet?",
    "c": null,
    "o": [
      "Using the `instance` attribute",
      "Using `parent_instance`",
      "Using `related_object`",
      "Using `owner`"
    ]
  },
  {
    "q": "Which method is called internally when `formset.is_valid()` is executed?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_max()"
    ]
  },
  {
    "q": "How can you prevent saving a formset if a certain condition fails?",
    "c": null,
    "o": [
      "Raise a ValidationError in clean() or use commit=False and skip saving",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "How can you ensure that a formset is saved atomically with its parent instance?",
    "c": null,
    "o": [
      "Wrap the save operations in a transaction.atomic() block",
      "Set can_delete=False",
      "Use min_num=1",
      "Override formset.__init__()"
    ]
  },
  {
    "q": "What is the purpose of the `management_form` in a Django formset?",
    "c": null,
    "o": [
      "It tracks the number of forms and indices submitted",
      "It stores model instance data",
      "It automatically validates foreign keys",
      "It handles JavaScript rendering of forms"
    ]
  },
  {
    "q": "What error occurs if the management form data is missing when submitting a formset?",
    "c": null,
    "o": [
      "ManagementForm data is missing",
      "ValidationError for empty fields",
      "IntegrityError on database save",
      "No error, formset treats all forms as new"
    ]
  },
  {
    "q": "Which property of a BaseInlineFormSet provides a form template for adding new forms dynamically?",
    "c": null,
    "o": [
      "empty_form",
      "initial_forms",
      "forms",
      "deleted_forms"
    ]
  },
  {
    "q": "How can you ensure that an inline formset only saves forms if the parent instance exists?",
    "c": null,
    "o": [
      "Save the parent instance first, then call formset.save()",
      "Use extra=0",
      "Set min_num=1",
      "Use can_delete=False"
    ]
  },
  {
    "q": "Which attribute contains forms that are currently marked for deletion in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "deleted_forms",
      "forms",
      "initial_forms",
      "empty_forms"
    ]
  },
  {
    "q": "How can you enforce that one field in a form matches another field across all forms in a FormSet?",
    "c": null,
    "o": [
      "Override clean() in the BaseInlineFormSet",
      "Use clean_<field>() in the form class",
      "Set max_num=1",
      "Use extra=0"
    ]
  },
  {
    "q": "What is the effect of `can_order=True` in a FormSet?",
    "c": null,
    "o": [
      "Adds an ORDER field to allow reordering forms",
      "Forces alphabetical ordering",
      "Prevents deletion",
      "Reverses save order"
    ]
  },
  {
    "q": "How can you ensure atomic saving of a parent instance and its inline formset?",
    "c": null,
    "o": [
      "Wrap save operations in transaction.atomic()",
      "Set can_delete=False",
      "Use min_num=1",
      "Override __init__() in the formset"
    ]
  },
  {
    "q": "Which method is called internally when formset.is_valid() is executed?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_min()"
    ]
  },
  {
    "q": "How can you skip saving a form in a ModelFormSet conditionally?",
    "c": null,
    "o": [
      "Use formset.save(commit=False) and selectively save instances",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "Which method should you override to perform validation that involves multiple forms in a FormSet?",
    "c": null,
    "o": [
      "clean()",
      "full_clean()",
      "clean_<field>()",
      "save(commit=False)"
    ]
  },
  {
    "q": "What happens if a form marked for deletion contains invalid data in a formset?",
    "c": null,
    "o": [
      "It is ignored during validation",
      "Raises a ValidationError",
      "Prevents saving of other forms",
      "Triggers field-level clean methods"
    ]
  },
  {
    "q": "How can you ensure that only forms with changed data are saved in a ModelFormSet?",
    "c": null,
    "o": [
      "Check form.has_changed() before saving",
      "Use extra=0",
      "Set min_num=1",
      "Set validate_max=True"
    ]
  },
  {
    "q": "Which attribute provides access to the parent instance in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "instance",
      "parent_instance",
      "related_object",
      "owner"
    ]
  },
  {
    "q": "How can you dynamically filter a ModelForm field queryset based on the parent instance?",
    "c": null,
    "o": [
      "Override __init__() in the form class and filter the queryset",
      "Set extra=0",
      "Use min_num=1",
      "Override save() method"
    ]
  },
  {
    "q": "What does `empty_permitted=True` allow in a FormSet form?",
    "c": null,
    "o": [
      "The form can be submitted without any data",
      "The form is required",
      "The form is automatically deleted",
      "Validation is skipped entirely"
    ]
  },
  {
    "q": "Which attribute contains forms marked for deletion in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "deleted_forms",
      "forms",
      "initial_forms",
      "empty_forms"
    ]
  },
  {
    "q": "What is the purpose of `formset.empty_form`?",
    "c": null,
    "o": [
      "Provides a template form for adding new forms dynamically",
      "Contains all initial forms",
      "Contains only deleted forms",
      "Validates all forms"
    ]
  },
  {
    "q": "Which property returns forms bound to existing model instances in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "initial_forms",
      "empty_forms",
      "deleted_forms",
      "ordered_forms"
    ]
  },
  {
    "q": "How can you enforce that numeric fields are positive across all forms in a ModelFormSet?",
    "c": null,
    "o": [
      "Implement clean_<field>() in the form class or override FormSet clean()",
      "Use max_num to limit forms",
      "Set extra=0",
      "Use can_delete=False"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` controls the maximum number of forms allowed in the formset?",
    "c": null,
    "o": [
      "max_num",
      "min_num",
      "extra",
      "can_delete"
    ]
  },
  {
    "q": "What happens if a submitted formset has more forms than `max_num`?",
    "c": null,
    "o": [
      "Extra forms beyond max_num are ignored",
      "Raises a ValidationError",
      "All forms are deleted",
      "Formset saves all forms regardless of max_num"
    ]
  },
  {
    "q": "Which property of a BaseInlineFormSet contains forms that have been changed by the user?",
    "c": null,
    "o": [
      "changed_forms",
      "forms",
      "deleted_forms",
      "empty_forms"
    ]
  },
  {
    "q": "How can you enforce that at least a minimum number of forms are submitted in a FormSet?",
    "c": null,
    "o": [
      "Use min_num and set validate_min=True",
      "Use max_num",
      "Use extra=0",
      "Set can_delete=False"
    ]
  },
  {
    "q": "Which attribute contains only the extra blank forms in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "empty_forms",
      "initial_forms",
      "deleted_forms",
      "ordered_forms"
    ]
  },
  {
    "q": "How can you prevent a formset from saving if a certain condition fails across multiple forms?",
    "c": null,
    "o": [
      "Raise a ValidationError in clean() or use commit=False to skip saving",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet points to the parent model instance?",
    "c": null,
    "o": [
      "instance",
      "parent_instance",
      "related_object",
      "owner"
    ]
  },
  {
    "q": "What happens when `formset.has_changed()` is called?",
    "c": null,
    "o": [
      "Returns True if any form in the formset has changed data",
      "Validates all forms",
      "Checks for deleted forms",
      "Returns the number of forms in the formset"
    ]
  },
  {
    "q": "How can you dynamically filter the queryset for a field in a ModelForm based on the parent instance?",
    "c": null,
    "o": [
      "Override __init__() in the form class and filter the queryset",
      "Set extra=0",
      "Use min_num=1",
      "Override save() method"
    ]
  },
  {
    "q": "Which method is called internally when `formset.is_valid()` is executed?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_max()"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` allows enforcing a minimum number of forms submitted?",
    "c": null,
    "o": [
      "min_num",
      "max_num",
      "extra",
      "can_delete"
    ]
  },
  {
    "q": "What happens if `validate_min=True` and the number of submitted forms is less than `min_num`?",
    "c": null,
    "o": [
      "A ValidationError is raised",
      "Extra forms are automatically added",
      "The FormSet is ignored",
      "Forms are deleted automatically"
    ]
  },
  {
    "q": "Which attribute contains forms marked for deletion in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "deleted_forms",
      "forms",
      "initial_forms",
      "empty_forms"
    ]
  },
  {
    "q": "Which property returns forms sorted according to the ORDER field when `can_order=True`?",
    "c": null,
    "o": [
      "ordered_forms",
      "forms",
      "active_forms",
      "sorted_forms"
    ]
  },
  {
    "q": "What does `empty_permitted=True` allow in a FormSet form?",
    "c": null,
    "o": [
      "The form can be submitted without any data",
      "The form is required",
      "The form is automatically deleted",
      "Validation is skipped entirely"
    ]
  },
  {
    "q": "What happens if a form marked for deletion contains invalid data?",
    "c": null,
    "o": [
      "It is ignored during validation",
      "Raises a ValidationError",
      "Prevents the formset from saving",
      "Triggers field-level clean methods"
    ]
  },
  {
    "q": "Which method should be overridden to enforce validation across all forms in a FormSet?",
    "c": null,
    "o": [
      "clean()",
      "full_clean()",
      "validate_all_forms()",
      "save(commit=False)"
    ]
  },
  {
    "q": "What does `formset.save(commit=False)` return in a ModelFormSet?",
    "c": null,
    "o": [
      "A list of unsaved model instances",
      "A single unsaved instance",
      "Nothing until commit=True",
      "A QuerySet of saved instances"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet points to the parent instance?",
    "c": null,
    "o": [
      "instance",
      "parent",
      "related_instance",
      "owner"
    ]
  },
  {
    "q": "What is the role of the `extra` argument in a FormSet?",
    "c": null,
    "o": [
      "Specifies how many extra blank forms to display",
      "Specifies minimum number of forms",
      "Specifies maximum number of forms",
      "Specifies forms to delete automatically"
    ]
  },
  {
    "q": "Which attribute contains only forms that are not marked for deletion in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "forms",
      "deleted_forms",
      "initial_forms",
      "empty_forms"
    ]
  },
  {
    "q": "How can you ensure that a formset does not save any forms if one of the forms fails a custom cross-form validation?",
    "c": null,
    "o": [
      "Raise a ValidationError in clean() of the BaseInlineFormSet",
      "Use min_num=1",
      "Set can_delete=False",
      "Use extra=0"
    ]
  },
  {
    "q": "Which property of a BaseInlineFormSet provides a template form for adding new forms dynamically via JavaScript?",
    "c": null,
    "o": [
      "empty_form",
      "forms",
      "initial_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "How can you enforce that numeric fields are positive across all forms in a ModelFormSet?",
    "c": null,
    "o": [
      "Implement clean_<field>() in the form class or override FormSet clean()",
      "Set max_num",
      "Use extra=0",
      "Set can_delete=False"
    ]
  },
  {
    "q": "Which method is called internally when formset.is_valid() is executed?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_min()"
    ]
  },
  {
    "q": "How can you skip saving a form in a ModelFormSet based on a custom condition?",
    "c": null,
    "o": [
      "Use formset.save(commit=False) and selectively save instances",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet points to the parent model instance?",
    "c": null,
    "o": [
      "instance",
      "parent_instance",
      "related_object",
      "owner"
    ]
  },
  {
    "q": "What happens if the management form is missing in a submitted formset?",
    "c": null,
    "o": [
      "A ManagementForm data is missing error is raised",
      "Formset validates as empty",
      "All forms are treated as deleted",
      "Validation silently fails"
    ]
  },
  {
    "q": "How can you enforce that at least one form is submitted in a FormSet?",
    "c": null,
    "o": [
      "Set min_num=1 and validate_min=True",
      "Use max_num",
      "Use extra=0",
      "Set can_delete=False"
    ]
  },
  {
    "q": "Which argument allows an inline formset to include extra blank forms for adding new objects?",
    "c": null,
    "o": [
      "extra",
      "min_num",
      "max_num",
      "can_delete"
    ]
  },
  {
    "q": "Which attribute contains forms that have changed data in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "changed_forms",
      "forms",
      "deleted_forms",
      "empty_forms"
    ]
  },
  {
    "q": "How can you enforce a custom validation across all forms in an inline formset?",
    "c": null,
    "o": [
      "Override clean() in BaseInlineFormSet",
      "Use clean_<field>() in the form class",
      "Set extra=0",
      "Use min_num=1"
    ]
  },
  {
    "q": "What happens if `can_order=True` is set in a formset?",
    "c": null,
    "o": [
      "An ORDER field is added to allow reordering forms",
      "Forms are automatically sorted alphabetically",
      "Forms cannot be deleted",
      "Forms are saved in reverse order"
    ]
  },
  {
    "q": "Which attribute contains only forms bound to existing model instances?",
    "c": null,
    "o": [
      "initial_forms",
      "forms",
      "empty_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "What is the effect of `empty_permitted=True` in a FormSet form?",
    "c": null,
    "o": [
      "The form can be submitted without any data",
      "The form is required",
      "The form is automatically deleted",
      "Validation is skipped entirely"
    ]
  },
  {
    "q": "How can you skip saving certain forms conditionally in a ModelFormSet?",
    "c": null,
    "o": [
      "Use formset.save(commit=False) and selectively save instances",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet points to the parent model instance?",
    "c": null,
    "o": [
      "instance",
      "parent_instance",
      "related_object",
      "owner"
    ]
  },
  {
    "q": "Which attribute provides a template form for adding new forms dynamically in JavaScript?",
    "c": null,
    "o": [
      "empty_form",
      "initial_forms",
      "forms",
      "deleted_forms"
    ]
  },
  {
    "q": "What happens if a form marked for deletion contains invalid data?",
    "c": null,
    "o": [
      "It is ignored during validation",
      "Raises a ValidationError",
      "Prevents saving of other forms",
      "Triggers field-level clean methods"
    ]
  },
  {
    "q": "Which method is called internally when `formset.is_valid()` is executed?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_min()"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` controls the maximum number of forms allowed in the formset?",
    "c": null,
    "o": [
      "max_num",
      "min_num",
      "extra",
      "can_delete"
    ]
  },
  {
    "q": "What happens if a submitted formset has more forms than `max_num`?",
    "c": null,
    "o": [
      "Extra forms beyond max_num are ignored",
      "Raises a ValidationError",
      "All forms are deleted",
      "Formset saves all forms regardless of max_num"
    ]
  },
  {
    "q": "Which property contains only the extra blank forms in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "empty_forms",
      "initial_forms",
      "deleted_forms",
      "ordered_forms"
    ]
  },
  {
    "q": "How can you enforce that at least a minimum number of forms are submitted in a FormSet?",
    "c": null,
    "o": [
      "Use min_num and set validate_min=True",
      "Use max_num",
      "Use extra=0",
      "Set can_delete=False"
    ]
  },
  {
    "q": "Which attribute contains forms marked for deletion in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "deleted_forms",
      "forms",
      "initial_forms",
      "empty_forms"
    ]
  },
  {
    "q": "How can you enforce cross-form validation in a FormSet?",
    "c": null,
    "o": [
      "Override clean() in the BaseInlineFormSet",
      "Use clean_<field>() in the form class",
      "Set extra=0",
      "Use min_num=1"
    ]
  },
  {
    "q": "Which attribute points to the parent model instance in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "instance",
      "parent_instance",
      "related_object",
      "owner"
    ]
  },
  {
    "q": "What is the purpose of `formset.empty_form`?",
    "c": null,
    "o": [
      "Provides a template form for adding new forms dynamically",
      "Contains all initial forms",
      "Contains only deleted forms",
      "Validates all forms"
    ]
  },
  {
    "q": "How can you skip saving a form in a ModelFormSet based on a custom condition?",
    "c": null,
    "o": [
      "Use formset.save(commit=False) and selectively save instances",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "Which method is automatically called when `formset.is_valid()` is executed?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_min()"
    ]
  },
  {
    "q": "Which attribute contains forms that are currently marked for deletion in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "deleted_forms",
      "forms",
      "initial_forms",
      "empty_forms"
    ]
  },
  {
    "q": "How can you enforce that at least one form is submitted in a FormSet?",
    "c": null,
    "o": [
      "Set min_num=1 and validate_min=True",
      "Use max_num",
      "Use extra=0",
      "Set can_delete=False"
    ]
  },
  {
    "q": "Which property of a BaseInlineFormSet provides a template form for adding new forms dynamically via JavaScript?",
    "c": null,
    "o": [
      "empty_form",
      "forms",
      "initial_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "How can you skip saving a form in a ModelFormSet based on a custom condition?",
    "c": null,
    "o": [
      "Use formset.save(commit=False) and selectively save instances",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` specifies the maximum number of forms allowed in the formset?",
    "c": null,
    "o": [
      "max_num",
      "min_num",
      "extra",
      "can_delete"
    ]
  },
  {
    "q": "Which method is automatically called when `formset.is_valid()` is executed?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_min()"
    ]
  },
  {
    "q": "Which attribute points to the parent instance in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "instance",
      "parent_instance",
      "related_object",
      "owner"
    ]
  },
  {
    "q": "How can you enforce cross-form validation in a FormSet?",
    "c": null,
    "o": [
      "Override clean() in the BaseInlineFormSet",
      "Use clean_<field>() in the form class",
      "Set extra=0",
      "Use min_num=1"
    ]
  },
  {
    "q": "What is the effect of `can_order=True` in a formset?",
    "c": null,
    "o": [
      "Adds an ORDER field to allow reordering forms",
      "Forces alphabetical sorting",
      "Prevents deletion",
      "Saves forms in reverse order"
    ]
  },
  {
    "q": "Which property provides access to forms that have changed data in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "changed_forms",
      "forms",
      "deleted_forms",
      "empty_forms"
    ]
  },
  {
    "q": "Which attribute contains only the forms bound to existing model instances in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "initial_forms",
      "forms",
      "empty_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "How can you ensure that numeric fields are positive across all forms in a ModelFormSet?",
    "c": null,
    "o": [
      "Implement clean_<field>() in the form class or override FormSet clean()",
      "Set max_num",
      "Use extra=0",
      "Use can_delete=False"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` allows adding extra blank forms for new objects?",
    "c": null,
    "o": [
      "extra",
      "min_num",
      "max_num",
      "can_delete"
    ]
  },
  {
    "q": "Which attribute contains forms marked for deletion in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "deleted_forms",
      "forms",
      "initial_forms",
      "empty_forms"
    ]
  },
  {
    "q": "What happens if a form marked for deletion contains invalid data?",
    "c": null,
    "o": [
      "It is ignored during validation",
      "Raises a ValidationError",
      "Prevents saving of other forms",
      "Triggers field-level clean methods"
    ]
  },
  {
    "q": "How can you skip saving certain forms conditionally in a ModelFormSet?",
    "c": null,
    "o": [
      "Use formset.save(commit=False) and selectively save instances",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "Which method is automatically called when `formset.is_valid()` is executed?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_min()"
    ]
  },
  {
    "q": "Which property provides access to forms that have changed data in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "changed_forms",
      "forms",
      "deleted_forms",
      "empty_forms"
    ]
  },
  {
    "q": "Which attribute provides a template form for adding new forms dynamically via JavaScript?",
    "c": null,
    "o": [
      "empty_form",
      "forms",
      "initial_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "How can you enforce cross-form validation in a FormSet?",
    "c": null,
    "o": [
      "Override clean() in the BaseInlineFormSet",
      "Use clean_<field>() in the form class",
      "Set extra=0",
      "Use min_num=1"
    ]
  },
  {
    "q": "Which attribute of a BaseInlineFormSet contains only forms bound to existing model instances?",
    "c": null,
    "o": [
      "initial_forms",
      "forms",
      "empty_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "Which argument in `inlineformset_factory` allows adding extra blank forms for creating new objects?",
    "c": null,
    "o": [
      "extra",
      "min_num",
      "max_num",
      "can_delete"
    ]
  },
  {
    "q": "Which property contains forms marked for deletion in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "deleted_forms",
      "forms",
      "initial_forms",
      "empty_forms"
    ]
  },
  {
    "q": "How can you skip saving a form in a ModelFormSet conditionally?",
    "c": null,
    "o": [
      "Use formset.save(commit=False) and selectively save instances",
      "Set can_delete=False",
      "Use extra=0",
      "Set min_num=1"
    ]
  },
  {
    "q": "Which property provides access to forms that have changed data in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "changed_forms",
      "forms",
      "deleted_forms",
      "empty_forms"
    ]
  },
  {
    "q": "What is the effect of `can_order=True` in a formset?",
    "c": null,
    "o": [
      "Adds an ORDER field to allow reordering forms",
      "Forces alphabetical sorting",
      "Prevents deletion",
      "Saves forms in reverse order"
    ]
  },
  {
    "q": "Which method is automatically called when `formset.is_valid()` is executed?",
    "c": null,
    "o": [
      "full_clean()",
      "clean()",
      "save(commit=False)",
      "validate_min()"
    ]
  },
  {
    "q": "Which attribute provides a template form for adding new forms dynamically via JavaScript?",
    "c": null,
    "o": [
      "empty_form",
      "forms",
      "initial_forms",
      "deleted_forms"
    ]
  },
  {
    "q": "How can you enforce cross-form validation in a FormSet?",
    "c": null,
    "o": [
      "Override clean() in the BaseInlineFormSet",
      "Use clean_<field>() in the form class",
      "Set extra=0",
      "Use min_num=1"
    ]
  },
  {
    "q": "Which attribute points to the parent instance in a BaseInlineFormSet?",
    "c": null,
    "o": [
      "instance",
      "parent_instance",
      "related_object",
      "owner"
    ]
  },
  {
    "q": "Which of the following is the correct way to create a basic Django form class?",
    "c": null,
    "o": [
      "class ContactForm(forms.Form):\n    name = forms.CharField()\n    email = forms.EmailField()",
      "class ContactForm(Form):\n    name = CharField()\n    email = EmailField()",
      "class ContactForm(forms.ModelForm):\n    name = models.CharField()\n    email = models.EmailField()",
      "class ContactForm(Form.ModelForm):\n    name = models.CharField()\n    email = models.EmailField()"
    ]
  },
  {
    "q": "What happens if a form fails validation in Django?",
    "c": null,
    "o": [
      "The form's `is_valid()` method returns False and errors are stored in `form.errors`",
      "The form raises a ValidationError and crashes the request",
      "The form automatically saves invalid data to the database",
      "The form silently ignores invalid fields and proceeds with clean data"
    ]
  },
  {
    "q": "Consider the following form. What is the purpose of the `clean_email` method?",
    "c": "class SignupForm(forms.Form):\n    username = forms.CharField(max_length=100)\n    email = forms.EmailField()\n\n    def clean_email(self):\n        email = self.cleaned_data['email']\n        if not email.endswith('.com'):\n            raise forms.ValidationError(\"Email must end with .com\")\n        return email",
    "o": [
      "To provide field-specific validation logic for the `email` field",
      "To override default validation for all form fields",
      "To automatically save the form data into the database",
      "To bypass the built-in EmailField validation completely"
    ]
  },
  {
    "q": "Which method must be overridden when customizing how a Django ModelForm saves data?",
    "c": null,
    "o": [
      "save(self, commit=True)",
      "validate(self)",
      "full_clean(self)",
      "render(self)"
    ]
  },
  {
    "q": "Given the following ModelForm, what does the `Meta` inner class do?",
    "c": "class BookForm(forms.ModelForm):\n    class Meta:\n        model = Book\n        fields = ['title', 'author']",
    "o": [
      "Specifies the model to base the form on and which fields to include",
      "Specifies how validation errors should be displayed",
      "Defines the default widgets for rendering fields",
      "Links the form to the template context processor"
    ]
  },
  {
    "q": "When handling form submissions in Django views, what is the common pattern to process a POST request?",
    "c": null,
    "o": [
      "Check `if request.method == 'POST':` then instantiate the form with `request.POST` and call `form.is_valid()`",
      "Always instantiate the form with `request.GET` and save the data",
      "Directly assign request.POST to the model instance and call `save()`",
      "Use form.cleaned_data without checking `is_valid()` first"
    ]
  },
  {
    "q": "In Django, which built-in form method ensures both field-specific and cross-field validation are applied?",
    "c": null,
    "o": [
      "full_clean()",
      "is_valid()",
      "validate_all()",
      "clean_fields()"
    ]
  },
  {
    "q": "What is the difference between `forms.Form` and `forms.ModelForm`?",
    "c": null,
    "o": [
      "`forms.Form` is manually defined and independent of models, while `forms.ModelForm` is automatically tied to a Django model",
      "`forms.Form` performs automatic database saving, while `forms.ModelForm` does not",
      "`forms.ModelForm` cannot perform custom validation, but `forms.Form` can",
      "`forms.Form` always requires Meta class, while `forms.ModelForm` does not"
    ]
  },
  {
    "q": "Which of the following correctly binds data to a Django form instance for validation?",
    "c": null,
    "o": [
      "form = ContactForm(request.POST)",
      "form = ContactForm(request.GET)",
      "form = ContactForm(model=request.POST)",
      "form = ContactForm(commit=False)"
    ]
  },
  {
    "q": "What does the following code snippet achieve?",
    "c": "if form.is_valid():\n    data = form.cleaned_data\n    process(data)",
    "o": [
      "Ensures only validated and sanitized data is accessed",
      "Automatically saves the data to the model instance",
      "Skips field validation and returns raw POST data",
      "Raises an exception if the form is not valid"
    ]
  },
  {
    "q": "In Django forms, what is the role of `widgets`?",
    "c": null,
    "o": [
      "They define how form fields are rendered in HTML",
      "They validate data entered into form fields",
      "They connect the form to the database",
      "They automatically handle CSRF protection"
    ]
  },
  {
    "q": "Given this ModelForm, how would you exclude certain model fields from the form?",
    "c": "class ProfileForm(forms.ModelForm):\n    class Meta:\n        model = Profile\n        fields = '__all__'",
    "o": [
      "Replace `fields = '__all__'` with `exclude = ['field_name']`",
      "Delete the field definition from the model",
      "Use a custom widget with `hidden=True`",
      "Call `form.remove_field('field_name')` in the view"
    ]
  },
  {
    "q": "Which statement about `form.errors` is correct?",
    "c": null,
    "o": [
      "It is a dictionary containing error messages for each invalid field",
      "It is a boolean indicating whether the form has any errors",
      "It automatically fixes invalid fields with default values",
      "It stores the exception stack trace of validation failures"
    ]
  },
  {
    "q": "What happens if you call `form.save(commit=False)` on a ModelForm?",
    "c": null,
    "o": [
      "It returns an unsaved model instance that can be modified before saving",
      "It immediately saves the form data into the database",
      "It prevents validation from running on the form",
      "It discards all data entered into the form"
    ]
  },
  {
    "q": "Consider the following code. What does `clean()` do here?",
    "c": "class LoginForm(forms.Form):\n    username = forms.CharField()\n    password = forms.CharField(widget=forms.PasswordInput)\n\n    def clean(self):\n        cleaned_data = super().clean()\n        if cleaned_data.get('username') == cleaned_data.get('password'):\n            raise forms.ValidationError(\"Username and password cannot be the same\")\n        return cleaned_data",
    "o": [
      "It performs cross-field validation involving multiple fields",
      "It replaces built-in field validators with custom ones",
      "It prevents the form from saving data to the model",
      "It automatically hashes the password field"
    ]
  },
  {
    "q": "Which option is true regarding CSRF protection when handling forms in Django?",
    "c": null,
    "o": [
      "You must include `{% csrf_token %}` inside `<form>` tags in templates",
      "CSRF tokens are automatically added by Django forms with no template changes",
      "ModelForms inherently bypass CSRF requirements",
      "CSRF protection is only necessary when using `GET` requests"
    ]
  },
  {
    "q": "Which of the following best describes the difference between `form.cleaned_data` and `form.data`?",
    "c": null,
    "o": [
      "`cleaned_data` contains validated and type-converted values, while `data` contains raw input values",
      "`cleaned_data` stores only error messages, while `data` stores valid values",
      "`cleaned_data` always includes all fields, while `data` only includes valid ones",
      "`cleaned_data` is available before validation, while `data` is available after validation"
    ]
  },
  {
    "q": "In Django, what is the effect of setting `required=False` on a form field?",
    "c": null,
    "o": [
      "The field becomes optional and may return an empty value",
      "The field will be hidden from rendering in the template",
      "The field will accept only default values",
      "The field will bypass all validation checks"
    ]
  },
  {
    "q": "What is the correct way to add a placeholder attribute to a Django form field?",
    "c": null,
    "o": [
      "name = forms.CharField(widget=forms.TextInput(attrs={'placeholder': 'Enter name'}))",
      "name = forms.CharField(placeholder='Enter name')",
      "name = forms.CharField(form.attrs={'placeholder': 'Enter name'})",
      "name = forms.CharField(widget=forms.TextField(placeholder='Enter name'))"
    ]
  },
  {
    "q": "Given the following form, what will happen if `age` is submitted with a value of 10?",
    "c": "class AgeForm(forms.Form):\n    age = forms.IntegerField()\n\n    def clean_age(self):\n        age = self.cleaned_data['age']\n        if age < 18:\n            raise forms.ValidationError(\"Must be at least 18 years old\")\n        return age",
    "o": [
      "The form will be invalid and `form.errors` will include an error for `age`",
      "The form will still be valid but `age` will be set to None",
      "The form will raise an exception and stop processing the request",
      "The form will accept the value without errors"
    ]
  },
  {
    "q": "When saving a ModelForm with `form.save()`, what happens if `commit=True`?",
    "c": null,
    "o": [
      "The model instance is created or updated and saved to the database",
      "The form instance itself is saved for reuse later",
      "The form skips validation and directly inserts data",
      "The model instance is created but not saved until explicitly committed"
    ]
  },
  {
    "q": "Which of the following best describes `form.as_p()`?",
    "c": null,
    "o": [
      "It renders the form fields wrapped in `<p>` HTML tags",
      "It prints the form fields in plain text format",
      "It renders only the form’s primary key field",
      "It automatically saves the form data as plain text"
    ]
  },
  {
    "q": "What does the `initial` argument in a Django form field definition do?",
    "c": null,
    "o": [
      "It provides a default value displayed when the form is rendered",
      "It sets the value stored in the database if no data is entered",
      "It overrides any user input with a fixed default",
      "It ensures the field always passes validation"
    ]
  },
  {
    "q": "Which method should you override if you want to apply validation logic across multiple fields in a Django form?",
    "c": null,
    "o": [
      "clean(self)",
      "cleaned_data(self)",
      "validate(self)",
      "full_clean(self)"
    ]
  },
  {
    "q": "Which of the following is TRUE about `form.is_valid()` in Django?",
    "c": null,
    "o": [
      "It runs all validation methods and returns True only if no errors are found",
      "It directly saves the form data to the database if valid",
      "It checks only field-specific validation but skips custom validators",
      "It must be called only once before accessing `form.data`"
    ]
  },
  {
    "q": "What is the correct way to restrict a Django ModelForm to only specific fields?",
    "c": null,
    "o": [
      "Use `fields = ['field1', 'field2']` inside the Meta class",
      "Delete unwanted fields directly from the model",
      "Set `only_fields` attribute in the form class",
      "Pass a list of fields as arguments when instantiating the form"
    ]
  },
  {
    "q": "Given the form below, what happens when the `clean_name` method raises a ValidationError?",
    "c": "class UserForm(forms.Form):\n    name = forms.CharField()\n\n    def clean_name(self):\n        raise forms.ValidationError(\"Invalid name\")",
    "o": [
      "The form becomes invalid and the error is associated with the `name` field",
      "The form immediately stops execution and raises an exception to the view",
      "The form is still valid but ignores the `name` field",
      "The error is stored globally in `form.non_field_errors()`"
    ]
  },
  {
    "q": "Which of the following is correct about `form.non_field_errors()`?",
    "c": null,
    "o": [
      "It returns errors that are not tied to a specific field, often from the `clean()` method",
      "It returns all field validation errors combined into one string",
      "It displays only widget-related rendering errors",
      "It includes only the first error for each field"
    ]
  },
  {
    "q": "How can you change the HTML input type of a field in a Django form?",
    "c": null,
    "o": [
      "By specifying a custom widget, e.g. `forms.CharField(widget=forms.PasswordInput)`",
      "By passing `input_type='password'` directly to the field constructor",
      "By modifying `field.type` dynamically in the view",
      "By using a template tag to replace input elements"
    ]
  },
  {
    "q": "When a form is bound with `request.POST`, what does it mean?",
    "c": null,
    "o": [
      "The form contains submitted user data and is ready for validation",
      "The form is empty and not ready for validation",
      "The form is automatically linked to a database model",
      "The form skips validation and uses initial values"
    ]
  },
  {
    "q": "What is the correct way to save a ModelForm but delay committing to the database?",
    "c": null,
    "o": [
      "instance = form.save(commit=False)",
      "instance = form.save(lazy=True)",
      "form.defer_save()",
      "form.save_without_commit()"
    ]
  },
  {
    "q": "Which of the following is TRUE about form inheritance in Django?",
    "c": null,
    "o": [
      "Forms can be subclassed to reuse fields and validation logic",
      "Forms cannot be subclassed; each form must be fully defined",
      "ModelForms cannot inherit from other ModelForms",
      "Inheritance only works if the parent form is abstract"
    ]
  },
  {
    "q": "What does the `empty_permitted` attribute in a Django form do?",
    "c": null,
    "o": [
      "It allows a form to pass validation even if no data is provided",
      "It automatically sets all fields to `required=False`",
      "It discards user input if the form is empty",
      "It prevents the form from being instantiated without data"
    ]
  },
  {
    "q": "In Django, how can you dynamically alter a form field in the view before rendering?",
    "c": null,
    "o": [
      "By modifying `form.fields['fieldname'].widget` or `form.fields['fieldname'].label`",
      "By editing the underlying model definition temporarily",
      "By creating a new form class dynamically using `type()`",
      "By overriding `form.__init__()` in the model itself"
    ]
  },
  {
    "q": "Given the following form, where will the validation error appear if passwords don't match?",
    "c": "class PasswordForm(forms.Form):\n    password1 = forms.CharField(widget=forms.PasswordInput)\n    password2 = forms.CharField(widget=forms.PasswordInput)\n\n    def clean(self):\n        cleaned_data = super().clean()\n        if cleaned_data.get('password1') != cleaned_data.get('password2'):\n            raise forms.ValidationError(\"Passwords do not match\")",
    "o": [
      "It will appear in `form.non_field_errors()`",
      "It will appear under `password1` field",
      "It will appear under `password2` field",
      "It will silently fail and form remains valid"
    ]
  },
  {
    "q": "Which of the following statements about `form.initial` is correct?",
    "c": null,
    "o": [
      "It sets the initial display values but does not override submitted data",
      "It forces default values even if the user submits data",
      "It is required for all form fields",
      "It is only applicable to ModelForms"
    ]
  },
  {
    "q": "What happens if you render a Django form without adding `{% csrf_token %}` in the template?",
    "c": null,
    "o": [
      "POST submissions will fail with a 403 Forbidden error due to CSRF protection",
      "The form will still work normally with POST requests",
      "The form will bypass validation entirely",
      "The form will store submitted values but skip saving"
    ]
  },
  {
    "q": "Which of the following is the correct way to apply custom error messages to a form field?",
    "c": null,
    "o": [
      "email = forms.EmailField(error_messages={'invalid': 'Enter a valid email address'})",
      "email = forms.EmailField(errors={'invalid': 'Enter a valid email address'})",
      "email = forms.EmailField(custom_error='Enter a valid email address')",
      "email = forms.EmailField(message_invalid='Enter a valid email address')"
    ]
  },
  {
    "q": "Which of the following best describes the `full_clean()` method of a Django form?",
    "c": null,
    "o": [
      "It runs field validation, clean methods, and populates `cleaned_data` and `errors`",
      "It saves the form directly to the database without validation",
      "It resets the form back to its initial values",
      "It only validates non-required fields"
    ]
  },
  {
    "q": "How can you render individual form fields manually in a Django template?",
    "c": null,
    "o": [
      "By accessing them with `{{ form.field_name }}`",
      "By looping `{{ field }}` in `{{ form.as_list }}`",
      "By using `{% render_field form.field_name %}` without extra setup",
      "By printing `{{ form.fields.field_name }}` directly"
    ]
  },
  {
    "q": "What happens if a ModelForm includes a field that is marked `editable=False` in the model?",
    "c": null,
    "o": [
      "The field will not appear in the form even if included in `fields`",
      "The field will appear but cannot be modified",
      "The field will raise a runtime error during form initialization",
      "The field will appear but always fail validation"
    ]
  },
  {
    "q": "Which of the following correctly validates and saves a ModelForm in a Django view?",
    "c": "if request.method == 'POST':\n    form = ArticleForm(request.POST)\n    if form.is_valid():\n        form.save()",
    "o": [
      "The model instance is created/updated in the database only if the form passes validation",
      "The form data is saved regardless of validation errors",
      "The form will raise an exception if data is invalid",
      "The form does not save automatically; `commit=True` must be passed explicitly"
    ]
  },
  {
    "q": "When using `forms.ChoiceField`, how do you define the set of allowed choices?",
    "c": null,
    "o": [
      "By passing a list/tuple of `(value, label)` pairs to the `choices` argument",
      "By passing only a list of labels, Django generates values automatically",
      "By defining a Meta class inside the form with `choices` attribute",
      "By assigning a model field with `choices` directly to the form"
    ]
  },
  {
    "q": "Which is the correct way to handle file uploads in a Django form?",
    "c": null,
    "o": [
      "Include `request.FILES` when instantiating the form: `form = UploadForm(request.POST, request.FILES)`",
      "Save the file manually in the view without using Django forms",
      "Use only `request.POST` since file data is included there automatically",
      "Add the file field in the model but not in the form"
    ]
  },
  {
    "q": "What does the following code achieve in a form field?",
    "c": "age = forms.IntegerField(validators=[MinValueValidator(18)])",
    "o": [
      "Ensures that only values 18 or higher are accepted for the `age` field",
      "Converts all values below 18 to None",
      "Automatically raises a database error if age < 18",
      "Sets the default value of `age` to 18"
    ]
  },
  {
    "q": "Which of the following best describes the purpose of `form.errors.as_json()`?",
    "c": null,
    "o": [
      "It serializes form errors into a JSON-compatible dictionary",
      "It removes errors from the form and resets validation state",
      "It logs errors into Django’s default logging system",
      "It converts error messages into HTML format"
    ]
  },
  {
    "q": "What is the default behavior of `blank=True` on a model field with respect to ModelForms?",
    "c": null,
    "o": [
      "The corresponding form field will not be required",
      "The field will be hidden in the form",
      "The field will bypass validation but still be required",
      "The form field will always render with no initial value"
    ]
  },
  {
    "q": "When overriding a ModelForm's `__init__` method, why might you modify `self.fields`?",
    "c": null,
    "o": [
      "To dynamically customize form fields based on runtime context",
      "To permanently change the model definition",
      "To avoid running default validators on the form",
      "To remove the need for a Meta class definition"
    ]
  },
  {
    "q": "Which of the following statements about `form.has_changed()` is correct?",
    "c": null,
    "o": [
      "It returns True if the submitted data differs from the initial data",
      "It always returns True once the form is bound",
      "It checks if validation has run successfully",
      "It compares submitted data with database values instead of initial data"
    ]
  },
  {
    "q": "What is the purpose of `error_class` in Django forms?",
    "c": null,
    "o": [
      "It allows customization of how errors are rendered in templates",
      "It determines which exceptions are raised during validation",
      "It sets the severity level of validation errors",
      "It hides error messages from being displayed"
    ]
  },
  {
    "q": "Given the following ModelForm, what happens if the `widgets` dictionary is defined in Meta?",
    "c": "class ProductForm(forms.ModelForm):\n    class Meta:\n        model = Product\n        fields = ['name', 'price']\n        widgets = {\n            'price': forms.NumberInput(attrs={'min': '0'})\n        }",
    "o": [
      "It customizes how the `price` field is rendered in HTML",
      "It overrides the database field type for `price`",
      "It disables validation for the `price` field",
      "It automatically converts `price` values to decimals"
    ]
  },
  {
    "q": "Which of the following is TRUE about `BaseModelFormSet`?",
    "c": null,
    "o": [
      "It is the base class for creating multiple model form instances together",
      "It is used only for inline form validation within templates",
      "It automatically saves all forms without validation",
      "It is limited to handling only one instance of a ModelForm"
    ]
  },
  {
    "q": "How can you change a field label in a Django form without modifying the model?",
    "c": null,
    "o": [
      "By setting `labels = {'field': 'New Label'}` in the Meta class",
      "By changing the model’s `verbose_name` attribute",
      "By setting `field.label = 'New Label'` in the model definition",
      "By overriding the template output only"
    ]
  },
  {
    "q": "What happens if a form is instantiated with no data (`form = ContactForm()`)?",
    "c": null,
    "o": [
      "The form is unbound and `is_valid()` will always return False",
      "The form is valid because no validation errors exist yet",
      "The form will contain only default values in `cleaned_data`",
      "The form is automatically bound to initial values"
    ]
  },
  {
    "q": "In Django formsets, what does `extra=0` mean?",
    "c": null,
    "o": [
      "No extra empty forms will be displayed in the formset",
      "The formset will not allow saving any data",
      "The formset requires at least one form to be filled",
      "The formset skips validation on all forms"
    ]
  },
  {
    "q": "Which method should you override to apply field-level validation in a Django form?",
    "c": null,
    "o": [
      "clean_<fieldname>()",
      "validate_<fieldname>()",
      "full_clean_field()",
      "field_validator()"
    ]
  },
  {
    "q": "What is the difference between `form.is_bound` and `form.is_valid()`?",
    "c": null,
    "o": [
      "`is_bound` checks if data has been provided to the form, while `is_valid` checks if that data passes validation",
      "`is_bound` validates fields individually, while `is_valid` validates all fields",
      "`is_bound` saves the form, while `is_valid` only prepares it for saving",
      "`is_bound` compares data with initial values, while `is_valid` compares data with the database"
    ]
  },
  {
    "q": "Which of the following statements about `ModelChoiceField` is correct?",
    "c": null,
    "o": [
      "It represents a foreign key relation as a dropdown in the form",
      "It allows multiple selections for a many-to-many relationship",
      "It automatically creates new related objects if they don’t exist",
      "It bypasses model-level validation for foreign keys"
    ]
  },
  {
    "q": "What is the correct way to define a hidden field in a Django form?",
    "c": null,
    "o": [
      "token = forms.CharField(widget=forms.HiddenInput())",
      "token = forms.CharField(hidden=True)",
      "token = forms.HiddenField()",
      "token = forms.CharField(widget=forms.TextInput(type='hidden'))"
    ]
  },
  {
    "q": "When using `form.save(commit=False)` in a ModelForm, what is the typical next step?",
    "c": null,
    "o": [
      "Modify the instance (e.g., assign additional fields) before calling `instance.save()`",
      "The form data is discarded and cannot be saved later",
      "The instance is saved automatically during template rendering",
      "You must call `form.commit()` to finalize the save"
    ]
  },
  {
    "q": "Which of the following is TRUE about `form.cleaned_data`?",
    "c": null,
    "o": [
      "It is only available after calling `is_valid()`",
      "It is populated as soon as the form is instantiated",
      "It contains both valid and invalid field values",
      "It is a method, not a dictionary-like object"
    ]
  },
  {
    "q": "What is the correct way to override a field in a ModelForm?",
    "c": "class UserForm(forms.ModelForm):\n    email = forms.EmailField(required=True)\n\n    class Meta:\n        model = User\n        fields = ['username', 'email']",
    "o": [
      "By redefining the field in the form class before the Meta class",
      "By modifying the model’s field definition directly",
      "By using `exclude` in Meta and re-adding the field manually",
      "By defining the field after the Meta class in the form"
    ]
  },
  {
    "q": "Which of the following is the main purpose of `inlineformset_factory`?",
    "c": null,
    "o": [
      "To create formsets for related models connected by a ForeignKey",
      "To generate forms dynamically without a model",
      "To allow validation across unrelated models",
      "To replace standard formsets in all cases"
    ]
  },
  {
    "q": "If a form is initialized with both `initial` data and `request.POST`, which takes precedence?",
    "c": null,
    "o": [
      "Data from `request.POST`",
      "Data from `initial`",
      "Whichever is defined first in the form",
      "Neither — both are merged into `cleaned_data`"
    ]
  },
  {
    "q": "What is the role of `form.add_error(field, error)` in Django?",
    "c": null,
    "o": [
      "It attaches a validation error to a specific field or to the form as a whole",
      "It removes an error from the form’s error dictionary",
      "It stops validation immediately and raises an exception",
      "It saves the form only if the error is resolved"
    ]
  },
  {
    "q": "Which of the following correctly describes `form.is_multipart()`?",
    "c": null,
    "o": [
      "It checks whether the form requires `enctype=\"multipart/form-data\"` for file uploads",
      "It verifies if the form contains multiple fields",
      "It splits form data into chunks for faster processing",
      "It ensures formsets can be nested inside the form"
    ]
  },
  {
    "q": "What is the difference between `ModelMultipleChoiceField` and `ModelChoiceField`?",
    "c": null,
    "o": [
      "`ModelMultipleChoiceField` allows selecting multiple related objects, while `ModelChoiceField` allows only one",
      "`ModelMultipleChoiceField` is only valid in ModelForms, while `ModelChoiceField` works in any form",
      "`ModelChoiceField` skips validation, while `ModelMultipleChoiceField` enforces it",
      "`ModelChoiceField` is used for reverse foreign keys, while `ModelMultipleChoiceField` is not"
    ]
  },
  {
    "q": "Which of the following is TRUE about Django formsets?",
    "c": null,
    "o": [
      "They manage multiple instances of the same form together",
      "They allow multiple unrelated forms to be grouped",
      "They automatically save all forms without validation",
      "They bypass CSRF checks since they handle multiple forms"
    ]
  },
  {
    "q": "Given the following form, what will be the initial value of `country` if the user does not provide input?",
    "c": "class ProfileForm(forms.Form):\n    country = forms.CharField(initial='USA')",
    "o": [
      "USA",
      "None",
      "Empty string",
      "It raises a validation error"
    ]
  },
  {
    "q": "Which of the following describes `BoundField` in Django?",
    "c": null,
    "o": [
      "It is the wrapper around a form field containing data and HTML rendering helpers",
      "It is a field that cannot be modified after validation",
      "It is used only for ModelForms with ForeignKey relationships",
      "It is an alias for `cleaned_data` values"
    ]
  },
  {
    "q": "What is the correct way to disable a field in a Django form at runtime?",
    "c": null,
    "o": [
      "form.fields['fieldname'].disabled = True",
      "form.fields['fieldname'].readonly = True",
      "form.fields['fieldname'].editable = False",
      "form.fields['fieldname'].active = False"
    ]
  },
  {
    "q": "If a form field defines both `initial` and `default` (via the model), which is shown when rendering the form?",
    "c": null,
    "o": [
      "The `initial` value from the form",
      "The `default` value from the model",
      "Neither — the field remains blank until user input",
      "Both values appear as a list of options"
    ]
  },
  {
    "q": "What happens if you call `form.full_clean()` manually before `form.is_valid()`?",
    "c": null,
    "o": [
      "It runs validation, populates `cleaned_data`, and fills `errors`",
      "It skips validation and forces the form to be valid",
      "It saves the form instance to the database",
      "It resets the form back to its initial state"
    ]
  },
  {
    "q": "Which attribute of a form field would you modify to change its help text?",
    "c": null,
    "o": [
      "help_text",
      "description",
      "tooltip",
      "label_help"
    ]
  },
  {
    "q": "When using Django’s `form.as_table()`, how are form fields rendered?",
    "c": null,
    "o": [
      "Each field is rendered as a row inside a `<tr>` element",
      "All fields are rendered inside `<td>` cells only",
      "Each field is wrapped inside a `<p>` tag",
      "Fields are rendered without any HTML wrapper"
    ]
  },
  {
    "q": "What is the main purpose of `min_length` and `max_length` arguments in a Django `CharField`?",
    "c": null,
    "o": [
      "They enforce validation rules on string length",
      "They define database column sizes directly",
      "They automatically truncate values exceeding limits",
      "They set placeholder text in rendered forms"
    ]
  },
  {
    "q": "Which of the following is TRUE about Django’s `form.errors`?",
    "c": null,
    "o": [
      "It is a dictionary mapping field names to lists of error messages",
      "It contains only one error message per form regardless of fields",
      "It is available even before running `is_valid()`",
      "It automatically translates error messages into all installed languages"
    ]
  },
  {
    "q": "In a ModelForm, what happens if you omit the `fields` and `exclude` attributes in the Meta class?",
    "c": null,
    "o": [
      "Django raises an ImproperlyConfigured error",
      "All model fields are included automatically",
      "Only primary key fields are included",
      "The form is created but remains unusable"
    ]
  },
  {
    "q": "Which of the following best describes `form.changed_data`?",
    "c": null,
    "o": [
      "It is a list of fields whose submitted values differ from their initial values",
      "It is a dictionary mapping changed fields to their new values",
      "It is always identical to `form.cleaned_data`",
      "It resets fields that were not modified by the user"
    ]
  },
  {
    "q": "How can you render errors for a specific field in a template?",
    "c": null,
    "o": [
      "{{ form.field_name.errors }}",
      "{{ form.errors.field_name }}",
      "{{ form.errors['field_name'] }}",
      "{{ field.error_list }}"
    ]
  },
  {
    "q": "What is the correct way to mark a field in a Django form as not required while still validating it if provided?",
    "c": null,
    "o": [
      "Set `required=False` when defining the field",
      "Set `blank=True` in the model and omit it from the form",
      "Use a validator that skips empty values",
      "Override the form’s `clean()` method and ignore missing fields"
    ]
  },
  {
    "q": "Which of the following best describes the behavior of `form.media` in Django?",
    "c": null,
    "o": [
      "It collects and renders all JavaScript and CSS required by the form’s widgets",
      "It manages uploaded files associated with the form",
      "It tracks multimedia input fields like ImageField and FileField",
      "It displays media previews for file fields automatically"
    ]
  },
  {
    "q": "In a Django form, which field type would you use to represent a boolean with a checkbox?",
    "c": null,
    "o": [
      "forms.BooleanField()",
      "forms.CheckboxInput()",
      "forms.CheckboxField()",
      "forms.ToggleField()"
    ]
  },
  {
    "q": "Given the following code, what is the role of `instance` when creating the form?",
    "c": "form = ProfileForm(request.POST, instance=profile)",
    "o": [
      "It binds the form to an existing model instance for editing",
      "It ensures that the form ignores database validation",
      "It forces the form to always create a new instance",
      "It disables field validation tied to the model"
    ]
  },
  {
    "q": "Which of the following is TRUE about `form.errors.as_ul()`?",
    "c": null,
    "o": [
      "It renders validation errors as an unordered HTML list",
      "It renders all errors inline next to each field",
      "It serializes errors into JSON format",
      "It prints errors in plain text without HTML"
    ]
  },
  {
    "q": "What is the difference between `validators` argument in a form field and `clean_<fieldname>()`?",
    "c": null,
    "o": [
      "Validators apply reusable checks, while `clean_<fieldname>()` defines custom per-form logic",
      "Validators override all built-in validation, while `clean_<fieldname>()` runs in addition",
      "Validators only run on ModelForms, while `clean_<fieldname>()` runs on regular forms",
      "Validators execute after `clean_<fieldname>()` regardless of order"
    ]
  },
  {
    "q": "Which of the following is the correct way to create a formset for a given form class?",
    "c": null,
    "o": [
      "MyFormSet = formset_factory(MyForm, extra=2)",
      "MyFormSet = FormSet(MyForm, size=2)",
      "MyFormSet = create_formset(MyForm, count=2)",
      "MyFormSet = MyForm.make_formset(extra=2)"
    ]
  },
  {
    "q": "What is the default widget for a Django `DateField`?",
    "c": null,
    "o": [
      "TextInput",
      "DateInput",
      "DateTimePicker",
      "SelectDateWidget"
    ]
  },
  {
    "q": "Which of the following statements about custom form field widgets in Django is TRUE?",
    "c": null,
    "o": [
      "Widgets control the HTML rendering of a field but do not affect validation",
      "Widgets perform both rendering and validation of data",
      "Widgets must always correspond to a built-in Django form field",
      "Widgets automatically apply CSRF protection to the form"
    ]
  },
  {
    "q": "What is the role of the `to_python()` method in a custom Django form field?",
    "c": null,
    "o": [
      "It converts the raw input value into the correct Python type",
      "It validates cross-field consistency between multiple fields",
      "It defines how the field is displayed in HTML",
      "It prevents the field from being included in cleaned_data"
    ]
  },
  {
    "q": "Which of the following is TRUE about `BaseFormSet.clean()`?",
    "c": null,
    "o": [
      "It allows cross-form validation across all forms in the formset",
      "It validates each individual form separately",
      "It disables the need for `form.clean()` methods",
      "It ensures at least one form in the formset is valid"
    ]
  },
  {
    "q": "What is the correct way to customize a ModelForm’s field order?",
    "c": null,
    "o": [
      "Use the `field_order` attribute in the form class",
      "Rearrange the model fields directly",
      "Override `__init__` and reassign `self.fields`",
      "Django enforces alphabetical ordering and cannot be changed"
    ]
  },
  {
    "q": "Which statement is TRUE about `disabled` fields in Django forms?",
    "c": null,
    "o": [
      "They render in the template but do not accept user input or appear in `cleaned_data`",
      "They raise a ValidationError if data is submitted",
      "They are hidden and excluded from HTML rendering",
      "They are validated but cannot be saved to the database"
    ]
  },
  {
    "q": "What is the main difference between `initial` values and `cleaned_data` in Django forms?",
    "c": null,
    "o": [
      "`initial` is used for display before submission, while `cleaned_data` holds validated values after submission",
      "`initial` overrides submitted values, while `cleaned_data` never changes",
      "`initial` applies only to ModelForms, while `cleaned_data` applies to all forms",
      "`initial` is stored in the database, while `cleaned_data` is not"
    ]
  },
  {
    "q": "Which Django class is extended to create a custom FormSet with additional logic?",
    "c": null,
    "o": [
      "BaseFormSet",
      "FormSetMixin",
      "CustomFormManager",
      "FormGroup"
    ]
  },
  {
    "q": "What does the `empty_label` attribute do in a Django `ModelChoiceField`?",
    "c": null,
    "o": [
      "It defines the display text for the empty/default option in dropdowns",
      "It hides the field if no choices are available",
      "It sets the default value stored in the database",
      "It raises a ValidationError when no selection is made"
    ]
  },
  {
    "q": "Which of the following best describes the difference between `clean()` in a form and `clean_<fieldname>()`?",
    "c": null,
    "o": [
      "`clean_<fieldname>()` validates individual fields, while `clean()` handles cross-field validation",
      "`clean()` runs before built-in validation, while `clean_<fieldname>()` runs after",
      "`clean()` is only available in ModelForms, while `clean_<fieldname>()` works in all forms",
      "`clean_<fieldname>()` saves data directly to the model, while `clean()` does not"
    ]
  },
  {
    "q": "Which parameter should be set when creating a custom widget to make it support multiple values?",
    "c": null,
    "o": [
      "allow_multiple_selected = True",
      "multi = True",
      "support_multiple = True",
      "widget_type = 'multi'"
    ]
  },
  {
    "q": "What is the purpose of `form.add_prefix(field_name)`?",
    "c": null,
    "o": [
      "It applies the form’s prefix to the given field name for namespacing",
      "It renames a field dynamically for rendering",
      "It appends extra text to error messages",
      "It sets a label prefix for form fields"
    ]
  },
  {
    "q": "In Django formsets, what is the role of the `can_delete` option?",
    "c": null,
    "o": [
      "It allows users to mark forms for deletion during formset submission",
      "It deletes all empty forms in a formset automatically",
      "It ensures that at least one form in the formset is deleted",
      "It removes forms from being rendered in the template"
    ]
  },
  {
    "q": "What is the default widget used for a `forms.EmailField`?",
    "c": null,
    "o": [
      "TextInput",
      "EmailInput",
      "Textarea",
      "InputField"
    ]
  },
  {
    "q": "What is the purpose of `form.has_changed()` in formsets?",
    "c": null,
    "o": [
      "It detects if any form in the formset has data different from its initial values",
      "It ensures all forms in the formset are valid",
      "It saves only forms with modified data",
      "It forces a reset of unchanged forms"
    ]
  },
  {
    "q": "Which attribute of a Django form field lets you specify HTML attributes like `class` and `id`?",
    "c": null,
    "o": [
      "widget = forms.TextInput(attrs={...})",
      "field_attrs = {...}",
      "html_properties = {...}",
      "style = {...}"
    ]
  },
  {
    "q": "What is the difference between `commit=True` and `commit=False` in `form.save()`?",
    "c": null,
    "o": [
      "`commit=True` saves the model instance to the database, while `commit=False` returns an unsaved instance for further modification",
      "`commit=True` validates data, while `commit=False` skips validation",
      "`commit=True` locks the model instance, while `commit=False` allows edits",
      "`commit=True` is for ModelForms, while `commit=False` is for regular forms"
    ]
  },
  {
    "q": "In Django, what is the role of the `BaseModelForm.clean()` method?",
    "c": null,
    "o": [
      "It allows custom validation logic that applies to multiple fields in a ModelForm",
      "It replaces all default field validation with custom logic",
      "It validates only the primary key field of the model",
      "It ensures database constraints are enforced at the form level"
    ]
  },
  {
    "q": "Which of the following best describes how `error_messages` works in a Django form field?",
    "c": null,
    "o": [
      "It overrides default error text for specific validation errors such as 'required' or 'invalid'",
      "It automatically translates error messages into the active language",
      "It disables validation errors and replaces them with empty strings",
      "It logs validation errors without showing them to the user"
    ]
  },
  {
    "q": "What is the correct way to display non-field errors in a Django template?",
    "c": null,
    "o": [
      "{{ form.non_field_errors }}",
      "{{ form.errors.non_field }}",
      "{{ form.all_errors }}",
      "{{ form.errors['__all__'] }}"
    ]
  },
  {
    "q": "What happens if you pass `empty_permitted=True` when instantiating a Django form?",
    "c": null,
    "o": [
      "The form can be left blank without raising validation errors",
      "The form ignores all validation rules entirely",
      "The form skips saving data to the database",
      "The form automatically fills with default values"
    ]
  },
  {
    "q": "Which method would you override in a custom widget to control how its HTML is rendered?",
    "c": null,
    "o": [
      "render()",
      "to_html()",
      "display()",
      "build_widget()"
    ]
  },
  {
    "q": "When using a `ModelMultipleChoiceField`, what type of object does `cleaned_data` contain for that field?",
    "c": null,
    "o": [
      "A QuerySet of model instances",
      "A list of primary key values",
      "A dictionary mapping IDs to models",
      "A single model instance"
    ]
  },
  {
    "q": "What is the purpose of the `error_css_class` attribute in a Django form?",
    "c": null,
    "o": [
      "It applies a CSS class to fields that fail validation",
      "It hides error messages completely",
      "It ensures that only one field per form can have an error",
      "It overrides the default HTML tag used for errors"
    ]
  },
  {
    "q": "Which statement is TRUE about using `exclude` in a ModelForm’s Meta class?",
    "c": null,
    "o": [
      "It prevents specific model fields from being included in the form",
      "It removes validation from excluded fields but still renders them",
      "It marks excluded fields as hidden inputs in the form",
      "It applies only to fields with `blank=True` in the model"
    ]
  },
  {
    "q": "What is the primary use of the `label_suffix` attribute in a Django form?",
    "c": null,
    "o": [
      "It customizes the character appended to form field labels (e.g., a colon)",
      "It defines a CSS class applied to form labels",
      "It specifies the HTML tag used for rendering labels",
      "It hides labels from being rendered in the template"
    ]
  },
  {
    "q": "Which of the following best describes the behavior of `form.is_bound`?",
    "c": null,
    "o": [
      "It returns True if the form has data bound to it (e.g., via POST or GET)",
      "It returns True only if the form is valid",
      "It indicates whether the form is linked to a model instance",
      "It shows whether the form is saved to the database"
    ]
  },
  {
    "q": "When using `form.as_p()`, what does Django output?",
    "c": null,
    "o": [
      "Form fields wrapped in `<p>` tags with labels and errors",
      "Form fields rendered as plain inputs without HTML",
      "Form fields wrapped in `<div>` tags with classes",
      "Form fields formatted into an HTML table"
    ]
  },
  {
    "q": "In Django forms, what is the role of `boundfield.as_widget()`?",
    "c": null,
    "o": [
      "It renders the HTML representation of a single form field",
      "It validates the field and raises errors if invalid",
      "It saves the value of the field to the database",
      "It converts a Python object into JSON for rendering"
    ]
  },
  {
    "q": "What is the effect of setting `use_required_attribute = False` in a Django Form class?",
    "c": null,
    "o": [
      "It disables automatic inclusion of the `required` HTML attribute on fields",
      "It disables server-side validation for required fields",
      "It marks all form fields as optional regardless of definition",
      "It hides required fields from rendering in templates"
    ]
  },
  {
    "q": "In Django ModelForms, what happens if you omit the `fields` and `exclude` attributes in the Meta class?",
    "c": null,
    "o": [
      "All model fields are included by default",
      "No fields are included, and the form cannot be used",
      "Only required fields are included automatically",
      "Only non-relational fields are included automatically"
    ]
  },
  {
    "q": "Which attribute of a Django form field allows you to override the text shown beside the field?",
    "c": null,
    "o": [
      "label",
      "help_text",
      "verbose_name",
      "description"
    ]
  },
  {
    "q": "What does `form.changed_data` return?",
    "c": null,
    "o": [
      "A list of field names that have values different from their initial values",
      "A dictionary of changed field names and their new values",
      "A boolean indicating if any field changed",
      "The entire set of cleaned data if changes exist"
    ]
  },
  {
    "q": "What happens if you call `is_valid()` on a Django form that has not been bound to any data?",
    "c": null,
    "o": [
      "It will always return False",
      "It will always return True",
      "It will raise a ValidationError",
      "It will skip validation and return None"
    ]
  },
  {
    "q": "In a Django ModelForm, how can you include additional non-model fields?",
    "c": null,
    "o": [
      "By declaring them explicitly as regular form fields in the form class",
      "By adding them to the Meta `fields` list even if not in the model",
      "By setting `extra_fields=True` in Meta",
      "By using a FormSet instead of a ModelForm"
    ]
  },
  {
    "q": "What is the role of `form.media` in Django forms?",
    "c": null,
    "o": [
      "It provides access to the JavaScript and CSS required by widgets in the form",
      "It specifies whether the form can handle file uploads",
      "It generates media queries for responsive design",
      "It determines if images can be embedded in the form"
    ]
  },
  {
    "q": "What is the behavior of `form.full_clean()`?",
    "c": null,
    "o": [
      "It runs validation on all fields and populates `cleaned_data` and `errors`",
      "It validates only required fields and ignores optional ones",
      "It saves the form instance directly to the database",
      "It skips individual field validation and only runs `clean()`"
    ]
  },
  {
    "q": "Which statement about `help_text` in Django forms is TRUE?",
    "c": null,
    "o": [
      "It provides additional descriptive text for a field, displayed in templates",
      "It is displayed only when a field raises a validation error",
      "It overrides the label text of a field",
      "It prevents a field from being required"
    ]
  },
  {
    "q": "Which Django form field would you use to validate an uploaded image?",
    "c": null,
    "o": [
      "forms.ImageField",
      "forms.FileField",
      "forms.MediaField",
      "forms.BinaryField"
    ]
  },
  {
    "q": "What is the purpose of the `initial` argument in a form field definition?",
    "c": null,
    "o": [
      "It sets a default value for the field before user input",
      "It locks the field from being changed",
      "It validates input against a minimum threshold",
      "It ensures the field must always be filled in"
    ]
  },
  {
    "q": "What does the `form.errors.as_json()` method return?",
    "c": null,
    "o": [
      "A JSON-encoded dictionary of validation errors",
      "A serialized string of all field values",
      "A JSON response ready to send via an API",
      "A list of cleaned fields in JSON format"
    ]
  },
  {
    "q": "Which of the following is TRUE about `ModelChoiceField` in Django forms?",
    "c": null,
    "o": [
      "It represents a single model instance, chosen from a QuerySet",
      "It always returns a list of selected objects",
      "It can only be used with ManyToMany relationships",
      "It automatically saves the chosen object to the database"
    ]
  },
  {
    "q": "What does setting `empty_label` in a `ModelChoiceField` do?",
    "c": null,
    "o": [
      "It specifies the text shown for the empty choice in a dropdown",
      "It hides the default empty option in dropdowns",
      "It disables validation for blank selections",
      "It sets the placeholder attribute for input widgets"
    ]
  },
  {
    "q": "Which statement best describes the difference between `FileField` and `ImageField` in Django forms?",
    "c": null,
    "o": [
      "`ImageField` validates that the uploaded file is a valid image, while `FileField` accepts any file type",
      "`FileField` automatically resizes images, while `ImageField` does not",
      "`FileField` stores files in memory, while `ImageField` writes them to disk",
      "`ImageField` works only in ModelForms, while `FileField` works in all forms"
    ]
  },
  {
    "q": "What happens when you call `form.save(commit=False)` on a ModelForm?",
    "c": null,
    "o": [
      "It returns an unsaved model instance that you can modify before saving",
      "It saves the model instance to the database immediately",
      "It validates the form but clears the `cleaned_data` dictionary",
      "It discards all initial values set in the form"
    ]
  },
  {
    "q": "How does Django determine the HTML name attribute for a form field?",
    "c": null,
    "o": [
      "By using the field’s name in the form class, optionally prefixed if the form has a prefix",
      "By using the field’s label text converted to lowercase",
      "By generating a UUID for each field",
      "By using the database column name from the model"
    ]
  },
  {
    "q": "Which of the following is TRUE about `BaseFormSet.is_valid()`?",
    "c": null,
    "o": [
      "It checks validity of all forms in the formset and only returns True if all are valid",
      "It validates only the first form in the formset",
      "It returns True if at least one form is valid",
      "It skips empty forms during validation and always returns True"
    ]
  },
  {
    "q": "What does the `max_num` parameter in a formset do?",
    "c": null,
    "o": [
      "It sets the maximum number of forms that can be submitted in the formset",
      "It defines the maximum number of fields allowed per form",
      "It limits the number of non-empty forms to be validated",
      "It restricts the number of forms that can contain errors"
    ]
  },
  {
    "q": "What is the role of `validators` in Django form fields?",
    "c": null,
    "o": [
      "They are callable functions used to enforce custom validation rules on field values",
      "They ensure that a form field cannot be left empty",
      "They automatically convert field values into database-friendly formats",
      "They provide error messages without raising exceptions"
    ]
  },
  {
    "q": "Which argument allows you to specify custom HTML attributes for a Django form widget?",
    "c": null,
    "o": [
      "attrs",
      "options",
      "properties",
      "extras"
    ]
  },
  {
    "q": "When defining a `ModelForm`, what is the effect of specifying `fields = '__all__'` in the Meta class?",
    "c": null,
    "o": [
      "All fields from the model are included in the form",
      "All fields except relational fields are included",
      "Only non-nullable fields are included",
      "Only required fields are included"
    ]
  },
  {
    "q": "In Django formsets, what does `can_delete=True` enable?",
    "c": null,
    "o": [
      "It adds a checkbox for each form allowing users to mark it for deletion",
      "It allows deletion of the entire formset in one step",
      "It disables validation errors for missing forms",
      "It prevents users from submitting duplicate forms"
    ]
  },
  {
    "q": "What does `form.cleaned_data.get('fieldname')` return if the field is not present?",
    "c": null,
    "o": [
      "None",
      "An empty string",
      "Raises a KeyError",
      "The field’s default value"
    ]
  },
  {
    "q": "What is the purpose of the `to_python()` method in a Django form field?",
    "c": null,
    "o": [
      "It converts the raw input into the appropriate Python data type",
      "It validates that input is not empty",
      "It serializes cleaned data into JSON",
      "It enforces that only strings are accepted as input"
    ]
  },
  {
    "q": "Which method would you override in a `ModelForm` to apply validation that depends on multiple model fields?",
    "c": null,
    "o": [
      "clean()",
      "validate()",
      "full_clean()",
      "is_valid()"
    ]
  },
  {
    "q": "What is returned by accessing `form.fields` on a Django form?",
    "c": null,
    "o": [
      "An ordered dictionary mapping field names to field instances",
      "A list of cleaned data values",
      "A dictionary of widget attributes for all fields",
      "A queryset of fields retrieved from the database"
    ]
  },
  {
    "q": "In Django form validation, what is the difference between `ValidationError` and raising a Python exception like `ValueError`?",
    "c": null,
    "o": [
      "`ValidationError` is caught by Django and attached to the form’s errors, while `ValueError` would crash the process unless handled",
      "`ValidationError` stops all validation immediately, while `ValueError` continues validation",
      "`ValidationError` can only be used in ModelForms, while `ValueError` works in regular forms",
      "`ValidationError` is ignored during form submission, while `ValueError` is displayed to the user"
    ]
  },
  {
    "q": "What is the effect of setting `required=False` on a Django form field?",
    "c": null,
    "o": [
      "The field becomes optional during validation",
      "The field is excluded from rendering in templates",
      "The field is ignored in `cleaned_data`",
      "The field cannot have validators attached"
    ]
  },
  {
    "q": "Which of the following best describes a `ModelMultipleChoiceField`?",
    "c": null,
    "o": [
      "It allows selecting multiple model instances from a QuerySet",
      "It is used for selecting multiple primitive values",
      "It represents a single instance but supports multiple widgets",
      "It automatically saves all selected objects to the database"
    ]
  },
  {
    "q": "How does Django handle form field errors internally?",
    "c": null,
    "o": [
      "Errors are stored in an `ErrorDict` mapping field names to lists of messages",
      "Errors are always logged to the server console",
      "Errors replace the field’s label text in the template",
      "Errors prevent the form from rendering until resolved"
    ]
  },
  {
    "q": "What does the `prefix` argument in a Django Form constructor do?",
    "c": null,
    "o": [
      "It adds a string prefix to field HTML `name` attributes to differentiate multiple forms",
      "It sets a default label prefix for all fields",
      "It determines the order in which fields are rendered",
      "It specifies a namespace for template rendering"
    ]
  },
  {
    "q": "Which method is used to customize saving logic in a `ModelForm` beyond just validation?",
    "c": null,
    "o": [
      "save()",
      "clean()",
      "full_clean()",
      "as_p()"
    ]
  },
  {
    "q": "In Django forms, how is a widget different from a field?",
    "c": null,
    "o": [
      "A widget controls how a field is rendered in HTML, while a field handles validation and data conversion",
      "A widget validates data, while a field stores the raw value",
      "A widget defines database column mapping, while a field defines UI representation",
      "A widget cannot be customized, while a field always can"
    ]
  },
  {
    "q": "What does `form.has_changed()` return?",
    "c": null,
    "o": [
      "A boolean indicating whether any form field values differ from initial values",
      "A list of fields that failed validation",
      "The total number of changed form fields",
      "A dictionary of updated field values"
    ]
  },
  {
    "q": "What happens if a field-specific clean method, such as `clean_email()`, raises a `ValidationError`?",
    "c": null,
    "o": [
      "The error is attached only to that field’s errors list",
      "The entire form is marked invalid without specifying the field",
      "The error overrides any errors from other fields",
      "The error is ignored if `clean()` also exists"
    ]
  },
  {
    "q": "What is the primary purpose of using `forms.HiddenInput` widget?",
    "c": null,
    "o": [
      "It renders the field as a hidden HTML input element while still validating its value",
      "It disables the field so it cannot be submitted",
      "It ensures the field is excluded from `cleaned_data`",
      "It marks the field as optional regardless of `required`"
    ]
  },
  {
    "q": "What does the `instance` argument do when passed to a `ModelForm`?",
    "c": null,
    "o": [
      "It binds the form to an existing model instance for editing",
      "It forces the form to create a new object every time",
      "It disables validation and directly loads database values",
      "It initializes the form with empty fields"
    ]
  },
  {
    "q": "In Django form validation, what happens when the `clean()` method of a form is overridden?",
    "c": null,
    "o": [
      "It allows implementing validation that depends on multiple fields",
      "It disables all field-level validation",
      "It automatically saves the model instance",
      "It removes default widget rendering"
    ]
  },
  {
    "q": "What does `form.add_error('fieldname', 'Error message')` do?",
    "c": null,
    "o": [
      "It attaches a validation error to the specified field",
      "It overrides all previous errors in the form",
      "It raises a `ValidationError` immediately",
      "It prevents the form from running field clean methods"
    ]
  },
  {
    "q": "What does setting `label=''` on a form field achieve?",
    "c": null,
    "o": [
      "It removes the label when rendering the field in templates",
      "It sets the label to be automatically generated from the model",
      "It hides the field entirely from the form",
      "It forces the field to display its help_text as the label"
    ]
  },
  {
    "q": "Which form rendering method wraps fields in paragraph tags?",
    "c": null,
    "o": [
      "as_p()",
      "as_ul()",
      "as_table()",
      "as_dict()"
    ]
  },
  {
    "q": "What does calling `is_multipart()` on a form return?",
    "c": null,
    "o": [
      "True if the form requires `enctype='multipart/form-data'` (e.g., file uploads)",
      "True if the form has more than one field",
      "True if the form can be split across multiple views",
      "True if the form inherits from `ModelForm`"
    ]
  },
  {
    "q": "What is the correct way to dynamically change queryset options of a field inside a Django form?",
    "c": "class MyForm(forms.Form):\n    category = forms.ModelChoiceField(queryset=Category.objects.none())\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.fields['category'].queryset = Category.objects.filter(active=True)",
    "o": [
      "By overriding the `__init__` method and assigning a new queryset",
      "By modifying the Meta class at runtime",
      "By setting `queryset` inside the field’s widget",
      "By overriding the `clean()` method"
    ]
  },
  {
    "q": "In a `ModelForm`, what happens if you exclude a field that is marked as `blank=False` in the model?",
    "c": null,
    "o": [
      "Form validation may fail unless the field has a default value at the model level",
      "The form automatically assigns a placeholder value",
      "The field is silently ignored during save()",
      "The form will always validate successfully regardless of the field"
    ]
  },
  {
    "q": "What is the purpose of `BaseModelForm.clean()` in Django?",
    "c": null,
    "o": [
      "It provides a hook for validation logic involving multiple model fields",
      "It validates individual field values before saving",
      "It forces all fields to run their validators again",
      "It overrides the model’s `full_clean()` method"
    ]
  },
  {
    "q": "Which of the following statements is TRUE about `form.errors.as_json()`?",
    "c": null,
    "o": [
      "It serializes form errors into a JSON-compatible structure",
      "It returns cleaned data in JSON format",
      "It converts field widgets into JSON for rendering",
      "It always returns an empty dictionary if validation fails"
    ]
  },
  {
    "q": "What does `form.initial` contain?",
    "c": null,
    "o": [
      "The initial data dictionary used to prepopulate form fields",
      "A copy of `cleaned_data` after validation",
      "The default values defined at the model level",
      "The data received from the request.POST object"
    ]
  },
  {
    "q": "How does Django handle non-field errors raised in a form’s `clean()` method?",
    "c": null,
    "o": [
      "They are stored in `form.non_field_errors()`",
      "They are attached to the first field in the form",
      "They are ignored unless explicitly re-raised",
      "They overwrite the form’s cleaned_data dictionary"
    ]
  },
  {
    "q": "What is the effect of specifying `fields = []` in the Meta class of a ModelForm?",
    "c": null,
    "o": [
      "No fields will be included in the form",
      "All model fields will be included",
      "Only primary key fields will be included",
      "It raises an ImproperlyConfigured error"
    ]
  },
  {
    "q": "What is the difference between `is_bound` and `is_valid` in Django forms?",
    "c": null,
    "o": [
      "`is_bound` checks if the form has data bound to it, while `is_valid` checks if that data passes validation",
      "`is_bound` validates all fields, while `is_valid` only checks required ones",
      "`is_bound` ensures all fields are rendered, while `is_valid` skips optional ones",
      "`is_bound` is only used in ModelForms, while `is_valid` applies to all forms"
    ]
  },
  {
    "q": "What does setting `widgets` in the Meta class of a ModelForm allow you to do?",
    "c": null,
    "o": [
      "Override the default widgets for model fields in the form",
      "Change validation rules for model fields",
      "Automatically apply CSS classes to form fields",
      "Exclude fields from rendering"
    ]
  },
  {
    "q": "What happens if you call `form.save(commit=False)` on a ModelForm?",
    "c": null,
    "o": [
      "It returns an unsaved model instance that can be modified before saving",
      "It saves the instance but skips validation",
      "It raises an error unless `commit=True` is used",
      "It permanently disables signals like `pre_save` and `post_save`"
    ]
  },
  {
    "q": "In Django forms, what is the role of `error_messages` in a field definition?",
    "c": null,
    "o": [
      "It allows overriding default validation error messages for that field",
      "It defines the order in which errors are displayed",
      "It determines whether errors are stored in the database",
      "It ensures that errors are logged to the console instead of shown"
    ]
  },
  {
    "q": "When using `inlineformset_factory`, what is its main purpose?",
    "c": null,
    "o": [
      "To create a set of forms for related objects linked by a ForeignKey",
      "To generate forms without explicitly declaring fields",
      "To allow inline JavaScript form validation",
      "To automatically save nested serializers"
    ]
  },
  {
    "q": "Which of the following is TRUE about `form.media` in Django?",
    "c": null,
    "o": [
      "It collects and renders JavaScript and CSS required by widgets in the form",
      "It stores the uploaded media files in the configured MEDIA_ROOT",
      "It is used to serve static files when rendering forms",
      "It defines the file encoding for multipart form submissions"
    ]
  },
  {
    "q": "What does `get_context()` on a form field’s widget return?",
    "c": null,
    "o": [
      "A dictionary containing data needed to render the widget",
      "The HTML string of the rendered widget",
      "The cleaned value of the field",
      "The queryset associated with the widget"
    ]
  },
  {
    "q": "In a Django form, what is the effect of `disabled=True` on a field?",
    "c": null,
    "o": [
      "The field is rendered but not editable, and its value is not submitted with the form",
      "The field is hidden from the user and skipped in validation",
      "The field is optional but still editable",
      "The field is excluded from initial data"
    ]
  },
  {
    "q": "What does `BaseFormSet.clean()` allow you to validate?",
    "c": null,
    "o": [
      "Validation that spans across multiple forms in the formset",
      "Validation for a single form’s fields only",
      "Validation of model constraints automatically",
      "Validation that bypasses field-specific validators"
    ]
  },
  {
    "q": "What happens if a ModelForm includes a field corresponding to a `ManyToManyField`?",
    "c": null,
    "o": [
      "The form will provide a multiple-select widget, and saving requires `save_m2m()` if `commit=False`",
      "The form automatically saves the relation without needing extra steps",
      "The field is ignored unless explicitly included in `widgets`",
      "The form raises an error because ManyToMany is unsupported"
    ]
  },
  {
    "q": "Which of the following statements is TRUE about `BoundField` objects in Django?",
    "c": null,
    "o": [
      "They represent a form field tied to a specific form instance and value",
      "They define the default widget for unbound fields",
      "They hold only the raw data from `request.POST` before validation",
      "They are used exclusively for model-level validation"
    ]
  },
  {
    "q": "What is the main difference between `form.is_valid()` and `form.full_clean()`?",
    "c": null,
    "o": [
      "`is_valid()` calls `full_clean()` and returns a boolean, while `full_clean()` performs the actual validation process",
      "`is_valid()` validates only required fields, while `full_clean()` validates all fields",
      "`is_valid()` skips model validation, while `full_clean()` enforces it",
      "`is_valid()` validates field widgets, while `full_clean()` validates models"
    ]
  },
  {
    "q": "How does Django ensure cross-site request forgery (CSRF) protection in forms?",
    "c": null,
    "o": [
      "By including a hidden `csrfmiddlewaretoken` input in forms rendered with `{% csrf_token %}`",
      "By hashing all form field values automatically",
      "By rejecting all POST requests unless explicitly allowed",
      "By encrypting form data in transit using Django’s session keys"
    ]
  },
  {
    "q": "In a `ModelForm`, what happens if you set `exclude = ('id',)` in Meta?",
    "c": null,
    "o": [
      "The primary key field is omitted from the form",
      "The form fails validation since primary key is required",
      "The form ignores all fields including id",
      "The form automatically generates a UUID as primary key"
    ]
  },
  {
    "q": "What is the effect of calling `form.errors.clear()` before rendering?",
    "c": null,
    "o": [
      "It removes all error messages, even if validation has failed",
      "It resets form data to initial values",
      "It prevents fields from being displayed",
      "It skips validation in subsequent calls to `is_valid()`"
    ]
  },
  {
    "q": "What does the `empty_label` attribute of a `ModelChoiceField` do?",
    "c": null,
    "o": [
      "It specifies the label for the empty option in a dropdown",
      "It prevents the user from selecting an empty option",
      "It forces the field to be required",
      "It replaces the default widget with a placeholder text box"
    ]
  },
  {
    "q": "Which of the following describes a `TypedChoiceField`?",
    "c": null,
    "o": [
      "A field that converts the selected choice into a specific Python type",
      "A field that enforces multiple choices to be selected",
      "A field that dynamically loads choices from the database",
      "A field that validates choices against regular expressions"
    ]
  },
  {
    "q": "When rendering a form manually in templates, how do you display all field errors?",
    "c": null,
    "o": [
      "By looping through `form.errors` in the template",
      "By calling `form.as_errors()`",
      "By using `{% render_errors form %}`",
      "By checking `form.cleaned_data` directly"
    ]
  },
  {
    "q": "In Django form handling, what is the purpose of `request.FILES`?",
    "c": null,
    "o": [
      "It provides access to uploaded file data when binding forms",
      "It stores serialized file metadata for form rendering",
      "It holds references to static file paths",
      "It is required even when no file uploads are present"
    ]
  },
  {
    "q": "What happens if you override a `ModelForm` field definition with a different field type?",
    "c": null,
    "o": [
      "The new field type replaces the automatically generated one",
      "The form raises an ImproperlyConfigured error",
      "The field is ignored because it does not match the model",
      "Django generates both fields, causing duplication"
    ]
  },
  {
    "q": "How can you ensure that a form field is only displayed but never modified by the user?",
    "c": null,
    "o": [
      "Set `disabled=True` on the field",
      "Set `editable=False` on the model field",
      "Use `forms.HiddenInput` as the widget",
      "Override the field’s `clean()` method to block changes"
    ]
  },
  {
    "q": "What does `has_changed()` on a Django form instance return?",
    "c": null,
    "o": [
      "True if any form data differs from its initial values",
      "True if validation has failed",
      "True if the form has been bound with request data",
      "True if the form is a ModelForm linked to an instance"
    ]
  },
  {
    "q": "When defining a custom widget, which method must be overridden to generate its HTML?",
    "c": null,
    "o": [
      "render()",
      "get_context()",
      "value_from_datadict()",
      "clean()"
    ]
  },
  {
    "q": "What is the role of `save_m2m()` in a ModelForm when `commit=False` is used?",
    "c": null,
    "o": [
      "It saves the many-to-many data for the form instance",
      "It saves only the foreign key fields",
      "It ensures that related model instances are deleted",
      "It triggers model signals without saving"
    ]
  },
  {
    "q": "What happens if `form.is_valid()` is called without first binding the form with data?",
    "c": null,
    "o": [
      "It always returns False since unbound forms cannot validate",
      "It raises a ValueError because no data is present",
      "It returns True if the form has no required fields",
      "It runs validation but skips cleaning"
    ]
  },
  {
    "q": "Which of the following is a valid use case for `BaseInlineFormSet`?",
    "c": null,
    "o": [
      "Customizing validation across multiple inline forms for related models",
      "Automatically adding default values to all formsets",
      "Converting regular forms into model-bound forms",
      "Applying CSS styling rules to inline fields"
    ]
  },
  {
    "q": "What happens if you define a `clean_<fieldname>()` method in a Django Form?",
    "c": null,
    "o": [
      "It performs custom validation for that specific field",
      "It overrides the form’s `clean()` method entirely",
      "It skips model validation for that field",
      "It disables default validators for that field"
    ]
  },
  {
    "q": "In a `ModelForm`, how does Django decide which database fields to include when neither `fields` nor `exclude` is specified?",
    "c": null,
    "o": [
      "All fields from the model will be included by default",
      "No fields will be included",
      "Only primary key and required fields are included",
      "It raises an ImproperlyConfigured error"
    ]
  },
  {
    "q": "What is the difference between `cleaned_data` and `initial` in Django forms?",
    "c": null,
    "o": [
      "`cleaned_data` contains validated user input, while `initial` contains default or pre-populated values",
      "`cleaned_data` holds model defaults, while `initial` comes from request.POST",
      "`cleaned_data` is set before validation, while `initial` is set after",
      "`cleaned_data` only applies to ModelForms, while `initial` applies to all forms"
    ]
  },
  {
    "q": "Which of the following best describes the `ValidationError` exception in Django forms?",
    "c": null,
    "o": [
      "It is raised when field or form validation fails",
      "It is raised when saving invalid models to the database",
      "It is raised when rendering templates with errors",
      "It is raised when CSRF validation fails"
    ]
  },
  {
    "q": "In a `FormSet`, what does the `extra` parameter control?",
    "c": null,
    "o": [
      "The number of additional empty forms displayed",
      "The maximum number of total forms allowed",
      "The validation rules applied across forms",
      "The deletion of forms marked with a delete checkbox"
    ]
  },
  {
    "q": "What happens if you override a `ModelForm` field with `required=False` while the model field has `blank=False`?",
    "c": null,
    "o": [
      "The form will allow blank input, but saving may fail due to model constraints",
      "The form enforces the model’s `blank=False` regardless",
      "The field will not appear in the form at all",
      "Django raises an ImproperlyConfigured error"
    ]
  },
  {
    "q": "What is the difference between `form.as_p()` and `form.as_table()`?",
    "c": null,
    "o": [
      "`as_p()` renders fields wrapped in <p> tags, while `as_table()` renders them in table rows",
      "`as_p()` renders only required fields, while `as_table()` renders all fields",
      "`as_p()` skips validation, while `as_table()` runs validation automatically",
      "`as_p()` outputs HTML-safe text, while `as_table()` outputs raw values"
    ]
  },
  {
    "q": "When using `form.cleaned_data['field']`, at what point is this data available?",
    "c": null,
    "o": [
      "Only after `form.is_valid()` has been called and returned True",
      "Immediately after the form is bound to data",
      "As soon as the form is instantiated",
      "Before validation but after `full_clean()`"
    ]
  },
  {
    "q": "Which of the following best explains the purpose of `form.non_field_errors()`?",
    "c": null,
    "o": [
      "It returns validation errors that are not associated with any specific field",
      "It lists database constraint errors only",
      "It shows errors for hidden fields only",
      "It provides default placeholder messages when no fields are invalid"
    ]
  },
  {
    "q": "What is the effect of setting `min_num` and `validate_min=True` in a FormSet?",
    "c": null,
    "o": [
      "It enforces that at least `min_num` forms must be submitted with valid data",
      "It ensures that `min_num` empty forms are always rendered",
      "It prevents extra empty forms from being created",
      "It enforces maximum form count validation"
    ]
  },
  {
    "q": "In Django, what happens if a form uses `required=False` but no input is provided?",
    "c": null,
    "o": [
      "The field will return `None` or an empty value in `cleaned_data`",
      "The form will raise a validation error",
      "The form automatically assigns the model’s default value",
      "The form excludes the field from `cleaned_data` entirely"
    ]
  },
  {
    "q": "What does `empty_permitted=True` mean when used in a FormSet form?",
    "c": null,
    "o": [
      "The form can be left empty without raising validation errors",
      "The form skips rendering unless required",
      "The form’s fields will not have `required` attributes",
      "The form will not be saved even if valid"
    ]
  },
  {
    "q": "Which method should be overridden to add cross-field validation logic in a custom Form?",
    "c": null,
    "o": [
      "clean()",
      "is_valid()",
      "full_clean()",
      "validate()"
    ]
  },
  {
    "q": "What is the default behavior of a `ModelChoiceField` when its queryset is empty?",
    "c": null,
    "o": [
      "It renders as an empty dropdown with no options",
      "It raises an error during form initialization",
      "It disables the field automatically",
      "It replaces the field with a CharField input"
    ]
  },
  {
    "q": "In a Django form, what does setting `label=''` (an empty string) on a field do?",
    "c": null,
    "o": [
      "It removes the label text when rendering the field",
      "It forces the field to be excluded from validation",
      "It replaces the label with the field name",
      "It hides the field from rendering"
    ]
  },
  {
    "q": "What happens if a file field is included in a form but the `<form>` tag lacks `enctype=\"multipart/form-data\"`?",
    "c": null,
    "o": [
      "The uploaded file will not be included in `request.FILES`",
      "Django automatically converts it to base64 and includes it in POST",
      "The file is stored in session instead of request",
      "The form raises a TemplateSyntaxError"
    ]
  },
  {
    "q": "What happens if a `ModelForm` includes a model field with `unique=True` and two identical values are submitted?",
    "c": null,
    "o": [
      "The form raises a validation error before saving to the database",
      "The form allows it but fails with an IntegrityError on save",
      "The form silently replaces one of the values",
      "The form ignores uniqueness checks unless explicitly added"
    ]
  },
  {
    "q": "What is the role of `BaseModelForm.clean()` in comparison to a model’s `clean()` method?",
    "c": null,
    "o": [
      "`BaseModelForm.clean()` applies form-level validation, while the model’s `clean()` applies model-level validation",
      "`BaseModelForm.clean()` overrides model validation completely",
      "`BaseModelForm.clean()` only validates foreign keys, while model `clean()` validates all fields",
      "`BaseModelForm.clean()` is called after saving, while model `clean()` is called before saving"
    ]
  },
  {
    "q": "If a form is unbound (`form.is_bound` is False), what does calling `form.errors` return?",
    "c": null,
    "o": [
      "An empty `ErrorDict`",
      "A list of default error messages",
      "A dictionary with None for each field",
      "Raises a ValidationError"
    ]
  },
  {
    "q": "What is the difference between `form.has_changed()` and `form.changed_data`?",
    "c": null,
    "o": [
      "`has_changed()` returns a boolean, while `changed_data` lists the names of changed fields",
      "`has_changed()` checks all fields, while `changed_data` checks only required fields",
      "`has_changed()` compares with cleaned_data, while `changed_data` compares with request.POST",
      "`has_changed()` works only on ModelForms, while `changed_data` works on all forms"
    ]
  },
  {
    "q": "Which statement about `inlineformset_factory()` is correct?",
    "c": null,
    "o": [
      "It creates a FormSet for editing related objects of a parent model",
      "It is used to generate nested forms within a single HTML form",
      "It converts any regular form into a formset automatically",
      "It only works with abstract base models"
    ]
  },
  {
    "q": "What happens when you set `use_required_attribute=False` in a Django form?",
    "c": null,
    "o": [
      "HTML5 'required' attributes will not be added to fields even if they are required",
      "The form skips server-side validation for required fields",
      "The form automatically converts required fields into optional fields",
      "The form cannot be rendered using `as_p()` or `as_table()`"
    ]
  },
  {
    "q": "What does `validate_unique()` do in a `ModelForm`?",
    "c": null,
    "o": [
      "It checks database uniqueness constraints across fields",
      "It ensures that no duplicate form data exists within the same FormSet",
      "It validates that each form field has a unique name",
      "It prevents duplicate POST submissions by tokenizing forms"
    ]
  },
  {
    "q": "When defining a custom `clean()` method in a Form, what must you always return?",
    "c": null,
    "o": [
      "The cleaned_data dictionary",
      "A list of valid field names",
      "A tuple of (is_valid, errors)",
      "The raw POST dictionary"
    ]
  },
  {
    "q": "What does `commit=False` in a `ModelForm.save()` allow you to do?",
    "c": null,
    "o": [
      "Modify the instance before saving it to the database",
      "Skip database validation permanently",
      "Bypass all form field cleaning logic",
      "Force the form to return a dictionary instead of a model instance"
    ]
  },
  {
    "q": "Which field is automatically excluded from a `ModelForm` unless explicitly included?",
    "c": null,
    "o": [
      "AutoField (e.g., primary key fields)",
      "CharField",
      "ForeignKey",
      "DateTimeField with auto_now"
    ]
  },
  {
    "q": "How can you enforce that at least one form in a FormSet must be valid and non-empty?",
    "c": null,
    "o": [
      "Override the FormSet’s `clean()` method",
      "Set `min_num=1` without validation",
      "Use `required=True` on all fields",
      "Use `extra=0` in the formset factory"
    ]
  },
  {
    "q": "What does `error_class` in Django forms allow you to customize?",
    "c": null,
    "o": [
      "The HTML class applied to form error messages",
      "The validation rules used by fields",
      "The HTTP status code returned on error",
      "The error handling behavior in templates"
    ]
  },
  {
    "q": "What is the main difference between `initial` and `instance` when instantiating a ModelForm?",
    "c": null,
    "o": [
      "`initial` pre-fills fields with values without linking to the database, while `instance` binds the form to a specific database object",
      "`initial` is applied after validation, while `instance` is applied before validation",
      "`initial` overrides model values permanently, while `instance` does not",
      "`initial` can only be used for ModelForms, while `instance` works for all forms"
    ]
  },
  {
    "q": "What happens if you define both `clean_fieldname()` and override `clean()` in a Form?",
    "c": null,
    "o": [
      "Both methods run, with `clean_fieldname()` running before `clean()`",
      "Only the `clean()` method is executed",
      "Only the `clean_fieldname()` method is executed",
      "It raises a MultipleValidationError"
    ]
  },
  {
    "q": "When working with file uploads, what is the role of the `FileSystemStorage` class?",
    "c": null,
    "o": [
      "It handles saving uploaded files to disk and retrieving them",
      "It validates file size and type before saving",
      "It converts uploaded files into memory streams only",
      "It ensures that only authenticated users can upload files"
    ]
  },
  {
    "q": "What is the primary use case of `modelformset_factory()`?",
    "c": null,
    "o": [
      "To create a formset tied to a particular model",
      "To create nested inline formsets automatically",
      "To extend a formset with custom widgets",
      "To generate forms for unrelated models in one formset"
    ]
  },
  {
    "q": "What is the default behavior of a `BooleanField` in a Django Form when left unchecked in the submitted data?",
    "c": null,
    "o": [
      "It returns False in `cleaned_data`",
      "It raises a ValidationError",
      "It is excluded from `cleaned_data`",
      "It defaults to None"
    ]
  },
  {
    "q": "In a `ModelForm`, how are model field `validators` treated?",
    "c": null,
    "o": [
      "They are automatically included in the form field validation process",
      "They are ignored unless explicitly passed to the form",
      "They must be redefined inside the form’s Meta class",
      "They only run when `form.save()` is called"
    ]
  },
  {
    "q": "What happens if a form has a `DateField` but the user submits an invalid date format?",
    "c": null,
    "o": [
      "A `ValidationError` is raised and added to the field’s errors",
      "The field is silently ignored and set to None",
      "Django attempts to coerce the input into a valid date",
      "The form raises a TypeError at runtime"
    ]
  },
  {
    "q": "Which of the following allows you to dynamically add extra fields to a Django form at runtime?",
    "c": null,
    "o": [
      "Assigning new fields to `self.fields` in the form’s `__init__` method",
      "Adding fields inside the form’s Meta class",
      "Calling `add_field()` on the form instance",
      "Passing a `dynamic_fields=True` argument to the form"
    ]
  },
  {
    "q": "What happens if you call `form.save(commit=True)` twice on the same bound ModelForm?",
    "c": null,
    "o": [
      "The object is saved twice, potentially creating duplicate database writes",
      "Django raises a `MultipleSaveError`",
      "The second call is ignored silently",
      "The second call clears cleaned_data before saving"
    ]
  },
  {
    "q": "When using `ModelMultipleChoiceField`, what does the `to_field_name` parameter control?",
    "c": null,
    "o": [
      "Which model field is used for lookups when validating submitted data",
      "The database column used for foreign key joins",
      "The HTML label shown in the form widget",
      "The sorting order of the queryset"
    ]
  },
  {
    "q": "Which of the following is TRUE about `BaseFormSet.is_valid()`?",
    "c": null,
    "o": [
      "It returns True only if all forms are valid and no formset-level errors exist",
      "It validates only the first form in the set",
      "It ignores empty forms regardless of `empty_permitted`",
      "It always raises an exception on invalid data"
    ]
  },
  {
    "q": "What is the behavior of a `HiddenInput` widget in a form?",
    "c": null,
    "o": [
      "It renders the field as a hidden `<input type=\"hidden\">` element",
      "It excludes the field from validation automatically",
      "It prevents the field from being included in POST data",
      "It makes the field optional regardless of model constraints"
    ]
  },
  {
    "q": "In a custom `Form.clean()` method, what happens if you forget to return `cleaned_data`?",
    "c": null,
    "o": [
      "The form will lose all cleaned field values",
      "The form will still work as expected",
      "Django automatically reconstructs `cleaned_data` from fields",
      "Only field-level `clean_fieldname()` methods will run"
    ]
  },
  {
    "q": "Which of the following is TRUE about `BaseForm.add_prefix()`?",
    "c": null,
    "o": [
      "It determines the HTML name attribute for form fields, useful in formsets",
      "It adds labels automatically to all fields",
      "It prefixes cleaned_data keys with the form’s class name",
      "It is used only for file upload forms"
    ]
  },
  {
    "q": "What is the primary purpose of `get_form_kwargs()` when overriding a `FormView`?",
    "c": null,
    "o": [
      "To pass additional arguments into the form’s constructor",
      "To inject CSRF tokens into form rendering",
      "To enforce validation logic outside the form",
      "To automatically save the form to the database"
    ]
  },
  {
    "q": "When a `ModelForm` is created with `fields='__all__'`, which fields are automatically excluded?",
    "c": null,
    "o": [
      "Auto-created primary key fields and many-to-many fields with `through`",
      "All nullable fields",
      "All ForeignKey fields",
      "DateTime fields with `auto_now_add` only"
    ]
  },
  {
    "q": "What is the role of `BaseFormSet.clean()`?",
    "c": null,
    "o": [
      "To implement validation across multiple forms in the formset",
      "To clean individual fields within each form",
      "To override the form’s default widget rendering",
      "To reinitialize the queryset before validation"
    ]
  },
  {
    "q": "Which statement about `BoundField` objects in Django is correct?",
    "c": null,
    "o": [
      "They represent a form field bound to data and provide rendering helpers",
      "They only exist for required fields in a form",
      "They skip validation and only render HTML",
      "They are used internally for formsets only"
    ]
  },
  {
    "q": "What is the effect of setting `empty_label=None` in a `ModelChoiceField`?",
    "c": null,
    "o": [
      "The dropdown will not include a blank choice",
      "The field cannot be left unselected",
      "The queryset is filtered to exclude null values",
      "The widget is rendered as a text field instead"
    ]
  },
  {
    "q": "In a `modelformset_factory`, what does setting `can_delete=True` do?",
    "c": null,
    "o": [
      "It adds a checkbox to each form allowing users to delete objects",
      "It disables saving new instances in the formset",
      "It prevents database-level deletions unless overridden",
      "It marks empty forms as deletable automatically"
    ]
  },
  {
    "q": "When overriding a `ModelForm`'s `save()` method, why might you call `super().save(commit=False)`?",
    "c": null,
    "o": [
      "To get an unsaved instance that can be modified before persisting",
      "To bypass field validation completely",
      "To create a new instance even if one already exists",
      "To prevent database writes permanently"
    ]
  },
  {
    "q": "Which argument in `modelform_factory()` allows you to include or exclude specific model fields?",
    "c": null,
    "o": [
      "`fields` and `exclude`",
      "`only` and `skip`",
      "`include` and `omit`",
      "`required` and `optional`"
    ]
  },
  {
    "q": "What happens if a field with `required=True` has an empty value in a submitted Django Form?",
    "c": null,
    "o": [
      "The form adds a ValidationError for that field",
      "The field is excluded from cleaned_data silently",
      "The field defaults to None and passes validation",
      "The form raises a TypeError immediately"
    ]
  },
  {
    "q": "What does setting `empty_permitted=True` on a form in a FormSet imply?",
    "c": null,
    "o": [
      "The form can be left blank and will not trigger validation",
      "The form will not render any fields in HTML",
      "The form is ignored when saving the formset",
      "The form bypasses CSRF token validation"
    ]
  },
  {
    "q": "How can you restrict a `ModelChoiceField` to display only a subset of objects?",
    "c": null,
    "o": [
      "By providing a custom `queryset` argument",
      "By setting `choices` in the Meta class",
      "By using `exclude` in the ModelForm’s Meta",
      "By overriding `cleaned_data`"
    ]
  },
  {
    "q": "In a form, what is the main role of the `to_python()` method in a Field class?",
    "c": null,
    "o": [
      "To convert raw input into the appropriate Python data type",
      "To enforce HTML rendering of widgets",
      "To check database integrity constraints",
      "To serialize data before saving"
    ]
  },
  {
    "q": "Which of the following statements about `MultiValueField` is TRUE?",
    "c": null,
    "o": [
      "It combines values from multiple widgets into one Python value",
      "It renders multiple fields but stores them separately",
      "It is only available for ModelForms",
      "It disables field-level validation"
    ]
  },
  {
    "q": "When creating a custom form widget, which method is primarily responsible for rendering the HTML?",
    "c": null,
    "o": [
      "`render()`",
      "`build_widget()`",
      "`as_html()`",
      "`to_html()`"
    ]
  },
  {
    "q": "Which Django form attribute holds errors that are not associated with a specific field?",
    "c": null,
    "o": [
      "`non_field_errors()`",
      "`general_errors`",
      "`global_errors()`",
      "`form_errors`"
    ]
  },
  {
    "q": "What does `BaseForm._post_clean()` do in the form lifecycle?",
    "c": null,
    "o": [
      "Runs model validation and calls the form’s `clean()` method",
      "Cleans individual fields before running validators",
      "Handles CSRF token validation",
      "Finalizes rendering of hidden fields"
    ]
  },
  {
    "q": "In Django’s `FormView`, what happens if `form_valid()` is not overridden?",
    "c": null,
    "o": [
      "The form is redirected to `get_success_url()` after saving",
      "The form raises an ImproperlyConfigured error",
      "The form is validated but not saved",
      "The form is discarded and re-rendered"
    ]
  },
  {
    "q": "Which parameter in `inlineformset_factory()` links the child formset to the parent model?",
    "c": null,
    "o": [
      "`fk_name`",
      "`related_field`",
      "`link_field`",
      "`parent_lookup`"
    ]
  },
  {
    "q": "If a custom validator raises a `ValidationError` in a Form field, where does the error message appear?",
    "c": null,
    "o": [
      "In the field’s error list accessible via `form[field].errors`",
      "In the form’s non-field errors",
      "In the database rollback logs",
      "Only in `form.cleaned_data`"
    ]
  },
  {
    "q": "What is the purpose of the `has_changed()` method on a Form?",
    "c": null,
    "o": [
      "It checks whether submitted data differs from the initial data",
      "It verifies if the form was saved successfully",
      "It detects whether any validators were skipped",
      "It determines if form widgets were re-rendered"
    ]
  },
  {
    "q": "Which widget would you use to render multiple selections as checkboxes?",
    "c": null,
    "o": [
      "`CheckboxSelectMultiple`",
      "`MultipleChoiceWidget`",
      "`RadioSelect`",
      "`SelectMultiple`"
    ]
  },
  {
    "q": "In Django forms, what does `is_bound` indicate?",
    "c": null,
    "o": [
      "Whether the form has data bound to it (e.g., via POST or GET)",
      "Whether the form is saved to the database",
      "Whether the form’s fields are marked as required",
      "Whether the form is part of a formset"
    ]
  },
  {
    "q": "What does calling `form.errors.as_json()` return?",
    "c": null,
    "o": [
      "A JSON-encoded string of all form errors",
      "A dictionary of errors grouped by field",
      "A Python list of error messages",
      "A JSON object containing only non-field errors"
    ]
  },
  {
    "q": "Which method in a custom form field class ensures that a value is valid at the Python object level?",
    "c": null,
    "o": [
      "`clean()`",
      "`validate()`",
      "`to_python()`",
      "`full_clean()`"
    ]
  },
  {
    "q": "If a form field uses `choices`, what happens if a submitted value is not part of the defined choices?",
    "c": null,
    "o": [
      "A ValidationError is raised for that field",
      "The value is accepted and stored as-is",
      "The field defaults to the first available choice",
      "The value is silently dropped from cleaned_data"
    ]
  },
  {
    "q": "In a `ModelForm`, what does setting `fields = []` inside the Meta class imply?",
    "c": null,
    "o": [
      "No fields from the model will be included in the form",
      "All model fields will be included by default",
      "Only nullable fields are included",
      "Only primary key fields are excluded"
    ]
  },
  {
    "q": "Which argument in a `FileField` specifies the maximum allowed file size?",
    "c": null,
    "o": [
      "There is no built-in argument; custom validation is required",
      "`max_length`",
      "`max_size`",
      "`filesize_limit`"
    ]
  },
  {
    "q": "How does Django determine the default HTML widget for a `CharField` in a Form?",
    "c": null,
    "o": [
      "By mapping the field type to a `TextInput` widget",
      "By checking the database column type",
      "By using the first widget listed in Meta",
      "By rendering as a `<textarea>` by default"
    ]
  },
  {
    "q": "Which attribute in a Form field determines if an empty string is converted to None?",
    "c": null,
    "o": [
      "`empty_value`",
      "`convert_empty`",
      "`default_none`",
      "`null_on_empty`"
    ]
  },
  {
    "q": "What is the effect of calling `full_clean()` on a ModelForm instance?",
    "c": null,
    "o": [
      "It runs field validation, form-wide validation, and model validation",
      "It saves the model instance immediately",
      "It clears all field values and errors",
      "It only validates model-level unique constraints"
    ]
  },
  {
    "q": "In Django, what does the `clean_<fieldname>()` method inside a Form class do?",
    "c": null,
    "o": [
      "It validates and cleans a single specific field",
      "It validates all fields together",
      "It skips validation for that field",
      "It overrides widget rendering for that field"
    ]
  },
  {
    "q": "Which keyword argument in a Form field specifies whether blank values are permitted during validation?",
    "c": null,
    "o": [
      "`required`",
      "`blank`",
      "`nullable`",
      "`optional`"
    ]
  },
  {
    "q": "What does `form.is_valid()` return if `is_bound=False`?",
    "c": null,
    "o": [
      "Always False",
      "Always True",
      "It raises a ValidationError",
      "It returns None"
    ]
  },
  {
    "q": "What is the purpose of the `error_messages` dictionary in a Form field?",
    "c": null,
    "o": [
      "To override default error messages for that field",
      "To store error logs for debugging",
      "To specify HTML rendering of error lists",
      "To pass error messages from views to forms"
    ]
  },
  {
    "q": "Which Django utility helps in rendering form fields with custom HTML layouts?",
    "c": null,
    "o": [
      "Form rendering APIs (`as_p`, `as_table`, `as_ul`)",
      "Custom validators",
      "Field factories",
      "Model manager methods"
    ]
  },
  {
    "q": "In a FormSet, what does `extra=2` imply?",
    "c": null,
    "o": [
      "Two extra blank forms are displayed for creating new instances",
      "Two forms are skipped during validation",
      "Two additional validation rules are applied",
      "Two database queries are made per form"
    ]
  },
  {
    "q": "What is the difference between `ModelMultipleChoiceField` and `ModelChoiceField`?",
    "c": null,
    "o": [
      "`ModelMultipleChoiceField` allows selecting multiple model instances, while `ModelChoiceField` allows only one",
      "They are identical except for different widget defaults",
      "`ModelChoiceField` bypasses validation, but `ModelMultipleChoiceField` enforces it",
      "One works only for ForeignKey, the other only for ManyToManyField"
    ]
  },
  {
    "q": "If you override the `save()` method of a `ModelForm`, what should you typically do after custom logic?",
    "c": null,
    "o": [
      "Call `super().save(commit=commit)`",
      "Call `form.full_clean()`",
      "Manually validate the form again",
      "Re-initialize the model instance"
    ]
  },
  {
    "q": "Which attribute of a Django form field specifies the default value if no data is provided?",
    "c": null,
    "o": [
      "`initial`",
      "`default`",
      "`placeholder`",
      "`start_value`"
    ]
  },
  {
    "q": "What does the `use_required_attribute=False` setting in a Django Form’s Meta class affect?",
    "c": null,
    "o": [
      "It disables adding HTML5 `required` attributes on rendered fields",
      "It disables Python-side validation for required fields",
      "It removes all validators for optional fields",
      "It forces required fields to allow blank input"
    ]
  },
  {
    "q": "When saving a `ModelForm` with `commit=False`, what must be done to persist related ManyToMany data?",
    "c": null,
    "o": [
      "Call `form.save_m2m()` after saving the instance",
      "Call `instance.refresh_from_db()`",
      "Manually insert records into the through table",
      "Nothing, ManyToMany data is saved automatically"
    ]
  },
  {
    "q": "What is the difference between `form.add_error()` and raising a `ValidationError` in `clean()`?",
    "c": null,
    "o": [
      "`add_error()` attaches an error to a specific field or to non-field errors explicitly, while `ValidationError` is caught and converted automatically",
      "`add_error()` skips validation logic, while `ValidationError` enforces it",
      "`add_error()` clears cleaned_data, while `ValidationError` preserves it",
      "They are functionally identical in all cases"
    ]
  },
  {
    "q": "In Django forms, which of the following methods would you override to implement custom cross-field validation?",
    "c": null,
    "o": [
      "`clean()`",
      "`full_clean()`",
      "`is_valid()`",
      "`validate_all()`"
    ]
  },
  {
    "q": "What happens if a field is listed in both `fields` and `exclude` in a ModelForm’s Meta class?",
    "c": null,
    "o": [
      "The field will be excluded",
      "The field will be included",
      "Django raises an ImproperlyConfigured error",
      "The field is ignored but triggers a warning"
    ]
  },
  {
    "q": "Which Django class provides low-level functionality for validating and cleaning data before rendering or saving?",
    "c": null,
    "o": [
      "`BaseForm`",
      "`FormMixin`",
      "`FormView`",
      "`FormManager`"
    ]
  },
  {
    "q": "Why would you use `prefetch_related` in a queryset passed to a `ModelChoiceField`?",
    "c": null,
    "o": [
      "To optimize database queries when rendering large option lists",
      "To enforce unique constraints before validation",
      "To skip form field validation",
      "To allow multiple choice selection automatically"
    ]
  },
  {
    "q": "What does the `hidden_widget` attribute of a form field specify?",
    "c": null,
    "o": [
      "Which widget to use when the field is rendered as hidden",
      "Whether the field is excluded from validation",
      "That the field should not appear in cleaned_data",
      "That the field’s errors are never displayed"
    ]
  },
  {
    "q": "Which method on a FormSet ensures validation across all its forms?",
    "c": null,
    "o": [
      "`clean()`",
      "`is_valid()`",
      "`validate_all()`",
      "`full_clean()`"
    ]
  },
  {
    "q": "If you define a `Meta.widgets` dictionary in a `ModelForm`, what happens?",
    "c": null,
    "o": [
      "The specified widgets replace the defaults for those fields",
      "The form ignores validation for fields with custom widgets",
      "The form renders with no widgets unless manually defined",
      "Widgets are applied only if the field is excluded from `fields`"
    ]
  },
  {
    "q": "What does the `label_suffix` argument in a Form control?",
    "c": null,
    "o": [
      "The character(s) appended to field labels (default ':')",
      "Whether labels are rendered in bold",
      "Whether labels are shown at all",
      "The placeholder text of form inputs"
    ]
  },
  {
    "q": "In Django, what does setting `disabled=True` on a form field do?",
    "c": null,
    "o": [
      "Renders the field as uneditable in HTML but still validates its value",
      "Excludes the field from form validation",
      "Removes the field entirely from rendering",
      "Marks the field as optional automatically"
    ]
  },
  {
    "q": "What is the role of `form.media` in Django forms?",
    "c": null,
    "o": [
      "It collects JavaScript and CSS required by widgets",
      "It specifies the upload handlers for FileFields",
      "It defines how to serialize form data",
      "It stores binary media attached to form submissions"
    ]
  },
  {
    "q": "Which management form field in a FormSet tracks how many total forms are being submitted?",
    "c": null,
    "o": [
      "`TOTAL_FORMS`",
      "`INITIAL_FORMS`",
      "`MAX_NUM_FORMS`",
      "`MANAGEMENT_COUNT`"
    ]
  },
  {
    "q": "In a ModelForm, when would you override `get_form_kwargs()` in a class-based view?",
    "c": null,
    "o": [
      "When you need to pass additional arguments into the form’s constructor",
      "When you want to disable form validation",
      "When you want to exclude fields dynamically",
      "When you want to change the default form class"
    ]
  },
  {
    "q": "What is the main purpose of the `validators` argument in a Django form field?",
    "c": null,
    "o": [
      "It allows attaching custom validation functions to the field",
      "It determines the HTML widget rendered",
      "It sets default values for the field",
      "It defines model relationships for the field"
    ]
  },
  {
    "q": "Which form method returns a dictionary of fields and their cleaned values only after validation?",
    "c": null,
    "o": [
      "`cleaned_data`",
      "`initial`",
      "`as_dict()`",
      "`get_data()`"
    ]
  },
  {
    "q": "When overriding the `save()` method of a ModelForm, what does `commit=True` ensure?",
    "c": null,
    "o": [
      "The instance is saved to the database immediately",
      "The instance is returned but not persisted",
      "The instance skips ManyToMany relationships",
      "Validation is bypassed before saving"
    ]
  },
  {
    "q": "What happens if you call `form.save(commit=False)` and do not save the instance afterward?",
    "c": null,
    "o": [
      "The instance is never persisted to the database",
      "Django raises a ValidationError",
      "The instance is automatically saved on cleanup",
      "The instance is saved only partially (excluding foreign keys)"
    ]
  },
  {
    "q": "Which attribute of a Form field is commonly used to supply custom HTML placeholder text?",
    "c": null,
    "o": [
      "`widget.attrs['placeholder']`",
      "`placeholder`",
      "`help_text`",
      "`initial`"
    ]
  },
  {
    "q": "Which FormSet management field prevents adding more forms than a specified limit?",
    "c": null,
    "o": [
      "`MAX_NUM_FORMS`",
      "`TOTAL_FORMS`",
      "`INITIAL_FORMS`",
      "`LIMIT_FORMS`"
    ]
  },
  {
    "q": "What is the default widget used for a `BooleanField` in Django forms?",
    "c": null,
    "o": [
      "CheckboxInput",
      "Select",
      "RadioSelect",
      "TextInput"
    ]
  },
  {
    "q": "What is the difference between `help_text` and `error_messages` in a Django form field?",
    "c": null,
    "o": [
      "`help_text` provides guidance displayed with the field, while `error_messages` customizes validation error messages",
      "`help_text` overrides validation, while `error_messages` does not",
      "`help_text` is used only for admin, `error_messages` only for templates",
      "They are interchangeable in functionality"
    ]
  },
  {
    "q": "In Django’s form handling, what does calling `form.has_changed()` check?",
    "c": null,
    "o": [
      "Whether the submitted data differs from the initial data",
      "Whether the form has been validated",
      "Whether any errors exist in the form",
      "Whether the form has been rendered"
    ]
  },
  {
    "q": "What is the primary role of `BaseModelForm` in Django?",
    "c": null,
    "o": [
      "It provides the underlying implementation for all ModelForms",
      "It handles rendering widgets for model fields",
      "It ensures ManyToMany fields are always saved automatically",
      "It manages queryset evaluation in form fields"
    ]
  },
  {
    "q": "Which method in a Django Form can you override to change how fields are initialized at runtime?",
    "c": null,
    "o": [
      "`__init__`",
      "`setup_fields`",
      "`full_clean`",
      "`reset`"
    ]
  },
  {
    "q": "What does `form.errors.as_json()` return?",
    "c": null,
    "o": [
      "Validation errors serialized as a JSON string",
      "A dictionary of cleaned_data",
      "A JSON representation of all form fields",
      "Error messages along with rendered HTML"
    ]
  },
  {
    "q": "Which field type is automatically used by a ModelForm for a Django `DateTimeField`?",
    "c": null,
    "o": [
      "`DateTimeField` with a `DateTimeInput` widget",
      "`CharField` with a `TextInput` widget",
      "`DateField` with a `DateInput` widget",
      "`ChoiceField` with a `Select` widget"
    ]
  },
  {
    "q": "What happens if a field is defined in a ModelForm but not present in the model?",
    "c": null,
    "o": [
      "It is treated as a normal form field, not bound to the model",
      "It raises an ImproperlyConfigured exception",
      "It is silently ignored",
      "It overrides an unrelated model field"
    ]
  },
  {
    "q": "Which argument of a Django form field is specifically used to show descriptive text below the field in templates?",
    "c": null,
    "o": [
      "`help_text`",
      "`description`",
      "`verbose_name`",
      "`tooltip`"
    ]
  },
  {
    "q": "What is the purpose of `inlineformset_factory` in Django?",
    "c": null,
    "o": [
      "To create a FormSet for related objects tied to a parent model",
      "To generate inline JavaScript validation for forms",
      "To merge multiple ModelForms into one",
      "To create a FormSet without requiring a management form"
    ]
  },
  {
    "q": "In Django, what does `form.non_field_errors()` return?",
    "c": null,
    "o": [
      "Errors not associated with any single field, often from cross-field validation",
      "All error messages from the form as a list",
      "Only validation errors attached to hidden fields",
      "A JSON representation of all form errors"
    ]
  },
  {
    "q": "What does setting `empty_label=None` in a `ModelChoiceField` do?",
    "c": null,
    "o": [
      "It removes the default blank option from the select widget",
      "It sets a placeholder instead of a blank choice",
      "It forces the queryset to exclude null values",
      "It disables the field entirely"
    ]
  },
  {
    "q": "Which Form method should be overridden to perform validation that depends on multiple fields together?",
    "c": null,
    "o": [
      "`clean()`",
      "`is_valid()`",
      "`validate_fields()`",
      "`full_clean()`"
    ]
  },
  {
    "q": "What happens if you override `__init__` in a Django form without calling `super().__init__()`?",
    "c": null,
    "o": [
      "The form will not initialize its fields correctly",
      "The form will still work but with no validation",
      "The form will render but ignore initial data",
      "The form will automatically load only model fields"
    ]
  },
  {
    "q": "What is the difference between `forms.ChoiceField` and `forms.ModelChoiceField`?",
    "c": null,
    "o": [
      "ChoiceField uses static choices, while ModelChoiceField is tied to a queryset",
      "ChoiceField automatically enforces foreign keys, while ModelChoiceField does not",
      "ChoiceField is only for Boolean values, while ModelChoiceField supports all models",
      "They are identical except for widget defaults"
    ]
  },
  {
    "q": "How does `is_valid()` differ from `full_clean()` in Django forms?",
    "c": null,
    "o": [
      "`is_valid()` runs `full_clean()` and returns a boolean, while `full_clean()` performs validation and populates errors/cleaned_data",
      "`is_valid()` validates only required fields, while `full_clean()` validates all",
      "`is_valid()` skips custom validators, while `full_clean()` applies them",
      "They are aliases of each other"
    ]
  },
  {
    "q": "What does `form.add_initial_prefix(field_name)` return?",
    "c": null,
    "o": [
      "The field name with the prefix used for initial data",
      "A boolean indicating whether the field has initial data",
      "The initial value of a field",
      "The help_text value of the field"
    ]
  },
  {
    "q": "Which argument of `modelform_factory` lets you exclude fields dynamically?",
    "c": null,
    "o": [
      "`exclude`",
      "`omit`",
      "`skip_fields`",
      "`ignore`"
    ]
  },
  {
    "q": "What is the effect of setting `required=False` on a Django form field?",
    "c": null,
    "o": [
      "The field becomes optional and does not raise a validation error if left blank",
      "The field is excluded from rendering in the form",
      "The field skips database constraints on save",
      "The field automatically uses a default value from the model"
    ]
  },
  {
    "q": "Which method in a Django `FormSet` is used to enforce validation rules across multiple forms together?",
    "c": null,
    "o": [
      "`clean()`",
      "`validate()`",
      "`is_valid_all()`",
      "`check_forms()`"
    ]
  },
  {
    "q": "What does the `form.prefix` attribute control?",
    "c": null,
    "o": [
      "The string prepended to HTML field names to differentiate multiple forms on the same page",
      "The label text displayed before each form field",
      "The name of the database table prefix for model forms",
      "The order in which form fields are validated"
    ]
  },
  {
    "q": "What is the default widget for a `ModelChoiceField` in Django?",
    "c": null,
    "o": [
      "Select",
      "TextInput",
      "RadioSelect",
      "CheckboxInput"
    ]
  },
  {
    "q": "When saving a `ModelForm` with `commit=False`, which type of relationship must be saved separately afterward?",
    "c": null,
    "o": [
      "ManyToMany relationships",
      "ForeignKey relationships",
      "OneToOne relationships",
      "CharField values"
    ]
  },
  {
    "q": "What does the `initial` argument in a form field do?",
    "c": null,
    "o": [
      "Specifies the default value displayed before user input",
      "Sets the placeholder text in the HTML widget",
      "Marks the field as optional during validation",
      "Determines if the field is stored in cleaned_data"
    ]
  },
  {
    "q": "Which Django form class would you use if you want to create multiple instances of the same form dynamically?",
    "c": null,
    "o": [
      "FormSet",
      "BaseForm",
      "FormGroup",
      "MultiForm"
    ]
  },
  {
    "q": "In a `ModelForm`, what happens if the `fields` attribute in the `Meta` class is set to `'__all__'`?",
    "c": null,
    "o": [
      "All model fields are included in the form except the automatically excluded ones",
      "All model fields are included, even primary keys and auto-created fields",
      "No model fields are included unless added manually",
      "Only required model fields are included automatically"
    ]
  },
  {
    "q": "Which attribute of a `Form` class in Django stores the dictionary of submitted form data before validation?",
    "c": null,
    "o": [
      "`data`",
      "`cleaned_data`",
      "`initial`",
      "`errors`"
    ]
  },
  {
    "q": "In Django forms, what does the `label` argument of a field define?",
    "c": null,
    "o": [
      "The human-readable name displayed next to the field in templates",
      "The tooltip text shown on hover",
      "The placeholder text rendered inside the input box",
      "The default value for the field"
    ]
  },
  {
    "q": "Which Django utility helps create a form class directly from a model class without writing the form fields manually?",
    "c": null,
    "o": [
      "`modelform_factory`",
      "`inlineformset_factory`",
      "`form_factory`",
      "`model_formset`"
    ]
  },
  {
    "q": "What happens if a form field defines both `initial` and the form is bound with user-submitted data?",
    "c": null,
    "o": [
      "The user-submitted data takes precedence over the `initial` value",
      "The `initial` value overwrites the submitted data",
      "Both are ignored and the field defaults to blank",
      "The form raises a validation error"
    ]
  },
  {
    "q": "Which method must be called to populate `cleaned_data` in a Django form?",
    "c": null,
    "o": [
      "`is_valid()`",
      "`get_cleaned_data()`",
      "`full_cleaned()`",
      "`validate()`"
    ]
  },
  {
    "q": "What is the main difference between `ModelFormSet` and `inlineformset_factory`?",
    "c": null,
    "o": [
      "`ModelFormSet` works with any model, while `inlineformset_factory` is tied to related models via ForeignKey",
      "`ModelFormSet` supports multiple models, while `inlineformset_factory` only supports one",
      "`inlineformset_factory` allows editing unrelated models, while `ModelFormSet` does not",
      "They are functionally identical but have different naming"
    ]
  },
  {
    "q": "In a Django form, what is the role of `error_messages` dictionary in a field definition?",
    "c": null,
    "o": [
      "It customizes default validation error messages for that field",
      "It disables field-level validation entirely",
      "It sets global error handlers for the form",
      "It replaces non_field_errors with field-specific errors"
    ]
  },
  {
    "q": "If a Django form is instantiated with `empty_permitted=True`, what does that mean?",
    "c": null,
    "o": [
      "The form can be submitted without any data and still considered valid",
      "The form will never run validation rules",
      "The form fields automatically become optional",
      "The form skips saving to the database even if commit=True"
    ]
  },
  {
    "q": "In Django forms, what does the `disabled=True` attribute on a field do?",
    "c": null,
    "o": [
      "Prevents user modification of the field value while still displaying it",
      "Removes the field completely from rendering",
      "Marks the field as optional during validation",
      "Ensures the field always validates as invalid"
    ]
  },
  {
    "q": "Which attribute of a Django field is typically used for accessibility and provides a short text for screen readers?",
    "c": null,
    "o": [
      "`label`",
      "`help_text`",
      "`verbose_name`",
      "`aria_label`"
    ]
  },
  {
    "q": "What is the purpose of `form.save(commit=False)` in a `ModelForm`?",
    "c": null,
    "o": [
      "It returns an unsaved model instance that can be modified before saving",
      "It validates the form but does not return an object",
      "It saves only non-relational fields of the model",
      "It forces ManyToMany fields to be ignored"
    ]
  },
  {
    "q": "Which method in Django forms is responsible for adding errors to individual fields programmatically?",
    "c": null,
    "o": [
      "`add_error()`",
      "`set_error()`",
      "`field_error()`",
      "`invalidate()`"
    ]
  },
  {
    "q": "In a `FormSet`, what does the management form contain?",
    "c": null,
    "o": [
      "Metadata such as total form count, initial form count, and max forms",
      "Validation errors for all forms in the set",
      "Cleaned data for every form in the set",
      "The queryset used to populate the forms"
    ]
  },
  {
    "q": "Which class should be subclassed to create a custom validator for Django forms?",
    "c": null,
    "o": [
      "`BaseValidator`",
      "`ValidationError`",
      "`ValidatorMixin`",
      "`FormValidator`"
    ]
  },
  {
    "q": "When overriding a field’s `clean()` method, what should it return?",
    "c": null,
    "o": [
      "The cleaned and validated value for that field",
      "The error message string",
      "A dictionary of all field values",
      "Nothing, as it updates cleaned_data automatically"
    ]
  },
  {
    "q": "In `ModelForm.Meta`, what does setting `widgets` allow you to do?",
    "c": null,
    "o": [
      "Customize the HTML widget used to render individual fields",
      "Change the default field labels globally",
      "Override the validation logic for model fields",
      "Exclude fields from the database schema"
    ]
  },
  {
    "q": "What happens if you call `form.errors` before calling `is_valid()`?",
    "c": null,
    "o": [
      "It triggers validation automatically and populates errors",
      "It raises an AttributeError",
      "It returns an empty dictionary regardless of errors",
      "It only shows errors from required fields"
    ]
  },
  {
    "q": "In a Django `Form`, what does `cleaned_data` contain after validation?",
    "c": null,
    "o": [
      "A dictionary of validated and converted field values",
      "The raw data dictionary submitted by the user",
      "Only initial values of the fields",
      "The error messages associated with invalid fields"
    ]
  },
  {
    "q": "Which argument can you pass to `inlineformset_factory` to restrict the maximum number of child forms?",
    "c": null,
    "o": [
      "`max_num`",
      "`limit`",
      "`cap`",
      "`restrict`"
    ]
  },
  {
    "q": "What is the effect of setting `unique=True` in a `ModelForm` field?",
    "c": null,
    "o": [
      "It enforces uniqueness validation at the form level",
      "It removes the field from rendering if duplicates exist",
      "It automatically adds the field to `Meta.exclude`",
      "It forces the field to accept only primary key values"
    ]
  },
  {
    "q": "How does Django treat hidden fields in forms?",
    "c": null,
    "o": [
      "They are still validated like visible fields",
      "They are ignored during validation",
      "They bypass cleaned_data population",
      "They must always be optional"
    ]
  },
  {
    "q": "Which `FormSet` attribute holds all the individual form instances?",
    "c": null,
    "o": [
      "`forms`",
      "`instances`",
      "`cleaned_forms`",
      "`all_forms`"
    ]
  },
  {
    "q": "What does `exclude = ['field_name']` in `ModelForm.Meta` do?",
    "c": null,
    "o": [
      "Prevents the specified model field from being included in the form",
      "Marks the field as optional",
      "Hides the field in the template but keeps it validated",
      "Deletes the field from the model on save"
    ]
  },
  {
    "q": "If a form field uses a callable for its `initial` argument, when is it evaluated?",
    "c": null,
    "o": [
      "Each time the form instance is created",
      "Once at server startup",
      "Only if the field has no submitted value",
      "Only when `is_valid()` is called"
    ]
  },
  {
    "q": "Which property of a Django form field determines the Python type that the cleaned data will have?",
    "c": null,
    "o": [
      "`to_python()`",
      "`clean()`",
      "`convert_type()`",
      "`field_type`"
    ]
  },
  {
    "q": "In a `ModelForm`, what does setting `Meta.labels` do?",
    "c": null,
    "o": [
      "Overrides the default labels for model fields in the form",
      "Adds help text under each field",
      "Defines placeholders for HTML widgets",
      "Specifies database column labels"
    ]
  },
  {
    "q": "How can you customize form-wide validation that depends on multiple fields?",
    "c": null,
    "o": [
      "Override the form’s `clean()` method",
      "Use `validators` on individual fields",
      "Set `error_messages` on the Meta class",
      "Call `full_clean()` directly in the view"
    ]
  },
  {
    "q": "What is the difference between `form.is_valid()` and `form.has_changed()`?",
    "c": null,
    "o": [
      "`is_valid()` runs validation, while `has_changed()` checks if submitted data differs from initial values",
      "`is_valid()` checks only required fields, while `has_changed()` validates all fields",
      "`is_valid()` checks model constraints, while `has_changed()` ignores them",
      "`is_valid()` and `has_changed()` are aliases for the same behavior"
    ]
  },
  {
    "q": "Which formset parameter enforces a minimum number of forms to be submitted?",
    "c": null,
    "o": [
      "`min_num`",
      "`required_num`",
      "`minimum_forms`",
      "`validate_min`"
    ]
  },
  {
    "q": "What happens if you override a field in a `ModelForm` that already exists in the model?",
    "c": null,
    "o": [
      "The overridden field replaces the model field in the form definition",
      "Both the overridden and model field versions appear in the form",
      "The overridden field is ignored during validation",
      "The form raises an error for duplicate fields"
    ]
  },
  {
    "q": "Which form attribute holds validation errors that are not tied to a specific field?",
    "c": null,
    "o": [
      "`non_field_errors()`",
      "`general_errors`",
      "`form_errors`",
      "`meta_errors`"
    ]
  },
  {
    "q": "What is the effect of setting `empty_label=None` in a `ModelChoiceField`?",
    "c": null,
    "o": [
      "It removes the default '---------’ empty option from the select widget",
      "It forces the field to always require input",
      "It replaces empty submissions with NULL in the database",
      "It allows multiple blank values for the field"
    ]
  },
  {
    "q": "In Django, what does the `use_required_attribute=False` option in a form’s `Meta` class do?",
    "c": null,
    "o": [
      "It disables adding the HTML5 `required` attribute to required fields",
      "It makes all fields optional during validation",
      "It prevents default values from being applied",
      "It ignores model-level null constraints"
    ]
  },
  {
    "q": "When overriding a `ModelForm`’s `save()` method, why is `super().save(commit=False)` commonly used?",
    "c": null,
    "o": [
      "To get an unsaved model instance for further customization before saving",
      "To bypass validation and save the model directly",
      "To create a duplicate of the model instance",
      "To force ManyToMany fields to save immediately"
    ]
  },
  {
    "q": "Which form field would you use to represent a model relation with a many-to-one mapping?",
    "c": null,
    "o": [
      "`ModelChoiceField`",
      "`ModelMultipleChoiceField`",
      "`ForeignKeyField`",
      "`RelatedField`"
    ]
  },
  {
    "q": "In a `ModelForm`, what does the `Meta.fields = '__all__'` declaration mean?",
    "c": null,
    "o": [
      "All fields of the associated model will be included in the form",
      "All fields are optional during validation",
      "All fields are excluded and must be added manually",
      "All form widgets will default to hidden"
    ]
  },
  {
    "q": "Which error is raised inside a field’s `clean()` method to indicate validation failure?",
    "c": null,
    "o": [
      "`ValidationError`",
      "`FormError`",
      "`InvalidFieldError`",
      "`FieldValidationException`"
    ]
  },
  {
    "q": "How can you ensure that a `ModelForm` excludes model fields automatically marked as `editable=False`?",
    "c": null,
    "o": [
      "They are excluded by default unless explicitly included",
      "You must set `exclude` in the Meta class",
      "You must override `__init__` to remove them",
      "They must be hidden manually in the template"
    ]
  },
  {
    "q": "In Django’s form handling, what is the role of `form.full_clean()`?",
    "c": null,
    "o": [
      "Runs all validation steps including field, form, and model validation",
      "Saves the form data into the database",
      "Clears previous errors from the form",
      "Populates initial values into `cleaned_data`"
    ]
  },
  {
    "q": "What is the purpose of `error_class` in a Django form?",
    "c": null,
    "o": [
      "It defines the class used to render validation error messages",
      "It specifies the default exception for validation errors",
      "It determines whether errors stop validation immediately",
      "It groups errors by severity level"
    ]
  },
  {
    "q": "What does the `initial` argument in a Django form field do?",
    "c": null,
    "o": [
      "Specifies the default value that will be pre-filled when the form is first rendered",
      "Overrides any submitted data with a fixed value",
      "Marks the field as optional",
      "Defines the placeholder text shown in the widget"
    ]
  },
  {
    "q": "In Django forms, what does the `empty_permitted=True` argument in a form mean?",
    "c": null,
    "o": [
      "The form can be submitted without any data and still not raise validation errors",
      "The form will automatically replace empty fields with `None`",
      "All required fields are converted into optional fields",
      "The form will not be included in formsets"
    ]
  },
  {
    "q": "What is the difference between `ModelChoiceField` and `ModelMultipleChoiceField`?",
    "c": null,
    "o": [
      "`ModelChoiceField` allows selecting one related object, while `ModelMultipleChoiceField` allows selecting multiple objects",
      "`ModelChoiceField` is for foreign key relationships, while `ModelMultipleChoiceField` is for all fields",
      "`ModelChoiceField` skips validation, while `ModelMultipleChoiceField` enforces it",
      "`ModelChoiceField` is used in forms, while `ModelMultipleChoiceField` is used in formsets"
    ]
  },
  {
    "q": "How does Django decide whether a form submission is bound or unbound?",
    "c": null,
    "o": [
      "A form is bound if it is initialized with data (e.g., `request.POST`), otherwise unbound",
      "A form is bound only if it has `commit=False` during saving",
      "A form is always bound once instantiated",
      "A form is bound if all required fields are filled"
    ]
  },
  {
    "q": "In a custom field validator, what is the correct way to signal validation failure?",
    "c": null,
    "o": [
      "Raise `ValidationError` with an appropriate error message",
      "Return `False` from the validator function",
      "Return `None` from the validator function",
      "Raise `FieldError` for the invalid value"
    ]
  },
  {
    "q": "Which of the following best describes the role of `form.is_valid()`?",
    "c": null,
    "o": [
      "Runs validation logic and returns `True` if no errors exist",
      "Saves the form to the database",
      "Populates `initial` values for the form",
      "Clears errors after a failed validation"
    ]
  },
  {
    "q": "What does the `prefix` argument in a Django form constructor do?",
    "c": null,
    "o": [
      "Adds a string before field names to distinguish multiple forms in the same page",
      "Defines the HTML id for the form element",
      "Sets a CSS class for form fields",
      "Indicates that the form is nested inside another form"
    ]
  },
  {
    "q": "How does `BaseInlineFormSet` differ from `BaseModelFormSet`?",
    "c": null,
    "o": [
      "`BaseInlineFormSet` is tied to a parent model instance, while `BaseModelFormSet` works without such relation",
      "`BaseInlineFormSet` supports only one form, while `BaseModelFormSet` supports many",
      "`BaseInlineFormSet` bypasses model validation, while `BaseModelFormSet` does not",
      "`BaseInlineFormSet` must always be empty initially"
    ]
  },
  {
    "q": "What happens when you call `form.save_m2m()` in a `ModelForm`?",
    "c": null,
    "o": [
      "It saves many-to-many relationships after the model instance has been saved",
      "It saves all fields including ForeignKey relations immediately",
      "It saves the form and commits the model to the database",
      "It validates many-to-many fields but does not persist them"
    ]
  },
  {
    "q": "Which method in a Django `Form` should you override to add validation for a single field?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_<fieldname>()`",
      "`check_<fieldname>()`",
      "`field_<fieldname>_validate()`"
    ]
  },
  {
    "q": "What is the default behavior of a `FileField` in Django forms if no file is uploaded?",
    "c": null,
    "o": [
      "It will return an empty value and trigger validation if required",
      "It will store an empty file object in `cleaned_data`",
      "It will raise `ValidationError` even if the field is optional",
      "It will attempt to create a blank file on the server"
    ]
  },
  {
    "q": "Which argument in a form field lets you attach HTML attributes like `class` or `placeholder`?",
    "c": null,
    "o": [
      "`widget=...`",
      "`attrs=...`",
      "`render=...`",
      "`ui=...`"
    ]
  },
  {
    "q": "What does calling `form.add_error(None, 'message')` do?",
    "c": null,
    "o": [
      "Adds a non-field error message to the form",
      "Adds a global exception that halts validation",
      "Attaches the error to every field in the form",
      "Overrides the default error dictionary"
    ]
  },
  {
    "q": "If a `ModelForm` uses `Meta.exclude` and `Meta.fields` together, which one takes precedence?",
    "c": null,
    "o": [
      "`exclude` takes precedence over `fields`",
      "`fields` takes precedence over `exclude`",
      "Both must be defined together or an error is raised",
      "They are ignored and Django defaults to `__all__`"
    ]
  },
  {
    "q": "In Django formsets, what is the purpose of the `can_delete=True` option?",
    "c": null,
    "o": [
      "It allows users to mark specific forms for deletion in the formset",
      "It ensures all forms are deleted when validation fails",
      "It disables `cleaned_data` for all forms",
      "It makes all fields optional by default"
    ]
  },
  {
    "q": "What is the effect of setting `disabled=True` on a Django form field?",
    "c": null,
    "o": [
      "The field is rendered in the form but its value cannot be changed by the user",
      "The field is hidden from rendering but still validated",
      "The field is excluded from validation entirely",
      "The field’s default value is always ignored"
    ]
  },
  {
    "q": "Which attribute of a Django form field lets you override the default error messages for that field?",
    "c": null,
    "o": [
      "`error_messages`",
      "`validators`",
      "`help_text`",
      "`default_errors`"
    ]
  },
  {
    "q": "What happens if you don’t call `is_valid()` before accessing `form.cleaned_data`?",
    "c": null,
    "o": [
      "An `AttributeError` is raised because `cleaned_data` is only populated after validation",
      "`cleaned_data` is available but may contain raw, unvalidated values",
      "`cleaned_data` contains only initial values of the fields",
      "The form will automatically run validation silently"
    ]
  },
  {
    "q": "In a `ModelForm`, how are model-level `unique_together` constraints validated?",
    "c": null,
    "o": [
      "By overriding the form’s `validate_unique()` method",
      "By checking them only in the model’s `save()` method",
      "By requiring explicit custom validators on each field",
      "They are not validated unless `unique=True` is set individually"
    ]
  },
  {
    "q": "What does setting `label_suffix=''` in a Django Form do?",
    "c": null,
    "o": [
      "It removes the default colon (`:`) appended to field labels",
      "It hides the field label entirely",
      "It adds an empty placeholder in place of the label",
      "It prevents required field markers from being displayed"
    ]
  },
  {
    "q": "Which method is responsible for creating form fields from model fields in a `ModelForm`?",
    "c": null,
    "o": [
      "`fields_for_model()`",
      "`get_fields()`",
      "`create_fields()`",
      "`build_form_fields()`"
    ]
  },
  {
    "q": "What is the correct way to handle file uploads in a Django form?",
    "c": null,
    "o": [
      "Pass both `request.POST` and `request.FILES` when instantiating the form",
      "Pass only `request.POST` and let Django handle `request.FILES` internally",
      "Set `enctype='application/json'` on the form tag",
      "Manually save files into `MEDIA_ROOT` before calling `form.is_valid()`"
    ]
  },
  {
    "q": "In Django formsets, what does the `validate_max=True` option enforce?",
    "c": null,
    "o": [
      "Ensures the number of submitted forms does not exceed `max_num`",
      "Ensures at least one form is submitted",
      "Ensures only valid forms are passed to the view",
      "Ensures all forms have unique values"
    ]
  },
  {
    "q": "What is the difference between `form.errors` and `form.non_field_errors()`?",
    "c": null,
    "o": [
      "`form.errors` returns a dictionary of field errors, while `non_field_errors()` returns errors not tied to a specific field",
      "`form.errors` always includes validation exceptions, while `non_field_errors()` ignores them",
      "`form.errors` is available only for bound forms, while `non_field_errors()` is always available",
      "`form.errors` is reset after validation, while `non_field_errors()` persists"
    ]
  },
  {
    "q": "Which method in a Django form can be overridden to customize the validation of all fields together?",
    "c": null,
    "o": [
      "`clean()`",
      "`full_clean()`",
      "`validate_all()`",
      "`check_fields()`"
    ]
  },
  {
    "q": "What is the effect of the `help_text` attribute in a Django form field?",
    "c": null,
    "o": [
      "Displays additional guidance for the user in the form template",
      "Specifies the default value for the field",
      "Marks the field as optional during validation",
      "Overrides the field’s label"
    ]
  },
  {
    "q": "In a `ModelForm`, how can you dynamically exclude certain fields at runtime?",
    "c": null,
    "o": [
      "Override the `__init__` method and modify `self.fields`",
      "Add the fields to `Meta.exclude` after instantiation",
      "Call `form.remove_field(field_name)` before validation",
      "Set `field.disabled = True` for each field"
    ]
  },
  {
    "q": "Which form attribute tracks whether the form has been bound to data?",
    "c": null,
    "o": [
      "`is_bound`",
      "`bound_data`",
      "`submitted`",
      "`cleaned`"
    ]
  },
  {
    "q": "What does the `modelform_factory` function do in Django?",
    "c": null,
    "o": [
      "Creates a `ModelForm` class dynamically from a given model",
      "Creates a formset for multiple model instances",
      "Merges multiple forms into one form class",
      "Generates a form instance with default values"
    ]
  },
  {
    "q": "Which argument in a form field specifies a callable or value to pre-fill the field initially?",
    "c": null,
    "o": [
      "`initial`",
      "`default`",
      "`placeholder`",
      "`value`"
    ]
  },
  {
    "q": "What is the purpose of `form.cleaned_data` in Django forms?",
    "c": null,
    "o": [
      "It stores validated and converted form data after calling `is_valid()`",
      "It stores the raw POST or GET data",
      "It defines default values for optional fields",
      "It holds error messages after validation"
    ]
  },
  {
    "q": "In Django formsets, which attribute indicates how many forms are initially displayed?",
    "c": null,
    "o": [
      "`initial_forms`",
      "`total_forms`",
      "`extra_forms`",
      "`max_forms`"
    ]
  },
  {
    "q": "In Django, what is the purpose of the `form.hidden_fields()` method?",
    "c": null,
    "o": [
      "Returns a list of all fields rendered as hidden inputs",
      "Returns all fields that are excluded from validation",
      "Returns all fields with `required=False`",
      "Returns only the primary key fields of the model"
    ]
  },
  {
    "q": "What is the difference between `initial` and `cleaned_data` in Django forms?",
    "c": null,
    "o": [
      "`initial` provides default values before user input, while `cleaned_data` contains validated data after form submission",
      "`initial` is used for validation, while `cleaned_data` sets default values",
      "`initial` is stored in the database, while `cleaned_data` is temporary",
      "`initial` is for file uploads only, while `cleaned_data` is for text fields"
    ]
  },
  {
    "q": "Which method in Django forms returns all the field values including hidden ones?",
    "c": null,
    "o": [
      "`form.visible_fields()` does not include hidden fields, while iterating `form` or `form.fields` accesses all fields",
      "`form.all_fields()`",
      "`form.hidden_fields()`",
      "`form.get_fields()`"
    ]
  },
  {
    "q": "In a `ModelForm`, what does setting `Meta.widgets` allow you to do?",
    "c": null,
    "o": [
      "Specify custom widgets for rendering fields",
      "Change field labels automatically",
      "Exclude fields dynamically at runtime",
      "Add custom validation logic"
    ]
  },
  {
    "q": "When using `inlineformset_factory`, what is the main difference from `modelformset_factory`?",
    "c": null,
    "o": [
      "`inlineformset_factory` is tied to a parent model through a ForeignKey, while `modelformset_factory` works with any model",
      "`inlineformset_factory` supports only one form at a time, while `modelformset_factory` supports multiple",
      "`inlineformset_factory` ignores validation, while `modelformset_factory` validates all fields",
      "`inlineformset_factory` creates form instances without a management form, while `modelformset_factory` requires it"
    ]
  },
  {
    "q": "What is the effect of `form.is_bound` being `False`?",
    "c": null,
    "o": [
      "The form has no user-submitted data and validation will not run",
      "The form will ignore `initial` values",
      "The form will raise a ValidationError if accessed",
      "The form will automatically bind to request data"
    ]
  },
  {
    "q": "Which method would you override to validate a `ModelForm` instance against unique constraints?",
    "c": null,
    "o": [
      "`validate_unique()`",
      "`clean()`",
      "`full_clean()`",
      "`clean_fields()`"
    ]
  },
  {
    "q": "How can you dynamically change the queryset for a `ModelChoiceField` in a `ModelForm`?",
    "c": null,
    "o": [
      "Override the field in `__init__` and assign a new queryset",
      "Set a new queryset in `Meta.fields`",
      "Use `form.cleaned_data` to replace the queryset",
      "Change the `initial` argument to a different model instance"
    ]
  },
  {
    "q": "In Django forms, which method returns only the fields that are visible (not hidden)?",
    "c": null,
    "o": [
      "`form.visible_fields()`",
      "`form.hidden_fields()`",
      "`form.all_fields()`",
      "`form.cleaned_fields()`"
    ]
  },
  {
    "q": "What is the purpose of the `empty_permitted` attribute in a Django form?",
    "c": null,
    "o": [
      "Allows the form to be submitted without any data and still considered valid",
      "Prevents the form from raising any validation errors",
      "Automatically fills missing fields with default values",
      "Disables validation for required fields only"
    ]
  },
  {
    "q": "Which argument of a Django `ModelForm` field can be used to provide extra information to the user below the field?",
    "c": null,
    "o": [
      "`help_text`",
      "`label`",
      "`placeholder`",
      "`initial`"
    ]
  },
  {
    "q": "In a `ModelForm`, how do you exclude specific fields from being included in the form?",
    "c": null,
    "o": [
      "Use the `exclude` attribute in the form’s `Meta` class",
      "Remove them from `form.fields` after instantiation",
      "Set `required=False` on the field",
      "Mark them as hidden in the template"
    ]
  },
  {
    "q": "What happens if you set `commit=False` when calling `save()` on a `ModelForm`?",
    "c": null,
    "o": [
      "The method returns an unsaved instance which can be modified before saving to the database",
      "The form is validated but not saved to `cleaned_data`",
      "The instance is saved immediately but ManyToMany fields are skipped",
      "Validation is skipped and the instance is saved with default values"
    ]
  },
  {
    "q": "Which method would you override to add custom validation for a single field in a Django form?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_<fieldname>()`",
      "`check_<fieldname>()`",
      "`field_<fieldname>_validate()`"
    ]
  },
  {
    "q": "In Django, which form attribute provides a dictionary of all validation errors?",
    "c": null,
    "o": [
      "`form.errors`",
      "`form.cleaned_data`",
      "`form.invalid_fields`",
      "`form.non_field_errors()`"
    ]
  },
  {
    "q": "What is the default widget used for a `BooleanField` in Django forms?",
    "c": null,
    "o": [
      "CheckboxInput",
      "Select",
      "RadioSelect",
      "TextInput"
    ]
  },
  {
    "q": "What does the `disabled` attribute in a Django form field do?",
    "c": null,
    "o": [
      "Prevents the user from modifying the field value while still rendering it",
      "Hides the field from the form completely",
      "Marks the field as optional during validation",
      "Automatically fills the field with initial data"
    ]
  },
  {
    "q": "Which method in Django forms is used to run all validation steps including field, form, and model validation?",
    "c": null,
    "o": [
      "`full_clean()`",
      "`is_valid()`",
      "`clean()`",
      "`validate_all()`"
    ]
  },
  {
    "q": "In Django, what does setting `required=False` on a form field accomplish?",
    "c": null,
    "o": [
      "The field becomes optional and does not raise a validation error if left blank",
      "The field is hidden from the form rendering",
      "The field automatically takes the model's default value",
      "The field bypasses `cleaned_data` population"
    ]
  },
  {
    "q": "Which attribute of a Django form field is used to display additional information to the user below the field?",
    "c": null,
    "o": [
      "`help_text`",
      "`label`",
      "`placeholder`",
      "`initial`"
    ]
  },
  {
    "q": "How can you dynamically change the queryset of a `ModelChoiceField` in a form?",
    "c": null,
    "o": [
      "Override the field in the form’s `__init__` method and assign a new queryset",
      "Modify `Meta.fields` after the form is instantiated",
      "Update the field in `cleaned_data`",
      "Change the `initial` argument of the field"
    ]
  },
  {
    "q": "In Django formsets, what does the `can_delete=True` option allow?",
    "c": null,
    "o": [
      "It allows users to mark specific forms for deletion",
      "It automatically deletes all empty forms",
      "It disables validation for deleted forms",
      "It makes all forms optional"
    ]
  },
  {
    "q": "What is the purpose of `form.non_field_errors()`?",
    "c": null,
    "o": [
      "Returns errors that are not associated with any specific field",
      "Returns a dictionary of all field errors",
      "Returns a boolean indicating if the form has errors",
      "Returns validation messages from model-level validators only"
    ]
  },
  {
    "q": "Which argument in a Django form field sets a default value for the field before user input?",
    "c": null,
    "o": [
      "`initial`",
      "`default`",
      "`placeholder`",
      "`value`"
    ]
  },
  {
    "q": "In Django, what is the difference between `form.is_valid()` and `form.full_clean()`?",
    "c": null,
    "o": [
      "`is_valid()` calls `full_clean()` and returns a boolean indicating form validity, while `full_clean()` performs validation but does not return a boolean",
      "`is_valid()` validates only required fields, while `full_clean()` validates all fields",
      "`is_valid()` skips custom validators, while `full_clean()` applies them",
      "They are functionally identical"
    ]
  },
  {
    "q": "Which Django form attribute is used to store the original data submitted by the user before validation?",
    "c": null,
    "o": [
      "`data`",
      "`initial`",
      "`cleaned_data`",
      "`raw_data`"
    ]
  },
  {
    "q": "What is the main purpose of `BaseInlineFormSet` in Django?",
    "c": null,
    "o": [
      "To manage multiple related model instances tied to a parent model via a ForeignKey",
      "To create a form for a single model instance",
      "To combine multiple unrelated forms into one",
      "To bypass validation for a group of forms"
    ]
  },
  {
    "q": "Which argument in a Django `ModelForm` allows you to exclude specific model fields from the form?",
    "c": null,
    "o": [
      "`Meta.exclude`",
      "`Meta.fields`",
      "`Meta.omit`",
      "`Meta.ignore`"
    ]
  },
  {
    "q": "What happens if you call `form.add_error('field_name', 'message')`?",
    "c": null,
    "o": [
      "Adds a validation error to the specified field",
      "Clears all previous errors for the field",
      "Skips validation for that field",
      "Raises a `ValidationError` immediately"
    ]
  },
  {
    "q": "In a `ModelForm`, what is the purpose of the `save_m2m()` method?",
    "c": null,
    "o": [
      "To save many-to-many relationships after the model instance has been saved",
      "To save the model instance immediately",
      "To bypass validation for many-to-many fields",
      "To remove all existing many-to-many relationships"
    ]
  },
  {
    "q": "Which method in a Django form is commonly overridden to validate a single field?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_<fieldname>()`",
      "`check_<fieldname>()`",
      "`field_<fieldname>_validate()`"
    ]
  },
  {
    "q": "What does the `prefix` argument in a Django form constructor do?",
    "c": null,
    "o": [
      "Adds a string before field names to differentiate multiple forms on the same page",
      "Defines the HTML id for the form",
      "Specifies a CSS class for all fields",
      "Indicates that the form is nested inside another form"
    ]
  },
  {
    "q": "What is the purpose of `form.errors.as_json()` in Django?",
    "c": null,
    "o": [
      "Returns form validation errors serialized as a JSON string",
      "Clears all errors from the form",
      "Returns only field names that have errors",
      "Automatically logs errors to the server console"
    ]
  },
  {
    "q": "Which form field type in Django is used to represent a many-to-many relationship?",
    "c": null,
    "o": [
      "`ModelMultipleChoiceField`",
      "`ModelChoiceField`",
      "`ManyToManyField`",
      "`ForeignKeyField`"
    ]
  },
  {
    "q": "In Django, which attribute of a form indicates if the form has been submitted with data?",
    "c": null,
    "o": [
      "`is_bound`",
      "`has_data`",
      "`submitted`",
      "`bound_data`"
    ]
  },
  {
    "q": "What does setting `empty_label=None` in a `ModelChoiceField` do?",
    "c": null,
    "o": [
      "Removes the default empty option in the select widget",
      "Forces the field to be required",
      "Automatically selects the first item in the queryset",
      "Adds a placeholder instead of a blank choice"
    ]
  },
  {
    "q": "Which Django function can be used to dynamically create a `ModelForm` class from a model?",
    "c": null,
    "o": [
      "`modelform_factory`",
      "`inlineformset_factory`",
      "`form_factory`",
      "`model_formset`"
    ]
  },
  {
    "q": "In a `ModelForm`, what does the `Meta.widgets` dictionary allow you to do?",
    "c": null,
    "o": [
      "Specify custom widgets for rendering specific fields",
      "Define custom validation rules for fields",
      "Exclude fields from the form dynamically",
      "Override initial values for fields"
    ]
  },
  {
    "q": "What happens when `form.has_changed()` is called?",
    "c": null,
    "o": [
      "Checks if the submitted data differs from the initial data",
      "Runs validation on all fields",
      "Clears all previous form errors",
      "Saves the form data to the database if valid"
    ]
  },
  {
    "q": "Which method in Django forms is used to add a validation error not associated with any field?",
    "c": null,
    "o": [
      "`add_error(None, 'message')`",
      "`raise_error('message')`",
      "`set_form_error('message')`",
      "`add_nonfield_error('message')`"
    ]
  },
  {
    "q": "In Django, what is the purpose of `form.cleaned_data`?",
    "c": null,
    "o": [
      "Stores validated and converted form data after calling `is_valid()`",
      "Contains the original POST or GET data submitted by the user",
      "Holds all form errors",
      "Stores initial values of the form fields"
    ]
  },
  {
    "q": "Which method in a Django form should be overridden to add custom form-wide validation?",
    "c": null,
    "o": [
      "`clean()`",
      "`clean_fields()`",
      "`full_clean()`",
      "`validate_all()`"
    ]
  },
  {
    "q": "What does the `help_text` attribute do in Django form fields?",
    "c": null,
    "o": [
      "Provides additional guidance to users when filling out the form",
      "Specifies a default value for the field",
      "Marks a field as optional",
      "Overrides the field label in templates"
    ]
  },
  {
    "q": "In Django formsets, which attribute specifies the total number of forms that can be submitted?",
    "c": null,
    "o": [
      "`total_form_count`",
      "`initial_form_count`",
      "`extra_forms`",
      "`max_forms`"
    ]
  },
  {
    "q": "How can you mark a field as optional in a Django form?",
    "c": null,
    "o": [
      "Set `required=False` on the field",
      "Set `empty_permitted=True` on the form",
      "Set `disabled=True` on the field",
      "Set `initial=None`"
    ]
  },
  {
    "q": "Which method in a Django `ModelForm` allows you to save a model instance without committing to the database immediately?",
    "c": null,
    "o": [
      "`save(commit=False)`",
      "`save_m2m()`",
      "`full_clean()`",
      "`clean()`"
    ]
  },
  {
    "q": "In Django, what does `form.add_error('field_name', 'message')` accomplish?",
    "c": null,
    "o": [
      "Adds a validation error to the specified field",
      "Clears all previous errors from the form",
      "Raises a ValidationError immediately",
      "Marks the field as optional during validation"
    ]
  },
  {
    "q": "What is the default widget used for a `CharField` in Django forms?",
    "c": null,
    "o": [
      "TextInput",
      "Textarea",
      "Select",
      "RadioSelect"
    ]
  },
  {
    "q": "What is the purpose of `form.is_bound` in Django forms?",
    "c": null,
    "o": [
      "Indicates whether the form has been submitted with data",
      "Checks if the form has any validation errors",
      "Determines if the form has changed since initialization",
      "Specifies if the form is tied to a model"
    ]
  },
  {
    "q": "In Django forms, which method returns only the hidden fields of the form?",
    "c": null,
    "o": [
      "`form.hidden_fields()`",
      "`form.visible_fields()`",
      "`form.all_fields()`",
      "`form.cleaned_data()`"
    ]
  },
  {
    "q": "Which Django form field type allows selecting a single related model instance?",
    "c": null,
    "o": [
      "`ModelChoiceField`",
      "`ModelMultipleChoiceField`",
      "`ForeignKeyField`",
      "`ManyToManyField`"
    ]
  },
  {
    "q": "What is the effect of setting `disabled=True` on a Django form field?",
    "c": null,
    "o": [
      "The field is displayed but cannot be modified by the user",
      "The field is hidden and skipped during validation",
      "The field becomes optional",
      "The field is automatically filled with initial data"
    ]
  },
  {
    "q": "Which Django formset option allows marking forms for deletion?",
    "c": null,
    "o": [
      "`can_delete=True`",
      "`allow_delete=True`",
      "`deletable=True`",
      "`remove_forms=True`"
    ]
  },
  {
    "q": "How do you provide a custom HTML widget for a field in a `ModelForm`?",
    "c": null,
    "o": [
      "Use the `widgets` dictionary in `Meta`",
      "Set the `widget` argument in `__init__`",
      "Override `cleaned_data` for the field",
      "Set `formfield_callback` in the view"
    ]
  },
  {
    "q": "Which attribute in Django forms stores validation errors not associated with any specific field?",
    "c": null,
    "o": [
      "`non_field_errors()`",
      "`form.errors`",
      "`field_errors`",
      "`global_errors`"
    ]
  },
  {
    "q": "When is the callable passed to a form field’s `initial` argument evaluated?",
    "c": null,
    "o": [
      "Each time a new form instance is created",
      "Only once at server startup",
      "After calling `is_valid()`",
      "When `cleaned_data` is accessed"
    ]
  },
  {
    "q": "In a Django `ModelForm`, what is the role of the `Meta.model` attribute?",
    "c": null,
    "o": [
      "Specifies the model that the form is associated with",
      "Defines the HTML template used for rendering the form",
      "Sets default initial values for all fields",
      "Specifies which fields are required"
    ]
  },
  {
    "q": "What is the effect of `form.has_changed()` in Django forms?",
    "c": null,
    "o": [
      "Returns `True` if submitted data differs from initial values",
      "Runs validation and returns a boolean indicating validity",
      "Clears all validation errors from the form",
      "Returns the list of fields with errors"
    ]
  },
  {
    "q": "Which Django method validates a single form field and can be customized for each field?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_field()`",
      "`check_<fieldname>()`",
      "`field_validate()`"
    ]
  },
  {
    "q": "In a Django form, what does the `label_suffix` attribute do?",
    "c": null,
    "o": [
      "Specifies the string appended to field labels (default is ':')",
      "Removes all labels from the form",
      "Overrides the field's `help_text`",
      "Specifies the CSS class for the label"
    ]
  },
  {
    "q": "Which method is called to save many-to-many relationships for a `ModelForm` when `commit=False` is used?",
    "c": null,
    "o": [
      "`save_m2m()`",
      "`full_clean()`",
      "`save(commit=True)`",
      "`cleaned_data.update()`"
    ]
  },
  {
    "q": "In Django formsets, what does the `min_num` option enforce?",
    "c": null,
    "o": [
      "Specifies the minimum number of forms that must be submitted",
      "Specifies the minimum number of fields per form",
      "Validates only required forms",
      "Restricts the number of extra forms added dynamically"
    ]
  },
  {
    "q": "How can you dynamically modify the queryset of a `ModelChoiceField` in a form?",
    "c": null,
    "o": [
      "Override the field in the form’s `__init__` method",
      "Change the `Meta.fields` attribute",
      "Modify `cleaned_data` after validation",
      "Set a new `initial` value for the field"
    ]
  },
  {
    "q": "Which Django form method returns errors as a JSON-encoded string?",
    "c": null,
    "o": [
      "`form.errors.as_json()`",
      "`form.non_field_errors()`",
      "`form.errors.to_json()`",
      "`form.json_errors()`"
    ]
  },
  {
    "q": "In Django, what is the purpose of the `form.add_error()` method?",
    "c": null,
    "o": [
      "Allows programmatically adding an error to a specific field or to the form as a whole",
      "Automatically clears all previous errors in the form",
      "Validates a field without modifying `cleaned_data`",
      "Marks a field as optional"
    ]
  },
  {
    "q": "Which Django form field type would you use to allow selecting multiple related model instances?",
    "c": null,
    "o": [
      "`ModelMultipleChoiceField`",
      "`ModelChoiceField`",
      "`ForeignKeyField`",
      "`ManyToManyField`"
    ]
  },
  {
    "q": "In a `ModelForm`, what does setting `Meta.fields = '__all__'` do?",
    "c": null,
    "o": [
      "Includes all fields of the associated model in the form",
      "Excludes all fields of the model",
      "Includes only the required fields of the model",
      "Automatically sets default initial values for all fields"
    ]
  },
  {
    "q": "How can you make a Django form field optional?",
    "c": null,
    "o": [
      "Set `required=False` on the field",
      "Set `disabled=True` on the field",
      "Set `initial=None` on the field",
      "Add the field to `Meta.exclude`"
    ]
  },
  {
    "q": "Which Django form attribute returns a list of visible fields in the form?",
    "c": null,
    "o": [
      "`form.visible_fields()`",
      "`form.hidden_fields()`",
      "`form.fields`",
      "`form.cleaned_data`"
    ]
  },
  {
    "q": "What does `inlineformset_factory` allow you to do in Django?",
    "c": null,
    "o": [
      "Create a formset for child models related to a parent model via a ForeignKey",
      "Create a formset for unrelated models",
      "Generate a single `ModelForm` dynamically",
      "Automatically validate all forms without a management form"
    ]
  },
  {
    "q": "Which argument in a Django form constructor differentiates multiple forms on the same page?",
    "c": null,
    "o": [
      "`prefix`",
      "`label_suffix`",
      "`initial`",
      "`widget`"
    ]
  },
  {
    "q": "What is the default widget for a Django `BooleanField`?",
    "c": null,
    "o": [
      "CheckboxInput",
      "RadioSelect",
      "Select",
      "TextInput"
    ]
  },
  {
    "q": "In Django, what does the `form.errors` attribute contain?",
    "c": null,
    "o": [
      "A dictionary mapping field names to lists of validation error messages",
      "A list of all form field names",
      "The cleaned and validated form data",
      "A boolean indicating if the form is valid"
    ]
  },
  {
    "q": "Which method in Django forms should be overridden for form-wide validation that involves multiple fields?",
    "c": null,
    "o": [
      "`clean()`",
      "`clean_fields()`",
      "`full_clean()`",
      "`validate_form()`"
    ]
  },
  {
    "q": "What is the purpose of `Meta.widgets` in a Django `ModelForm`?",
    "c": null,
    "o": [
      "Specify custom widgets for rendering individual fields",
      "Override default field labels",
      "Exclude fields from the form",
      "Set default values for fields"
    ]
  },
  {
    "q": "How can you dynamically exclude fields from a Django `ModelForm` at runtime?",
    "c": null,
    "o": [
      "Override the `__init__` method and remove fields from `self.fields`",
      "Set them in `Meta.exclude` after instantiation",
      "Mark them as hidden in the template",
      "Set `required=False` for each field"
    ]
  },
  {
    "q": "Which argument allows you to pre-fill a field with a default value in a Django form?",
    "c": null,
    "o": [
      "`initial`",
      "`default`",
      "`placeholder`",
      "`value`"
    ]
  },
  {
    "q": "What does calling `form.save(commit=False)` in a `ModelForm` do?",
    "c": null,
    "o": [
      "Returns an unsaved instance of the model which can be modified before saving",
      "Immediately saves the model to the database",
      "Skips form validation and returns data as-is",
      "Raises an exception if the model has required fields"
    ]
  },
  {
    "q": "In Django, which method adds an error that is not associated with a specific field?",
    "c": null,
    "o": [
      "`form.add_error(None, 'message')`",
      "`form.add_error('all', 'message')`",
      "`form.add_non_field_error('message')`",
      "`form.raise_error('message')`"
    ]
  },
  {
    "q": "What is the purpose of the `help_text` attribute in Django form fields?",
    "c": null,
    "o": [
      "Provides guidance to the user for filling out the field",
      "Specifies a default value for the field",
      "Marks the field as optional",
      "Changes the HTML label of the field"
    ]
  },
  {
    "q": "Which Django form method is called to validate all fields, run custom validation, and populate `cleaned_data`?",
    "c": null,
    "o": [
      "`full_clean()`",
      "`is_valid()`",
      "`clean()`",
      "`clean_fields()`"
    ]
  },
  {
    "q": "What is the purpose of the `empty_permitted` attribute in a Django form?",
    "c": null,
    "o": [
      "Allows the form to be submitted without any data without raising validation errors",
      "Marks all fields as optional",
      "Prevents form validation entirely",
      "Automatically fills empty fields with default values"
    ]
  },
  {
    "q": "How does Django determine if a form is bound or unbound?",
    "c": null,
    "o": [
      "A form is bound if it is instantiated with data (e.g., `request.POST` or `request.FILES`), otherwise unbound",
      "A form is bound if all required fields are filled",
      "A form is always bound once instantiated",
      "A form is unbound until `is_valid()` is called"
    ]
  },
  {
    "q": "In Django `ModelForm`, what is the purpose of `Meta.exclude`?",
    "c": null,
    "o": [
      "Specifies model fields that should be excluded from the form",
      "Specifies fields that must be filled by the user",
      "Overrides default widgets for fields",
      "Automatically generates initial values for excluded fields"
    ]
  },
  {
    "q": "Which form attribute indicates if the form data has been modified from its initial values?",
    "c": null,
    "o": [
      "`has_changed()`",
      "`is_valid()`",
      "`is_bound`",
      "`changed_data`"
    ]
  },
  {
    "q": "What does setting `required=False` on a Django form field accomplish?",
    "c": null,
    "o": [
      "The field becomes optional and validation will not fail if left empty",
      "The field is hidden in the form",
      "The field is automatically filled with default values",
      "Validation is skipped for the entire form"
    ]
  },
  {
    "q": "Which Django form attribute provides a dictionary of validation errors keyed by field name?",
    "c": null,
    "o": [
      "`errors`",
      "`cleaned_data`",
      "`non_field_errors()`",
      "`validation_errors`"
    ]
  },
  {
    "q": "In a Django formset, what is the purpose of `extra`?",
    "c": null,
    "o": [
      "Specifies the number of extra blank forms to display in addition to existing forms",
      "Indicates the minimum number of forms required",
      "Limits the maximum number of forms that can be submitted",
      "Specifies which forms are required for validation"
    ]
  },
  {
    "q": "Which Django form method should you call to check if a form has valid data before saving?",
    "c": null,
    "o": [
      "`is_valid()`",
      "`full_clean()`",
      "`clean()`",
      "`clean_fields()`"
    ]
  },
  {
    "q": "In Django, how can you provide a custom error message for a specific form field?",
    "c": null,
    "o": [
      "Use the `error_messages` attribute in the field definition",
      "Override the `cleaned_data` attribute",
      "Set a custom message in `help_text`",
      "Use `initial` to provide a message"
    ]
  },
  {
    "q": "What does the `form.non_field_errors()` method return?",
    "c": null,
    "o": [
      "Errors that are not associated with any particular field",
      "All errors including field-specific errors",
      "A list of fields that failed validation",
      "The cleaned data for the form"
    ]
  },
  {
    "q": "Which method in Django forms can be overridden to validate an individual field?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_<fieldname>()`",
      "`check_<fieldname>()`",
      "`field_<fieldname>_validate()`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what is the purpose of `Meta.fields`?",
    "c": null,
    "o": [
      "Specifies the fields from the model to include in the form",
      "Excludes fields that should not be rendered",
      "Specifies default widgets for fields",
      "Marks fields as optional"
    ]
  },
  {
    "q": "What is the effect of calling `form.save(commit=False)` in a `ModelForm`?",
    "c": null,
    "o": [
      "Returns an unsaved model instance which can be modified before saving",
      "Saves the model instance immediately",
      "Skips validation and returns raw form data",
      "Raises an exception if any required fields are missing"
    ]
  },
  {
    "q": "Which argument allows you to distinguish multiple forms on the same page in Django?",
    "c": null,
    "o": [
      "`prefix`",
      "`label_suffix`",
      "`initial`",
      "`widget`"
    ]
  },
  {
    "q": "What is the purpose of the `help_text` attribute in a Django form field?",
    "c": null,
    "o": [
      "Provides guidance to the user about the expected input",
      "Specifies a default value for the field",
      "Marks the field as optional",
      "Overrides the label of the field"
    ]
  },
  {
    "q": "In Django, what does the `form.cleaned_data` dictionary contain?",
    "c": null,
    "o": [
      "Validated and converted data from the form fields after calling `is_valid()`",
      "All raw input data from POST or GET requests",
      "All fields with errors and their messages",
      "Default values of fields before user input"
    ]
  },
  {
    "q": "Which method in Django forms is called automatically during `is_valid()` to perform field and form validation?",
    "c": null,
    "o": [
      "`full_clean()`",
      "`clean()`",
      "`clean_fields()`",
      "`validate_all()`"
    ]
  },
  {
    "q": "In a Django `ModelForm`, how can you exclude certain fields from being included in the form?",
    "c": null,
    "o": [
      "Use the `exclude` attribute in the `Meta` class",
      "Remove the fields from `form.fields` after instantiation",
      "Set `required=False` for each field",
      "Mark them as disabled"
    ]
  },
  {
    "q": "What is the effect of setting `required=False` on a Django form field?",
    "c": null,
    "o": [
      "The field becomes optional and will not raise a validation error if left blank",
      "The field is hidden from the form",
      "The field is automatically filled with initial data",
      "The form will skip all validation for this field"
    ]
  },
  {
    "q": "Which Django form attribute indicates whether the form is bound to data?",
    "c": null,
    "o": [
      "`is_bound`",
      "`has_changed()`",
      "`errors`",
      "`cleaned_data`"
    ]
  },
  {
    "q": "What does `form.add_error(None, 'message')` do in Django?",
    "c": null,
    "o": [
      "Adds a non-field error to the form",
      "Adds an error to all fields in the form",
      "Clears previous errors and adds a new one",
      "Automatically raises a `ValidationError`"
    ]
  },
  {
    "q": "Which Django form method returns all fields that are visible (not hidden)?",
    "c": null,
    "o": [
      "`visible_fields()`",
      "`hidden_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "In Django, what does the `inlineformset_factory` function allow you to do?",
    "c": null,
    "o": [
      "Create a formset for related child objects connected to a parent model via a ForeignKey",
      "Create a formset for unrelated models",
      "Generate a single `ModelForm` dynamically",
      "Automatically validate forms without a management form"
    ]
  },
  {
    "q": "Which Django form method allows you to determine which fields have changed from their initial values?",
    "c": null,
    "o": [
      "`has_changed()`",
      "`is_bound()`",
      "`cleaned_data()`",
      "`changed_fields()`"
    ]
  },
  {
    "q": "In a Django `ModelForm`, what is the purpose of `Meta.widgets`?",
    "c": null,
    "o": [
      "Specify custom widgets for individual fields",
      "Exclude fields from the form",
      "Provide default initial values",
      "Mark fields as optional"
    ]
  },
  {
    "q": "What is the role of the `prefix` argument when creating a Django form instance?",
    "c": null,
    "o": [
      "Distinguishes multiple forms on the same page to avoid field name collisions",
      "Sets a prefix for field labels",
      "Adds a prefix to all error messages",
      "Automatically sets the HTML form ID attribute"
    ]
  },
  {
    "q": "Which Django form attribute provides a dictionary of all validation errors keyed by field name?",
    "c": null,
    "o": [
      "`errors`",
      "`cleaned_data`",
      "`non_field_errors()`",
      "`validation_messages`"
    ]
  },
  {
    "q": "In Django formsets, what is the purpose of the `can_order` option?",
    "c": null,
    "o": [
      "Allows forms in the formset to be reordered by the user",
      "Validates the forms in the order they were submitted",
      "Limits the number of forms that can be submitted",
      "Automatically orders forms alphabetically by a field"
    ]
  },
  {
    "q": "What does `form.is_valid()` return in Django?",
    "c": null,
    "o": [
      "True if the form data passes all validations, False otherwise",
      "The cleaned data dictionary",
      "A list of validation errors",
      "The bound status of the form"
    ]
  },
  {
    "q": "Which Django field type is used to represent a selection of multiple related model instances?",
    "c": null,
    "o": [
      "`ModelMultipleChoiceField`",
      "`ModelChoiceField`",
      "`ForeignKeyField`",
      "`ManyToManyField`"
    ]
  },
  {
    "q": "In Django forms, what is the purpose of the `help_text` attribute?",
    "c": null,
    "o": [
      "Provides guidance to the user for filling out a field",
      "Sets a default value for the field",
      "Marks the field as optional",
      "Overrides the label of the field"
    ]
  },
  {
    "q": "In Django `ModelForm`, what happens if you set `Meta.fields` to a list of field names?",
    "c": null,
    "o": [
      "Only the specified model fields are included in the form",
      "All model fields are included automatically",
      "All fields are excluded except those listed in `Meta.exclude`",
      "The form raises an error if any field is missing from the model"
    ]
  },
  {
    "q": "What does `form.has_changed()` return in Django?",
    "c": null,
    "o": [
      "True if any field value differs from its initial value, False otherwise",
      "True if the form is bound to data, False if unbound",
      "The list of fields that have errors",
      "A dictionary of fields with cleaned data"
    ]
  },
  {
    "q": "Which Django form method returns only hidden fields in a form?",
    "c": null,
    "o": [
      "`hidden_fields()`",
      "`visible_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "In Django, how do you dynamically change the queryset for a `ModelChoiceField`?",
    "c": null,
    "o": [
      "Override the field in the form’s `__init__` method and assign a new queryset",
      "Modify `Meta.fields` after instantiation",
      "Change the initial value in `cleaned_data`",
      "Set `empty_label=None` for the field"
    ]
  },
  {
    "q": "Which method in Django forms can be overridden to validate a single field?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_<fieldname>()`",
      "`check_<fieldname>()`",
      "`field_<fieldname>_validate()`"
    ]
  },
  {
    "q": "In Django, what does the `inlineformset_factory` function do?",
    "c": null,
    "o": [
      "Creates a formset for child objects related to a parent model via a ForeignKey",
      "Creates a formset for unrelated models",
      "Generates a single `ModelForm` dynamically",
      "Automatically validates forms without a management form"
    ]
  },
  {
    "q": "What is the effect of setting `disabled=True` on a Django form field?",
    "c": null,
    "o": [
      "The field is displayed but cannot be modified by the user",
      "The field is hidden and skipped during validation",
      "The field is automatically filled with initial data",
      "Validation is skipped for that field"
    ]
  },
  {
    "q": "Which Django form attribute returns errors not associated with any field?",
    "c": null,
    "o": [
      "`non_field_errors()`",
      "`errors`",
      "`cleaned_data`",
      "`validation_errors`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what does the `save_m2m()` method do?",
    "c": null,
    "o": [
      "Saves many-to-many relationships after the model instance has been saved",
      "Saves the model instance without validation",
      "Clears all many-to-many relationships",
      "Validates only the many-to-many fields"
    ]
  },
  {
    "q": "What is the purpose of the `empty_label` argument in a `ModelChoiceField`?",
    "c": null,
    "o": [
      "Specifies the label for an empty choice in a dropdown",
      "Automatically selects the first item in the queryset",
      "Hides the empty choice from the dropdown",
      "Marks the field as optional"
    ]
  },
  {
    "q": "Which Django form method returns `True` if the form is valid and `False` otherwise?",
    "c": null,
    "o": [
      "`is_valid()`",
      "`full_clean()`",
      "`clean()`",
      "`has_changed()`"
    ]
  },
  {
    "q": "In Django, what is the effect of setting `initial` for a form field?",
    "c": null,
    "o": [
      "Pre-fills the field with a default value when the form is rendered",
      "Overrides the field label",
      "Marks the field as required",
      "Automatically validates the field"
    ]
  },
  {
    "q": "Which Django form attribute tracks which fields have changed from their initial values?",
    "c": null,
    "o": [
      "`changed_data`",
      "`cleaned_data`",
      "`initial`",
      "`is_bound`"
    ]
  },
  {
    "q": "What is the purpose of the `help_text` attribute in a Django form field?",
    "c": null,
    "o": [
      "Provides additional guidance to the user about the field",
      "Marks the field as optional",
      "Sets a default value for the field",
      "Overrides the field label"
    ]
  },
  {
    "q": "In Django, which form attribute contains a dictionary of all field-specific validation errors?",
    "c": null,
    "o": [
      "`errors`",
      "`cleaned_data`",
      "`non_field_errors()`",
      "`validation_messages`"
    ]
  },
  {
    "q": "Which Django form argument allows multiple forms on the same page to avoid field name collisions?",
    "c": null,
    "o": [
      "`prefix`",
      "`label_suffix`",
      "`initial`",
      "`widget`"
    ]
  },
  {
    "q": "In Django forms, what does the `empty_permitted` attribute do?",
    "c": null,
    "o": [
      "Allows the form to be submitted without any data without raising validation errors",
      "Marks all fields in the form as optional",
      "Prevents the form from running any validation",
      "Automatically fills empty fields with default values"
    ]
  },
  {
    "q": "Which Django form attribute is used to store the data submitted by the user before validation?",
    "c": null,
    "o": [
      "`data`",
      "`cleaned_data`",
      "`initial`",
      "`bound_data`"
    ]
  },
  {
    "q": "In a Django `ModelForm`, which attribute is used to exclude fields from the form?",
    "c": null,
    "o": [
      "`Meta.exclude`",
      "`Meta.fields`",
      "`Meta.omit`",
      "`Meta.ignore`"
    ]
  },
  {
    "q": "What is the default widget for a Django `BooleanField`?",
    "c": null,
    "o": [
      "CheckboxInput",
      "RadioSelect",
      "Select",
      "TextInput"
    ]
  },
  {
    "q": "Which Django form method can be used to programmatically add a validation error to a field?",
    "c": null,
    "o": [
      "`add_error('field_name', 'message')`",
      "`raise_error('field_name', 'message')`",
      "`set_error('field_name', 'message')`",
      "`error_field('field_name', 'message')`"
    ]
  },
  {
    "q": "In Django, what does `form.non_field_errors()` return?",
    "c": null,
    "o": [
      "Errors that are not associated with any specific field",
      "All errors including field-specific errors",
      "A list of field names that failed validation",
      "The cleaned data for the form"
    ]
  },
  {
    "q": "Which method in Django forms validates all fields and populates `cleaned_data`?",
    "c": null,
    "o": [
      "`full_clean()`",
      "`is_valid()`",
      "`clean()`",
      "`clean_fields()`"
    ]
  },
  {
    "q": "What does the `prefix` argument in a Django form constructor do?",
    "c": null,
    "o": [
      "Adds a string before field names to distinguish multiple forms on the same page",
      "Adds a prefix to all field labels",
      "Specifies the HTML id attribute for the form",
      "Specifies a CSS class for the form"
    ]
  },
  {
    "q": "In Django `ModelForm`, what happens if you call `save(commit=False)`?",
    "c": null,
    "o": [
      "Returns an unsaved model instance which can be modified before saving",
      "Immediately saves the model instance to the database",
      "Skips validation and returns the form data",
      "Raises an exception if any required fields are missing"
    ]
  },
  {
    "q": "Which Django form method returns `True` if the submitted data is valid?",
    "c": null,
    "o": [
      "`is_valid()`",
      "`full_clean()`",
      "`clean()`",
      "`has_changed()`"
    ]
  },
  {
    "q": "What is the purpose of `Meta.widgets` in a Django `ModelForm`?",
    "c": null,
    "o": [
      "Specify custom widgets for rendering individual fields",
      "Exclude fields from the form",
      "Provide default initial values",
      "Mark fields as optional"
    ]
  },
  {
    "q": "Which Django form attribute indicates whether the form has been submitted with data?",
    "c": null,
    "o": [
      "`is_bound`",
      "`has_changed()`",
      "`errors`",
      "`cleaned_data`"
    ]
  },
  {
    "q": "In Django forms, what does the `help_text` attribute do?",
    "c": null,
    "o": [
      "Provides additional guidance to the user about the field",
      "Marks the field as optional",
      "Sets a default value for the field",
      "Overrides the field label"
    ]
  },
  {
    "q": "Which Django field type is used to allow selecting multiple related model instances?",
    "c": null,
    "o": [
      "`ModelMultipleChoiceField`",
      "`ModelChoiceField`",
      "`ForeignKeyField`",
      "`ManyToManyField`"
    ]
  },
  {
    "q": "In Django formsets, what is the purpose of `can_delete=True`?",
    "c": null,
    "o": [
      "Allows users to mark specific forms for deletion",
      "Automatically deletes empty forms",
      "Disables validation for deleted forms",
      "Makes all forms optional"
    ]
  },
  {
    "q": "Which Django form method returns only the visible fields of the form?",
    "c": null,
    "o": [
      "`visible_fields()`",
      "`hidden_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what is the purpose of the `Meta.model` attribute?",
    "c": null,
    "o": [
      "Specifies the model that the form is associated with",
      "Defines the template used for rendering the form",
      "Sets default initial values for all fields",
      "Specifies which fields are required"
    ]
  },
  {
    "q": "Which Django form attribute holds a dictionary of validation errors keyed by field name?",
    "c": null,
    "o": [
      "`errors`",
      "`cleaned_data`",
      "`non_field_errors()`",
      "`validation_messages`"
    ]
  },
  {
    "q": "In Django, what does the `inlineformset_factory` function do?",
    "c": null,
    "o": [
      "Creates a formset for child objects related to a parent model via a ForeignKey",
      "Creates a formset for unrelated models",
      "Generates a single `ModelForm` dynamically",
      "Automatically validates forms without a management form"
    ]
  },
  {
    "q": "What does the `disabled` attribute in a Django form field do?",
    "c": null,
    "o": [
      "Prevents the user from modifying the field value while still rendering it",
      "Hides the field from the form completely",
      "Marks the field as optional during validation",
      "Automatically fills missing fields with default values"
    ]
  },
  {
    "q": "Which Django form method allows adding an error that is not associated with any specific field?",
    "c": null,
    "o": [
      "`add_error(None, 'message')`",
      "`raise_error('message')`",
      "`set_form_error('message')`",
      "`add_non_field_error('message')`"
    ]
  },
  {
    "q": "In a Django `ModelForm`, what does `Meta.exclude` do?",
    "c": null,
    "o": [
      "Specifies model fields that should be excluded from the form",
      "Specifies fields that must be filled by the user",
      "Overrides default widgets for fields",
      "Automatically generates initial values for excluded fields"
    ]
  },
  {
    "q": "What does `form.has_changed()` indicate in Django forms?",
    "c": null,
    "o": [
      "Whether any field value differs from its initial value",
      "Whether the form is bound to data",
      "Whether the form has any validation errors",
      "Whether all fields are required"
    ]
  },
  {
    "q": "Which argument in a Django form constructor helps differentiate multiple forms on the same page?",
    "c": null,
    "o": [
      "`prefix`",
      "`label_suffix`",
      "`initial`",
      "`widget`"
    ]
  },
  {
    "q": "In Django, which method would you override to add custom form-wide validation involving multiple fields?",
    "c": null,
    "o": [
      "`clean()`",
      "`clean_fields()`",
      "`full_clean()`",
      "`validate_all()`"
    ]
  },
  {
    "q": "Which Django form attribute stores the original submitted data before validation?",
    "c": null,
    "o": [
      "`data`",
      "`cleaned_data`",
      "`initial`",
      "`bound_data`"
    ]
  },
  {
    "q": "What does the `required=False` argument do in a Django form field?",
    "c": null,
    "o": [
      "Makes the field optional so it can be left blank without raising a validation error",
      "Hides the field from rendering",
      "Automatically fills the field with the initial value",
      "Skips validation for all fields in the form"
    ]
  },
  {
    "q": "Which Django form attribute returns a list of hidden fields?",
    "c": null,
    "o": [
      "`hidden_fields()`",
      "`visible_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "What is the purpose of the `help_text` attribute in Django form fields?",
    "c": null,
    "o": [
      "Provides additional guidance to the user about the expected input",
      "Sets a default value for the field",
      "Marks the field as optional",
      "Overrides the field label"
    ]
  },
  {
    "q": "Which Django field type allows the selection of a single related model instance?",
    "c": null,
    "o": [
      "`ModelChoiceField`",
      "`ModelMultipleChoiceField`",
      "`ForeignKeyField`",
      "`ManyToManyField`"
    ]
  },
  {
    "q": "In a Django formset, what is the function of `can_order=True`?",
    "c": null,
    "o": [
      "Allows forms in the formset to be reordered by the user",
      "Limits the number of forms that can be submitted",
      "Specifies the minimum number of forms required",
      "Automatically sorts forms alphabetically by a field"
    ]
  },
  {
    "q": "Which Django form method returns a dictionary of all validation errors keyed by field name?",
    "c": null,
    "o": [
      "`errors`",
      "`cleaned_data`",
      "`non_field_errors()`",
      "`validation_messages`"
    ]
  },
  {
    "q": "In Django `ModelForm`, which method should you call to save the model instance without immediately writing it to the database?",
    "c": null,
    "o": [
      "`save(commit=False)`",
      "`save_m2m()`",
      "`full_clean()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "What is the effect of setting `empty_label=None` in a `ModelChoiceField`?",
    "c": null,
    "o": [
      "Removes the default empty option in the select widget",
      "Makes the field required automatically",
      "Selects the first item in the queryset by default",
      "Adds a placeholder instead of a blank choice"
    ]
  },
  {
    "q": "Which Django form attribute indicates whether a form has been bound to data?",
    "c": null,
    "o": [
      "`is_bound`",
      "`has_changed()`",
      "`cleaned_data`",
      "`errors`"
    ]
  },
  {
    "q": "In Django forms, what is the purpose of the `label_suffix` attribute?",
    "c": null,
    "o": [
      "Specifies a string appended to the field label (default is ':')",
      "Overrides the field's help_text",
      "Marks the field as optional",
      "Sets a CSS class for the field label"
    ]
  },
  {
    "q": "Which Django form method returns only the fields that are visible (not hidden)?",
    "c": null,
    "o": [
      "`visible_fields()`",
      "`hidden_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "What does the `inlineformset_factory` function in Django allow you to do?",
    "c": null,
    "o": [
      "Create a formset for child objects related to a parent model via a ForeignKey",
      "Generate a single `ModelForm` dynamically",
      "Create a formset for unrelated models",
      "Automatically validate forms without a management form"
    ]
  },
  {
    "q": "Which method in Django forms is used to add a validation error to a specific field programmatically?",
    "c": null,
    "o": [
      "`add_error('field_name', 'message')`",
      "`raise_error('field_name', 'message')`",
      "`set_error('field_name', 'message')`",
      "`error_field('field_name', 'message')`"
    ]
  },
  {
    "q": "Which Django form field type allows selecting multiple related model instances?",
    "c": null,
    "o": [
      "`ModelMultipleChoiceField`",
      "`ModelChoiceField`",
      "`ForeignKeyField`",
      "`ManyToManyField`"
    ]
  },
  {
    "q": "In Django forms, which method checks if any field values differ from their initial values?",
    "c": null,
    "o": [
      "`has_changed()`",
      "`is_bound()`",
      "`full_clean()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "What is the purpose of `Meta.fields` in a Django `ModelForm`?",
    "c": null,
    "o": [
      "Specifies which fields from the model should be included in the form",
      "Specifies which fields to exclude from the form",
      "Provides default initial values for the fields",
      "Sets custom widgets for the fields"
    ]
  },
  {
    "q": "Which Django form method returns errors not tied to any specific field?",
    "c": null,
    "o": [
      "`non_field_errors()`",
      "`errors`",
      "`cleaned_data`",
      "`validation_messages`"
    ]
  },
  {
    "q": "How can you make a Django form field optional?",
    "c": null,
    "o": [
      "Set `required=False` on the field",
      "Set `disabled=True` on the field",
      "Remove the field from `Meta.fields`",
      "Set `empty_label=None`"
    ]
  },
  {
    "q": "Which Django form attribute provides guidance to users about expected input for a field?",
    "c": null,
    "o": [
      "`help_text`",
      "`label_suffix`",
      "`initial`",
      "`error_messages`"
    ]
  },
  {
    "q": "What does `form.cleaned_data` contain after calling `is_valid()`?",
    "c": null,
    "o": [
      "A dictionary of validated and converted field values",
      "The raw data submitted in POST or GET",
      "All field errors",
      "The list of fields that changed from their initial values"
    ]
  },
  {
    "q": "Which Django form method validates a single field and can be customized for that field?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_<fieldname>()`",
      "`check_<fieldname>()`",
      "`field_<fieldname>_validate()`"
    ]
  },
  {
    "q": "What does the `can_delete` option in a Django formset allow?",
    "c": null,
    "o": [
      "Allows users to mark individual forms for deletion",
      "Automatically deletes blank forms",
      "Makes all forms optional",
      "Limits the total number of forms that can be submitted"
    ]
  },
  {
    "q": "In Django forms, what is the purpose of the `full_clean()` method?",
    "c": null,
    "o": [
      "Performs validation for all fields, runs custom clean methods, and populates `cleaned_data`",
      "Returns `True` if the form is valid and `False` otherwise",
      "Checks which fields have changed from their initial values",
      "Adds programmatic errors to the form"
    ]
  },
  {
    "q": "Which Django form attribute holds the initial data used when the form is rendered?",
    "c": null,
    "o": [
      "`initial`",
      "`data`",
      "`cleaned_data`",
      "`bound_data`"
    ]
  },
  {
    "q": "In a Django `ModelForm`, what is the effect of `Meta.exclude`?",
    "c": null,
    "o": [
      "Specifies model fields that should not be included in the form",
      "Specifies fields that must be filled by the user",
      "Overrides default widgets for fields",
      "Automatically generates initial values for excluded fields"
    ]
  },
  {
    "q": "What does setting `disabled=True` on a Django form field do?",
    "c": null,
    "o": [
      "Renders the field but prevents the user from modifying it",
      "Hides the field from the form",
      "Marks the field as optional",
      "Fills the field automatically with the initial value"
    ]
  },
  {
    "q": "Which Django form method returns only hidden fields of the form?",
    "c": null,
    "o": [
      "`hidden_fields()`",
      "`visible_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "What is the purpose of the `prefix` argument when creating a Django form?",
    "c": null,
    "o": [
      "Distinguishes multiple forms on the same page to prevent field name collisions",
      "Adds a string before field labels",
      "Specifies the HTML id attribute for the form",
      "Sets a CSS class for all fields in the form"
    ]
  },
  {
    "q": "Which Django field type allows a user to select a single related model instance?",
    "c": null,
    "o": [
      "`ModelChoiceField`",
      "`ModelMultipleChoiceField`",
      "`ForeignKeyField`",
      "`ManyToManyField`"
    ]
  },
  {
    "q": "In Django, which method adds a non-field error to a form?",
    "c": null,
    "o": [
      "`add_error(None, 'message')`",
      "`raise_error('message')`",
      "`set_form_error('message')`",
      "`add_non_field_error('message')`"
    ]
  },
  {
    "q": "Which Django form method returns `True` if the form has no validation errors?",
    "c": null,
    "o": [
      "`is_valid()`",
      "`full_clean()`",
      "`has_changed()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "What does the `help_text` attribute in Django form fields do?",
    "c": null,
    "o": [
      "Provides additional guidance to users about the expected input",
      "Marks a field as optional",
      "Overrides the field label",
      "Sets a default value for the field"
    ]
  },
  {
    "q": "In Django `ModelForm`, what does `save_m2m()` do?",
    "c": null,
    "o": [
      "Saves many-to-many relationships after saving the model instance",
      "Saves the model instance without validation",
      "Clears all many-to-many relationships",
      "Automatically validates many-to-many fields"
    ]
  },
  {
    "q": "Which Django form attribute contains the validated and converted data after calling `is_valid()`?",
    "c": null,
    "o": [
      "`cleaned_data`",
      "`data`",
      "`errors`",
      "`changed_data`"
    ]
  },
  {
    "q": "How do you provide a custom widget for a field in a Django `ModelForm`?",
    "c": null,
    "o": [
      "Specify it in the `widgets` dictionary inside the `Meta` class",
      "Override the field in the form’s `__init__` method",
      "Set a widget in `cleaned_data`",
      "Set `initial` to a widget instance"
    ]
  },
  {
    "q": "Which Django form method can you override to perform validation on a single field?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_<fieldname>()`",
      "`check_<fieldname>()`",
      "`field_<fieldname>_validate()`"
    ]
  },
  {
    "q": "In a Django formset, what is the purpose of the `extra` option?",
    "c": null,
    "o": [
      "Specifies the number of additional empty forms to display",
      "Sets the minimum number of required forms",
      "Limits the maximum number of forms",
      "Determines which forms are optional"
    ]
  },
  {
    "q": "What does setting `required=False` on a Django form field do?",
    "c": null,
    "o": [
      "Makes the field optional so it can be left blank without raising a validation error",
      "Disables the field",
      "Automatically fills it with initial data",
      "Skips validation for the entire form"
    ]
  },
  {
    "q": "In Django, what does the `initial` argument for a form field do?",
    "c": null,
    "o": [
      "Pre-fills the field with a default value when the form is rendered",
      "Marks the field as required",
      "Overrides the field label",
      "Adds a CSS class to the field"
    ]
  },
  {
    "q": "Which Django form attribute indicates which fields have changed from their initial values?",
    "c": null,
    "o": [
      "`changed_data`",
      "`cleaned_data`",
      "`data`",
      "`initial`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what is the effect of setting `Meta.fields = '__all__'`?",
    "c": null,
    "o": [
      "Includes all fields from the model in the form",
      "Excludes all fields from the model",
      "Includes only required fields",
      "Automatically sets default initial values for all fields"
    ]
  },
  {
    "q": "Which Django form method returns only the visible (non-hidden) fields?",
    "c": null,
    "o": [
      "`visible_fields()`",
      "`hidden_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "What does the `empty_permitted` attribute do in a Django form?",
    "c": null,
    "o": [
      "Allows the form to be submitted with no data without raising validation errors",
      "Marks all fields as optional",
      "Skips validation entirely",
      "Automatically fills empty fields with default values"
    ]
  },
  {
    "q": "Which Django formset option allows forms to be reordered by the user?",
    "c": null,
    "o": [
      "`can_order=True`",
      "`can_delete=True`",
      "`extra`",
      "`min_num`"
    ]
  },
  {
    "q": "What is the purpose of the `label_suffix` attribute in Django forms?",
    "c": null,
    "o": [
      "Specifies a string appended to the field label (default is ':')",
      "Overrides the help_text for the field",
      "Marks the field as optional",
      "Sets a CSS class for the label"
    ]
  },
  {
    "q": "Which Django form method adds an error to a specific field programmatically?",
    "c": null,
    "o": [
      "`add_error('field_name', 'message')`",
      "`raise_error('field_name', 'message')`",
      "`set_error('field_name', 'message')`",
      "`error_field('field_name', 'message')`"
    ]
  },
  {
    "q": "In Django forms, what does `form.is_bound` indicate?",
    "c": null,
    "o": [
      "Whether the form has been bound to submitted data",
      "Whether the form is valid",
      "Whether the form has any errors",
      "Whether any fields have changed"
    ]
  },
  {
    "q": "Which Django form method returns all validation errors that are not associated with a specific field?",
    "c": null,
    "o": [
      "`non_field_errors()`",
      "`errors`",
      "`full_clean()`",
      "`cleaned_data`"
    ]
  },
  {
    "q": "In Django `ModelForm`, how can you dynamically exclude fields at runtime?",
    "c": null,
    "o": [
      "Override the `__init__` method and remove fields from `self.fields`",
      "Set `Meta.exclude` after instantiation",
      "Mark the fields as optional",
      "Set `required=False` for each field"
    ]
  },
  {
    "q": "What does setting `can_delete=True` do in a Django formset?",
    "c": null,
    "o": [
      "Allows users to mark specific forms for deletion",
      "Automatically deletes empty forms",
      "Makes all forms optional",
      "Limits the maximum number of forms"
    ]
  },
  {
    "q": "Which Django form attribute holds the raw data submitted by the user before validation?",
    "c": null,
    "o": [
      "`data`",
      "`cleaned_data`",
      "`initial`",
      "`bound_data`"
    ]
  },
  {
    "q": "In a Django form, which attribute can be used to mark a field as optional?",
    "c": null,
    "o": [
      "`required=False`",
      "`disabled=True`",
      "`initial=None`",
      "`empty_label=None`"
    ]
  },
  {
    "q": "What is the purpose of `form.cleaned_data` in Django forms?",
    "c": null,
    "o": [
      "Contains validated and converted data from the form fields",
      "Contains the raw submitted data",
      "Contains all field errors",
      "Tracks which fields have changed from their initial values"
    ]
  },
  {
    "q": "Which argument in a Django form helps differentiate multiple forms on the same page?",
    "c": null,
    "o": [
      "`prefix`",
      "`label_suffix`",
      "`initial`",
      "`widget`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what does `save(commit=True)` do?",
    "c": null,
    "o": [
      "Saves the model instance to the database immediately",
      "Returns an unsaved model instance for manual saving",
      "Skips validation and returns raw form data",
      "Raises an exception if any required fields are missing"
    ]
  },
  {
    "q": "Which Django form method is automatically called during `is_valid()` to populate `cleaned_data`?",
    "c": null,
    "o": [
      "`full_clean()`",
      "`clean()`",
      "`clean_fields()`",
      "`validate_all()`"
    ]
  },
  {
    "q": "How can you programmatically add an error to a Django form field?",
    "c": null,
    "o": [
      "`add_error('field_name', 'message')`",
      "`raise_error('field_name', 'message')`",
      "`set_error('field_name', 'message')`",
      "`error_field('field_name', 'message')`"
    ]
  },
  {
    "q": "In Django, which form attribute provides the initial values for the fields when the form is rendered?",
    "c": null,
    "o": [
      "`initial`",
      "`data`",
      "`cleaned_data`",
      "`bound_data`"
    ]
  },
  {
    "q": "What does the `inlineformset_factory` function do in Django?",
    "c": null,
    "o": [
      "Creates a formset for child objects related to a parent model via a ForeignKey",
      "Generates a single `ModelForm` dynamically",
      "Creates a formset for unrelated models",
      "Validates forms without a management form"
    ]
  },
  {
    "q": "Which Django form attribute tracks which fields have been modified from their initial values?",
    "c": null,
    "o": [
      "`changed_data`",
      "`cleaned_data`",
      "`errors`",
      "`initial`"
    ]
  },
  {
    "q": "In a Django formset, what is the purpose of the `extra` option?",
    "c": null,
    "o": [
      "Specifies the number of additional empty forms to display",
      "Sets the minimum number of forms required",
      "Limits the maximum number of forms",
      "Determines which forms are optional"
    ]
  },
  {
    "q": "What is the effect of setting `disabled=True` on a Django form field?",
    "c": null,
    "o": [
      "Renders the field but prevents the user from modifying it",
      "Hides the field from the form completely",
      "Marks the field as optional",
      "Automatically fills it with the initial value"
    ]
  },
  {
    "q": "Which Django form method can be overridden to perform custom validation that involves multiple fields?",
    "c": null,
    "o": [
      "`clean()`",
      "`clean_fields()`",
      "`full_clean()`",
      "`validate_all()`"
    ]
  },
  {
    "q": "In Django `ModelForm`, how do you exclude certain fields from the form?",
    "c": null,
    "o": [
      "Use the `exclude` attribute in the `Meta` class",
      "Remove them from `Meta.fields` after instantiation",
      "Mark the fields as optional",
      "Set `required=False` for each field"
    ]
  },
  {
    "q": "What does the `help_text` attribute in a Django form field do?",
    "c": null,
    "o": [
      "Provides guidance to the user about the expected input",
      "Sets a default value for the field",
      "Marks the field as optional",
      "Overrides the label of the field"
    ]
  },
  {
    "q": "Which Django form attribute provides all validation errors keyed by field name?",
    "c": null,
    "o": [
      "`errors`",
      "`cleaned_data`",
      "`non_field_errors()`",
      "`validation_messages`"
    ]
  },
  {
    "q": "In Django, what does setting `required=False` on a form field accomplish?",
    "c": null,
    "o": [
      "Makes the field optional and prevents validation errors if left blank",
      "Disables the field",
      "Automatically fills the field with the initial value",
      "Skips validation for all fields"
    ]
  },
  {
    "q": "Which Django form method returns only hidden fields in a form?",
    "c": null,
    "o": [
      "`hidden_fields()`",
      "`visible_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "In Django forms, which attribute tracks the original data submitted by the user?",
    "c": null,
    "o": [
      "`data`",
      "`cleaned_data`",
      "`initial`",
      "`bound_data`"
    ]
  },
  {
    "q": "What is the purpose of the `prefix` argument when instantiating a Django form?",
    "c": null,
    "o": [
      "Distinguishes multiple forms on the same page to prevent field name collisions",
      "Adds a string before field labels",
      "Specifies the HTML id attribute for the form",
      "Sets a CSS class for all fields"
    ]
  },
  {
    "q": "In Django `ModelForm`, what is the effect of calling `save(commit=False)`?",
    "c": null,
    "o": [
      "Returns an unsaved model instance which can be modified before saving",
      "Saves the model instance immediately",
      "Skips validation and returns the form data",
      "Raises an exception if required fields are missing"
    ]
  },
  {
    "q": "Which Django form method is used to validate all fields and populate `cleaned_data`?",
    "c": null,
    "o": [
      "`full_clean()`",
      "`is_valid()`",
      "`clean()`",
      "`clean_fields()`"
    ]
  },
  {
    "q": "In Django forms, how do you programmatically add an error to a field?",
    "c": null,
    "o": [
      "`add_error('field_name', 'message')`",
      "`raise_error('field_name', 'message')`",
      "`set_error('field_name', 'message')`",
      "`error_field('field_name', 'message')`"
    ]
  },
  {
    "q": "What does the `initial` argument do for a Django form field?",
    "c": null,
    "o": [
      "Pre-fills the field with a default value when rendered",
      "Marks the field as required",
      "Overrides the label of the field",
      "Adds a CSS class to the field"
    ]
  },
  {
    "q": "Which Django form attribute contains the validated and converted data after calling `is_valid()`?",
    "c": null,
    "o": [
      "`cleaned_data`",
      "`data`",
      "`errors`",
      "`changed_data`"
    ]
  },
  {
    "q": "In a Django formset, what does the `extra` option specify?",
    "c": null,
    "o": [
      "The number of additional empty forms to display",
      "The minimum number of forms required",
      "The maximum number of forms allowed",
      "Which forms are optional"
    ]
  },
  {
    "q": "What is the effect of setting `disabled=True` on a Django form field?",
    "c": null,
    "o": [
      "Prevents the user from modifying the field while rendering it",
      "Hides the field from the form",
      "Marks the field as optional",
      "Automatically fills the field with initial value"
    ]
  },
  {
    "q": "Which Django form method returns only the visible fields?",
    "c": null,
    "o": [
      "`visible_fields()`",
      "`hidden_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "In Django forms, what does the `empty_permitted` attribute allow?",
    "c": null,
    "o": [
      "Allows the form to be submitted with no data without raising validation errors",
      "Marks all fields as optional",
      "Skips validation for the entire form",
      "Automatically fills empty fields with default values"
    ]
  },
  {
    "q": "Which Django form attribute tracks which fields have been changed from their initial values?",
    "c": null,
    "o": [
      "`changed_data`",
      "`cleaned_data`",
      "`errors`",
      "`initial`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what is the effect of setting `Meta.fields = '__all__'`?",
    "c": null,
    "o": [
      "Includes all fields from the model in the form",
      "Excludes all fields from the model",
      "Includes only required fields",
      "Automatically sets default initial values for all fields"
    ]
  },
  {
    "q": "Which Django form method returns validation errors that are not tied to any specific field?",
    "c": null,
    "o": [
      "`non_field_errors()`",
      "`errors`",
      "`full_clean()`",
      "`cleaned_data`"
    ]
  },
  {
    "q": "What does setting `required=False` on a Django form field do?",
    "c": null,
    "o": [
      "Makes the field optional so it can be left blank without raising a validation error",
      "Disables the field",
      "Automatically fills it with initial data",
      "Skips validation for the entire form"
    ]
  },
  {
    "q": "Which Django form attribute holds the original submitted data before validation?",
    "c": null,
    "o": [
      "`data`",
      "`cleaned_data`",
      "`initial`",
      "`bound_data`"
    ]
  },
  {
    "q": "Which Django form argument helps differentiate multiple forms on the same page?",
    "c": null,
    "o": [
      "`prefix`",
      "`label_suffix`",
      "`initial`",
      "`widget`"
    ]
  },
  {
    "q": "In Django `ModelForm`, which method saves many-to-many relationships after saving the model instance?",
    "c": null,
    "o": [
      "`save_m2m()`",
      "`save(commit=False)`",
      "`full_clean()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "In Django forms, which method determines if the submitted form data is valid?",
    "c": null,
    "o": [
      "`is_valid()`",
      "`full_clean()`",
      "`clean()`",
      "`has_changed()`"
    ]
  },
  {
    "q": "What does the `label_suffix` attribute in Django forms specify?",
    "c": null,
    "o": [
      "The string appended to a field's label (default is ':')",
      "The help text displayed for the field",
      "A CSS class applied to the field",
      "A placeholder value for the field"
    ]
  },
  {
    "q": "Which Django form method returns a list of fields that have changed from their initial values?",
    "c": null,
    "o": [
      "`has_changed()`",
      "`changed_data`",
      "`cleaned_data`",
      "`data`"
    ]
  },
  {
    "q": "In Django, what does the `disabled` attribute do for a form field?",
    "c": null,
    "o": [
      "Renders the field but prevents user input",
      "Hides the field from the form",
      "Marks the field as optional",
      "Fills the field automatically with initial value"
    ]
  },
  {
    "q": "Which Django form method returns all hidden fields in a form?",
    "c": null,
    "o": [
      "`hidden_fields()`",
      "`visible_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what does the `Meta.model` attribute define?",
    "c": null,
    "o": [
      "The model that the form is associated with",
      "The template used to render the form",
      "Default initial values for fields",
      "Which fields are required"
    ]
  },
  {
    "q": "Which Django form method can be overridden to validate a single field?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_<fieldname>()`",
      "`check_<fieldname>()`",
      "`field_<fieldname>_validate()`"
    ]
  },
  {
    "q": "What is the purpose of the `extra` option in a Django formset?",
    "c": null,
    "o": [
      "Specifies the number of additional empty forms to display",
      "Sets the minimum number of forms required",
      "Limits the maximum number of forms",
      "Determines which forms are optional"
    ]
  },
  {
    "q": "In Django forms, what is the effect of setting `empty_label=None` in a `ModelChoiceField`?",
    "c": null,
    "o": [
      "Removes the default empty choice from the dropdown",
      "Makes the field optional automatically",
      "Selects the first item in the queryset by default",
      "Adds a placeholder instead of a blank choice"
    ]
  },
  {
    "q": "Which Django form attribute contains all errors keyed by field name?",
    "c": null,
    "o": [
      "`errors`",
      "`cleaned_data`",
      "`non_field_errors()`",
      "`validation_messages`"
    ]
  },
  {
    "q": "What does the `can_order` option in a Django formset do?",
    "c": null,
    "o": [
      "Allows the user to reorder forms in the formset",
      "Validates the forms in the order submitted",
      "Limits the number of forms that can be submitted",
      "Automatically sorts forms alphabetically"
    ]
  },
  {
    "q": "Which Django form attribute provides guidance text for the user?",
    "c": null,
    "o": [
      "`help_text`",
      "`label_suffix`",
      "`initial`",
      "`error_messages`"
    ]
  },
  {
    "q": "In Django `ModelForm`, how do you include all model fields except specific ones?",
    "c": null,
    "o": [
      "Use `Meta.exclude` with a list of field names",
      "Set `Meta.fields` to `'__all__'`",
      "Override `__init__` to remove fields",
      "Use `required=False` for excluded fields"
    ]
  },
  {
    "q": "Which Django form method returns only the visible fields in a form?",
    "c": null,
    "o": [
      "`visible_fields()`",
      "`hidden_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "What does `form.cleaned_data` contain after calling `is_valid()`?",
    "c": null,
    "o": [
      "Validated and converted field values",
      "Raw submitted data",
      "All field errors",
      "Fields that have changed from their initial values"
    ]
  },
  {
    "q": "In Django forms, what is the purpose of the `prefix` argument?",
    "c": null,
    "o": [
      "Distinguishes multiple forms on the same page to avoid name collisions",
      "Adds a string before field labels",
      "Specifies the HTML id for the form",
      "Sets a CSS class for all fields"
    ]
  },
  {
    "q": "In Django forms, which method returns `True` if any field has changed from its initial value?",
    "c": null,
    "o": [
      "`has_changed()`",
      "`is_valid()`",
      "`full_clean()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "What is the purpose of `Meta.widgets` in a Django `ModelForm`?",
    "c": null,
    "o": [
      "Specifies custom widgets to render specific fields",
      "Excludes fields from the form",
      "Sets initial values for fields",
      "Marks fields as optional"
    ]
  },
  {
    "q": "Which Django form attribute indicates whether the form is bound to submitted data?",
    "c": null,
    "o": [
      "`is_bound`",
      "`has_changed()`",
      "`cleaned_data`",
      "`errors`"
    ]
  },
  {
    "q": "In Django forms, how can you mark a field as optional?",
    "c": null,
    "o": [
      "Set `required=False` on the field",
      "Set `disabled=True` on the field",
      "Remove the field from `Meta.fields`",
      "Set `empty_label=None`"
    ]
  },
  {
    "q": "Which Django form method returns a dictionary of all validation errors keyed by field name?",
    "c": null,
    "o": [
      "`errors`",
      "`cleaned_data`",
      "`non_field_errors()`",
      "`validation_messages`"
    ]
  },
  {
    "q": "In a Django formset, what is the function of the `can_delete` option?",
    "c": null,
    "o": [
      "Allows users to mark forms for deletion",
      "Automatically deletes empty forms",
      "Makes all forms optional",
      "Limits the total number of forms"
    ]
  },
  {
    "q": "Which Django form method returns a list of hidden fields?",
    "c": null,
    "o": [
      "`hidden_fields()`",
      "`visible_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what does `Meta.exclude` do?",
    "c": null,
    "o": [
      "Specifies model fields to exclude from the form",
      "Specifies fields to include in the form",
      "Overrides default widgets",
      "Automatically generates initial values for excluded fields"
    ]
  },
  {
    "q": "In Django forms, what does the `full_clean()` method do?",
    "c": null,
    "o": [
      "Validates all fields, runs custom clean methods, and populates `cleaned_data`",
      "Returns `True` if the form is valid",
      "Checks which fields have changed",
      "Adds programmatic errors to the form"
    ]
  },
  {
    "q": "Which Django form attribute stores the initial data for fields when the form is rendered?",
    "c": null,
    "o": [
      "`initial`",
      "`data`",
      "`cleaned_data`",
      "`bound_data`"
    ]
  },
  {
    "q": "In Django forms, what is the purpose of the `help_text` attribute?",
    "c": null,
    "o": [
      "Provides guidance to users about expected input",
      "Marks the field as optional",
      "Overrides the field label",
      "Sets a default value"
    ]
  },
  {
    "q": "Which Django form method can be overridden to validate a single field?",
    "c": null,
    "o": [
      "`clean_<fieldname>()`",
      "`validate_<fieldname>()`",
      "`check_<fieldname>()`",
      "`field_<fieldname>_validate()`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what is the purpose of `Meta.fields`?",
    "c": null,
    "o": [
      "Specifies which model fields are included in the form",
      "Excludes certain fields from the form",
      "Sets default widgets for fields",
      "Marks fields as optional"
    ]
  },
  {
    "q": "What does the `can_order` option do in a Django formset?",
    "c": null,
    "o": [
      "Allows the user to reorder forms",
      "Automatically validates forms in order",
      "Limits the number of forms submitted",
      "Sorts forms alphabetically"
    ]
  },
  {
    "q": "Which Django form method returns only visible (non-hidden) fields?",
    "c": null,
    "o": [
      "`visible_fields()`",
      "`hidden_fields()`",
      "`all_fields()`",
      "`cleaned_data()`"
    ]
  },
  {
    "q": "In Django `ModelForm`, what does `save(commit=False)` accomplish?",
    "c": null,
    "o": [
      "Returns an unsaved model instance which can be modified before saving",
      "Saves the instance immediately",
      "Skips validation",
      "Raises an error if required fields are missing"
    ]
  }
]