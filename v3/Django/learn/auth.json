{
    "id": "learn_auth",
    "topicId": "auth",
    "topicTitle": "Authentication",
    "description": "Master user authentication, authorization, and custom user models",
    "baseKP": 100,
    "slides": [
        {
            "id": "auth_1",
            "type": "content",
            "title": "Introduction to Authentication",
            "content": "# Authentication in Django 🔐\n\nDjango's built-in auth system handles users, permissions, and sessions!\n\n## What you'll learn:\n- **User authentication and authorization** - Login, permissions\n- **Custom user models** - Extending the User model\n- **User login/logout** - Session management\n\n## Django Auth Features\n\n- 👤 **User model** - Ready to use\n- 🔑 **Password hashing** - Secure by default\n- 📝 **Session management** - Login state\n- 🛡️ **Permissions** - Control access\n- 👥 **Groups** - Batch permissions\n- 🔧 **Built-in views** - Login, logout, password reset\n\n## Quick Setup\n\n```python\n# settings.py - Already included by default!\nINSTALLED_APPS = [\n    'django.contrib.auth',       # Auth framework\n    'django.contrib.contenttypes', # Required by auth\n    'django.contrib.sessions',    # Session framework\n    ...\n]\n\nMIDDLEWARE = [\n    'django.contrib.sessions.middleware.SessionMiddleware',\n    'django.contrib.auth.middleware.AuthenticationMiddleware',\n    ...\n]\n```\n\n> 💡 **Django's auth is battle-tested** - used by Instagram, Pinterest, and millions of sites!"
        },
        {
            "id": "auth_2",
            "type": "content",
            "title": "User Model Basics",
            "content": "# The User Model 👤\n\n## Built-in User Fields\n\n```python\nfrom django.contrib.auth.models import User\n\n# User fields:\nuser.username       # Required, unique\nuser.password       # Hashed password\nuser.email          # Email address\nuser.first_name     # First name\nuser.last_name      # Last name\nuser.is_active      # Account active?\nuser.is_staff       # Can access admin?\nuser.is_superuser   # All permissions?\nuser.date_joined    # Registration date\nuser.last_login     # Last login time\n```\n\n## Creating Users\n\n```python\n# Method 1: create_user (handles password hashing)\nuser = User.objects.create_user(\n    username='john',\n    email='john@example.com',\n    password='securepassword123'\n)\n\n# Method 2: create_superuser\nsuperuser = User.objects.create_superuser(\n    username='admin',\n    email='admin@example.com',\n    password='adminpass123'\n)\n\n# Command line\npython manage.py createsuperuser\n```\n\n## Password Handling\n\n```python\n# NEVER store raw passwords!\nuser.password = 'mypassword'  # ❌ WRONG!\n\n# Use set_password (hashes automatically)\nuser.set_password('mypassword')  # ✅ Correct!\nuser.save()\n\n# Check password\nif user.check_password('mypassword'):\n    print('Password correct!')\n```\n\n> ⚠️ **Never store or compare raw passwords!** Always use `set_password()` and `check_password()`."
        },
        {
            "id": "auth_3",
            "type": "content",
            "title": "User Login/Logout",
            "content": "# Login and Logout 🚪\n\n## Login View\n\n```python\nfrom django.contrib.auth import authenticate, login, logout\nfrom django.shortcuts import render, redirect\n\ndef login_view(request):\n    if request.method == 'POST':\n        username = request.POST['username']\n        password = request.POST['password']\n        \n        # Authenticate user\n        user = authenticate(request, username=username, password=password)\n        \n        if user is not None:\n            login(request, user)  # Create session\n            return redirect('home')\n        else:\n            return render(request, 'login.html', {'error': 'Invalid credentials'})\n    \n    return render(request, 'login.html')\n```\n\n## Logout View\n\n```python\ndef logout_view(request):\n    logout(request)  # Clears session\n    return redirect('login')\n```\n\n## Using Django's Built-in Views\n\n```python\n# urls.py - Much easier!\nfrom django.contrib.auth import views as auth_views\n\nurlpatterns = [\n    path('login/', auth_views.LoginView.as_view(\n        template_name='registration/login.html'\n    ), name='login'),\n    \n    path('logout/', auth_views.LogoutView.as_view(\n        next_page='home'\n    ), name='logout'),\n]\n```\n\n## Settings\n\n```python\n# settings.py\nLOGIN_URL = '/login/'           # Redirect here if not logged in\nLOGIN_REDIRECT_URL = '/home/'   # After successful login\nLOGOUT_REDIRECT_URL = '/'       # After logout\n```"
        },
        {
            "id": "auth_quiz_1",
            "type": "quiz",
            "title": "Auth Quiz",
            "content": "Test your authentication knowledge!",
            "quizQuestion": "Which function creates a session for an authenticated user?",
            "quizOptions": [
                "authenticate()",
                "login()",
                "create_session()",
                "start_session()"
            ],
            "correctOptionIndex": 1
        },
        {
            "id": "auth_4",
            "type": "content",
            "title": "Authorization - Permissions",
            "content": "# Authorization 🛡️\n\n## Protecting Views with Decorators\n\n```python\nfrom django.contrib.auth.decorators import login_required, permission_required\n\n# Require login\n@login_required\ndef dashboard(request):\n    return render(request, 'dashboard.html')\n\n# Require specific permission\n@permission_required('blog.add_article')\ndef create_article(request):\n    pass\n\n# Multiple permissions\n@permission_required(['blog.add_article', 'blog.change_article'])\ndef manage_articles(request):\n    pass\n```\n\n## In Templates\n\n```html\n{% if user.is_authenticated %}\n    <p>Welcome, {{ user.username }}!</p>\n    <a href=\"{% url 'logout' %}\">Logout</a>\n{% else %}\n    <a href=\"{% url 'login' %}\">Login</a>\n{% endif %}\n\n{% if perms.blog.add_article %}\n    <a href=\"{% url 'article_create' %}\">Create Article</a>\n{% endif %}\n```\n\n## Class-Based View Mixins\n\n```python\nfrom django.contrib.auth.mixins import LoginRequiredMixin, PermissionRequiredMixin\n\nclass ArticleCreateView(LoginRequiredMixin, CreateView):\n    model = Article\n    fields = ['title', 'content']\n    login_url = '/login/'  # Custom login URL\n\nclass ArticleDeleteView(PermissionRequiredMixin, DeleteView):\n    model = Article\n    permission_required = 'blog.delete_article'\n```\n\n> 💡 **Use mixins before other parent classes** for proper method resolution order!"
        },
        {
            "id": "auth_5",
            "type": "content",
            "title": "Custom User Model",
            "content": "# Custom User Model 👤\n\nExtend Django's User with your own fields!\n\n## ⚠️ Do This at Project Start!\n\n```python\n# accounts/models.py\nfrom django.contrib.auth.models import AbstractUser\nfrom django.db import models\n\nclass CustomUser(AbstractUser):\n    # Add custom fields\n    bio = models.TextField(blank=True)\n    avatar = models.ImageField(upload_to='avatars/', blank=True)\n    phone = models.CharField(max_length=20, blank=True)\n    date_of_birth = models.DateField(null=True, blank=True)\n    \n    def __str__(self):\n        return self.username\n```\n\n## Configure Settings\n\n```python\n# settings.py\nAUTH_USER_MODEL = 'accounts.CustomUser'  # BEFORE first migration!\n```\n\n## Reference in Models\n\n```python\nfrom django.conf import settings\n\nclass Article(models.Model):\n    author = models.ForeignKey(\n        settings.AUTH_USER_MODEL,  # Use this, not User directly!\n        on_delete=models.CASCADE\n    )\n```\n\n## Create Migrations\n\n```bash\npython manage.py makemigrations accounts\npython manage.py migrate\n```\n\n> ⚠️ **Always define a custom user model at the start** of your project! Changing later is very difficult."
        },
        {
            "id": "auth_6",
            "type": "content",
            "title": "User Registration",
            "content": "# User Registration 📝\n\n## Registration Form\n\n```python\n# forms.py\nfrom django import forms\nfrom django.contrib.auth.forms import UserCreationForm\nfrom .models import CustomUser\n\nclass RegistrationForm(UserCreationForm):\n    email = forms.EmailField(required=True)\n    \n    class Meta:\n        model = CustomUser\n        fields = ['username', 'email', 'password1', 'password2']\n```\n\n## Registration View\n\n```python\n# views.py\nfrom django.shortcuts import render, redirect\nfrom django.contrib.auth import login\nfrom .forms import RegistrationForm\n\ndef register(request):\n    if request.method == 'POST':\n        form = RegistrationForm(request.POST)\n        if form.is_valid():\n            user = form.save()\n            login(request, user)  # Auto-login after registration\n            return redirect('home')\n    else:\n        form = RegistrationForm()\n    \n    return render(request, 'registration/register.html', {'form': form})\n```\n\n## Password Reset (Built-in)\n\n```python\n# urls.py\nfrom django.contrib.auth import views as auth_views\n\nurlpatterns = [\n    path('password_reset/', auth_views.PasswordResetView.as_view(), name='password_reset'),\n    path('password_reset/done/', auth_views.PasswordResetDoneView.as_view(), name='password_reset_done'),\n    path('reset/<uidb64>/<token>/', auth_views.PasswordResetConfirmView.as_view(), name='password_reset_confirm'),\n    path('reset/done/', auth_views.PasswordResetCompleteView.as_view(), name='password_reset_complete'),\n]\n```\n\n> 💡 **Django provides complete password reset flow** - just add templates!"
        },
        {
            "id": "auth_quiz_2",
            "type": "quiz",
            "title": "Custom User Quiz",
            "content": "Test your custom user knowledge!",
            "quizQuestion": "Which setting tells Django to use a custom User model?",
            "quizOptions": [
                "USER_MODEL",
                "CUSTOM_USER",
                "AUTH_USER_MODEL",
                "DEFAULT_USER"
            ],
            "correctOptionIndex": 2
        },
        {
            "id": "auth_7",
            "type": "content",
            "title": "Summary",
            "content": "# Excellent Work! 🎉\n\nYou've mastered Django Authentication!\n\n## User Authentication and Authorization\n- ✅ Built-in User model with essential fields\n- ✅ `authenticate()` and `login()` functions\n- ✅ `@login_required` decorator\n- ✅ `@permission_required` for permissions\n- ✅ Template tags: `user.is_authenticated`\n\n## Custom User Models\n- ✅ Extend `AbstractUser` for custom fields\n- ✅ Set `AUTH_USER_MODEL` in settings\n- ✅ Reference with `settings.AUTH_USER_MODEL`\n- ✅ Do this before first migration!\n\n## User Login/Logout\n- ✅ `login()` creates session\n- ✅ `logout()` clears session\n- ✅ Built-in views: `LoginView`, `LogoutView`\n- ✅ Password reset views included\n\n## Key Settings\n\n```python\nAUTH_USER_MODEL = 'accounts.CustomUser'\nLOGIN_URL = '/login/'\nLOGIN_REDIRECT_URL = '/dashboard/'\nLOGOUT_REDIRECT_URL = '/'\n```\n\n## Best Practices\n- 💡 Always use custom User model\n- 💡 Never store raw passwords\n- 💡 Use `LoginRequiredMixin` for CBVs\n\n> 🚀 **Next Steps:** Learn about Django Admin!"
        }
    ]
}