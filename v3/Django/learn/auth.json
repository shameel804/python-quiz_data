{
    "id": "learn_django_auth",
    "topicId": "auth",
    "topicTitle": "Authentication",
    "description": "Secure your app with Django's Authentication System",
    "baseKP": 65,
    "slides": [
        {
            "id": "django_auth_1",
            "type": "content",
            "title": "Authentication vs Authorization",
            "content": "# Auth Basics 🔐\n\n- **Authentication**: Verifying *who* a user is (Login).\n- **Authorization**: Verifying what an authenticated user *is allowed to do* (Permissions).\n\n## The `User` Model\nDjango provides a built-in `User` model with fields like:\n- `username`\n- `password` (Hashed! Never simple text)\n- `email`\n- `is_staff` (Can access Admin)\n- `is_superuser` (Has all permissions)"
        },
        {
            "id": "django_auth_2",
            "type": "content",
            "title": "Login & Logout",
            "content": "# Login and Logout 🚪\n\nDjango provides built-in views, but you usually want to wrap them in your own logic or templates.\n\n```django\nfrom django.contrib.auth import authenticate, login, logout\n\ndef login_view(request):\n    # ... get username/pass from form ...\n    user = authenticate(username=u, password=p)\n    if user is not None:\n        login(request, user)\n        # User is now logged in!\n        return redirect('home')\n    else:\n        # Invalid login\n\ndef logout_view(request):\n    logout(request)\n    return redirect('login')\n```"
        },
        {
            "id": "django_auth_3",
            "type": "content",
            "title": "Access Control",
            "content": "# Limiting Access 🛑\n\nHow do you stop non-logged-in users from seeing a page?\n\n## Using Decorators\n```django\nfrom django.contrib.auth.decorators import login_required\n\n@login_required\ndef private_dashboard(request):\n    return render(request, 'dashboard.html')\n```\nIf a user isn't logged in, they are redirected to `LOGIN_URL` (defined in settings).\n\n## In Templates\n```html\n{% if user.is_authenticated %}\n    <p>Welcome, {{ user.username }}</p>\n{% else %}\n    <a href=\"{% url 'login' %}\">Login</a>\n{% endif %}\n```"
        },
        {
            "id": "django_auth_quiz_1",
            "type": "quiz",
            "title": "Auth Quiz",
            "content": "Check your understanding",
            "quizQuestion": "Which decorator ensures a user is logged in before accessing a view?",
            "quizOptions": [
                "@user_passes_test",
                "@login_required",
                "@staff_member_required",
                "@permission_required"
            ],
            "correctOptionIndex": 1
        },
        {
            "id": "django_auth_4",
            "type": "content",
            "title": "Custom User Models",
            "content": "# Custom User Models 👤\n\n**Best Practice**: Always set up a custom user model at the start of a project, even if you don't think you need it yet.\n\nAllows you to log in with Email instead of Username, or add fields like `date_of_birth` directly to the user.\n\n**steps:**\n1. Create `class MyUser(AbstractUser): pass`\n2. Update `settings.py`: `AUTH_USER_MODEL = 'myapp.MyUser'`\n3. Register in `admin.py`.\n\n> ⚠️ **Warning**: Changing the User model mid-project is very difficult and requires complex database migrations."
        },
        {
            "id": "django_auth_quiz_2",
            "type": "quiz",
            "title": "Models Quiz",
            "content": "Best practices check",
            "quizQuestion": "Why should you define a Custom User Model early?",
            "quizOptions": [
                "It's faster than the default",
                "To confuse hackers",
                "It's hard to change later if requirements grow",
                "Django requires it by default"
            ],
            "correctOptionIndex": 2
        }
    ]
}